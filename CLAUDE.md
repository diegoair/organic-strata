# CLAUDE.md — Organica Project Memory

> This file is read by Claude at the start of every session.  
> It contains all the context needed to work on this project without re-explaining.  
> Keep it updated as the project evolves.

---

## Who

**Diego** — designer/artist, founder of a design and art studio.

---

## What

**Organica** is two things simultaneously:
1. Diego's visual language system
2. A series of tools to develop, generate, automate and deploy multiple languages.

This is a creative tool, made for designers and artists to experiment with and compose new visual languages.
It is designed to reduce the gap between the raster world and the vector world — which is why most outputs are a vector format that can find their way into the physical world through screenprint and traditional printing, across media and formats.

---

## The Methodology

Core principle: a visual system is a **grammar** — rules that generate infinite variations while remaining coherent.

Modules in Organica:
- **Form** — seeds, elements, components, patterns and symbols. Vector shapes that enable and define the mark.
- **Canvas** — the space where the language is created
- **Grid** — composer of grids used to constrain the canvas
- **Color** — a series of tools to handle different palette methodologies and colour combinations
- **Typography** — a tool to define and create typography
- **Motion** — a series of animation engines
- **Scale** — vector-first, screen to mural
- **Rhizome** — a node-based canvas that chains the other tools into a pipeline, composing a full flexible visual system end to end

---

## The Tools (Live)

| Tool | URL | What it does |
|---|---|---|
| **Hub** | `theorganicalanguage.vercel.app` | Entry point, links to all tools |
| **Genesis** | `theorganicalanguage.vercel.app/genesis/` | The seed library (restructured Aug 30–31, 2026 — was a 4-mode motion workbench). Three modes: **Library** (the home — a full-width tile grid (fixed "cozy" density) under three stacked rows: the Library/Create mode nav, a **Sets** row (per-set chips + `+` new set + a `⚙` toggle that reveals an inline manage panel — per-user-set rename / `↑↓` reorder / armed `×` delete; `deleteSet` also drops any member now in no other set; built-in "Base Seeds" pinned first, controls-less), and a **Type** filter (Source was removed Aug 31 — dead on user sets, the parametric/vector tile badge covers the split); active set + Type filter persist in `localStorage['organica.library.view']` and survive set switches; the built-in **Base Seeds** set = 13 organic forms + 6 procedural primitives, both synthesized; **click a seed → Edit**), **Create** (new shapes only — Draw / Generate / a `+` icon that imports an SVG file — Save mints a `user-…` seed), and **Edit** (opened from a Library tile; a **parametric** seed shows its generator sliders, a **vector** seed's every contour becomes editable **bezier anchors** via Paper.js `importSVG({expandShapes:true})` → `CompoundPath`; the floatbar is the sole home of both mode nav and the verbs, in one canonical order **Create · Edit · Save · Back(=Undo) · Delete · Library · Export** + context extras (Import after Create, Duplicate after Edit, Clear after Back); **Save** opens the **Save popover** — Name / Type / underlying-layer / (Create) Save-to — dirty-gated in Edit, a user seed overwrites in place, a built-in auto-forks to "My Seeds"; **Delete** is a two-click armed icon; **Export** downloads the seed as a standalone `.svg`). the Edit header also carries an interactive **"In sets"** block — a checkbox per user set that adds/removes the seed id from `set.forms` in place, no copy minted, guarded so a seed always stays in ≥1 set). In the **Library** page the Library/Create mode nav sits **above the filter bar** (the header is just the logo). A Set is a plain ordered list of seed ids. Compose, the 55-form animated catalog, and Import-as-a-mode were removed; the catalog is archived at `genesis/archive/indicators-55.html`. |
| **Indicators** | *(archived)* | The 55-form animated catalog. `genesis/indicators.html` now redirects to `/genesis/`; the page is preserved at `genesis/archive/indicators-55.html`. |
| **Spore** | `theorganicalanguage.vercel.app/spore/` | Generative stippling from images — SVG mark library, zoom/pan, PNG/JPG/SVG export, Figma |
| **Pollen** | `theorganicalanguage.vercel.app/pollen/` | Advanced stippling — blue-noise engine, Circle/Polygon/Line points, Adaptive duotone, presets, PNG/JPG/SVG export, Figma |
| **Living Path** | `theorganicalanguage.vercel.app/livingpath/` | Generative font/path modification (web port of ivangrozny/LivingPath). Vector + Raster engines, layer groups + blend modes, 9 raster algorithms, 24 presets, live text specimen, installable OTF export (Web Worker), .lvp projects |
| **Halide** | `theorganicalanguage.vercel.app/halide/` | Photo → true 1-bit dithered portrait. Floyd–Steinberg / Atkinson / Bayer ordered / flat threshold; background removal (colour-key, Pick/Auto), sticker Outline, Background fill, interactive drag-to-position square crop; PNG/JPG/vector SVG (optional contour-traced "Simplify shapes")/Figma export. Manual: `docs/HALIDE.md` |
| **Komorebi** | `theorganicalanguage.vercel.app/komorebi/` | 木漏れ日 — dappled sunlight through a canopy, real-time on the GPU (WebGL2). Light-cookie/gobo canopy (procedural fBm or uploaded silhouette) projected in perspective; **penumbra/pinhole** physics (small gaps → round coins of light); post-process **god rays** (radial-blur occlusion buffer); Scene + Gobo (tileable pattern) modes; periodic wind → **seamless WebM loop**; PNG/JPG/WebM + posterised tone-band **SVG separation** (Halide's tracer)/Figma. Manual: `docs/KOMOREBI.md` |
| **Camo Turing** | `theorganicalanguage.vercel.app/camo-turing/` | GPU (WebGL2/Three.js) Gray-Scott reaction-diffusion — successor to Camouflage's Disruptive mode, removed from the repo. Live f/k sliders, drop-mark seeding, **Layers** (stack independent sims, blended in field space — Normal/Diff/Multiply/Screen/Add/Darken/Exclusion — with per-layer preset + colour), Paper background image/stencil (embeds real SVG on export), **anisotropic diffusion** (real parallel stripes — nearest-neighbour taps, not bilinear) with verified animal/plant presets (Leopard/Cheetah/Tiger/Zebra/Watermelon rind). PNG/JPG/SVG/Figma. Manual: `docs/CAMO-TURING.md` |
| **Warping** | `theorganicalanguage.vercel.app/warping/` | Domain-warped value-noise / Worley pattern generator — covers the pattern family Gray-Scott can't honestly produce: **Wood grain** and **Marble veining** (fbm turbulence), **Cellular network** (Worley F1/F2, e.g. giraffe-style reticulation), **Level curves** (topographic contours). **Warp** (the tool's namesake) bends the sampled coordinates with a low-frequency noise field before any pattern runs, independent of the pattern itself. Canvas2D/JS, same reasoning as Komorebi's own move off WebGL. PNG/JPG/SVG (posterised tone-band separation, same tracer as Komorebi/Halide)/Figma. Manual: `docs/WARPING.md` |
| **Loom** | `theorganicalanguage.vercel.app/loom/` | Universal Grid Generator — a constraint-driven layout-grid engine, not a column generator. Canvas Manager → Grid Definition Engine → Constraint Engine (**Kiwi solver** for equal-by-default/overridable sizing, **direct parametric math** for continuous-function generators — chosen per generator, not globally) → Universal JSON Model → renderers (live HTML/CSS Grid, SVG, PNG, Figma via the existing shared plugin). **Phase 1 MVP**: two generators, **Bento** (Kiwi, seeded cell-merge spans) and **Sinusoidal** (parametric, sine-modulated track sizes). First Organica tool built as native ES modules rather than single-file. Manual: `docs/LOOM.md` |
| **Membrane** | `theorganicalanguage.vercel.app/membrane/` | Generative form-growth tool — a point-set (circle/line/point, procedural/image/text-seeded) driven across the canvas by a chosen Movement pattern (float-to-mouse, orbit, figure-8, Follow-path…), leaving an accumulated trail. Canvas panel, Palette (Ink/Background swatches + RMX multi-colour mode), mouse-wheel zoom/pan via the shared `Organica.createZoomPan`, spacebar pause, real vector SVG export (a capped/throttled recording of recent frames — `state.shapeHistory`, the same "record marks as data" approach as Spore/Pollen), PNG, and Video (MediaRecorder). Promoted from `explorations/membrane/`, the template this project's other exploration→production migrations (Vortex) followed. |
| **Vortex** | `theorganicalanguage.vercel.app/vortex/` | A 3D conical helix of independently-drifting line segments, orbited by its own real perspective camera (drag-orbit, Shift-drag-roll, Camera distance as the zoom — deliberately NOT `Organica.createZoomPan`, a 3D scene needs one camera, not two overlapping zoom concepts). Resting → transitioning → running state machine: hovering (or Play) grows the spiral from a single seed line on an ease-in ramp. RMX palette (chip UI, up to 8 colours — raised from Membrane's 5, no shader array-size bound here) doubles as the segments' own ink, no separate Ink swatch. Numeric, reproducible **Seed** drives the per-segment noise/spin/orbit randomness (`p.noiseSeed` + a `mulberry32`-derived offset). Redraws from scratch every frame (no accumulation), so PNG export is a genuine re-render at scale (not an upsampled bitmap) and SVG export is just the current frame's already depth-sorted segment list — no history/throttle machinery needed. Promoted from `explorations/vortex/`, migrated the same way as Membrane. |
| **FVS** | `theorganicalanguage.vercel.app/fvs/` | Flexible Visual System engine — Figma's own Elements→Components model: an authored **Element** (isosceles triangle, or an Arc with a live Thickness slider 1–100% — one shared `arcGeometry()` generator, not 4 fixed presets — an **uploaded SVG**, or a **Seed picked from Genesis Creator's own shared library** via `Organica.loadLoomGrid`'s sibling read of `organica.library.forms`, first drawable shape, bbox-fit) gets a transformed copy placed into every cell of a grid — Square 2×2/3×3/4×4, or an **imported Loom grid** (`Organica.loadLoomGrid`, rect or polygon cellShape, any cell count; the 7 four-cell-only symmetry rules grey out automatically outside a 4-cell grid, Random/Exhaustive fall back to an honest per-cell-independent draw). Elements are real SVG path `d` strings (not polygon point-lists, needed for the arcs' curves) — `ELEMENT_TYPES[type].geometry(params) → {d, normTx, normTy, normScale}`, rendered via `<path>` (SVG) / `ctx.fill(new Path2D(d))` (Canvas2D). Colour lives in a centralised **Palette** — Ink (RMX chip UI, 1–8 colours, `colorAt(i)` cycling by cell index) + **Paper** (background swatch, wired into the gallery, PNG, and SVG export alike). Full transform vocabulary — Rotation (0/90/180/270°), Flip (none/H/V/both), Scale (Small/Medium/Large %, editable) — feeds **8 named symmetry rules** (Identity, Pinwheel, Mirror/Kaleidoscope, Diagonal mirror, Checkerboard, Row mirror, Column mirror, Radial — Radial uses the grid's true rotational cell order, `TL,TR,BR,BL`, not Pinwheel's row-major order, which is what lets 4 arcs tile into a real full circle) plus a **Manual** 2×2 mini-editor. **Random seeded** and **Exhaustive** draw from a `FAMILIES` registry of the same named rules rather than choosing 4 cells independently, gated by which axes are active — every generated result is still a real symmetry; Exhaustive stays hard-capped at 512, refusing cleanly with the exact count above cap. One item-descriptor array (`cx,cy,rotation,flipH,flipV,scale,color`) feeds both the Canvas2D PNG export and the SVG string, same discipline as Vortex's own `render.js`/`svgexport.js`. An always-visible **Element preview strip** shows the current Element at 0/90/180/270° + each flip before generating a full grid of it, and a local **Component Library** (`Organica.presetStore`) saves/reloads full self-contained snapshots (Element incl. an upload's own geometry, Grid, Palette, exact cells) for reuse — Figma export deliberately not wired in yet, since the shared `Organica.sendToFigma` channel is still broken product-wide (see Loom's own August 14 session note). Symbols/Pattern-tiling/Applications tiers (the rest of the Figma-component-library reference brief) are later phases. |
| **Colornet** | `theorganicalanguage.vercel.app/colornet/` | Channel-separation + recolour tool, inspired by (not a clone of) the desktop app Coloraster — drop a photo, split it into N luminance-band **channels** (v1 scope; true RGB/CMYK channel split is v2), each independently recolourable (hex + a live CMYK readout), reorderable, blendable (Normal/Multiply), with per-channel **transform** (position/scale/rotation/flip — genuinely new raster-layer math, no prior art elsewhere in the repo). Reuses the shared luminance-band tracer (`Organica.traceContours`/`contoursToPathD`, same technique as Komorebi/Warping's own `buildSVG()`) — confirmed format-agnostic, works identically on an uploaded photo. **WYSIWYG discipline** (same as Spore/Pollen): one `buildPlan()` resolves every channel's transform matrix once, and preview/PNG/JPG/plates/SVG all read that same plan — verified with a byte-exact (0-byte-diff) preview-vs-raster-export test. Promoted `Organica.rgbToCmyk`/`cmykToRgb` out of TuneSutra into `shared/core.js` (TuneSutra's own code comment had flagged them as promotion candidates). PNG/JPG/SVG/per-channel-plate export, `Organica.presetStore` (settings only, never the uploaded image). |
| **Blob Boundary** | `theorganicalanguage.vercel.app/blob-boundary/` | Mask-shape morph (circle/diagonal band/steep band/organic blob/starburst, GSAP MorphSVGPlugin cycling through 3 chosen presets) with a fixed dot lattice classified live every frame against the mask's own current geometry (`SVGGeometryElement.isPointInFill`) — dots deep inside stay small and separate, dots straddling the boundary enlarge and overlap into a gooey fringe with no blur/goo filter, dots outside hide. Migrated from `explorations/blob-boundary/`, Aug 27, 2026 — the third exploration→production migration (after Membrane, Vortex), the first to follow the new `docs/UI-SHELL.md` §6b checklist written specifically because the first two skipped steps. |
| **Radial** | `theorganicalanguage.vercel.app/radial/` | One tool for the Book of Shapes "radial" pattern family (~16 of 18 patterns). A polar coordinate field — Placement (grid rings×spokes / phyllotaxis golden-angle / spiral arms) with an optional Shape modulation (rose curve / spiral shear / domain warp) — drawn by one of seven Render modes (ring outlines / spokes / mesh / filled cells / marks / arc bands / chaos circles). Pure vector: `buildScene(P,W,H)` → tagged primitive set, and the inline-SVG preview IS the exact export string (no tracing); PNG rasterises the same primitives. The geometry/render core (`buildScene`/`sceneSVG`/`drawSceneCanvas` + `BP`/`BUILTIN_PRESETS`) was extracted to `shared/radial.js` at Pulsar's arrival (Aug 29). 17 built-in presets, `Organica.presetStore('radial')`. |
| **Pulsar** | `theorganicalanguage.vercel.app/pulsar/` | The generative **Motion** tool over Radial. Drives Radial's parameter object `P` with time-varying **tracks** (a wave — sine / triangle / looping-noise / ramp-once / spin — per parameter, added to the base value, clamped to the parameter's real range) and redraws `Organica.radial.buildScene` from scratch every frame (the Vortex model — deterministic, never accumulates). 4 track slots + a Whole-field section (`rotate` as a group transform, the one track later expressible as animated SVG; `zoom`). 4 one-click **Motion modes** mapped to `docs/ANIMATION-SYSTEM.md`'s physics patterns — **Pulse** (Internal Pressure), **Bloom** (Growth by Tracing, one-shot), **Drift** (Environmental Forces), **Orbit** (Differential Rotation) — each re-mapped per placement/render so no mode leaves a dead control; Gravity deliberately excluded (a directional pull breaks radial symmetry). Seamless-loop WebM: every periodic track's Cycles/loop is a whole integer and noise is circular-sampled (Komorebi's single-phase-angle trick), so frame 0 ≡ frame N; a scene with any `ramp-once`/phase-`spin` track is flagged "one-shot — will not loop". Floatbar Play/Pause/Stop + Loop popover (`#rg-loop-seconds`, 0 = indefinite / doubles as video length); Export = PNG frame + Video (`captureStream(30)` + `MediaRecorder`, MP4/WebM). One `renderFrameAt(tNorm)` serves preview, PNG and every video frame. 8 built-in animated scenes, `Organica.presetStore('pulsar')`. Element-count guard (~6500) refuses to animate a too-heavy scene. SVG-frame export + a Soul-style per-primitive stagger/reveal layer are deferred. |
| **Mycel** | `theorganicalanguage.vercel.app/mycel/` | Generative mycelial-network growth. A point set (procedural **Scatter** or an imported **Loom** grid) grows branching chains outward from a chosen set of **Roots** via a space-colonisation algorithm — Growth step / Influence radius / Kill radius / Max iterations — with per-segment **Curve wobble**. Playback ramp (Duration). Real vector **SVG** export (`buildExportSVG` over the accumulated `buildChains` segment list) + PNG (×1/×2/×4), `Organica.presetStore`. Its wire-curve maths was later reused, made interactive, by Rhizome. Hub group "Motion & growth". |
| **Mote** | `theorganicalanguage.vercel.app/mote/` | Video / webcam → stipple (promoted Sep 1, 2026 from `scratchpad/mote.html`, an Aug 31 prototype). **Source lives in the floatbar** — `＋` opens a local video **File**, a camera icon starts the live **Webcam** (`getUserMedia`), a mirror icon flips the webcam (enabled only while it's live; baked into the field so preview + export inherit it). Each frame is stippled through **`shared/pollen-engine.js`** (the dart-thrower + tone pipeline + line/streamline geometry, extracted from Pollen at Mote's arrival — Pollen refactored to consume the same module, verified byte-identical via a seeded-RNG point-array diff). Three temporal-coherence strategies: **Fixed grid** (A — place once, per-frame resample tone/size), **Flowing** (B — one Balzer-style relaxation step toward each frame's density field + spawn/cull; Gentle/Medium/Turbulent presets over 5 raw params behind a disclosure), **Re-stipple** (C — full re-dart per frame, fixed `mulberry32` seed; live preview bursts, Render for a clean pass). Real-time `requestVideoFrameCallback` loop + a frame-accurate **offline encoder** (`MediaStreamTrackGenerator` + timestamped `VideoFrame` — the only non-realtime encoder in the repo; stays Mote-local). Floatbar: source icons · Play/Pause · Stop · **Export** popover · a live stacked `ms / fps / pts` readout. **Export popover** — *Still*: **PNG** · **SVG** (`buildMoteSVG`, a byte-faithful mirror of `paintPoints` → resolution-independent vector, `viewBox 0 0 workW workH`, `<circle>` for dots / `<path>` for lines, FVS-style `<metadata>` — for posters / print). *Video — single*: **Record** (wall-clock via `Organica.recorder`) · **Render** (frame-accurate `MediaStreamTrackGenerator` + `VideoFrame`, source aspect at the **Size** select). *Video — batch*: `sz-*` checkboxes — **Master** (source aspect · Size) + 5 by-ratio social presets (`MOTE_SOCIAL`: Square 1:1 1080, Vertical 9:16 1080×1920, Portrait 4:5, Landscape 16:9, Widescreen 3:2 — the Membrane/Loom/Vortex `CANVAS_PRESETS` names + 9:16) + a **Frame** seg (**Crop** = centre-crop to fill / **Fit** = letterbox on Paper) → **Export batch** runs `renderJobs(jobs)`: each ticked size is a full independent pass (its own `cropFor` centre-crop, its own stipple layout, its own `MediaRecorder`) → one finished `mote-<label>-*.mp4`. Jobs temporarily hijack the module render globals (`workW/workH`, `frameCanvas` dims, `basePoints`/`points`/`targetCount`) and restore + repaint after. A **Size** select (1920 → 7680 / 8K, `EXPORT_BASE`/`EXPORT_HARD`, upscale allowed since the geometry re-renders crisp) drives the master + the single Record/Render; video bitrate cap 240 Mbps. `paintPoints` gained `opt.offX/offY` for the Fit letterbox. *(The Sep 1 SVG-sequence export — `.svgz`-per-frame in a `.zip` via `shared/zip.js` — was **removed** Sep 1: a real 3 s / line-preset export was 382 MB, 17 MB per frame, and hung renderers; `shared/zip.js` deleted with it.)* **Kiosk / fullscreen** mode (header button + `k`) hides all chrome, canvas on black filling the viewport, auto-plays — for live interactive installs. `Organica.presetStore('mote')` + 9 curated built-ins, each shown as a **live-rendered thumbnail** in a custom listbox (native `<select>` can't carry previews). `?cam` boot shortcut. Accent `--tool #2f9e8f` (sea-green teal — the Motion band's cool gap). |
| **TuneSutra** | `theorganicalanguage.vercel.app/tunesutra/` | Colour-harmony palette tool. Builds a palette from a **Harmony** rule (Monochromatic / Analogous / Complementary / Split-complementary / Triadic / Tetradic), each colour editable in **RGB / HSB / CMYK**, shown as an RMX chip strip (min 3 / max 7) and previewed applied to a **garment pictogram** — zones mapped to at most 3 roles (body / hem / accent), one shared `GARMENT_ZONES` constant feeding both the SVG view and the Canvas2D PNG export. PNG (×1/×2/×4) + `svgString()` SVG, `Organica.presetStore`. Hub group "Coloring & palette". |
| **Rhizome** | `theorganicalanguage.vercel.app/rhizome/` | Node-based visual workflow canvas — chains the other Organica tools as pipeline stages. Two-tier node model: **Tier 1 native** (zero-porting wrappers on already-pure functions — Loom's generators as real ES modules, `Organica.loadLoomGrid`, `Organica.traceContours`) + **Tier 2 bridge** (a hidden sandboxed iframe loading the real tool page + a `rhizome-set-input` / `rhizome-output-ready` postMessage protocol, ~20–30 lines added to each bridged tool). Typed ports (`svg`/`image`/`grid`/`color`/`number`/`points`) with 3 disclosed auto-adapters (`svg→points`, `grid→points`, `grid→svg`). Execution: Kahn topological sort (throws a readable error on a cycle), per-node dirty-flag cache, **serialized `recompute()`** (a call while one is in flight re-runs once after, never concurrently — fixed a real iframe-`pending`-map race). Canvas: per-node drag (writes position back to state), live wire-follow (Mycel's curve maths, made interactive), `panAlways` pan at any zoom. Undo/redo (JSON-snapshot stack, checkpoints only), Shift-drag marquee multi-select, variadic **Merge** inputs (1–6). 17 node types incl. bridges for Genesis / Komorebi / Warping / Soul / Camo Turing / Membrane / Living Path / Spore / Pollen / Halide + Export (PNG/SVG/→Figma). **Phase 0 fixed the product-wide dead Figma bridge** (`figma-plugin/code.js` never listened for `'organica-svg'`). Native ES modules, like Loom. Manual: `docs/RHIZOME.md`. Deferred: Vortex / TuneSutra / Mycel bridges, a thumbnail graph-preset picker. |

---

## Repo Structure

```
organic-strata/          ← GitHub repo name (diegoair/organic-strata)
├── index.html           ← Organica hub (dark, animated noise field)
├── genesis/
│   ├── index.html       ← Genesis — the seed library (Aug 30, 2026 restructure): two modes, Library + Create. Sets = plain ordered id lists. Base Seeds built-in = 13 organic + 6 procedural, synthesized
│   ├── creator.html     ← thin redirect to /genesis/ (kept so old links resolve)
│   ├── library.html     ← thin redirect to /genesis/ (same)
│   ├── indicators.html  ← thin redirect to /genesis/ (the 55-form catalog moved to archive/, Aug 30, 2026)
│   ├── archive/
│   │   └── indicators-55.html  ← the full 55-form animated catalog, self-contained. Links animations.css + page.css absolutely
│   ├── animations.css ← 78 @keyframes + 55 .aNN rules — append-only. Archive-support only now (Genesis renders static); still linked by the hub bento + archive/indicators-55.html
│   ├── page.css      ← :root palette + catalog page chrome (archive/indicators-55.html)
│   ├── forms.js      ← SVG markup for the 13 Base Seeds + ORGANIC_LABELS — PORT AS DATA. Reduced from 56 on Aug 30, 2026 (ids unchanged; slug-key/metadata restructure is a tracked follow-up)
│   ├── defs.js       ← Shared SVG defs (goo filters + chips) — INJECT ONCE
│   └── genesis-creator.js    ← RETIRED, loaded by no page — record of the old plain-composer's decorative layer (palettes/backgrounds/per-shape colour/Randomize) that was never ported
├── colornet/
│   └── index.html       ← Colornet — channel-separation + recolour tool (luminance bands v1), see Tools table
├── blob-boundary/
│   └── index.html       ← Blob Boundary — mask-morph + edge-scatter tool, migrated from explorations/, see Tools table
├── spore/
│   └── index.html       ← Spore — generative stippling (single-file, vanilla)
├── pollen/
│   └── index.html       ← Pollen — advanced stippling, blue-noise engine (single-file, vanilla)
├── halide/
│   └── index.html       ← Halide — photo → 1-bit dithered portrait (single-file, vanilla)
├── komorebi/
│   └── index.html       ← Komorebi — WebGL2 volumetric light / god rays / dapple (single-file, vanilla)
├── camo-turing/
│   └── index.html       ← Camo Turing — GPU (WebGL2/Three.js) Gray-Scott reaction-diffusion (single-file)
├── warping/
│   └── index.html       ← Warping — domain-warped value-noise / Worley pattern generator (single-file, vanilla, Canvas2D)
├── loom/                 ← Universal Grid Generator — the one tool NOT single-file (native ES modules)
│   ├── index.html       ← shell — canvas preview + panel, loads js/main.js as a module
│   └── js/
│       ├── canvas-manager.js     ← physical space: size/unit/margin/safe area/bleed
│       ├── json-model.js         ← the Universal JSON Model — single source of truth
│       ├── constraint-engine.js  ← Kiwi solver + parametric solver, chosen per generator
│       ├── generators/           ← registry.js + bento.js + sinusoidal.js (Phase 1 of ~26 planned)
│       └── renderers/            ← html-renderer.js (live CSS Grid) + svg-renderer.js + raster-renderer.js
├── membrane/             ← Generative form-growth (point-set + Movement pattern + accumulated trail)
│   ├── index.html
│   └── js/               ← state.js, canvas.js, main.js, color.js, movement.js, svgexport.js, …
├── vortex/               ← 3D conical helix, own perspective camera, resting→transitioning→running
│   ├── index.html
│   └── js/               ← state.js, geometry.js (helix/camera/noise math), palette.js, render.js, canvas.js, svgexport.js, main.js
├── fvs/                  ← Flexible Visual System — Elements→Components grid/rule engine (single-file)
│   └── index.html
├── radial/               ← Book of Shapes "radial" family — polar field → 7 render modes (single-file, vanilla)
│   └── index.html       ← DOM glue only; geometry/render core is shared/radial.js
├── pulsar/               ← Motion tool over Radial — parameter tracks + rAF redraw → seamless-loop WebM (single-file)
│   └── index.html
├── mote/                 ← Video / webcam → stipple — 3 coherence modes + offline frame-accurate encoder (single-file). Consumes shared/pollen-engine.js
│   └── index.html
├── mycel/                ← Mycelial-network growth — space-colonisation branching from Roots, SVG/PNG (single-file, vanilla)
│   └── index.html
├── tunesutra/            ← Colour-harmony palette on a garment pictogram — RGB/HSB/CMYK, RMX strip (single-file, vanilla)
│   └── index.html
├── livingpath/           ← Generative font/path modification — Vector + Raster engines, OTF export (single-file, web port)
│   └── index.html
├── rhizome/              ← Node-based workflow canvas — chains other tools as pipeline stages (native ES modules, like Loom)
│   ├── index.html        ← shell — canvas/panel/floatbar, loads js/main.js as a module
│   ├── _test-mvp.html    ← the original isolated prototype (2-node drag/wire/connect test) — kept as record
│   └── js/
│       ├── graph-model.js       ← canonical model — nodes[]/edges[], everything else derived
│       ├── execution-engine.js  ← topoSort (Kahn, throws on cycle) + dirty-flag cache + serialized recompute()
│       ├── port-types.js        ← PortType enum + display metadata
│       ├── adapters.js          ← svg→points / grid→points / grid→svg / grid→image, the only auto-conversions
│       ├── node-registry.js     ← id → {meta, compute}
│       ├── nodes/                ← Tier 1 native (loom-grid-generator/geometry, contour-trace, svg-to-points, merge, export) + Tier 2 bridges (genesis/komorebi/warping/camo-turing/membrane/livingpath/spore/pollen/halide) + bridge-iframe.js (the generic Tier-2 factory)
│       ├── canvas/               ← pan-zoom.js (wraps Organica.createZoomPan), node-drag.js, wires.js, ports.js
│       └── renderers/            ← node-card.js, inspector-panel.js (populates the shared right panel)
├── shared/              ← Cross-tool system assets
│   ├── tokens.css ← Manrope + type scale + spacing + the surface palette (--ink/--paper/--mid/--accent/--panel/--border defaults) + --danger (load FIRST)
│   ├── shell.css  ← the app-shell skeleton: reset, body, #app, #canvas-wrap, .org-stage (+ zoom-cursor states), #zoom-hud, #drop-hint. Load AFTER floatbar, BEFORE the tool <style>. Linked by 14 tools (not Genesis/Rhizome/FVS — own canvas surface)
│   ├── core.js    ← download, presets, Figma, tracer, zoom/pan, hex/CMYK utils, mulberry32
│   ├── palette.js ← the Palette component: Organica.palette.swatch(target, opts) — string target = attach a #cp-/#hex-/#sw- colour row, element target = build an RMX chip strip — plus .colorAt (score→colour) / .mix. Paired CSS: palette.css. Folded in organica-palette-chip.js + core's createColorSwatch (Aug 29). Load AFTER core
│   ├── palette.css ← paired sheet for palette.js — .rmx-* chip CSS (custom-prop tunable) + .icon-btn. Link only for generate/RMX mode; .color-* stays in panel.css
│   ├── noise.js   ← hash2/vnoise/fbm/ridgedFbm/voronoiF1F2/simplex2/simplex3/simplexFbm2/simplexFbm3 — shared by Komorebi, Camo Turing, Warping, Radial/Pulsar
│   ├── motion.js  ← parsePrimitives (any SVG → primitive list) — used by Rhizome's svg-to-points node + svg→points adapter. PATTERNS/staggerDelay/animate/morph retained-but-unused since Soul's removal (Aug 31, 2026). Load AFTER noise
│   ├── radial.js  ← Radial's polar-field geometry/render core (buildScene/sceneSVG/drawSceneCanvas + PARAM_RANGES + BP/BUILTIN_PRESETS), shared by Radial and Pulsar — load AFTER noise, BEFORE the tool script
│   ├── pollen-engine.js ← Organica.pollenEngine — the stippling core: processLuma/buildField (tone), computePoints (pure dart-thrower, injectable rng + field + isCancelled/onProgress), spacingAt/gradAngle, ranged/dropoutSkip/pointAngle/pointRGBA, strokeCenterline/smoothOpenPath[D]/makeFieldAngle/fieldStrokePts. Shared by Pollen + Mote; each keeps its own paintPoints + SVG serializer. Load AFTER core + palette
│   ├── recorder.js ← Organica.recorder({canvas,tool,duration,bitrate,mimeCandidates,onStart,onStatus,onStateChange}) → {toggle,start,stop,isRecording}. captureStream + MediaRecorder + 5-entry MIME fallback (overridable) + optional explicit videoBitsPerSecond + negotiated-mimeType ext sniff. Used by Pulsar/Camo Turing/Vortex/Membrane/Mote. No paired CSS. Load AFTER core
│   ├── seeds-panel.js ← Organica.seedsPanel({target,idPrefix,tabs,slots,subControls,extraControls,genesis,text,svg,image,applyMode,onSeed,onTabChange,onFontReady,onError}) — the tabbed Genesis/SVG/Text(/Image) source picker. Emits svgString + a raw descriptor; each tool keeps its own model step. .PRIMORDIAL = the shared curated form list. Used by Membrane + Camo Turing (2 instances) + Living Path (.PRIMORDIAL only). Paired: seeds-panel.css. Load AFTER core
│   ├── seeds-panel.css ← paired sheet — .shape-grid/.shape-thumb/.thumb-num only (--seeds-grid-cols tunable); .seg-ctrl/.upload-btn stay in panel.css
│   ├── (also: header.css · panel.css · floatbar.css · mobile-gate.css · paper.js · transformer.js · tokens.json — same drop-the-prefix rule)
│   ├── vendor/            ← third-party, isolated from first-party (2026-08-30): gsap.min.js + gsap-morphsvg (Blob Boundary, free "no charge" license), kiwi.min.js (Loom, MIT/BSD-3), three.module.js (Camo Turing), opentype.min.js (Membrane/Camo Turing text seeds), paper-full.min.js (Genesis draw/edit), manrope-variable.ttf (the type-seed font). See THIRD-PARTY-NOTICES.md
│   └── _template.html      ← Start a new tool from this
│   NOTE: files here are named by role, no `organica-` prefix — the `shared/` path is the namespace (renamed 2026-08-30). The JS API global stays `window.Organica.*`.
├── docs/
│   ├── SESSION-LOG.md   ← Full historical session notes (split out of CLAUDE.md Aug 29, 2026) — read on-demand
│   ├── VISION.md        ← Full system vision and methodology
│   ├── ROADMAP.md       ← Development phases and open questions
│   ├── ANIMATION-SYSTEM.md  ← Animation pattern documentation
│   ├── SHARED-LIBRARY.md ← Genesis organic-* forms library contract + storage conventions
│   ├── SHARED-COMPONENTS.md ← The Organica.* JS/CSS component system + the reusable centralization pattern (Palette is the worked example; backlog of concerns queued for the same treatment)
│   ├── DESIGN-SYSTEM.md ← Manrope, type scale, tokens, Figma mapping
│   ├── UI-SHELL.md      ← The standard tool layout + behaviour contract
│   ├── KOMOREBI.md      ← Komorebi manual
│   ├── CAMO-TURING.md   ← Camo Turing manual
│   ├── WARPING.md       ← Warping manual
│   ├── LOOM.md          ← Loom (Universal Grid Generator) manual
│   ├── RHIZOME.md       ← Rhizome (node workflow canvas) manual
│   ├── HALIDE.md        ← Halide manual
│   ├── SPORE.md · POLLEN.md · LIVINGPATH.md ← per-tool manuals
│   └── archive/SOUL.md  ← manual for the retired Soul tool (removed Aug 31, 2026)
├── vercel.json          ← Routing: / → hub, /genesis/ → genesis, per-tool rewrites
├── LICENSE              ← All-rights-reserved proprietary notice (repo is private on GitHub — see Aug 27 session note)
├── THIRD-PARTY-NOTICES.md ← Every vendored library's real license, audited against its own file header
└── CLAUDE.md            ← This file
```

---

## Technical Stack

| Layer | Technology |
|---|---|
| Frontend | Vanilla HTML/CSS/JS — no framework, no build step |
| Modules | Mostly single-file per tool; Loom and Rhizome are native ES modules |
| Animation | Genesis forms: pure CSS `@keyframes`. Blob Boundary: vendored **GSAP** + MorphSVG. Radial/Pulsar/Warping/Komorebi/Camo Turing/Membrane/Vortex: own `requestAnimationFrame` redraw loops |
| SVG processing | All client-side now — `Organica.traceContours` (rectilinear contour tracer in `shared/core.js`), used by Komorebi/Warping/Halide/Colornet. The old Python/OpenCV/vtracer backend went with Strata (Aug 26, 2026) |
| Vendored libs | GSAP + MorphSVG (Blob Boundary), Kiwi.js (Loom), Three.js (Camo Turing), Paper.js (Genesis draw/edit), opentype.js + Manrope (Membrane/Camo Turing text seeds), p5.js (Membrane, CDN) — see `THIRD-PARTY-NOTICES.md` |
| Deployment | Vercel (team slug `studiorann`), no build step |
| Repository | GitHub — `diegoair/organic-strata` (private) |
| Design integration | Figma (manual now, direct push in roadmap) |

---

## Critical Rules — Never Break These

- **Design tokens — mandatory, every new development.** Never hardcode a font-size, spacing (padding/margin/gap), radius, or UI-chrome colour — use the matching token from `shared/tokens.css` (`--fs-*`, `--space-*`, `--radius-*`, `--ink`/`--paper`/`--panel`/`--mid`/`--accent`/`--border`/`--border-strong`/`--tool`/`--accent-warm`/`--danger`/`--track-bg`). The surface palette (`--ink` … `--border`) has defaults in the token file now — a tool declares only `--tool` plus the rare override. If the value you need doesn't exist on any existing scale, **stop and ask before adding a new token** — don't invent one silently and don't fall back to a raw px/hex "just this once." Live reference with every token and component, rendered from the real CSS: `/design-system/` (linked from the hub). Full narrative + Figma mapping: `docs/DESIGN-SYSTEM.md`. Two real exceptions, not loopholes: (1) a tool's own **content** colour (Halide's ink/paper for the dithered image, Pollen's point colour) is user data, not design-system chrome — don't tokenise it just because it happens to default near `--ink`/`--paper`; (2) a genuine pill/stadium shape (`border-radius` = half the element's height, e.g. a toggle switch) isn't a corner radius — forcing it onto `--radius-sm/md/lg` flattens the capsule. Typography specifically: **Manrope only**, via `--font`, never a second typeface.
- **Shared code** — `shared/core.js` owns download, preset storage + legacy migration, Figma postMessage, colour hex validation, the rectilinear contour tracer and zoom/pan. Don't re-implement these in a tool; if one needs different behaviour, extend the core
- **New tool** — start from `shared/_template.html`, not by copying a neighbour. Contract: `docs/UI-SHELL.md`
- **`genesis/animations.css`** — never modify existing rules, only append new ones (the 55 forms depend on the exact timings/selectors). Archive-support now — Genesis renders static and doesn't link it; live consumers are the hub bento + `genesis/archive/indicators-55.html`. Any consumer linking it **must** define `--ink` and `--bg-cell` — see `docs/SHARED-LIBRARY.md`. (The `organic-library.css` `@import` shim was retired 2026-08-30 — link `page.css` then this file directly.)
- **localStorage keys** — always `organica.<tool>.<thing>`. When renaming a key, migrate forward and **leave the legacy key in place** (user presets/forms are real work); never delete it in the same release
- **`forms.js` / `defs.js`** — treat as data, port verbatim
- **Color in SVG forms** — always `fill: var(--ink)` / `stroke: var(--ink)`, never hardcode hex
- **Internal links** — always relative (`/genesis/`, `/loom/`), never absolute Vercel URLs
- **New base seeds** — add the SVG string + label to `forms.js`; Genesis renders it static. New *animated* forms belong in `genesis/archive/indicators-55.html` (or Pulsar / the tool's own engine for motion) — see ANIMATION-SYSTEM.md

---

## Animation System — Quick Reference

6 patterns, each maps to real physics:

1. **Internal Pressure** — `scale` + asymmetric `cubic-bezier` → breath, heartbeat, lung
2. **Gravity + Viscosity** — `translateY` + `scaleY` → drops, honey, lava
3. **Growth by Tracing** — `stroke-dashoffset` → vine, roots, tree rings
4. **Collective Behaviour** — staggered `animation-delay` + goo filter → swarm, metaballs
5. **Environmental Forces** — continuous `translate`, `linear` easing → wind, tide, sedimentation
6. **Differential Rotation** — counter-spinning layers at different speeds → orbit, shard rotation

Duration range `1.4s–14s` maps to real-world time scales. See `docs/ANIMATION-SYSTEM.md` for full detail.

These 6 originated as Genesis's hand-typed CSS `@keyframes`. **Pulsar** maps 4 of them (Pulse / Bloom / Drift / Orbit) onto Radial's parameter tracks; Membrane / Vortex / Blob Boundary each embody one physically. *(Soul rebuilt all 6 parametric on GSAP + 2 new patterns, but was removed Aug 31, 2026 — see `docs/archive/SOUL.md`.)* The taxonomy is still the reference; the delivery is no longer CSS-only.

---

## Output Formats Diego Produces

- Tavole grafiche / composition studies
- Posters (print + digital)
- Web pages (animated SVG)
- Branding systems for clients
- Mural schemas (large format, scale-agnostic vector)
- Art installations (generative animation loops)

---

## Roadmap Priorities

*The original numbered phases (mid-2026) have largely landed and are folded into shipped tools; this is the current picture. Priorities themselves are Diego's call — see `docs/ROADMAP.md` for the full narrative.*

- **Done / shipped** — Genesis depth (unified library + save/load + SVG/PNG export + bezier Edit); the pattern family (Warping, Camo Turing, Radial, FVS Symbols) covering what "Phase 4 — pattern engine" pointed at; the Motion engine (Pulsar over Radial; each generative tool also carries its own); the workflow layer (Rhizome).
- **Figma push** — the shared `Organica.sendToFigma` channel was **fixed** by Rhizome's Phase 0 (`figma-plugin/code.js` now listens for `'organica-svg'`), so it works for the 7 tools that call it (Spore/Pollen/Halide/Komorebi/Loom/Camo Turing/Warping). Still to do: wire an Export→Figma tab into the tools that only have `sendToFigma()` defined but not surfaced (FVS, Radial, …).
- **Output pipeline** — print PDF, mural schema, installation loop. Still open.
- **Opening to test users** — desktop-only gate + Vercel Analytics shipped; still open: access control for external testers, error monitoring, a feedback channel, and the cross-browser pass (see Backlog).

---

## Open Questions (Decisions Pending)

- **Typography** — Living Path (font/path modification, OTF export) is the type module in practice. Still open: is there a role for type *inside* the visual language beyond specimen/lettering — a Genesis-style generative type system?
- **Colour** — TuneSutra (harmony rules + garment preview), Colornet (channel separation), and the shared Palette component now cover this. The old "3 Genesis palettes" framing is retired; still open: how the tools' palettes stay coherent with each other across a whole system.
- **Client workflow** — what does the Organica handoff look like for a client branding project? Still open.
- **Mural scale** — largest format needed? DPI requirements? Still open.

---

## Backlog

- **Cloud sync (Supabase)** — auth + per-user persistence, shipped 2 Sep 2026. Project `dgzxsqdrktqgpnewxtwf` ("Organica", eu-west-1). Client is the vendored `@supabase/supabase-js` UMD; `shared/supabase.js` (`Organica.sb`, config inline — anon key is public, RLS is the guard) → `shared/auth.js` (`Organica.auth`, Google OAuth + magic link, **client-side auto-gate**: any page loading `auth.js` needs a session; `/sign-in` `/privacy` `/terms` are public, `window.ORGANICA_PUBLIC` opts out) → `shared/auth-badge.js` (email + Sign out in `.org-header`) → `shared/store.js` (`Organica.store(tool)` aka `Organica.presetStore` — sync `read()`/`write()` over a localStorage cache + `pull()`/`push()`/`onSync()`; `Organica.store.library` for Genesis). **Tables** (all RLS on): `presets (user_id, tool, name, data jsonb, pk(user_id,tool,name))` + `seeds (user_id, seed_id, name, svg, type, gen_type, gen_params, data jsonb)` — policy "own rows" (ALL), granted to `authenticated`, no `anon` read. `base_seeds (seed_id, name, svg, type, gen_type, gen_params, data jsonb, position)` — policy "read all" (SELECT), granted to `anon`+`authenticated`, **no write policy/grant** so only the project owner edits it via the dashboard. Genesis's 19 Base Seeds live in `base_seeds`; `genesis/forms.js` + `proceduralForm()` are the first-paint/offline fallback and must stay byte-aligned with the table (verified aligned 2 Sep — if you edit one, mirror the other, DB wins). Genesis user library = one row per seed in `seeds` + set structure in `presets` `tool='library' name='meta'`; the old `tool='library' name='__blob__'` row is a dead migration fallback, safe to `delete` in a later release. Keep-alive: **GitHub Action** `.github/workflows/keepalive.yml` (daily REST+Auth ping vs the 7-day free-tier pause).
  - **Open items:** (1) **`[contact]` placeholder** in `privacy/`+`terms/` — deployed live with the literal string; swap in a real address (+ the Google consent-screen contact). (2) **"to continue to …supabase.co"** on Google sign-in — Google shows the raw Supabase callback host (unverified OAuth app, `*.vercel.app` can't be domain-verified); fix = Supabase **custom auth domain** (Pro ~$25/mo + an owned domain), cosmetic, deferred. (3) Invite-only: Google sign-in currently creates a user on first login (public sign-up still on) — turn it off + allowlist once testers are onboarded.

- **Hub home-page performance** — `index.html` is ~1.48MB; ~1.4MB of that is 4 inline tool-export SVGs (Halide 757KB/**16,374 `<rect>`**, Camo Turing 401KB/2 path, Membrane 151KB/220 path, Komorebi 101KB/2 path). Only Halide's is pathological. **Verified 2026-08-30**: a vertical rect-merge yields **0%** — the dither has no vertical coherence (each Floyd-Steinberg row diffuses differently), and it's already horizontally run-length merged. So the only real fixes are (a) rectilinear contour-trace the 16k rects → one `<path fill-rule="evenodd">` (what Halide's own "Simplify shapes" does — needs the tracer + a pixel-diff to verify), or (b) move the 4 big SVGs to external files + lazy-load the off-screen bento cells (IntersectionObserver). Both change the hub's structure/appearance and need a scoped pass with real visual verification — not a mid-stream hack. Left for a dedicated session.

- **Mote — deferred polish** (Sep 1, 2026): (1) **"Hatch flow" cool variant.** The Aug-31 prototype had *two* hatch-flow presets; the 18→9 curation kept only `pollen-hatch-flow` (black strokes on cream). The dropped one — `hatch-flow` — was RMX `RAMP_COOL` (`['#0e1622','#274157','#5f8aa0','#a9cdd6','#eef4f2']`) on dark `#0c1116`, `strokeLen 16`, `size 1.6`, `dropout 45`, `map: 'tonernd'`, `spMin 2`. Re-add as a 10th built-in ("Hatch flow · cool") or fold into "Hatch flow" — ~5 min in `mote/index.html` `BUILTIN_PRESETS`. (2) **Webcam device picker.** The `enumerateDevices` camera `<select>` was dropped when Source moved into the floatbar; the webcam now uses the default device only. Re-add as a small popover on the `#btn-cam` icon (the Pulsar `Organica.popover` pattern) if multi-camera choice is needed for an install.

- **Cross-browser verification before opening to test users** — every tool has only ever been exercised through this session's own Chromium-based Browser pane; real Safari/Firefox behaviour is unverified. Prioritized by which real API each tool actually depends on (checked in the code, not guessed):
  - 🔴 **Camo Turing** — WebGL2 + Three.js (Safari's WebGL2 support has historically been the most fragile of the three engines); generate a preset, confirm the canvas actually renders.
  - 🔴 **Living Path** — Web Worker-driven OTF export; generate and download a font, Worker path/CORS behaviour differs across browsers.
  - 🔴 **Camo Turing / Membrane** — `MediaRecorder` video export; Safari's MP4/WebM codec support differs meaningfully from Chrome/Firefox — record a short export on each.
  - 🟡 **Rhizome** — iframe-sandbox bridge nodes; connect two nodes, confirm the bridge round-trips a real value.
  - 🟡 **Loom / FVS** — Kiwi.js constraint solver; generate a grid, confirm no console error.
  - 🟢 **All tools** — Manrope variable font + Google Fonts load; a quick visual pass, low real risk.
  For each: open on real Safari and Firefox, exercise the one risky action, check the console (Cmd+Opt+C on Safari, F12 on Firefox) for errors. Not something this session can execute directly — no real Safari/Firefox access from the Chromium-only Browser pane — logged here for Diego to run through himself before wider test-user access.

- **Colornet improvement plan (from the Stochaster videos)** — **verified 2026-08-30: all four items have since shipped.** 🟢 Shuffle + Random Colors (`btn-shuffle`/`btn-random-colors` → `shuffleChannels()` Fisher–Yates / `randomizeColors()`); 🟡 Channel-swap gesture (`armOrSwapChannel` — click a name to arm, click another to swap); 🟡 Batch processing (`btn-batch` + `#batch-input` multi-file, per-image export of the current config); 🔴 **AM Screening as a real per-channel render mode** (`screenMode: am/noise/dither/flat`, per-channel `screenAngle` [15/75/0/45° defaults] / `screenFreq` / `dotShape` incl. a real PostScript **Euclidean** spot function, 3 catalogued presets). The "reuse evaluated" note stands: Dotraster's Noise mode ≈ Pollen's engine; its Pattern mode ≈ a Halide extension. Nothing left to build here — if extending, run through Plan Mode.

- **Inline `style="display:none"` → `hidden` attribute sweep** — ~180 static inline visibility toggles across the suite (loom ~40, fvs ~30, genesis 25, camo-turing 21, pulsar 13, radial/membrane 10 each, …). A future pass to the HTML `hidden` attribute + a `[hidden]{display:none!important}` safety rule (needed because flex/grid containers override bare `hidden`). Deliberately left out of the 2026-08-30 shell/style-centralization pass. Watch for the ones that are JS-toggled via `el.style.display` — those must keep an inline `style` so clearing it works (see the `.u-hidden` note in `design-system` §utilities). Also queued in `docs/SHARED-COMPONENTS.md` §3.
- **Follow-ups from the 2026-08-30 shell pass** — all closed as of 2026-08-30: ~~zoom/pan JS~~, ~~`.chan-card` → `.org-layer-card`~~, ~~Spore `.upload-mark-btn`~~, ~~Genesis `.upload-btn`~~ (reduced to its 2 real deltas), ~~8 camo-turing/fvs/genesis inline file inputs → `.org-file-input`~~, ~~`.hud-btn`/`#compute-bar` folded into `shell.css`~~. **Not a dup after all:** `design-system`'s `::-webkit-scrollbar` is a page-global 6px style, not the `#panel`-scoped 3px one — left in place. **Still open:** ~~Genesis's inline `margin:2px 0 6px`~~ done — per the design-system rule a one-off doesn't earn a token, so `.panel-hint` got `margin: 0 0 var(--space-2)` in its rule and both raw-px inline overrides were dropped (the third `.panel-hint` also had `margin-bottom:8px`); net shift 2px, imperceptible. `_template.html` Export → floatbar.
- **Centralization mini-projects** — **`Organica.seedsPanel`** and **`Organica.recorder`** both shipped Aug 30, 2026 (see `docs/SHARED-COMPONENTS.md` §3). `mulberry32` is effectively resolved (6 tools delegate via a 1-line wrapper; Loom's `js/generators/*.js` deliberately excluded per Aug 26 D1; `transformer.js` fallback deliberate; `noise.js` keeps its own — it loads before core in Warping/Loom).

---

## Session Notes

*Full historical log (100+ dated entries, June 2026 onward) lives in [`docs/SESSION-LOG.md`](docs/SESSION-LOG.md) — consult it on-demand for context on past decisions. Only the most recent entries are kept inline below. When adding a new note, append it here; periodically migrate older inline entries to the log to keep this section short.*

- **September 1, 2026 — Mote promoted from `scratchpad/mote.html` to `/mote/`, under the "Motion & growth" hub group.** Diego: "analizza e pianifica come importare il test mote nel tool sotto la sezione Motion". Three decisions (AskUserQuestion): **extend `shared/recorder.js`** (not keep a local recorder), a **full interaction redesign** (not just shell-conformance), and — for the stipple core — an analysis; the recommendation, taken, was **extract `shared/pollen-engine.js` now** (Pollen + Mote as the two consumers, the `radial.js` "extract at the second consumer" precedent). **Phase 0 — `shared/pollen-engine.js`:** the tone pipeline (`processLuma`/`buildField`), the pure dart-thrower `computePoints(o)` (injectable `rng` / `field` / `isCancelled` / `onProgress` / `hideBand` / `maxPoints` — the shape Mote's prototype had already proven), `spacingAt`/`gradAngle`, `ranged`/`dropoutSkip`/`pointAngle` (radians) / `pointRGBA` (with Mote's `video` branch), and the curved-line + field-streamline geometry (`strokeCenterline`/`smoothOpenPath`[`D`]/`makeFieldAngle`/`fieldStrokePts`). Scoped to the **intersection** — each tool keeps its own `paintPoints` (Pollen dispatches SVG-form marks, Mote draws literal dots) and Pollen keeps its SVG serializer. **`pollen/index.html` refactored** to consume it: the dart-core in `computePoints` (spatial grid + closures + phase loop + Sobel map, lines 1020–1079) replaced by one `PE.computePoints({...})` call with the DOM plumbing (progress bar, `#vl-total`, `computeToken` cancel) threaded back through `onProgress`/`isCancelled`; `processLuma`/`brightnessMap` inner loop → `PE.*`; `pointRGBA` → `PE.pointRGBA`; local `ranged`/`dropoutSkip`/`strokeCenterline`/`smoothOpenPath`[`D`]/`makeFieldAngle`/`fieldStrokePts` deleted, destructured from `PE` at the top; `pointAngle` (returns **degrees**, callers convert) + `sampleWorkB` (reads Pollen's `fieldB` globals) kept local. **Gate (Pollen's dart-thrower has no seed, so end-to-end byte-identical is impossible):** a **seeded-RNG point-array diff** — `PE.computePoints` vs a verbatim copy of the old inline algorithm, same `mulberry32(12345)` + same field → **438 = 438 points, every `{x,y,b,rnd,ga}` field identical**. Plus functional: all 6 Pollen built-in presets regenerate cleanly (9,454 / 2,744 / 5,679 / 8,170 / 8,197 / 4,639 pts), `buildSVG` → valid 761 KB / 9,166-mark SVG, `render()` repaints, `stopCompute()` works, console clean. **Phase 1 — `mote/index.html`** built from `shared/_template.html` (single file, ~700 lines, matches Pulsar). Shell: 5 shared sheets + `palette.css`, real `.org-header--tool`, `class="org-stage"`, `#panel` 248px, `Organica.autoLabelPanel` (0 unlabeled controls). **Panel redesign:** new **Source** section (File / Webcam / Demo seg + a `enumerateDevices` camera `<select>` shown when >1, Mirror checkbox, Capture-seconds slider); **Preset** on `Organica.presetStore('mote')` + 9 curated human-named built-ins + Save/Delete/name-row; **Coherence** seg relabelled **Fixed grid / Flowing / Re-stipple** (A/B/C) with plain hints, "Re-place points" button (was "Relattice"), Re-stipple carries an offline-preview `.panel-note`; **Flow tuning** (B only) = Gentle/Medium/Turbulent seg over the 5 raw params behind a `<details>`; Stipple / Marks / Colour (`palette.swatch` attach for `ink`/`paper` at `cp-`/`hex-`/`sw-` ids + `palette.swatch($('rmx-palette'),{min:2,max:8})` for the RMX strip) / Tone kept. **Floatbar** Pulsar-shaped: Play/Pause · Stop · **Export** popover (PNG frame · **Record** = wall-clock via `Organica.recorder` · **Render** = the offline `MediaStreamTrackGenerator` + timestamped-`VideoFrame` encoder, kept Mote-local — the only non-realtime encoder in the repo). The prototype's `?stream` synthetic path **dropped** (dev-only, never booted headless); `?demo` / `?cam` boot shortcuts kept. `renderMode`/`sec-b`/`seg-render` and the codec-internals hint copy gone. **Phase 2 — `shared/recorder.js`** gained `bitrate` (`number | () => number` → `videoBitsPerSecond`) and `mimeCandidates` (`string[]`, overrides the built-in list) — fully back-compatible (Pulsar/Camo/Vortex/Membrane untouched when absent); Mote passes an explicit ~0.25 bit/px/frame target + H.264 **High** (`avc1.640028`). **Phase 3 — §6b checklist:** hub `index.html` — `nav__link--mote` added to the **Motion & growth** group (between Mycel and Blob Boundary) + `:hover`/`::after` accent rules, **and the old `nav__link--exp` Mote line removed**; `vercel.json` `/mote/(.*)` rewrite; `scratchpad/mote.html` **deleted** (`scratchpad/testclip.mp4` stays, gitignored); `shared/tokens.json` `color.accent.mote`; `README.md` architecture line; `docs/DESIGN-SYSTEM.md` §5 accent table + Hub-nav-categories row. **Accent `--tool: #2f9e8f`** (sea-green teal, hue ~172) — §6b registry grepped: the Motion band is otherwise all-warm bar Vortex's indigo, and this sits clear of FVS's yellower green `#2c8c62` and Camo Turing's cyan `#21707f`; nav-hover `#22796d` (AA-darkened). **Verified** on the no-store dev server: `?demo` boots (status "demo · work 950×713 · live", ~74 fps, 7,011 pts), all 3 coherence modes animate, Flow Gentle/Medium/Turbulent write the sliders, preset save → `localStorage['organica.mote.presets']` → reload round-trip → delete; `?cam` → getUserMedia denied → "Camera unavailable: Permission denied" (Browser pane has no camera); hub shows Mote in Motion & growth, not Explorations. **Not committed at time of writing** — Diego's branch is `staging`; goes to prod after further refinement. `docs/MOTE.md` deferred (Membrane/Vortex precedent — write once the shape is confirmed). `shared/tokens.json` still missing `pulsar`/`radial`/`blob-boundary`/`rhizome` (pre-existing, not this pass).
  - **Same-session follow-ups (Diego, on `staging`):** (1) canvas surround was an off-system near-black `#14140f` → dropped, `shell.css`'s `var(--panel)` applies. (2) centre `＋` drop-icon was inert → wired to the file picker (Halide/Spore pattern). (3) feature-debug sweep: fixed the Capture readout (`id` vs `data-for`), a camera-denial seg/`source` mismatch, an off-grid Flow-Gentle `b-rate`, and a non-deterministic "Video colour" preset (no `ink`). (4) **Source moved into the floatbar** — `＋` = open file, camera icon = webcam, mirror icon = flip webcam (`aria-pressed`, enabled only while the cam is live); the whole **Demo source removed** (incl. `?demo`, `drawDemo`, the seg); `source` is now `'file' | 'webcam'`. Capture-seconds → a **Length** slider inside the Export popover (`#row-length`, shown only for webcam). (5) the `ms · fps · pts` perf readout moved from a canvas overlay into the floatbar (`#perf`). Panel is now Preset-first (7 sections). Verified: `＋`/drop-icon open the picker, file load (real dims + duration), mirror disabled for file / would enable on a live cam, Export popover carries FPS/Format/Length, perf readout live-updates in the bar, 0 unlabeled controls, console clean. (6) perf trio restacked number-over-unit; **font check** — it was `ui-monospace` (a Manrope-only-rule violation) → `var(--font)` + `font-variant-numeric: tabular-nums`; no monospace anywhere in the tool now. (7) preset `<select>` → a custom listbox with a **live-rendered effect thumbnail** per row (each preset's params run through the real dart-thrower + `paintPoints` on a fixed 120×80 synthetic tone field, cached, lazy on first open).
  - **Sep 1 (still later) — SVG / installation export.** Diego wants Mote for art installations on very large displays (both pre-rendered loops and live/interactive). **`buildMoteSVG(pts, P, W, H)`** — byte-faithful mirror of `paintPoints` (dots → `<circle>`, flow lines → bare `<path>` absolute coords, centreline lines → `<g transform="translate rotate"><path>`), `viewBox 0 0 workW workH` at unit 1 like Pollen's `buildSVG`, `<metadata>{tool:'Mote',coherence,marks,w,h,exportedAt}` (FVS pattern), `mix-blend-mode:multiply` wrap for overpaint. Reuses `E.smoothOpenPathD`/`strokeCenterline`/`fieldStrokePts`/`makeFieldAngle`/`pointAngle`/`ranged`/`dropoutSkip`/`pointRGBA` (all already exported). **`shared/zip.js`** — new: `Organica.zip()` → `{add,blob}`, minimal STORE-only (method 0) + CRC32, no dep (verified: `unzip -t` clean, sig bytes correct, `.svgz` round-trips via `DecompressionStream`). **SVG sequence** — `renderSmooth` refactored to `renderSequence(kind: 'video'|'svg')` sharing the seek/grab/buildField/stepFrameAsync loop; `'svg'` gzips `buildMoteSVG` per frame (`CompressionStream`, guarded) → `frames/frame-NNNNN.svgz` + `sequence.json` in the zip → `mote-seq-*.zip`. **Size control** — `EXPORT_LONG 1920` → `EXPORT_BASE 1920` / `EXPORT_HARD 7680`; a Size `<select>` (HD→8K) in the popover drives `sizeExportCanvas` (upscale now allowed — geometry re-renders crisp) + the `#out-size` readout; video bitrate cap `90e6 → 240e6`. **Kiosk mode** — `body.kiosk` class + Fullscreen API (`toggleKiosk`, header button + `k`), hides header/panel/floatbar, canvas on black, auto-plays; `fullscreenchange` drops the class on Esc. `video.play()` calls got `.catch(()=>{})` (benign autoplay-interrupt rejection). **Verified** on the no-store server with `scratchpad/testclip.mp4`: SVG frame = 2740 `<circle>` for 2740 pts, `viewBox 0 0 950 711`, metadata parses, rasterise-vs-canvas diff 1.3% (temporal, structurally faithful); line presets emit both `<path>` shapes correctly; SVG sequence → 298-frame / 27 MB zip with manifest; Size readout `3840 × 2872` / `7680 × 5744` / `1920 × 1436`; kiosk hides all chrome + black bg + toggles back; PNG/coherence/preset-thumbnails regression-clean; 0 unlabeled controls; console clean. Docs: `docs/SHARED-COMPONENTS.md` §3 (`Organica.zip`), `docs/UI-SHELL.md` §7, `docs/ROADMAP.md` Phase 6.
  - **Sep 1 (still later) — the SVG sequence was a dead end; replaced by composited multi-size video.** Diego's first real sequence export (`/Users/diego/Downloads/mote-seq-1788290182385/`) was **382 MB for ~3 s** — 89 frames, a line preset → **53,720 `<path>` marks / 17 MB of SVG per frame**; a single frame hangs macOS Quick Look; no mainstream compositor ingests an `.svgz` image sequence. SVG-per-frame doesn't scale to real stipple density. Diego wants **finished playable files**, not a sequence to assemble. **Removed:** `renderSequence('svg')` branch, `gzipBytes`, the "Sequence" popover block, `<script src="/shared/zip.js">`, and **`shared/zip.js` itself** (its only consumer). The single-frame **SVG** stays (posters/print). `docs/SHARED-COMPONENTS.md` §3 `Organica.zip` → backlog "candidate util". **Added — `renderJobs(jobs)`** (the old `renderSequence('video')` generalised): a **Video — batch** popover block — `sz-master` (source aspect · Size) + 5 `sz-*` checkboxes over **`MOTE_SOCIAL`** (`1x1` 1080², `9x16` 1080×1920, `4x5`, `16x9`, `3x2` — the Membrane/Loom/Vortex `CANVAS_PRESETS` names + 9:16) + a **Crop / Fit** seg → **Export batch** renders each ticked size as a full independent pass: `cropFor(outW,outH)` centre-crops the source for that aspect (or whole-frame + letterbox for Fit), a job-local working res (`≤ Detail`), fresh `computePoints`, its own `MediaRecorder` → one `mote-<label>-*.mp4`. `grabFrame(crop)` gained a source-rect arg; `paintPoints` gained `opt.offX/offY` (Fit letterbox translate, wrapped in `save/restore`). Jobs **hijack + restore** the module render globals (`workW/workH`, `frameCanvas` dims, `basePoints`/`points`/`targetCount`, `curField`, `lastPaintedPts`) and repaint the stage after. `renderSmooth`/`renderBatch` now `return renderJobs(...)` (the missing `return` detached the first test run). **Verified** on the no-store server with `scratchpad/testclip.mp4`: batch of Master + `9x16` Fit → `mote-master-*.mp4` (1920×1436, 2.4 MB, `avc1.640032`) + `mote-9x16-*.mp4` (1080×1920, 965 KB, `avc1.640028`) — real H.264-High bytes, exact `VideoFrame` dims per job; Fit letterbox `offY 556`, bar pixels == Paper `#f5f2ec`; Square 1:1 → 1080×1080; live `workW/workH` (950×711) restored, plays after; popover has no "Sequence"/`.zip`/`.svgz` wording, no `zip.js` script; 0 unlabeled controls; console clean (only the env 404). Docs: this note + Tools row, `docs/UI-SHELL.md` §7, `docs/ROADMAP.md` Phase 6 (Social sized presets → done).
  - **Sep 1 (still later) — WYSIWYG fixes on the batch export** (Diego: "l'export non è WYSIWYG"). A `mote-master-*.mp4` from an 800²-ish source rendered a **denser, finer stipple than the preview**. Two causes: (1) **`applyPreset` never re-latticed** — it wrote `sl-sp-*`/`sl-phases`/`sl-detail` but only called `reinit()` when *detail* changed, so a preset that keeps the default Detail but tightens spacing left `basePoints` at the *previous* lattice (e.g. 2,740 pts on screen while a fresh `computePoints` with the new sliders gives ~7–15k). Fixed: `applyPreset` now `reinit()`s on any source (`workW` truthy) — a preset shift always rebuilds the lattice. (2) **`renderJobs` derived the job working res as `min(outW, detail)`** — which *upscales* past a small source's pixels, unlike `onReady()`'s `workDims(srcW, srcH, detail)` (`Math.min(1, …)`, never upscales). Fixed: jobs now use `workDims(crop.sw, crop.sh, detail)`, matching the live pipeline exactly. Plus: for a **Fixed-grid (A) master** whose framing == the live preview (Crop, dims equal), `renderJobs` now **reuses the live `basePoints` verbatim** instead of re-seeding from frame 0 → the export is byte-identical to the on-screen stipple (modes B/C, Fit, and social crops still re-lattice their own framing — B/C mutate `points` in place so a shared ref would corrupt the snapshot). **Verified:** after `applyPreset('Fine dots')` the preview `pts-hint` jumps 2,740 → 7,190 and the master export reuses that same 7,190-point lattice (0 `computePoints` calls in the job); a same-frame, same-resolution pixel diff of the export's frame 0 vs a reference `paintPoints` render = **0.000%**. Social 9:16 still re-latices (`computePoints` called). Console clean.

- **August 31, 2026 (still later same day) — Genesis Library: real Sets management + Source filter removed + view persistence.** Diego, after "valuta come poter gestire al meglio il management dei sets sources and type": all four threads, Source **removed** (no replacement), deleting a set **deletes** its now-orphaned `user-*` seeds, sets-management UI on a **dedicated row** (not a popover). `genesis/index.html` only. **A — Sets CRUD:** new `.lib-sets` row (its own row above `.lib-filters`) holds the `Sets` label + `#sets-seg` chips (moved out of `.lib-filters`) + `#btn-new-set` (`+`) + `#btn-manage-sets` (`⚙` toggle). `⚙` un-`hidden`s `#sets-manage-list` inline below the chips: `renderSetsManager()` builds one `.sm-row` per set — built-in "Base Seeds" is label-only; user rows get `.sm-name` (click → transient `<input class="set-name-input">`, same commit/Esc/blur as `showNewSetInput` → `renameSet`), `↑`/`↓` (`reorderSet`, `disabled` at first/last user row), `×` (`armDeleteSet` — 2-click armed, `.is-armed`, 3s disarm → `deleteSet`). `renameSet` rejects empty + case-insensitive dup names; `reorderSet` array-swaps in `state.library.sets` clamped to index ≥ 1; `deleteSet` removes the set, then `state.library.forms = filter(f => !(member && in no surviving set))` (orphan-delete), falls back to `BUILT_IN_SET_ID` if the active set went. `renderSetsList()` calls `renderSetsManager()` at its tail so the chip bar + panel never drift. `onSetsManagerRowClick` is a `#sets-manage-list` delegate for `↑↓×`; rename is the inline-input path. `migrateLibrary` gains one line — pin the built-in to `sets[0]` for older blobs (`bi > 0` guard = idempotent, no new set field). **B — seed↔set membership:** new `#edit-sets` div in the Edit header (the old read-only "In sets: N" `<dl>` row dropped from `syncEditPanel`). `renderEditSets()` — for a `user-*` seed, a checkbox per non-built-in set (checked = member); `toggleMembership(setId, formId, on)` `push`/`splice`s `set.forms` directly (no mint), guarded so unchecking a seed's **last** membership is blocked with a status pointing at Delete. Built-in seed → count + "Save a copy" hint, no checkboxes. Wired via an `#edit-sets` `change` delegate in `bindCreatorControls`. **C — Source removed:** the whole Source `.lib-filters__group` deleted; `state.filter` → `{ type }`; `filteredForms()` collapses to the Type predicate. `.lib-filters` now holds only the Type seg — `syncFilterUI` / the seg click delegate are already generic and unchanged (the `key === 'set'` guards go dead-but-harmless). **D — Type pills + view persistence:** `syncTypePills()` writes `#type-pills` `.active` from `S.type` (fixes the latent stale-pill bug), called from `resetCreate` + `loadSeedIntoEdit`. New key `organica.library.view` (`{activeSetId, filter}`) via `saveView()` / `loadView()` (value-whitelisted on read via `TYPE_VALUES`, separate from `STORAGE_KEY` so a corrupt view blob can't hurt library data); `loadView()` added to `init()` right after `loadLibrary()`; `activateSet` **stops wiping `state.filter`** and calls `saveView()`; the filter delegate calls `saveView()`. **Verified** on the no-store dev server + `node -c` clean: `⚙` expands the panel (Base Seeds first, controls-less; Diego Seeds `↑` disabled, My Seeds `↓` disabled); `renameSet` ok / dup-rejected / empty-rejected; `reorderSet` moves the chip **and** `#ctrl-set` option order, built-in stays first; `deleteSet` keeps a seed still in another set, orphan-deletes one that isn't, deleting the active set → Base Seeds; `migrateLibrary` idempotent + `sets[0].id === 'organic-forms'`; membership checkboxes add/remove without minting (chip count tracks), last-membership uncheck blocked, built-in shows only the hint; Source seg gone (`.lib-filters .seg` count 1); type pill syncs on Edit-load + Create-reset; reload restores `{activeSetId:'set_my-seeds', filter:{type:'asset'}}`, corrupt view blob doesn't throw, set switch no longer resets the Type filter; `saveForm` + `duplicateFromEdit` regression-clean. All test residue reverted — Diego's real library (Base / Diego Seeds→`user-…383032` / My Seeds→`user-…358118`, original order) restored, `organica.library.view` cleared. CSS: `.lib-sets` + `.lib-sets__bar` + `.seg--sets` re-scoped (was `.lib-filters`-scoped), `#sets-manage-list` / `.sm-row*`, `#edit-sets` / `.es-row` — tokens only. Docs: `docs/UI-SHELL.md` §Genesis, this note + Tools row. **Not committed.**

- **August 31, 2026 (later same day) — Genesis: mode nav split between the Library page and the floatbar.** Diego: Library/Create as buttons **above the filter bar** in the Library page; Create/Edit navigation **in the floatbar**; Edit gets no standalone tab (you still enter Edit by clicking a seed). Header `.org-header__context` (the old `#mode-tabs` with Library/Create/Edit) removed — the header is now just the logo. `#mode-tabs` moved into a new `.lib-modenav` above `.lib-filters` (shared `.org-tabs` segmented control, **Library / Create** only). The **floatbar** (`data-modes="create edit"`) rebuilt to one canonical order: **Create · Edit · Save · Back · Delete · Library · Export**, with context extras slotted by owner — Import (Create) after Create, Duplicate (Edit) after Edit, Clear (Create) after Back. New floatbar buttons: `#fb-mode-create` / `#fb-mode-edit` (disabled until `S.editForm`) / `#fb-mode-library` (all carry `data-mode`, so `bindTabs`/`setMode` now query `document.querySelectorAll('[data-mode]')` not `#mode-tabs`); `#btn-delete-fb` — the panel's `#edit-delete` moved here as a two-click armed icon (`.is-armed` tint + aria-label swap, 3s disarm), shown only for a non-built-in seed in Edit; `#btn-export-svg` — new, downloads the current seed as a standalone `.svg` (`<svg viewBox="0 0 200 200"><style>svg{--ink:#0a0a0a}</style>…`), disabled when `!formMarkup()`. `#btn-undo` relabelled aria "Back" (behaviour unchanged — Paper undo, shown for a live canvas only). Seps renumbered `fb-sep-1..4` (`fb-sep-3` gated on `canDelete`). `syncEditPanel` lost its dead `$('edit-delete')` line (Delete visibility is `syncFloatbar`'s job now). CSS: `.lib-modenav { padding: 14px 20px 0 }`; `.org-floatbar__btn[aria-pressed="true"]` given the ink-fill active look (matches the nav tabs). **Verified** on the no-store dev server: Library shows LIBRARY/CREATE above the filters, header logo-only; click a seed → Edit, floatbar order Create·Edit·Save·Back·Delete·Library·Export with Import/Duplicate/Clear hidden per mode; built-in seed → Delete + `fb-sep-3` hidden, user seed → both shown; parametric seed → no Back (no live canvas), vector seed → Back shown; Save disabled until a slider/anchor edit flips `S.dirty`; Export produces valid standalone SVG (`Organica.downloadText`, no console error); `fb-mode-library` returns to Library, `fb-mode-create` runs `resetCreate` (blank Freehand, Edit re-disabled). Console clean bar the pre-existing `_vercel/insights` 404. Docs: `docs/UI-SHELL.md` (§Genesis three-modes + `--editor` row), CLAUDE.md Tools row + this note. `node -c` clean. **Not committed.**

- **August 28, 2026 (still later same day) — Radial: two presets that were namesakes only, fixed against the live originals; +1 bonus preset.** Diego asked to re-verify `chaos_circles` and `line_based_circles` against Book of Shapes. Both were **name-only** — the preset rendered a different image. `line_based_circles` (real: ~8 concentric circles each *deconstructed* into short disconnected **tangent** dashes, plotter-art look) was shipping as connected jittered 40-gon `<polygon>` rings. `chaos_circles` (real: ~83 large, near-concentric noise-perturbed circle *outlines* piled into a tangle, own noise phase each) was shipping as a jittered lattice of small `<circle>` marks. Two fixes, one pass: **(1)** added a **Line direction: radial | tangent** option to Marks mode (one `+π/2` on the segment angle, in `markSVG` + `drawSceneCanvas`) — a grid of tangent line-marks *is* line-based circles; verified tangent measures 90° to the radius, radial 0°. **(2)** new **`loops` render mode ("Chaos circles")** — draws `Circle count` closed loops, each a 72-pt circle of `Circle radius`, edge wobbled by `Organica.noise.simplex2` with a per-loop phase + random rotation + slight radius spread + gentle centre drift from a dedicated `mulberry32` stream (`seed * 40503`) so they don't nest; params Circle count / Circle radius / Noise. First tuning (`radius 0.5`) read as a cramped wool-ball donut — Diego flagged it; bumped to `radius 0.85 / noise 0.16` for the reference's big-overlapping-circles-with-clean-centre look. Both presets rewritten to the new modes. Diego also liked the *old* Line-Based Circles look, so it's kept as a **"Jittered Rings"** preset (17 total). Group-C audit result: Spiral Dot Field + Halftone Sphere were already faithful; these two were not. Verified: all 17 presets render + WYSIWYG true, loop seed reproducible, 0 unlabeled controls, zero console errors on a fresh tab. `radial/index.html` only. Shipped to production.

- **August 28, 2026 (still later same day) — FVS Symbols: a generative per-cell rule layer, from `bookofshapes.com/patterns/concentric_arc_truchet_3` ("Arc Truchet Butterfly").** Diego: that tool (a Cols×Rows grid, one arc motif per cell, per-cell rotation 0°/90° from a `col + rowShift·row` oscillator) is basically FVS's Symbols model — "in teoria abbiamo tutto" — asked to evaluate integrating it into Symbol definition and to make Symbol authoring "molto più veloce e generativa", so a Loom grid becomes a new editable FVS instance while Components stay fixed. Two Explore agents mapped it: FVS Symbols already imports Loom grids and has full per-cell editing, but authoring is manual-cell-by-cell or a 50/50 random `Generate` — **no rule engine**. Components' own `FAMILIES` rules are all 2×2-only (hand-derived 4-cell literals), not reusable for N×M. **Loom already publishes every saved grid to `localStorage['organica.loom.presets']`** as a `buildModel()` object → directly `Organica.loadLoomGrid`-able; FVS just never read it. Decisions (Diego, via AskUserQuestion): full rule set; support any Loom grid (rect via native `col/row`, regular polygon via centroid binning) + a direct saved-grid picker; rule apply is lock-aware **and** has a hard "Reset & apply to all". **Out of scope, stated**: the concentric-arc "butterfly" Element itself — that's `2 × Arc Count` stacked semicircle paths per cell; FVS renders one path / one fill per cell and reuses one Element per Component, so it needs a new multi-ring `wedgePathD` geometry + cell shape-stacking. The rule layer works with whatever Element/Component is already the cell content. **Shipped in `fvs/index.html`**: `#sel-symbol-fill` gains a **Rule** mode with `#symbol-rule-block`; **`SYMBOL_RULES`** registry (`oscillator`/`checkerboard`/`rows`/`columns`/`radial`/`wave`/`random`), each `{read(), fn(ctx,p,rng) → {rotation?,flipH?,flipV?,scale?}}` — N×M-general, per-cell; **`cellColRow(grid)`** — the genuinely new helper: rect Loom cells read `col/row` from the JSON + `grid.params.cols/rows`; polygon cells bin `resolveGridCells` centres into sorted y→row / x→col bands; always also fills `cx,cy,nx,ny,angle` so Radial/Wave/Random work on any grid; **`applySymbolRule({resetAll})`** mirrors `generateSymbolCells()` — rebuilds only each unlocked cell's transforms from the rule, never touches `source`/content/`fitMode`/anchor/padding; **Vary** checkboxes (Rotation/Flip/Scale) gate which axes the rule writes; a per-cell **`locked`** flag (checkbox in the Cell-properties panel) makes `Apply rule` skip a hand-edited cell, `Reset & apply to all` ignores it; **direct Loom-grid picker** `#sel-symbol-loom-preset` reads `Organica.presetStore('loom')` (repopulated on tab switch) + the two built-ins, so a grid designed in `/loom/` is one click, no JSON upload; **"Apply to all cells"** toggle in the Choose-content overlay + a `Set content for all cells…` button in the rule block → `contentTarget()` routes tile-picks to every cell instead of the selection, so one Component tiles the whole grid then the rule varies it (~4 clicks to a full generative Symbol); the rule state (`RULE_CONTROL_IDS`) is saved into `buildSymbolLibraryEntry().rule` and restored by `applyRuleState()` in `applySymbolLibraryEntryToUI` — additive, old entries without `rule` still load (cells are also stored resolved). **Verified live** (2×rAF awaits): Bento grid → Rule → Oscillator → rotations `[0,0,90,0,0,0,90]` from real `col/row`, Row shift live-changes the flow; Arc content for all + oscillator renders the flowing quarter-circle Truchet composition (screenshot); Hexagonal (17 polygon cells) → centroid binning gives cols:3/rows:5, oscillator produces only `{0,90}`; every rule fills with the right distinct-rotation count; Vary Rotation-only leaves flip/scale byte-identical, enabling them changes them; a saved Loom grid appears in the picker and loads as a new instance; lock survives a rule re-apply, "Reset & apply to all" overrides it; save→mutate-UI→`applySymbolLibraryEntryToUI` restores rule+fill+params+cells; **WYSIWYG** — `buildSymbolSVG()` and the rendered DOM have identical element counts (svg/metadata/defs/clipPath×12/rect×25/g×36/path×12) and byte-identical first-path `d` (the `!==` on raw strings is browser `innerHTML` re-serialisation, same artifact as Radial); Components tier + Symbols Generate/Manual modes regression-clean; 0 unlabeled controls; zero console errors on a genuinely fresh tab. `fvs/index.html` only. Shipped to production.

- **August 28, 2026 (still later same day) — FVS: the "Arc truchet" Element, the piece flagged out of scope in the rule-layer pass, built.** Diego: "let's work on the Out of scope — the concentric-arc butterfly Element itself." The earlier note said it needed "cell shape-stacking" (one FVS cell holds one path) — that turned out to be avoidable: `arcTruchetPathD(count, ratio)` returns **one compound `d`** (2 fans × `count` closed annular half-rings from the top & bottom edge midpoints, non-overlapping so no `fill-rule` needed), which drops into FVS's existing one-`<path>`-per-cell model unchanged. Inscribed exactly in the 0–100 box (max radius 50, the fans meet at the centre — `getBBox` → `{0,0,100,100}`), so **no per-cell clip is needed** anywhere (Element preview strip, Components gallery, Symbols). **One real bug, caught by `getBBox` not by eye**: the first sweep-flag choice put both fans bulging *outward* (bbox `y:-50 h:200`, overflowing the box by 50 each side) — SVG's `sweep=1` on a left→right arc bulges UP in y-down space, and the inner arc (right→left) takes the opposite flag; fixed and re-verified bbox is exactly the unit box. Wired through: `SEED_TYPES.arctruchet`, `#sel-seed-type`, a `#seed-arctruchet-block` (Arc count 3–12 / Arc ratio 10–95%), `getSeed()` (ratio stored 0–1), `syncSeedUI()`, the two sliders' listeners, `buildLibraryEntry`/`applyLibraryEntryToUI` round-trip, and the Symbols tier — `buildSymbolItems` + the Choose-content overlay + `generateSymbolCells` all get a fixed `SYMBOL_ARC_TRUCHET = {arcCount:5, arcRatio:0.7}` (Symbols cells carry no per-seed params, same as they already ignore Base/Height/Thickness). Verified: preview strip shows the butterfly at 0/90/180/270° + flips; Components + Radial rule → concentric-arc mandalas; **Symbols + built-in Bento + Oscillator rule → a faithful `concentric_arc_truchet_3` reproduction** (screenshot); 7 `<path>` for 7 cells, WYSIWYG element counts match; arc-param save/reload round-trips (0–1 internal form); Triangle/Arc seeds unaffected; 0 unlabeled controls; zero console errors on a fresh tab. `fvs/index.html` only. **Not committed — waiting for an explicit "porta in prod".**

- **August 29, 2026 — Shipped Pulsar (`/pulsar/`), the generative Motion tool over Radial, plus `shared/radial.js`.** Diego asked for a Motion tool that animates "some of Radial's parameters", with an analysis of what's possible. Explore + Plan agents first; decisions via AskUserQuestion: name **Pulsar**; **extract** Radial's geometry core to a shared module (not fork); **lean v1** — parameter tracks + 4 Motion modes + whole-field rotate/zoom + Video & PNG-frame export + 8 scenes, deferring a Soul-style per-primitive stagger/reveal layer and SVG-frame export; and confirmed the other deferrals (Soul-engine DrawSVG/MorphSVG per-primitive effects — a Radial export dropped into Soul already covers those; true animated `.svg`; a Rhizome node; `jitterPhase` line-boil). **Architecture (the analysis's conclusion)**: Soul can't regenerate geometry, so bloom / warp-flow / petal-morph / threshold-dissolve are all outside it — Pulsar instead drives Radial's camelCase `P` with time-varying tracks and calls `Organica.radial.buildScene` → `drawSceneCanvas` every frame (Vortex's redraw-from-scratch model; `buildScene` re-seeds its own RNG from `P.seed` each call, so no drift/accumulation). **`shared/radial.js`** (extracted at the second consumer, same as `noise.js`/`motion.js`): `warpPoint`/`buildPoints`/`arcPathD`/`buildScene`/`sceneSVG`/`drawSceneCanvas`/`BP`/`BUILTIN_PRESETS` moved verbatim + a new `PARAM_RANGES` map. Four inert hooks added, each an exact no-op when unset: `+ (P.warpPhase||0)` on the warp noise-domain offset (field flows), `+ (P.fillPhase||0)` on the fill offset (dissolve wave), `+ (P.loopPhase||0)` inside the chaos-circle noise (tangle writhes), and a `P.rotate`/`P.zoom` group transform in `drawSceneCanvas`/`sceneSVG` only. `markSVG`'s hardcoded `SIZE/2` mark-angle centre parameterised to `prim.W/2, prim.H/2` (identical at 760). `radial/index.html` now just aliases `Organica.radial.*` and keeps its DOM glue. **Verified byte-identical**: captured `currentSVG()` (length + hash) for all 17 Radial built-in presets on `main` before the refactor, re-ran after — **0 mismatches**. **Track model**: `{param, wave, amount, cycles-per-loop, phase, ease}`. `wave` = sine / triangle / looping-noise (circular-sampled `fbm(cosθ, sinθ)` so integer cycles/loop return exactly — Komorebi's single-phase trick) / ramp-once / spin. `amount` is in the parameter's own units, added to the base and clamped to `PARAM_RANGES` (rounded for int params); the amount slider's own range follows the parameter (`count` needs ~760, a fraction ~1) via `amountRangeFor()`. `cycles/loop` is a 1–4 integer so periodic tracks always close the loop; a scene with any `ramp-once` or phase-`spin` track shows a "one-shot — will not loop" note in the panel and the loop popover. Whole-field `rotate` is a `<g transform>` / `ctx.rotate` (the one track later expressible as animated SVG), not baked into every point. **Motion modes** (`applyMode`) fill the 4 track slots + whole-field + loop length, **re-mapped per placement/render** so no mode leaves a dead control — a real bug caught in verification: Drift's default `warpPhase`/`warpAmt` tracks are inert on `arcs`/`loops` renders (which never touch the warp pipeline), and Bloom's `rings` ramp is inert on `loops`; fixed by giving arcs (`arcSpan`/`arcGrowth`/`arcGap`…) and loops (`loopPhase`/`loopCount`…) their own mode track sets, and adding `loopCount` to the Chaos-circles param group (it was missing, so `writeTracks` silently dropped the Bloom track). **Gravity deliberately excluded** — a single global directional pull destroys the centre symmetry that defines a radial field. **Verified end to end** on a fresh no-store server: all 8 scenes render + move, 7 seamless + Phyllo Bloom correctly flagged one-shot; every Motion mode × 9 placement/render combos writes tracks and produces visible motion; PNG frame exports (555 KB at ×2), Video exports a real 3.9 MB `video/mp4;codecs=avc1` that auto-stops at the loop length; Play→Pause (freezes in place) → Play (resumes, no jump) → Stop (frame 0); element guard fires at 7323 el (mesh 72×50 + diagonals), CEIL 6500; zoom/pan CSS transform doesn't touch the PNG bitmap; 60–62 fps on both a light (rings) and mid-weight (fill, ~1500 cells) scene; 0 of ~40 controls unlabeled; console clean (only the pre-existing Vercel-insights 404). Radial re-regressed byte-identical again after all Pulsar edits. Hub nav ("Motion & growth" group, accent `#dd5140` / nav hover `#c4402f`) + `vercel.json` rewrite added. `docs/PULSAR.md` not written (optional; this note + the file's own header comment cover it). Deferred, not built: the Swarm stagger-reveal layer, SVG-frame export (trivially `sceneSVG` + the whole-field `<g transform>`), animated `.svg`, a Rhizome node, `jitterPhase`. **Follow-up same session** (Diego: "verifica che sia il componente Palette centralizzato"): the colour section was first a verbatim copy of Radial's older `syncColor` + bare `<input type=color>` markup (the pre-`color-swatch-wrap` pattern whose latent popup-anchor bug `panel.css` documents). Re-done properly — split into a `Palette` section (**`Organica.createColorSwatch('ink'|'paper', …)`** from `core.js`, the same single-swatch component FVS/Vortex/Colornet use, with `.color-swatch-wrap` markup) + a `Base — seed & jitter` section; local `syncColor` and the local `input[type=color]{…}` hack removed. RMX chips (`createPaletteChips`) deliberately not used — a radial field draws in one ink, `sceneSVG` takes one `P.ink`/`P.paper`. Verified: swatches paint, both edit paths (hex field / native picker) update `buildParams().ink/paper`, scene colours round-trip through save/load, 0 unlabeled, console clean.

- **August 30, 2026 — Palette code centralised into one component: `shared/palette.js` + paired `shared/palette.css`.** Diego: "voglio che tutti i componenti Palette siano in un unico file, serviti diversamente attraverso tutti i tool — iniziamo a centralizzare funzioni." Three Explore agents mapped the state: the RMX chip *builder JS* was already de-duplicated into `organica-palette-chip.js` (7 consumers) but its header still claimed otherwise; `createColorSwatch` lived in `core.js` (5 consumers); `Organica.Palette.colorAt` in `palette.js` (2 consumers, Pollen/Spore); the `.rmx-*` CSS was **not** shared — 5 byte-identical `<style>` copies + a divergent TuneSutra 30×26 variant + Vortex additionally re-declaring `.color-*` at 26×22; `mulberry32` triplicated. Decisions via AskUserQuestion (all after a first pass): **consolidation only** (no tool behaviour change), **merge** the single-swatch and RMX-chip UIs into one polymorphic **`Organica.palette.swatch(target, opts)`** — `target` string ⇒ ATTACH mode (wire pre-existing `#cp-`/`#hex-`/`#sw-<prefix>`), `target` element ⇒ GENERATE mode (build `.rmx-color` chips; `opts.max>1` = RMX strip); both shapes return the **same** object `{get,set,getColors,setColors(arr,{notify}),setActive(i),rebuild}`; **paired CSS file** `palette.css` (not folded into `panel.css` — `.color-*` stays there, universally linked & documented in `/design-system/`). Additive `chips` improvements: `setColors(arr,{notify})` (default off), `setActive(i)` + module-managed `.chip-active` (absorbs TuneSutra's `wrap.children` reach-in). `P.mulberry32` deleted (0 callers) → use core's. Back-compat: `Organica.createColorSwatch` / `createPaletteChips` / `Organica.Palette.colorAt` are thin aliases delegating to `Organica.palette.*` — **every existing call site untouched** (~14 of them); `createColorSwatch`'s legacy `onChange(hex, rgb255)` 2-arg signature preserved by attach mode. `organica-palette-chip.js` **deleted**; its 7 `<script>` tags repointed to `palette.js`; the 7 tools that use generate mode add `<link palette.css>`; pulsar + colornet (attach mode, loaded bare core) gained `<script palette.js>`. Local `<style>` cleanup: deleted the 5 identical `.rmx-*` + 5 `.icon-btn` copies; Spore/Pollen add `#rmx-palette{--rmx-palette-mb:0}` (their strip has no bottom gap); Vortex keeps its `.color-*` overrides (→ Phase 2); TuneSutra keeps its rounded flex-centred `+`/`×` local, tunes the cell via `--rmx-cell-w/h`/`--rmx-cell-radius`/`--rmx-cell-border`; FVS keeps `.fvs-library-item .rmx-x`. Header comment `*/`-in-block-comment bug caught by `node -c`. **Verified** on the no-store server: all 7 chip consumers (camo-turing/membrane/vortex/fvs/tunesutra/pollen/spore) — strip renders same chip count + 36×28 cell (TuneSutra 30×26 rounded `border-strong`, chip-active outline in `--tool`), `+`/`×` add/remove within min/max, chip background repaints on recolour; all 5 attach consumers (membrane/vortex/fvs/pulsar/colornet) — swatch edits repaint `sw-*` and update tool params, 2-arg `onChange` still delivered; `Organica.palette.colorAt` byte-identical across tone/posterize/random/tonernd + solid/adaptive/mix, alias `=== `; Vortex `.color-swatch-wrap` still 26×22 (untouched); zero `<script src=…organica-palette-chip.js>` anywhere (5 remaining string matches are deliberate history prose). `docs/UI-SHELL.md` §2 + §7, `design-system/index.html` (new "Palette — RMX chip strip" section + TOC), CLAUDE.md Repo Structure updated. **Deferred (Phase 2):** migrate the 8 hand-rolled `syncColor` tools to `palette.swatch` attach mode (Komorebi/Warping/Radial/Camo/Halide/Blob + Spore/Pollen `swatch-`→`sw-` id rename); migrate the ~14 alias call sites to `Organica.palette.*` then delete the aliases + core breadcrumb; TuneSutra → `setActive`/`notify`; port Membrane `js/color.js rmxColorAt` + Camo Turing's JS SVG-export path → `palette.colorAt`; reconcile Vortex's `.color-*` overrides; `mulberry32` 3rd copy in `transformer.js`; livingpath bare-input + tunesutra RGB/HSB editor. **Not committed** — pending Diego's "porta in prod" (same branch as the still-uncommitted Pulsar work).

---

- **August 30, 2026 (still later same day) — Palette consolidation Phase 2: every colour-picker tool now on `Organica.palette.swatch`, aliases removed; new `docs/SHARED-COMPONENTS.md` captures the reusable pattern.** Diego: "salva la tabella come repository e piano di quello che dobbiamo fare. come approccio lo useremo su altri moduli. procedi alla phase 2." **New doc** `docs/SHARED-COMPONENTS.md` — the 8-step centralization playbook (inventory → canonical home → one polymorphic entry point → back-compat aliases → repoint+delete → docs → verify zero-diff → Phase 2 remove aliases), the Palette adoption table, and a backlog of the same-shaped concerns (zoom/pan inline copies, `mulberry32` ×3+9, Seeds panel, `.upload-btn` drop-zone, floatbar `captureStream` recorder). **Wave A** (`sw-`-id tools, byte-parallel `syncColor`): komorebi (4 scene colours), warping, radial, camo-turing (keeps its `THREE.Color` uniform push in `onChange`), blob-boundary (sets `bgRect`/`boundary` SVG `fill` in `onChange`) → `Organica.palette.swatch('<prefix>', {initial, onChange})`, markup wrapped in `.color-swatch-wrap` (fixes the latent popup-anchor bug), local `syncColor` + `input[type=color]{…}` hack deleted, `<script palette.js>` added. **Real bug caught in verification**: radial + warping threw `Cannot access 'renderQueued' before initialization` — the `swatches` block sat where `syncColor` used to (mid-script), and `initial:` → `onChange: scheduleRender` → `if (renderQueued)` hit the `let` in TDZ; fixed by moving the `swatches` population into the INIT block (after `scheduleRender`/its vars), keeping only `const swatches = {}` at the old spot. Now a documented rule in SHARED-COMPONENTS.md. **Wave B** (`swatch-<x>` → `sw-<x>` id rename): halide (`ink/paper/bgfill`, `swapColors()` → `swatches.ink.get()/.set()`), spore (3-arg `syncColor`/`openColorPicker`/`syncHex` deleted; label after the hex field kept), pollen (`setColorAll`/`randomColor`/`COLOR_IDS` deleted; `.ctrl-label` → `.color-name`; random buttons → `#btn-random-<x>` id so the component wires them). **Wave C** (retire aliases): ~14 call sites migrated — `Organica.createColorSwatch(…)` → `Organica.palette.swatch(…)` (membrane/vortex/fvs/pulsar/colornet), `Organica.createPaletteChips({wrap:X, …})` → `Organica.palette.swatch(X, {…})` (camo-turing/membrane/vortex/fvs/tunesutra/pollen/spore), `Organica.Palette.colorAt` → `Organica.palette.colorAt` (pollen/spore); then the whole alias block deleted from `palette.js` + the core breadcrumb trimmed; `Organica.Palette.mulberry32` dropped (grep: 0 callers). **Verified** on the no-store server, all 14 tools: `Organica.createColorSwatch`/`createPaletteChips`/`Organica.Palette` all `undefined`, no tool throws; attach swatches paint from `initial` + edit repaints `sw-*` + tool side effects fire (camo-turing render goes mint-green on ink edit — screenshot; blob `bgRect`/`boundary` fills track; halide `swapColors` swaps); RMX strips add/remove within min/max + repaint on recolour (TuneSutra 30×26 rounded, `(colors,index,kind)` onChange + `highlightActiveChip` still work; fvs min 1); `palette.colorAt` byte-identical (tone `#808080` at 0.5); radial 4-preset regression BYTE-IDENTICAL; 0 console errors beyond the pre-existing environment 404s. Docs: `docs/UI-SHELL.md` §7 `syncColor` note rewritten (resolved), `CLAUDE.md` Repo Structure + this note. **Export check** (Diego: "fai un check sull'export"): drove real SVG/PNG exports on the migrated tools — camo-turing SOLID SVG carries `hex-ink`/`hex-paper`, RMX SVG carries exactly the chip colours (`buildRandomGridSVG(…, rmxColors)` — `rmxColors` stays synced via the migrated `onChange`); radial SVG (ink→stroke, paper→`<rect>`); halide SVG (real dithered portrait, only the 2 swatch colours); fvs component SVG (paper + the 3 cycled palette colours); pollen SVG (`fill="#bg"` + `fill="rgb(255,34,0)"` for the alpha'd point colour); spore colour plumbing + `palette.colorAt` mid-stop. **No export regression.** **TuneSutra Phase-3 done**: `highlightActiveChip()` → one-liner `chips.setActive(state.activeIndex)`; the `rmx-palette.children` reach-in gone, `.chip-active` survives add/remove rebuilds (verified: active follows to the new chip on add, clamps on remove). **Still Phase 3** (own sessions, need output/screenshot sign-off, tracked in `SHARED-COMPONENTS.md` §3): Membrane `rmxColorAt` (its `tonernd` smooth-lerp vs `colorAt`'s discrete stop) + Camo Turing JS-export `rmxLerpColor` (THREE.Color linear-space lerp vs `palette.mix` sRGB) → `palette.colorAt`; Vortex `.color-*` 26×22 → shared 18×18; livingpath bare `<input>` + read-only span → `.color-row` + `palette.swatch`. `transformer.js`'s `mulberry32Fallback` is deliberately kept (guarded `Organica.mulberry32 ? … : fallback`).

- **August 30, 2026 (still later same day) — Palette Phase 3 finished.** Diego: "andiamo avanti con la phase 3". **Vortex** — deleted its local `.color-row`/`.color-name`/`.color-swatch-wrap`(26×22)/`.color-hex`(70px mono) overrides; the Background row now uses the shared 18×18 `.color-*` + Manrope hex field, consistent with every other tool (before/after screenshots; bg swatch edit + `#btn-random-bg` still work, canvas bg tracks). **livingpath** — the bare `<input type=color id="cInk">` + read-only `.val` span became a real `.color-row` (`sw-ink`/`cp-ink`/`hex-ink` editable) driven by `Organica.palette.swatch('ink'|'bg', {initial, onChange: repaint})`; `id` renames `cInk`→`cp-ink`/`cBg`→`cp-bg` (the 4 `$('#cp-*').value` reads still work since the component keeps `#cp-*` synced), `loadProject()`'s colour apply → `swatches.*.set()`, the `.swatch`+`.val` CSS + the `['cInk','cBg']` listener loop deleted; added `<script palette.js>` (it loads bare core). Verified: 2 `.color-row`, swatches paint from `initial`, both edit paths work, `.lvp` project round-trip intact, `repaint()` early-returns safely (no font yet). **Membrane `rmxColorAt` + Camo Turing export `rmxLerpColor`** — analysed, **left as intentional separate variants** (not TODO): both are the Camo-Turing-GLSL colour lineage (posterize `floor`; tonernd smooth-lerp ±1.5; Camo's `THREE.Color.lerp` is in the renderer's linear working space, r160 `ColorManagement` on, to match `DISPLAY_FRAG`), where `Organica.palette.colorAt` is the Pollen lineage (posterize `round`; tonernd discrete ±1.2; sRGB lerp). Merging would shift Membrane's RMX output + break Camo's SVG-vs-canvas match. Comments added in both files; `SHARED-COMPONENTS.md` §3 reclassified. Palette consolidation is now **complete** — the only remaining `syncColor` is in `shared/_template.html` (the scaffold). **Not committed** — same branch, pending "porta in prod".

- **August 30, 2026 (still later same day) — Inline / page-style cleanup: new `shared/shell.css`, surface palette + `--danger` into tokens, `.org-modal` + `.org-layer-card` + `.upload-btn`/`.org-file-input` into `panel.css`.** Diego: "verifica se ci sono stili in linea o definiti nelle pagine e trova una strategia per centralizzarli." 3 Explore agents mapped it: header/panel/floatbar/palette were already centralized, but the **app-shell skeleton** (reset, `body`, `#app`, `#canvas-wrap`, the canvas element's shadow + zoom-cursor states, `#zoom-hud`, `#drop-hint`, `.upload-btn`, `#file-input`) was copied near-verbatim in ~17 tools; 15/17 also repeated byte-identical `--ink/--paper/--mid/--accent/--panel/--border` in their own `:root`. Approved scope (AskUserQuestion): **shell + modal + layer-card**; **centralize the palette `:root` defaults**; **include the inline token fixes** + add **`--danger: #a03828`**. Out: converging Genesis's ~140-line local panel re-impl, and a repo-wide `display:none` → `hidden` sweep (→ Backlog). **Done:** (1) `shared/shell.css` — the shell skeleton; the canvas element gets `class="org-stage"` (its id varies per tool) for the shadow + `.zoomed`/`.panning`/`.picking` cursor states. Linked by 14 tools (komorebi, warping, radial, pulsar, blob-boundary, vortex, membrane, halide, spore, pollen, soul, camo-turing, colornet, loom), each keeping only genuine deltas (`#canvas-wrap { padding: 0 }` for edge-to-edge canvases, a lighter `box-shadow: …0.12)` on the photo tools, bespoke `#stage-frame`/`#preview` frames). Genesis / Rhizome / FVS keep their own canvas surface (just dropped their `:root` palette lines). (2) `tokens.css` — surface palette + `color-scheme:light` as `:root` defaults + `--danger`; also fixed a **pre-existing bug**: a premature `*/` at line 178 was closing the SPACING-UTILITIES comment early (comment count was 40 `/*` / 41 `*/`), which would break `.mt-*`/`.u-hidden`/`.u-flex1` parsing. (3) `panel.css` — `.upload-btn` + `.org-file-input` (six tools' near-identical copies), `.org-modal` / `__panel` / `__header` / `__title` (backdrop skeleton; `display` deliberately not set so each dialog keeps its own `style.display` toggle; width via `--org-modal-w`), `.org-layer-card` / `__head` / `__body` with `.layer-card` / `.chan-card` **aliased** onto it (mapping not renaming, same as the Strata/Living Path aliases). (4) Retrofits: FVS ×2 + Genesis ×2 + Colornet ×1 modals → `.org-modal*`; Camo Turing + Colornet layer/channel cards trimmed to inner controls; **7 tools'** hidden file inputs → `.org-file-input` (komorebi/halide/spore/pollen/soul/colornet/membrane — camo-turing ×5, fvs ×2, genesis ×1 inline sr-only blocks still pending, → backlog); FVS's error reds (`#a03828` ×3 / `#c0392b` ×1 / `#rule-total-hint.over-cap` / the `MISSING_COMPONENT_COLOR` JS const) + Rhizome's `#b3403a` → `var(--danger)`; 2 of Genesis's 3 inline raw-px spacings → `var(--space-*)` (`margin:2px 0 6px` left — 2px has no token). (5) `shared/_template.html` rewritten to current conventions (links all 5 sheets, `class="org-stage"`, `Organica.palette.swatch`, no `syncColor`, `#zoom-hud`). (6) Docs: `docs/UI-SHELL.md` §2/§3/§6b/§7, `docs/SHARED-COMPONENTS.md` §3 backlog, `design-system/index.html` (links shell.css, `--danger` swatch, new "Shell" nav group + `#shell`/`#modal`/`#upload`/`#layer-card` sections, 5-file architecture table), this note. **Verified** on the no-store server: all 14 adopted tools + rhizome/fvs + genesis + design-system render — canvas centred, stage shadow, panel unchanged, floatbar present, console clean (only the pre-existing env 404); `.org-modal` computed styles byte-match the old FVS/Genesis values (fixed / rgba(10,10,10,.45) / centred / z-index 200 / 420px panel / 14px pad); `.org-layer-card` computed border/padding/gap match Camo Turing's old locals; tokens.css comment count balanced (42/42). Genesis's own `--fs-sm` (11px, locally defined) confirmed NOT a broken token — left alone. Deferred, → backlog: `Organica.modal` JS helper (not built — `.org-modal` is CSS-only, tools keep their `.style.display` toggle); `.hud-btn`/`#compute-bar` not folded into shell.css (left local in Spore/Pollen); `::-webkit-scrollbar` still local in `design-system`; the 8 pending inline file inputs. **Committed** `5320974`, pushed to `main`.

- **August 30, 2026 (still later same day) — Shell-pass follow-ups: zoom/pan JS + `.chan-card` rename done; CLAUDE.md modernised.** Diego picked "sezione 1" of the backlog. **`.chan-card` → `.org-layer-card`** — full rename in Colornet (element class, `__head`/`__body`, `.chan-card--armed` → `.chan-armed`) + `.layer-card` → `.org-layer-card` in Camo Turing (incl. the 2 `querySelectorAll('.…__head/.…__body …')` in its card-builder JS); the aliases were then **deleted** from `panel.css` — only `.org-layer-card` remains. No `querySelector`/`closest`/`classList` touched `.chan-card`, so it was a clean sweep. Verified live: Colornet 4 channels — border/padding/gap/radius computed-identical to the old values, active border = `--tool`, armed border = `--accent-warm` dashed (on a non-active card), a real 0↔2 swap works; Camo Turing add/reorder(↑)/dot-toggle/remove all work through the renamed selectors. **Zoom/pan JS** — Spore, Pollen, Halide's ~55-line inline `applyZoom`/`zoomBy`/wheel/pan/dblclick/⌘± copy each replaced by one `Organica.createZoomPan({canvas, wrap, isReady, onChange})` call + a 1-line `resetZoom()` wrapper (kept as a global because the HUD's `onclick` and the image-load path call it). Spore *gains* the ⌘+/⌘-/⌘0 shortcuts and the pre-wheel slider-blur it never had. Verified live in all three: wheel→115 %, HUD `.visible`, `.zoomed` class, `zoom-level` text, dblclick + `resetZoom()` → `scale(1)`, ⌘= → `scale(1.2)`; console clean on fresh tabs. **CLAUDE.md** — Diego rewrote *Who* / *What* / *Methodology* to the current scope; verified + fixed refuses (`seriesof`, `enaabling`, `teh`, `troughout`, `constraind`, `methodolg`, `Ryzhome`→`Rhizome`); **Technical Stack** rebuilt (the "no JS animation libraries" line was false — GSAP; the "Python + OpenCV + vtracer" line was dead — went with Strata); **Tools table + Repo Structure** gained the 3 missing live tools (**Mycel**, **TuneSutra**, **Rhizome**) + livingpath/ + the missing manuals; **Studio Rann** branding removed (footer → "Organica", Deployment → team slug only); **Roadmap** + **Open Questions** de-staled (Figma bridge is fixed for 7 tools via Rhizome Phase 0; typography = Living Path; colour = TuneSutra/Colornet/Palette). Committed `5320974` + `42e547d`. **Then the shell-pass tail** (`a92d91a` + one more): Spore `.upload-mark-btn` → `.upload-btn` (+ delta); Genesis `.upload-btn` reduced to its 2 real deltas (softer hover + bottom margin; `--fs-xs`==`--fs-small`, `--ink-muted`==`--mid`, `8px`==`--space-3` so byte-identical); `.hud-btn` + `#compute-bar` (byte-identical in Spore + Pollen) folded into `shell.css`; the 8 remaining inline sr-only file inputs (camo-turing ×5, fvs ×2, genesis ×1) → `class="org-file-input"` (all verified 1px-clip + clickable, no JS touches their `.style`). **`design-system`'s `::-webkit-scrollbar` is NOT a dup** — page-global 6px vs the `#panel`-scoped 3px — left in place; the plan's claim was wrong. **Verification cross-check** (`node xref.js`): all ~190 inline `display:none` toggles are JS-driven, so the `hidden` sweep needs a real `Organica.show()` helper, not a markup swap. **Colornet improvement plan verified fully shipped** (Shuffle/Random Colors, channel-swap gesture, batch processing, AM Screening per-channel render mode) — backlog entry retired. **Hub perf**: a vertical rect-merge on Halide's 16,374-`<rect>` bento SVG yields 0 % (dither has no vertical coherence); real fix is contour-trace-to-`<path>` or lazy-loaded external assets — needs a scoped visual-verification pass. Genuine open centralization mini-projects (deferred, per Diego): `Organica.seedsPanel`, `Organica.recorder`. All committed + pushed to `main`, deployed.

- **August 30, 2026 (still later same day) — The two open centralization mini-projects: `Organica.recorder` shipped in full; `Organica.seedsPanel` shipped + adopted by Soul & Membrane (Camo Turing pending).** Diego: "procediamo con l'analisi… i 2 mini-progetti veri." Two Explore inventories mapped every copy; decisions via AskUserQuestion: build **both full**; recorder **Soul is OUT** (SVG-DOM + GSAP, no live canvas — keeps its per-frame rasteriser + local recorder); seedsPanel **full component** (tab strip + panes + slot system + per-tool output adapter). **`shared/recorder.js`** (no paired CSS — ships no UI): `Organica.recorder({canvas,tool,fps,duration,durationPadMs,onStart,onStatus,onStateChange}) → {toggle,start,stop,isRecording}`. `canvas` may be a **function** (p5 tools whose `state.p.canvas` isn't up at construct time). Merged best version of the 5 copies: superset **5-entry MIME list** (`mp4;avc1`→`mp4`→`webm;vp9`→`webm;vp8`→`webm`, each `isTypeSupported` in try/catch), extension **sniffed from the negotiated `mediaRecorder.mimeType`** (not the candidate pair — the browser may pick a codec not first in the list), `duration` omitted = manual-stop / number|fn = auto-stop (a function is resolved at `start()` so **Pulsar** reads `loopSec` then; `durationPadMs:90` keeps the closing frame). Migrated **Pulsar** (`pulsar/index.html:1109-1144`, auto-stop, `onStart:()=>play(true)`), **Camo Turing** (`:2936-2994`, module ctx, `onStart:()=>{if(!running)togglePlay()}`, constructed in INIT), **Vortex** (`js/main.js:269-322`) + **Membrane** (`js/main.js:475-528`) — both already byte-identical, both keep `addEventListener` wiring + write status into `#record-hint`. Each keeps its own floatbar button; `toggleRecording` became a 1-line delegate. **Verified** on the no-store server, all 4: fresh tab, console clean (only the env 404); Start→button relabel + status ("Recording 6s…" preserved on Pulsar); Pulsar auto-stops at `loopSec+90ms` → "Video"/"Recording saved"; manual stop on the other 3; `Organica.download` called once per cycle with `<tool>-<ts>.mp4` + `video/mp4;codecs=avc1` (ext sniffed); force-run path fires playback first. **MediaRecorder byte output is ~0 in this Chromium-only Browser pane for BOTH mp4 and webm** — an environment limit (no real encoder / no canvas-frame capture headless), identical to what the old code produced here; the full behavioural chain is what's verified (already logged in the cross-browser backlog). **`shared/seeds-panel.js` + `seeds-panel.css`**: `Organica.seedsPanel({target,idPrefix,tabs,order,initial,slots,subControls,extraControls,genesis,text,svg,image,applyMode,onSeed,onTabChange,onFontReady,onError}) → {el,getTab,setTab,getDescriptor,getSVGString,apply,emit,slot,refreshGenesisGrid,setFont,setText,destroy}`. Polymorphic on `typeof target` (element = generate / string = attach). **Core decision — emit BOTH**: `onSeed(result)` carries `svgString` (canonical vector, matches Soul + the "primitives are vectors always" contract) **and** a raw `descriptor` (`{kind,formId,svgText,text,font,glyphPaths,fusedPath,imageEl,perGlyph}`), so Camo's canvas-mask path and Membrane's point-array path each keep their own model step — the component centralises only the *picker + the vector serialisation* (Genesis grid, tab strip, panes, SVG read, opentype load/parse/font-upload, per-glyph vs fused SVG assembly, `bakeComputedGeometry`). `text.perGlyph` (Soul/Membrane one `<path>` per glyph vs Camo/LP fused), `applyMode:'immediate'|'manual'` (Camo defers to its own Apply button), `genesis.bakeGeometry` (only Camo — injects `/genesis/animations.css` if absent), `text.preloadFont:false` (Membrane keeps its own `loadMembraneFont`/`seeds/text.js`). **Slot system**: custom tabs (`slots[]`, `render(pane)` called ONCE, pane shown/hidden never rebuilt so bound listeners survive), `subControls.<kind>(pane)` (inject rows into a built-in pane once), `extraControls(host)` (rows below the pane stack). CSS = `.shape-grid`/`.shape-thumb`/`.thumb-num` lifted byte-for-byte from Soul's + Camo's identical local `<style>` blocks; `--seeds-grid-cols` tunable. **`.PRIMORDIAL = [7,56,1,2,14,33,38,31]`** on the component (was byte-identical in 5 files). **Soul migrated** (`soul/index.html`): local seeds `<style>` + `PRIMORDIAL`/`populateGenesisGrid`/`setSeedSource`/`applyTextSeed`/`opentype.load` deleted, markup → `<div id="seeds-host">`, `Organica.seedsPanel({tabs:['genesis','svg','text'], genesis:{initialForm:null}, text:{perGlyph:true, initial:'Soul', applyButton:true}, applyMode:'immediate', onSeed:r=>{if(r.svgString)loadSVG(r.svgString)}})` in INIT; drop-hint "+" + canvas-drop kept (still route to `loadSVG`). Verified: 3 tabs, genesis pick → "1 primitives loaded", "AB" + Apply → 2 primitives (per glyph), CSS computed-identical to the old local rules. **Membrane migrated** (`membrane/`): Procedural + Image are **custom slots** (Membrane keeps 100% of its own image DOM + listeners — `#seedsrc-image` reparented into the slot pane, `imgHolder.hidden=false`), Text is the built-in tab with `preloadFont:false` (`#font-status-hint` reparented via `subControls.text`, `seeds/text.js` + `loadMembraneFont`/`isFontReady` untouched, word field's input → `onSeed` → `state.seedText` + `seedFromText()`), `onTabChange:id=>{state.seedSource=id; syncMembraneSeedRows(); reseedCurrent()}` replaces `syncSeedSourceUI` + the tab listener loop; `#txt-seedtext` listener deleted, font-hint toggle → `.hidden`. Verified: 3 tabs switch, panes toggle, `#btn-upload-image`/`#font-status-hint` reparented correctly, `row-imgscale` tracks the active source, console clean. **Camo Turing migrated** (same session, after Diego: "procedi con la migrazione di Camo Turing") — the plan's "hardest": **two `Organica.seedsPanel` instances** (`seedsB` `idPrefix:'seedsB'` / `seedsA` `idPrefix:'seedsA'`, distinct generated ids, no collision), both `applyMode:'manual'` + `genesis.bakeGeometry:true` + `genesis.initialForm` 1/2, seedsB also `allowFontUpload:true` + `onError → #seed-status-hint`. The Mode/Size/`#row-boundary-fk`/`#paper-img-hint`/Two-seeds/`#seedsrc-a-block`/famine/Apply rows **stay as plain markup siblings** after `#seeds-host-b` (they aren't tab-scoped) — no `extraControls` needed. Deleted: `seedSourceType`/`seedGenesisForm`/`seedSvgText`/`seedFont`/`seedFontBuf`/`seedImageEl` + the `A` variants; `populateGenesisGrid(A)`; `setSeedSource(A)`; `onTextChange`; `withFont`; the 5 hidden `#svg-input`/`#image-input`/`#font-input`(+`-a`) elements + their change handlers; `window.setSeedSource(A)`/`window.onTextChange`. Kept + rewired: `rasterizeCurrentSeed` → `seedsB.getDescriptor()` switch (`getSVGString()` for genesis/svg — genesis path is baked+serialised by the component now; `d.text`+`d.font` for text; `d.imageEl` for image); `rasterizeShapeA` → `seedsA.getDescriptor()` likewise; `applyPaperImageMode` → `seedsB.getDescriptor()` (`d.imageEl`/`d.svgText`); `rasterizeTextSource` gains a `fontArg` param (was `withFont`-wrapped, now sync); `rasterizeImageSource` drops its `seedImageEl` default; `applySeed`'s status strings `seedSourceType` → `seedsB.getDescriptor().kind`. `ensureGenesisRasterHost`/`bakeComputedGeometry`/`rasterizeGenesisSource` **kept** as internal helpers — `rasterizeGenesisSource(formId, cw, ch, done)` parameterised by form id — only the 3 `window.test*` two-shape probes still call it (they want a fixed form regardless of the panel's tab). **Verified live** (fresh no-store tab, console clean bar the env 404): both panels build (B genesis+form 1 active / A genesis+form 2 active, 8 thumbs each); genesis-form seed → a real reaction-diffusion ring pattern (bakeGeometry pipeline works end-to-end); **text "S" via the component's opentype font → a clean "S" letterform pattern**; fill / trace / boundary all apply without error; Two-seeds ON → `#seedsrc-a-block` + `#seedb-label` show, famine apply clean; `bgimage` with no image → the correct "needs an Image or SVG source" message (proves `applyPaperImageMode` reads the descriptor). Recorder (migrated earlier same session) still fine on the same page. **Living Path** (Diego: "procedi con living path") — right-sized to **`PRIMORDIAL` only**: `<script seeds-panel.js>` added, `const PRIMORDIAL = Organica.seedsPanel.PRIMORDIAL` replaces the local array (the 5th byte-identical copy — now the constant lives in exactly one place). Living Path's own Font/SVG/Genesis picker stays bespoke on purpose — its `.seg`/`data-src`/`.on` tab strip, `[data-pane]`+`hidden` panes, `.forms`/`.f` grid with its own CSS, visible `.drop` zones, CDN opentype, and the "Font" tab being a full OTF-export workbench (glyph-range chips / alternates / `.lvp` projects) genuinely don't map onto the shared component, and it links none of the shared panel CSS; a full adoption is gated on a separate "Living Path → shared shell" pass. Verified live: 8-form Genesis grid builds unchanged, a thumb click still fires `setSource(fromSVGString(F[num]),…,'genesis · form N')`, console clean, zero visual change (screenshot). `shared/_template.html` gained commented `recorder.js`/`seeds-panel.js` include lines. `docs/SHARED-COMPONENTS.md` §3 + `docs/UI-SHELL.md` §2 updated. **Still to do:** a `design-system/` demo section for the seeds panel. **Not committed** — pending Diego's "porta in prod" (same still-uncommitted branch as Pulsar + the Palette waves).

- **August 30, 2026 (still later same day) — Shared-library files renamed: the redundant `organica-` / `organic-` prefix dropped; third-party isolated into `shared/vendor/`.** Diego: "i file iniziano tutti con organica… se un domani dovessimo cambiare il nome sarebbe un problema. i file devono avere una semantica e naming in linea con regole di buon sviluppo del codice." Two Explore inventories first (reference map + dynamic-path risk; the JS/CSS/storage identifier map). **The `shared/` folder already namespaces its files, so `organica-` on every filename repeated the project name and coupled ~30 HTML files to it.** Confirmed safe: no build, no CI, no `package.json`; `vercel.json` rewrites are prefix passthroughs (no filenames); **exactly one** dynamically-built path (`seeds-panel.js` `ensureAnimationsCss` — a substring guard + a hardcoded `/genesis/organic-animations.css` fallback); every other `organica-*`/`organic-*` in JS is a comment. Decisions via AskUserQuestion: **flat, prefix removed** (the folder is the namespace; JS+CSS pairs stay side by side); **Genesis renamed too** (same rule — resolves the three-way `organica` / `organic-strata` / `organic-` spelling split); **all 8 vendored files → `shared/vendor/`** (font included — makes the flat first-party names unambiguous). **Renamed** (`git mv`, history follows): `shared/organica-core.js`→`core.js`, `-tokens.css/.json`→`tokens.*`, `-header/-panel/-floatbar/-shell/-mobile-gate.css`→bare, `-palette.js/.css`→`palette.*`, `-noise/-motion/-radial/-paper/-transformer/-recorder.js`→bare, **`-seedspanel.js/.css`→`seeds-panel.js/.css`** (also un-ran-together the name — the JS identifier stays `Organica.seedsPanel`); `genesis/organic-forms.js`→`forms.js`, `-defs.js`→`defs.js`, `-animations.css`→`animations.css`, `-page.css`→`page.css`; **`genesis/organic-library.css` retired** (dead `@import` shim — nothing `<link>`ed it; `page.css`+`animations.css` link directly now). **Moved to `shared/vendor/`**: `gsap.min.js` + `gsap-morphsvg/drawsvg`, `kiwi.min.js`, `three.module.js`, `opentype.min.js`, `paper-full.min.js`, `manrope-variable.ttf`. **References rewritten** in one atomic commit via a scripted `perl` sweep over all tracked `*.{html,js,css,md}`: literal `<link>`/`<script>` (all ~28 tool `index.html` + `_template.html` + `design-system` + `genesis/*.html` + hub + the `_test-*.html` scratch files), the one dynamic path (`seeds-panel.js` guard `'organic-animations.css'`→`'animations.css'` + fallback `/genesis/organic-animations.css`→`/genesis/animations.css`), the ES import (`camo-turing` `import … from '/shared/vendor/three.module.js'`), the two dynamic font refs (`seeds-panel.js` `fetch` + `membrane/js/seeds/text.js` `opentype.load` → `/shared/vendor/manrope-variable.ttf`), the load-order header comments in every renamed file (`noise.js`/`radial.js`/etc. sibling names + `radial.js`'s runtime `throw`), `THIRD-PARTY-NOTICES.md` (the `shared/vendor/` paths), and the docs (this file's Repo-Structure tree + Critical Rules, `UI-SHELL.md` §2, `SHARED-COMPONENTS.md` playbook step 2, `SHARED-LIBRARY.md` + `ANIMATION-SYSTEM.md` — the "add new form animations to X" guidance now points at `genesis/animations.css`, `README.md`, `SOUL.md`, `LOOM.md`). **Deliberately NOT touched** (an "if we ever rebrand" checklist, in the commit body): `window.Organica` / `Organica.*` (a library carrying its name as its global is standard — `THREE`, `gsap`, `jQuery`; ~800 refs, aliasable in one line if ever needed); `ORGANIC_FORMS` / `ORGANIC_MOTION` globals; `'organic-forms'` the built-in-set data id in Genesis; `.org-*` CSS classes (component-namespace prefix — and already inconsistent, most control classes carry none); `organica.<tool>.*` localStorage keys (**persisted user data** — needs a forward-migration per the Critical Rule); `'organica-svg'` Figma postMessage type (cross-process contract with the plugin); the `<title> … — Organica` / `<b>Organica</b>` logo text (~60 cosmetic); the repo/folder name `organic-strata`. **Verified**: `grep` for any `organica-*.{js,css,json}` / `organic-{forms,defs,animations,page,library}.{js,css}` filename ref → zero. Browser pass on the no-store server — every tool + hub + `genesis/` + `design-system/` load styled, console clean (only the env 404), no `/shared/*` or `/genesis/*` 404s; exercised the risky paths (Camo Turing `three.module.js` ES import + a baked Genesis seed → `genesis/animations.css` via `ensureAnimationsCss`; Soul/Membrane Text seed → `shared/vendor/manrope-variable.ttf`; Loom `kiwi.min.js` grid; Genesis `paper-full.min.js` Draw). The `.claude/worktrees/` mirror keeps the old names — ignored (detached branch). **Committed + pushed to `main`.**

- **August 30, 2026 (still later same day) — Genesis restructured: from a 4-mode motion workbench to "the seed library".** Diego: keep only base forms that integrate with the seed module + Set creation; single-click a seed → its data, double-click → Create/editor; strip Indicators to the filter-bar layout and archive the animations; drop the right Grid column; fold Import into Create; land in the full library. "valuta tutto sia da un punto di vista di UX che concettuale." Assessment (agreed): the 55 animated forms never functioned as *seeds* — only 8 (`PRIMORDIAL`) fed other tools; motion is Soul + Pulsar's job; two parallel catalogs (`forms.js` vs `indicators.html`'s hand-inlined 55) were a drift risk. **Decisions via AskUserQuestion:** Set = a plain ordered list of seed ids (drop `gridConfig`/`formLayout`/spans/alignment/drag-fill); `ORGANIC_FORMS` genuinely shrinks to 13 (ids unchanged `[1,2,3,7,9,13,14,21,26,28,37,41,56]`; slug-key/metadata restructure = tracked follow-up); one built-in **"Base Seeds"** set = 13 organic (synth) + 6 procedural primitives (synth), merging in the old separate `basic-seeds` set; single-click → inline full-row detail card, double-click → Create editor (fork built-ins); keep the left Sets sidebar, right panel Create-only, main = filter bar (Source / Type / Density) + responsive auto-fill grid; Duplicate while Base Seeds active → auto "My Seeds" user set. **`genesis/index.html`:** removed the Compose mode wholesale (+ its ~340-line JS section, CSS, markup grid), gutted the layout model (`applyGridVars`/`getLayoutMap`/`layoutFor`/`setLayoutFor`/`updateFormLayout`/`updateGridConfig`/`alignToCSS`/`selectForm`/… all deleted), `fixForm35()` gone. New: `ORGANIC_KEEP`/`PROCEDURAL_IDS`/`PROCEDURAL_CANON`/`MY_SEEDS_SET_ID` consts; `proceduralForm(id)` (synth, not stored); `migrateLibrary(lib)` (idempotent, never drops `forms[]` — ensures/renames the `organic-forms` set to "Base Seeds", removes `basic-seeds`, drops plain canonical primitive entries but keeps user-renamed/referenced ones, strips `gridConfig`/`formLayout`); `resolveFormById` reordered (organic-N synth → stored lookup → procedural synth fallback — fixes a user-renamed primitive showing canonical); `getFormsForSet`/`filteredForms`/`renderGrid`/`createTile`/`buildDetailCard`/`svgFacts`/`setsContaining`/`ensureMySeedsSet`/`duplicateSeed`/`deleteSeed`/`syncFilterUI`/`openInCreateEditor`/`syncImportUI`/`loadDensity`. `.app` is 2-col (`[sets | grid]`); `.app--create` adds the right panel. Import folded into Create as an always-visible "Import SVG" section; every `S.mode === 'import'` → `!!S.importInner`; `saveForm` guards the `genType` recipe on `!S.importInner` and ends with `openSeedDetailById`. Density persists in `localStorage['organica.library.density']`. **`genesis/forms.js`** reduced 56 → 13 (form 7's `.sp` splash baked to literal `cx/cy/r`); `ORGANIC_LABELS` trimmed. **`genesis/indicators.html`** → thin redirect to `/genesis/`; the full page moved to **`genesis/archive/indicators-55.html`** (self-contained, back-link fixed to `/genesis/`). **Ripple:** `shared/seeds-panel.js` `PRIMORDIAL` → the 13; `pollen/index.html` + `spore/index.html` local literal → the 13 (+ "keep in sync" note); `livingpath/index.html` `ORGANIC_FORMS[38]`→`[13]`, `payload.formId || 38`→`|| 13`; hub `index.html` `BENTO_FORMS` rebuilt to the 13 surviving ids, `fillPhyllotaxisSpiral()` + its call deleted, bento-link text de-staled. **Verified** on the no-store server: Genesis fresh → Library home, 2 tabs, Base Seeds = 19 tiles (13 organic + 6 primitives), filter bar, single-click detail card (Type `asset · built-in`, ID, viewBox, elements, In sets; Name readonly, no Delete), double-click → Create 3-col with the shape on the artboard, filters (Organic → 13 / Primitives → 6 / My seeds → 0), Duplicate a primitive while Base Seeds active → auto "My Seeds" set with "Circle copy"; `migrateLibrary` idempotent. Hub: 24 bento cells, 0 empty, `fillPhyllotaxisSpiral` undefined. Soul: 13 thumbs, `PRIMORDIAL` = 13 ids. Camo Turing: 26 thumbs (2 panels), canvas renders. Pollen: 13 forms + its own "wavy line" primitive. Spore: 13 marks, right labels. Living Path: 13-form Genesis grid, no `[38]`. FVS / Colornet: boot clean reading `organica.library.forms`. All redirect stubs (`indicators`/`creator`/`library`.html) → `/genesis/`; archive page serves (63 KB, forms intact). Every page console-clean bar the pre-existing `_vercel/insights` 404. Docs updated: `docs/SHARED-LIBRARY.md` (blob shape, Base Seeds built-in, `migrateLibrary`), `docs/UI-SHELL.md` (Genesis = 2 modes, `.app`/`.app--create`, `PRIMORDIAL` = 13), `docs/ANIMATION-SYSTEM.md` (Genesis static, catalog archived, `fixForm35` retired), `docs/SHARED-COMPONENTS.md` (`PRIMORDIAL` = 13 + pollen/spore literals), CLAUDE.md (Tools table + Repo tree + Critical Rules). **Follow-up (named in `docs/ROADMAP.md`):** `forms.js` deep restructure — slug keys, per-form metadata block, drop the `class="aNN"` hooks, update every consumer + `PRIMORDIAL` to slugs. Core restructure committed as `642d940` (`genesis/index.html` + `forms.js`); the ripple (hub / livingpath / pollen / spore / `seeds-panel.js`), the indicators archive move, and the docs are a separate pending commit. **Not pushed** — pending Diego's "porta in prod" (same still-unpushed branch as Pulsar + the Palette waves + the rename).

- **August 31, 2026 (later same day) — Genesis trim: header status + "Export .js" + the transform-preview strip removed.** Diego: the header's live status line ("3 points · seed ready") and the **Export .js** action add clutter; the right-panel "Preview — rotation & flip" strip too. **`genesis/index.html`:** deleted `<output class="org-header__status">` + `<div class="org-header__actions">` (the `#btn-export-set` button) — `Organica.status()` is null-safe so all `setStatus(...)` calls became harmless no-ops (left in place); deleted `exportActiveSet()` + its binding (it serialized the active user set as a standalone `window.SET_<NAME> = {FORMS, LABELS}` `.js` file — a back-up / static-import escape hatch that **nothing in the repo consumes**; set data still lives in `localStorage['organica.library.forms']`, no data loss); deleted `#transform-preview` + its label + `renderTransformPreview()` + `TRANSFORM_PREVIEW_STATES` + the `renderArt()` call + the `.transform-preview` CSS (`compositeMarkup`'s now-unused `idSuffix` param left, harmless). Genesis's header is now just logo + mode tabs. **Verified:** header has no status/Export; Create panel has no Preview strip; draw → Save popover → save still works; `Base Seeds` in the Save-to dropdown is `disabled` (un-pickable) **and** `saveForm` bails with a message if it's somehow the value (verified: force-set + Save → no seed saved, popover stays open). Console clean. Docs: `docs/UI-SHELL.md`, `CLAUDE.md`. **Committed** next.

- **August 31, 2026 (later same day) — Genesis Create: "Save to Library" moved to a floatbar popover.** Diego: the Save-to-Library button should be a floatbar icon (tooltip "Save to library"); clicking it opens a popover with the settings that were in the right-panel **Seed** section. **`genesis/index.html`:** the whole `<section>` "Seed" (Name / Type pills / underlying-layer block / Save-to `<select>` + new-set row / `#btn-save`) was relocated into **`#save-popover`** — a `.org-popover` inside a `.org-popover-wrap` in the floatbar, opened by **`#btn-save-open`** (a disk icon) via `Organica.popover()`. `#btn-save-edit` was **removed** — Edit's Save is now the same `#btn-save-open`/popover (so Name/Type stay editable in Edit); the popover open + Save click replaces the old armed-2-click confirm. `syncFloatbar()` shows the Save button in Create + Edit and sets the popover title / Save-button label per mode (`Save to library` / `Save changes` / built-in `Save a copy to My Seeds`); `#save-to-block` keeps `data-modes="create"` so Edit's popover has no Save-to. `saveForm()` + `confirmAndSave()` now return `true`/`false` so the popover only closes on a successful save. `confirmAndSave` lost its `btn`/armed param. **`populateSets()`** now lists **every** set in Library-bar order (built-in "Base Seeds" shown but `disabled` — can't save into the synthesized set) + `+ New set…`, and defaults to `state.activeSetId` (falling back to the first user set); `resetCreate()` clears `#ctrl-set` + re-runs `populateSets()` on entering Create so the dropdown tracks whatever set the Library was showing. `saveForm` bails with a message if a built-in set is somehow selected. **Verified** on the no-store dev server: panel has no Seed section; Create — draw → `#btn-save-open` enables → popover (Name focused, Type 2×2 grid, Save-to, "Save to Library") → save mints a `user-*` seed, popover closes, returns to Library; Edit built-in — dirty → popover "Save a copy" → forks to My Seeds, `S.editForm` becomes `user-*`, stays in Edit; Edit user seed — rename in popover + "Save changes" → `forms[].svg`/`name` rewritten, `S.dirty` false; Mask pill → underlying-layer block shows in the popover; `+ New set…` → new-set row shows. Console clean. `node -c` clean. Docs: `docs/UI-SHELL.md`, `CLAUDE.md` (Tools row + this note). **Committed** next.

- **August 31, 2026 (later same day) — Small Genesis Library / floatbar fixes.** (a) **Create** now opens a **blank canvas** (Freehand, nothing drawn) when entered from another mode — `resetCreate()` in `bindTabs` clears any leftover Edit/import state. (b) The SVG-import affordance moved from the Source panel to the **centre floatbar** as an Import icon (Create only). (c) **Floatbar hover tooltip centralised** into `shared/floatbar.css` — `.org-floatbar__btn[aria-label]:hover::after { content: attr(aria-label) }`, one instant styled pill, suppressed while a popover is open, **font pinned to `--fs-small` (10px)** (the Genesis-local rule had used `var(--fs-xs)`, a token only Genesis defines → undefined elsewhere → tooltips inherited an inconsistent per-tool size). Removed the Genesis-local copy; Rhizome's 3 buttons with `title`+`aria-label` folded the shortcut into `aria-label`, dropped `title`. Documented in `design-system/index.html` (Floating toolbar §). (d) **Removed the Library density toggle** — `state.density` / `densityVars()` / `loadDensity()` / the Density seg all gone; the grid is fixed at "cozy" (`TILE_MIN 130px` / `TILE_GAP 12px`). Legacy `localStorage['organica.library.density']` left in place, no longer read. Filter bar is now Sets · Source · Type. Docs: `docs/UI-SHELL.md`, `CLAUDE.md` Tools row. **Committed** `dd2ba65` (a–c) + this note (d).

- **August 31, 2026 (later same day) — Soul deleted.** Diego: "valuta, ma credo che soul abbia zero valore e penso di eliminarlo." Three Explore passes concurred: Soul's premise ("animate ANY tool's SVG export") is only half-delivered — `parsePrimitives` walks a hard-coded 7-tag allowlist (`circle/ellipse/rect/path/polygon/polyline/line`), silently dropping `<text>`/`<use>`/`<image>`/filters/gradients/clip-paths/embedded rasters (Camo Turing's raster-SVG export → nothing; Living Path's 24 raster presets → nothing); no `transform` composition; wobble stalls past ~3000 primitives; SVG export is a *static* mid-motion snapshot (no animated SVG / Lottie); nothing downstream consumes a Soul output; the Rhizome `soul-pass` bridge node was an inert pass-through. Pulsar owns the one motion input anyone pipelines (Radial → looping WebM); Membrane / Vortex / Blob Boundary carry their own intrinsic motion. **Decisions (AskUserQuestion):** full removal; `shared/motion.js` stays intact (Rhizome still uses `parsePrimitives` for its `svg-to-points` node + `svg→points` adapter — `PATTERNS`/`staggerDelay`/`animate`/`morph`/`organicBlobPath` go dead-but-harmless); `docs/SOUL.md` → `docs/archive/SOUL.md`. **Deleted:** `soul/index.html`, `shared/vendor/gsap-drawsvg.min.js` (the one fully-orphaned asset — Blob Boundary loads GSAP core + MorphSVG but never DrawSVG), `rhizome/js/nodes/soul-bridge.js` (+ its 2 lines in `node-registry.js`; `soul-pass` node id gone — a saved graph with one would fail to load, acceptable, none are persisted). **Edited:** hub `index.html` (nav link + `.nav__link--soul` CSS + bento `TOOL_PREVIEWS` entry + `.tp-soul` keyframe rules), `vercel.json` (rewrite), `shared/tokens.json` (`soul` accent), `THIRD-PARTY-NOTICES.md` (GSAP entry → Blob Boundary, drop DrawSVG; opentype/p5/Manrope "Used by"), `shared/motion.js` header, `docs/RHIZOME.md` (bridge table 10→9), `docs/ANIMATION-SYSTEM.md` / `docs/VISION.md` / `docs/ROADMAP.md` / `docs/DESIGN-SYSTEM.md` / `docs/SHARED-COMPONENTS.md` / `docs/SHARED-LIBRARY.md` / `docs/UI-SHELL.md` / `design-system/index.html` (de-stale), CLAUDE.md (Tools row + tree + stack table + this note). Comment-only Soul mentions in mycel/pulsar/fvs/membrane/livingpath left as history. **Verified** on the no-store dev server: hub loads, "Motion & growth" nav has 5 links, bento no empty cell; Rhizome loads (ES-module `node-registry.js` import graph still resolves), "add node" menu bridge category has no Soul, an `svg-to-points` node still parses via `Organica.motion.parsePrimitives`; Blob Boundary Play still morphs (GSAP core + MorphSVG intact); membrane/camo-turing/livingpath/design-system boot clean; `vercel.json` + `tokens.json` parse; `grep -ri soul` → only the deliberately-left comments. **Not committed.**

- **August 31, 2026 — Genesis: new "Edit" mode + bezier-editable seeds.** Diego: from Library, click → straight to editing the shape (drop the detail card); add Duplicate + Save icons to the centre floatbar; make the shape editable via bezier; a new **Edit** mode next to Create (Create = new shapes only); a label distinguishing a parametric seed from a vector one. **Decisions (AskUserQuestion):** every raw-SVG seed → all contours become bezier anchors (`paper.project.importSVG({expandShapes:true})`); parametric seeds (non-freehand `genType`) keep their generator sliders; Save is dirty-gated + armed-confirm to overwrite (a built-in → auto-fork to "My Seeds"); Duplicate forks the on-screen edits; undo = ~20-step stack; floatbar only, no panel Save. **`shared/paper.js`** — `createPaperDrawEditor` gained an internal `mode` (`'draw'` = the single hand-drawn `paper.Path`, unchanged / `'multi'` = a `paper.CompoundPath` of imported contours), new `importSVG(svgString)` / `getImportedStyleHint()` (a `<line>`/`<polyline>` seed → Stroke) / `isMulti()`, per-contour `_manual` sets, `resetInternal()` on every re-seed, a `{kind:'compound',json,smooth}` serialize variant (legacy `{json,closed,smooth,manual}` still loads). Draw-mode paths byte-identical; only Genesis + `_test-paper-draw.html` consume it. **`genesis/index.html`** — `S.mode` gains `'edit'`; `S.editForm/editKind/dirty/loadingEdit`; `state.openDetailFormId` + `buildDetailCard`/`openSeedDetail`/`openInCreateEditor`/the seed-picker overlay all deleted; `createTile` single-click → `enterEdit`; `loadSeedIntoEdit` (parametric → `S.gen` from `genParams` + sliders / freehand → `drawEditor.load` / raw → `drawEditor.importSVG(form.svg)`); `syncEditPanel` (badge + facts `<dl>` + built-in/filter notes + Delete) / `syncFloatbar` / `markDirty` / `confirmAndSave` / `duplicateFromEdit`; `isCreatorMode()` now Create **or** Edit, `isFreehand` → `isDrawMode`; `geometry()` emits `fill-rule="evenodd"` when `drawEditor.isMulti()`; Create's Import SVG section (note + "+ Upload .svg" + paste box) replaced by one `+` icon (file only); panel Save-to + Save button wrapped `data-modes="create"`. **Verified** on the no-store dev server: Library tiles show `parametric`/`vector` badges (basic-arc parametric, rest vector); click a vector organic seed → Edit, badge + facts + built-in note, draw-canvas active, Paper anchor overlay; caterpillar (5 circles) / flower (9 ellipses + circle, 36 segs) / sun (circle + 8 lines → Stroke style, rays visible) each open with every contour independently draggable; anchor drag → `S.dirty`, Save enables, status "· unsaved"; a user seed → armed 2-click → `forms[].svg` rewritten; a built-in → forked to My Seeds, Edit follows the copy; Duplicate forks the edits; undo reverts the last drag; parametric seed (basic-arc) → sliders, no canvas; Create still draws Freehand (3-click test) + `+` imports an SVG file + `#handles-layer` stays empty; console clean bar the pre-existing `_vercel/insights` 404. `node -c` clean on both files. Docs: `docs/UI-SHELL.md` (Genesis = 3 modes), `docs/SHARED-LIBRARY.md` (`genType` drives Edit), `shared/paper.js` header, this note + Tools row. **Not committed.**

- **August 30, 2026 (still later same day) — Genesis Library: left Sets sidebar removed; set picker + "new set" folded into the filter bar.** Diego: "elimina la colonna sinistra di library e inserisci crea sets e l'elenco nella barra dei filtri." `genesis/index.html` only. Deleted the `<aside class="sets-panel">` (title + `+` + `#sets-list`) and its CSS (`.sets-panel*`, `.sets-list`, `.set-item*`), plus the now-redundant `.set-bar` heading row (`#set-bar-name`/`#set-bar-count` + `syncSetBar()` — the active set + count still show in the shared header status line). `.app` grid: Library `1fr` (was `var(--panel-width-left) 1fr`), `.app--create` `1fr var(--panel-width-right)` (was 3-col). New first group in `.lib-filters`: `<div class="seg seg--sets" data-filter="set" id="sets-seg">` + a `#btn-new-set` `+` button; `renderSetsList()` rewritten to render `<button class="seg-btn" data-set-id>` chips (name + dim `.n` count) into `#sets-seg` instead of `<li>`s; `activateSet()` calls `renderSetsList()` for active-state instead of toggling `.set-item`; `syncFilterUI()` + the `.lib-filters .seg` click delegate both early-return on `key === 'set'` (the per-chip listener calls `activateSet`); `showNewSetInput()` inserts its `.set-name-input` before `#btn-new-set` (Enter commits → `createNewSet`, Esc/blur cancels). **Verified** on the no-store server: Library single-column (no sidebar), filter bar = Sets · Source · Type · Density; 3 set chips (Base Seeds 19 active / Imports 1 / My Seeds 1); switching chips re-renders the grid + moves active; `+` → inline input → Enter creates "ZZ Probe" and activates it; double-click a seed → Create `[614 | 260]` 2-col with the right panel; back to Library → `1fr`; console clean bar the pre-existing `_vercel/insights` 404. Docs: `docs/UI-SHELL.md` §4c + §Genesis + §shell-divergence, `CLAUDE.md` Tools row. **Not committed.**

---

*Organica · Updated August 2026*
