# CLAUDE.md — Organica Project Memory

> This file is read by Claude at the start of every session.  
> It contains all the context needed to work on this project without re-explaining.  
> Keep it updated as the project evolves.

---

## Who

**Diego** — solo designer/artist, Studio Rann.  
This is his primary creative and production tool. Not a side project.

---

## What

**Organica** is two things simultaneously:
1. Studio Rann's visual language system
2. The AI-powered toolset to develop, generate, and deploy that language

The visual language is built on three references tuned together:
- **Biomimicry** — perfect organic geometries (nautilus, phyllotaxis, cellular growth)
- **Raw organic nature** — imperfect, living forms (drop marks, erosion, growth patterns)
- **Street art language** — bold marks, scale, presence, immediacy — the calibration mechanism that makes the other two feel grounded, not academic

The fundamental gesture is the **drop mark**: pigment dropped onto a surface, governed by gravity, viscosity, surface tension. Not drawn — allowed to happen. Two atoms: **drop + brushstroke**. Everything else derives from these.

---

## The Methodology

Based on **"Flexible Visual Systems"** by Martin Lorenz.  
Core principle: a visual system is a **grammar** — rules that generate infinite variations while remaining coherent.

Modules in Organica:
- **Form** — drop marks, brushstrokes, traced hand-drawn shapes
- **Pattern** — tiling/repetition (manual now, automated in roadmap)
- **Grid** — Genesis composer N×N cells
- **Color** — 3 base palettes (sky & sun / earth / deep sea), expandable
- **Typography** — undefined, TBD
- **Motion** — 55 organic CSS animations (Genesis library)
- **Scale** — vector-first, screen to mural

---

## The Tools (Live)

| Tool | URL | What it does |
|---|---|---|
| **Hub** | `theorganicalanguage.vercel.app` | Entry point, links to all tools |
| **Genesis** | `theorganicalanguage.vercel.app/genesis/` | 55 animated organic forms + grid composer |
| **Indicators** | `theorganicalanguage.vercel.app/genesis/indicators.html` | Full catalog of all 55 forms |
| **Spore** | `theorganicalanguage.vercel.app/spore/` | Generative stippling from images — SVG mark library, zoom/pan, PNG/JPG/SVG export, Figma |
| **Pollen** | `theorganicalanguage.vercel.app/pollen/` | Advanced stippling — blue-noise engine, Circle/Polygon/Line points, Adaptive duotone, presets, PNG/JPG/SVG export, Figma |
| **Living Path** | `theorganicalanguage.vercel.app/livingpath/` | Generative font/path modification (web port of ivangrozny/LivingPath). Vector + Raster engines, layer groups + blend modes, 9 raster algorithms, 24 presets, live text specimen, installable OTF export (Web Worker), .lvp projects |
| **Halide** | `theorganicalanguage.vercel.app/halide/` | Photo → true 1-bit dithered portrait. Floyd–Steinberg / Atkinson / Bayer ordered / flat threshold; background removal (colour-key, Pick/Auto), sticker Outline, Background fill, interactive drag-to-position square crop; PNG/JPG/vector SVG (optional contour-traced "Simplify shapes")/Figma export. Manual: `docs/HALIDE.md` |
| **Komorebi** | `theorganicalanguage.vercel.app/komorebi/` | 木漏れ日 — dappled sunlight through a canopy, real-time on the GPU (WebGL2). Light-cookie/gobo canopy (procedural fBm or uploaded silhouette) projected in perspective; **penumbra/pinhole** physics (small gaps → round coins of light); post-process **god rays** (radial-blur occlusion buffer); Scene + Gobo (tileable pattern) modes; periodic wind → **seamless WebM loop**; PNG/JPG/WebM + posterised tone-band **SVG separation** (Halide's tracer)/Figma. Manual: `docs/KOMOREBI.md` |
| **Camo Turing** | `theorganicalanguage.vercel.app/camo-turing/` | GPU (WebGL2/Three.js) Gray-Scott reaction-diffusion — successor to Camouflage's Disruptive mode, removed from the repo. Live f/k sliders, drop-mark seeding, **Layers** (stack independent sims, blended in field space — Normal/Diff/Multiply/Screen/Add/Darken/Exclusion — with per-layer preset + colour), Paper background image/stencil (embeds real SVG on export), **anisotropic diffusion** (real parallel stripes — nearest-neighbour taps, not bilinear) with verified animal/plant presets (Leopard/Cheetah/Tiger/Zebra/Watermelon rind). PNG/JPG/SVG/Figma. Manual: `docs/CAMO-TURING.md` |
| **Warping** | `theorganicalanguage.vercel.app/warping/` | Domain-warped value-noise / Worley pattern generator — covers the pattern family Gray-Scott can't honestly produce: **Wood grain** and **Marble veining** (fbm turbulence), **Cellular network** (Worley F1/F2, e.g. giraffe-style reticulation), **Level curves** (topographic contours). **Warp** (the tool's namesake) bends the sampled coordinates with a low-frequency noise field before any pattern runs, independent of the pattern itself. Canvas2D/JS, same reasoning as Komorebi's own move off WebGL. PNG/JPG/SVG (posterised tone-band separation, same tracer as Komorebi/Halide)/Figma. Manual: `docs/WARPING.md` |
| **Loom** | `theorganicalanguage.vercel.app/loom/` | Universal Grid Generator — a constraint-driven layout-grid engine, not a column generator. Canvas Manager → Grid Definition Engine → Constraint Engine (**Kiwi solver** for equal-by-default/overridable sizing, **direct parametric math** for continuous-function generators — chosen per generator, not globally) → Universal JSON Model → renderers (live HTML/CSS Grid, SVG, PNG, Figma via the existing shared plugin). **Phase 1 MVP**: two generators, **Bento** (Kiwi, seeded cell-merge spans) and **Sinusoidal** (parametric, sine-modulated track sizes). First Organica tool built as native ES modules rather than single-file. Manual: `docs/LOOM.md` |
| **Soul** | `theorganicalanguage.vercel.app/soul/` | The animation engine — pattern + stagger applied to primitives parsed from ANY Organica tool's own SVG export (no per-tool adapter, one parser: `shared/organica-motion.js`), instead of Genesis's per-form hand-typed CSS keyframes. Genesis's 6 physics patterns rebuilt parametric on **GSAP** (vendored, free "no charge" license), plus 2 patterns not in Genesis at all — **Organic wobble** (continuous, non-repeating: each primitive samples `simplex3(x,y,t)`'s own real time axis at its own position every frame) and **Morph** (MorphSVGPlugin, circle ↔ noise-perturbed blob — the first pattern that changes a primitive's own shape, not just position/scale). DrawSVG for Growth-by-tracing. Stagger (index/distance/simplex-noise delay) is what gives "Collective behaviour" for free, composed on top of any pattern — not a pattern of its own. Loom is the natural upstream layout source (its cell list is exactly the "many positioned elements" shape a stagger wants). Primitives render as real SVG DOM (not canvas) since GSAP/DrawSVG/MorphSVG need real elements. **Seeds panel** (Genesis/SVG/Text — same tabbed component as Camo Turing's own, no Image tab) feeds sources in: Genesis's 8 curated forms, SVG upload/drop, or typed text converted to real per-glyph vector paths (opentype.js + vendored Manrope, one primitive per LETTER so stagger addresses individual glyphs). Everything canvas-level (Open/Play/Stop/Loop/Export/status) lives in one shared `org-floatbar`, including **video export** (canvas.captureStream + MediaRecorder, MP4/WebM, same technique as Camo Turing's recorder) alongside PNG/SVG. Zoom/pan via the same shared `Organica.createZoomPan` every other tool uses. Manual: `docs/SOUL.md` |
| **Membrane** | `theorganicalanguage.vercel.app/membrane/` | Generative form-growth tool — a point-set (circle/line/point, procedural/image/text-seeded) driven across the canvas by a chosen Movement pattern (float-to-mouse, orbit, figure-8, Follow-path…), leaving an accumulated trail. Canvas panel, Palette (Ink/Background swatches + RMX multi-colour mode), mouse-wheel zoom/pan via the shared `Organica.createZoomPan`, spacebar pause, real vector SVG export (a capped/throttled recording of recent frames — `state.shapeHistory`, the same "record marks as data" approach as Spore/Pollen), PNG, and Video (MediaRecorder). Promoted from `explorations/membrane/`, the template this project's other exploration→production migrations (Vortex) followed. |
| **Vortex** | `theorganicalanguage.vercel.app/vortex/` | A 3D conical helix of independently-drifting line segments, orbited by its own real perspective camera (drag-orbit, Shift-drag-roll, Camera distance as the zoom — deliberately NOT `Organica.createZoomPan`, a 3D scene needs one camera, not two overlapping zoom concepts). Resting → transitioning → running state machine: hovering (or Play) grows the spiral from a single seed line on an ease-in ramp. RMX palette (chip UI, up to 8 colours — raised from Membrane's 5, no shader array-size bound here) doubles as the segments' own ink, no separate Ink swatch. Numeric, reproducible **Seed** drives the per-segment noise/spin/orbit randomness (`p.noiseSeed` + a `mulberry32`-derived offset). Redraws from scratch every frame (no accumulation), so PNG export is a genuine re-render at scale (not an upsampled bitmap) and SVG export is just the current frame's already depth-sorted segment list — no history/throttle machinery needed. Promoted from `explorations/vortex/`, migrated the same way as Membrane. |
| **FVS** | `theorganicalanguage.vercel.app/fvs/` | Flexible Visual System engine — Figma's own Elements→Components model: an authored **Element** (isosceles triangle, or an Arc with a live Thickness slider 1–100% — one shared `arcGeometry()` generator, not 4 fixed presets — an **uploaded SVG**, or a **Seed picked from Genesis Creator's own shared library** via `Organica.loadLoomGrid`'s sibling read of `organica.library.forms`, first drawable shape, bbox-fit) gets a transformed copy placed into every cell of a grid — Square 2×2/3×3/4×4, or an **imported Loom grid** (`Organica.loadLoomGrid`, rect or polygon cellShape, any cell count; the 7 four-cell-only symmetry rules grey out automatically outside a 4-cell grid, Random/Exhaustive fall back to an honest per-cell-independent draw). Elements are real SVG path `d` strings (not polygon point-lists, needed for the arcs' curves) — `ELEMENT_TYPES[type].geometry(params) → {d, normTx, normTy, normScale}`, rendered via `<path>` (SVG) / `ctx.fill(new Path2D(d))` (Canvas2D). Colour lives in a centralised **Palette** — Ink (RMX chip UI, 1–8 colours, `colorAt(i)` cycling by cell index) + **Paper** (background swatch, wired into the gallery, PNG, and SVG export alike). Full transform vocabulary — Rotation (0/90/180/270°), Flip (none/H/V/both), Scale (Small/Medium/Large %, editable) — feeds **8 named symmetry rules** (Identity, Pinwheel, Mirror/Kaleidoscope, Diagonal mirror, Checkerboard, Row mirror, Column mirror, Radial — Radial uses the grid's true rotational cell order, `TL,TR,BR,BL`, not Pinwheel's row-major order, which is what lets 4 arcs tile into a real full circle) plus a **Manual** 2×2 mini-editor. **Random seeded** and **Exhaustive** draw from a `FAMILIES` registry of the same named rules rather than choosing 4 cells independently, gated by which axes are active — every generated result is still a real symmetry; Exhaustive stays hard-capped at 512, refusing cleanly with the exact count above cap. One item-descriptor array (`cx,cy,rotation,flipH,flipV,scale,color`) feeds both the Canvas2D PNG export and the SVG string, same discipline as Vortex's own `render.js`/`svgexport.js`. An always-visible **Element preview strip** shows the current Element at 0/90/180/270° + each flip before generating a full grid of it, and a local **Component Library** (`Organica.presetStore`) saves/reloads full self-contained snapshots (Element incl. an upload's own geometry, Grid, Palette, exact cells) for reuse — Figma export deliberately not wired in yet, since the shared `Organica.sendToFigma` channel is still broken product-wide (see Loom's own August 14 session note). Symbols/Pattern-tiling/Applications tiers (the rest of the Figma-component-library reference brief) are later phases. |

---

## Repo Structure

```
organic-strata/          ← GitHub repo name (diegoair/organic-strata)
├── index.html           ← Organica hub (dark, animated noise field)
├── genesis/
│   ├── index.html       ← Genesis Creator composer
│   ├── indicators.html  ← 55-form catalog
│   ├── organic-animations.css ← 77 @keyframes + 55 .aNN rules — the REUSABLE half (append-only)
│   ├── organic-page.css      ← :root palette + Genesis catalog page chrome
│   ├── organic-library.css   ← @import shim: page + animations (historic entry point)
│   ├── organic-forms.js      ← SVG markup for 55 forms — PORT AS DATA
│   ├── organic-defs.js       ← Shared SVG defs (goo filters + chips) — INJECT ONCE
│   └── genesis-creator.js    ← Composer interaction logic
├── spore/
│   └── index.html       ← Spore — generative stippling (single-file, vanilla)
├── pollen/
│   └── index.html       ← Pollen — advanced stippling, blue-noise engine (single-file, vanilla)
├── halide/
│   └── index.html       ← Halide — photo → 1-bit dithered portrait (single-file, vanilla)
├── komorebi/
│   └── index.html       ← Komorebi — WebGL2 volumetric light / god rays / dapple (single-file, vanilla)
├── camo-turing/
│   └── index.html       ← Camo Turing — GPU (WebGL2/Three.js) Gray-Scott reaction-diffusion (single-file)
├── warping/
│   └── index.html       ← Warping — domain-warped value-noise / Worley pattern generator (single-file, vanilla, Canvas2D)
├── loom/                 ← Universal Grid Generator — the one tool NOT single-file (native ES modules)
│   ├── index.html       ← shell — canvas preview + panel, loads js/main.js as a module
│   └── js/
│       ├── canvas-manager.js     ← physical space: size/unit/margin/safe area/bleed
│       ├── json-model.js         ← the Universal JSON Model — single source of truth
│       ├── constraint-engine.js  ← Kiwi solver + parametric solver, chosen per generator
│       ├── generators/           ← registry.js + bento.js + sinusoidal.js (Phase 1 of ~26 planned)
│       └── renderers/            ← html-renderer.js (live CSS Grid) + svg-renderer.js + raster-renderer.js
├── soul/                 ← Animation engine — pattern + stagger over primitives from any tool's SVG export
│   └── index.html       ← shell — Source (load SVG) / Primitives (parsed stats) / Motion (pattern+stagger+play) panels
├── membrane/             ← Generative form-growth (point-set + Movement pattern + accumulated trail)
│   ├── index.html
│   └── js/               ← state.js, canvas.js, main.js, color.js, movement.js, svgexport.js, …
├── vortex/               ← 3D conical helix, own perspective camera, resting→transitioning→running
│   ├── index.html
│   └── js/               ← state.js, geometry.js (helix/camera/noise math), palette.js, render.js, canvas.js, svgexport.js, main.js
├── fvs/                  ← Flexible Visual System — Elements→Components grid/rule engine (single-file)
│   └── index.html
├── rhizome/              ← Node-based workflow canvas — chains other tools as pipeline stages (native ES modules, like Loom)
│   ├── index.html        ← shell — canvas/panel/floatbar, loads js/main.js as a module
│   ├── _test-mvp.html    ← the original isolated prototype (2-node drag/wire/connect test) — kept as record
│   └── js/
│       ├── graph-model.js       ← canonical model — nodes[]/edges[], everything else derived
│       ├── execution-engine.js  ← topoSort (Kahn, throws on cycle) + dirty-flag cache + serialized recompute()
│       ├── port-types.js        ← PortType enum + display metadata
│       ├── adapters.js          ← svg→points / grid→points / grid→svg / grid→image, the only auto-conversions
│       ├── node-registry.js     ← id → {meta, compute}
│       ├── nodes/                ← Tier 1 native (loom-grid-generator/geometry, contour-trace, svg-to-points, merge, export) + Tier 2 bridges (genesis/komorebi/warping/soul/camo-turing) + bridge-iframe.js (the generic Tier-2 factory)
│       ├── canvas/               ← pan-zoom.js (wraps Organica.createZoomPan), node-drag.js, wires.js, ports.js
│       └── renderers/            ← node-card.js, inspector-panel.js (populates the shared right panel)
├── shared/              ← Cross-tool system assets
│   ├── organica-tokens.css ← Manrope + type scale + spacing (load FIRST)
│   ├── organica-core.js    ← download, presets, Figma, tracer, zoom/pan
│   ├── organica-noise.js   ← hash2/vnoise/fbm/ridgedFbm/voronoiF1F2/simplex2/simplex3/simplexFbm2/simplexFbm3 — shared by Komorebi, Camo Turing, Warping, Soul
│   ├── organica-motion.js  ← Soul's own contract: parsePrimitives (any SVG → primitive list), PATTERNS, staggerDelay, animate — load AFTER noise
│   ├── gsap.min.js, gsap-morphsvg.min.js, gsap-drawsvg.min.js ← vendored (free "no charge" license, gsap.com/standard-license) — Soul's timing/morph/draw engine
│   ├── kiwi.min.js         ← Cassowary constraint solver (vendored, MIT/BSD-3-Clause) — Loom's Bento generator
│   └── _template.html      ← Start a new tool from this
├── docs/
│   ├── VISION.md        ← Full system vision and methodology
│   ├── ROADMAP.md       ← Development phases and open questions
│   ├── ANIMATION-SYSTEM.md  ← Animation pattern documentation
│   ├── SHARED-LIBRARY.md ← Shared-library contract + storage conventions
│   ├── DESIGN-SYSTEM.md ← Manrope, type scale, tokens, Figma mapping
│   ├── UI-SHELL.md      ← The standard tool layout + behaviour contract
│   ├── KOMOREBI.md      ← Komorebi manual
│   ├── CAMO-TURING.md   ← Camo Turing manual
│   ├── WARPING.md       ← Warping manual
│   ├── LOOM.md          ← Loom (Universal Grid Generator) manual
│   └── SOUL.md          ← Soul (animation engine) manual
├── vercel.json          ← Routing: / → hub, /genesis/ → genesis, per-tool rewrites
└── CLAUDE.md            ← This file
```

---

## Technical Stack

| Layer | Technology |
|---|---|
| Frontend | Vanilla HTML/CSS/JS — no framework |
| Animations | Pure CSS `@keyframes` — no JS animation libraries |
| SVG processing | Python + OpenCV + vtracer (local backend only) |
| Deployment | Vercel — Studio Rann account (team slug: studiorann) |
| Repository | GitHub — `diegoair/organic-strata` |
| Design integration | Figma (manual now, direct push in roadmap) |

---

## Critical Rules — Never Break These

- **Design tokens — mandatory, every new development.** Never hardcode a font-size, spacing (padding/margin/gap), radius, or UI-chrome colour — use the matching token from `shared/organica-tokens.css` (`--fs-*`, `--space-*`, `--radius-*`, `--ink`/`--paper`/`--panel`/`--mid`/`--border`/`--border-strong`/`--tool`/`--accent-warm`/`--track-bg`). If the value you need doesn't exist on any existing scale, **stop and ask before adding a new token** — don't invent one silently and don't fall back to a raw px/hex "just this once." Live reference with every token and component, rendered from the real CSS: `/design-system/` (linked from the hub). Full narrative + Figma mapping: `docs/DESIGN-SYSTEM.md`. Two real exceptions, not loopholes: (1) a tool's own **content** colour (Halide's ink/paper for the dithered image, Pollen's point colour) is user data, not design-system chrome — don't tokenise it just because it happens to default near `--ink`/`--paper`; (2) a genuine pill/stadium shape (`border-radius` = half the element's height, e.g. a toggle switch) isn't a corner radius — forcing it onto `--radius-sm/md/lg` flattens the capsule. Typography specifically: **Manrope only**, via `--font`, never a second typeface.
- **Shared code** — `shared/organica-core.js` owns download, preset storage + legacy migration, Figma postMessage, colour hex validation, the rectilinear contour tracer and zoom/pan. Don't re-implement these in a tool; if one needs different behaviour, extend the core
- **New tool** — start from `shared/_template.html`, not by copying a neighbour. Contract: `docs/UI-SHELL.md`
- **`organic-animations.css`** — never modify existing rules, only append new ones (the 55 forms depend on the exact timings/selectors). This is the split-out pure half of the old `organic-library.css`; the append-only rule now lives here. Any consumer linking it **must** define `--ink` and `--bg-cell` — see `docs/SHARED-LIBRARY.md`
- **`organic-library.css`** — now an `@import` shim (`organic-page.css` + `organic-animations.css`, in that order — the order is load-bearing for the cascade). Don't put rules back into it
- **localStorage keys** — always `organica.<tool>.<thing>`. When renaming a key, migrate forward and **leave the legacy key in place** (user presets/forms are real work); never delete it in the same release
- **`organic-forms.js` / `organic-defs.js`** — treat as data, port verbatim
- **Color in SVG forms** — always `fill: var(--ink)` / `stroke: var(--ink)`, never hardcode hex
- **Internal links** — always relative (`/genesis/`, `/loom/`), never absolute Vercel URLs
- **New forms** — follow the pattern in ANIMATION-SYSTEM.md, test in indicators.html first

---

## Animation System — Quick Reference

6 patterns, each maps to real physics:

1. **Internal Pressure** — `scale` + asymmetric `cubic-bezier` → breath, heartbeat, lung
2. **Gravity + Viscosity** — `translateY` + `scaleY` → drops, honey, lava
3. **Growth by Tracing** — `stroke-dashoffset` → vine, roots, tree rings
4. **Collective Behaviour** — staggered `animation-delay` + goo filter → swarm, metaballs
5. **Environmental Forces** — continuous `translate`, `linear` easing → wind, tide, sedimentation
6. **Differential Rotation** — counter-spinning layers at different speeds → orbit, shard rotation

Duration range `1.4s–14s` maps to real-world time scales. See `docs/ANIMATION-SYSTEM.md` for full detail.

---

## Output Formats Diego Produces

- Tavole grafiche / composition studies
- Posters (print + digital)
- Web pages (animated SVG)
- Branding systems for clients
- Mural schemas (large format, scale-agnostic vector)
- Art installations (generative animation loops)

---

## Roadmap Priorities (in order)

1. **Phase 2** — Genesis depth: export SVG/PNG/GIF, save/load, more forms, rotation/opacity controls
2. **Phase 3** — Figma direct push: Genesis → Figma (the shared `Organica.sendToFigma` channel is currently broken product-wide — see Loom's own August 14 session note)
3. **Phase 4** — Pattern engine: tiling, grid variants, density control
4. **Phase 5** — (retired — was Strata AI, moot since Strata's removal on August 26, 2026)
5. **Phase 6** — Output pipeline: print PDF, mural schema, installation loop

---

## Open Questions (Decisions Pending)

- Typography module — what role does type play? System font or custom?
- Color system — how are colors managed beyond the 3 Genesis palettes?
- Client workflow — what does the Organica handoff look like for client branding?
- Mural scale — largest format needed? DPI requirements?

---

## Backlog

- **Hub home-page performance** — `index.html` grew to ~1.48MB after the August 27, 2026 bento redesign embedded 4 real tool-export SVGs inline (Camo Turing/Komorebi/Halide/Membrane), the largest being Halide's un-simplified 775KB/16,375-`<rect>` export. Fine as a one-off disclosed tradeoff at the time, but worth a real pass: re-export Halide with "Simplify shapes" on (rect-tile → traced path, the same 4,789→4 win documented in Halide's own July 24 session note), consider lazy-loading/deferring off-screen masonry cells instead of parsing all 27 at once, and audit whether any future added preview should be capped by size before it ships. See the August 27, 2026 (still later same day) session note for the full asset-by-asset breakdown.

---

## Session Notes

*Add dated notes here as the project evolves:*

- **June 2026** — Completed full setup: hub live at theorganicalanguage.vercel.app, Genesis Creator integrated with 55 forms, Strata with Smart+ at /strata/, Vercel team renamed to studio_rann, docs/ folder created with VISION.md, ROADMAP.md, ANIMATION-SYSTEM.md. CLAUDE.md added to root.
- **June 9, 2026** — Shipped **Spore** (`/spore/`, generative stippling) and **Pollen** (`/pollen/`, advanced stippling). Pollen: variable-radius blue-noise engine, Circle/Polygon/Line points with Size/Angle Range + Random + Warping, Gamma/Contrast/Overpaint/Hide Zone, Rotation/Flip/Invert, Solid/Adaptive colour with RGBA + Random, presets, and WYSIWYG PNG/JPG/SVG/Figma export (export serialises the exact preview points). Both are single-file vanilla HTML/CSS/JS. Hub nav + `/spore/` + `/pollen/` routes added.
- **July 23, 2026** — Prototyped **Halide** (`/halide/`), a single-file dither-portrait tool inspired by evaluating ditherface.com (a manual $150-per-slot commission service, not a tool). Real Floyd–Steinberg + Atkinson error diffusion and recursive-Bayer ordered dithering (verified: no prior dithering code existed in the repo — algorithms written from scratch), plus a flat-threshold baseline. Reuses Pollen's shell verbatim where it fit: drop-zone, zoom/pan, tone pipeline (rotation/flip/invert/gamma/contrast, + a new Bias shift), PNG/JPG/SVG export, and the `organica-svg` → Figma postMessage protocol. Square-crop toggle for avatar framing. One `runs` (row run-length-encoded ink cells) feeds preview canvas, raster export, and true-vector SVG output alike — same shared-function WYSIWYG discipline as Pollen/Living Path. 8 built-in presets + localStorage custom presets. Not yet added to `docs/` as a dedicated manual — do that once the prototype's shape is confirmed with Diego.
- **July 24, 2026** — Closed out the first full arc on **Halide**, validated against a real test photo (`halide/test-photos/`, deliberately kept out of git). Shipped: **background removal** (colour-key, global threshold — not connected flood-fill, which let lighting gradients bleed the flood into the subject; **Pick…**/Auto reference, since border-average contaminates the moment the subject touches the frame edge); a **sticker Outline** (largest-connected-component + enclosed-hole-fill for the silhouette, diagonal-corner-touch bridging so the ring always reads solid); **Background fill** outside the outline (pure subtraction, independent of Paper); an **interactive drag-to-position square crop** (replaced Crop X/Y sliders entirely, per direct feedback that a slider divorced from the visual is worse than dragging the frame on the photo); a **true colour swap** (Swap Colors now only trades hex values — verified byte-identical shapes before/after — it no longer silently toggles Invert, which is a real image change, not a recolour); the **Ditherface** preset (Atkinson + square + background-removed + Outline + Background fill, the validated reference-matching look, replacing the now-redundant "Studio Rann Ink"); and **"Simplify shapes"**, an opt-in SVG-export toggle that traces each region into one rectilinear `<path>` (evenodd fill-rule for holes) instead of a pile of `<rect>` tiles — verified pixel-exact against the tiled version on every tested case, 4,789 rects → 4 paths on the reference photo. Evaluated and **explicitly declined**: swapping the square/pixel mark for Genesis organic forms (would duplicate Pollen, and would undo the Simplify win since organic marks don't merge like rectilinear cells do); autonomous/hover-reactive pixel animation (autonomous fits the existing Motion vocabulary and is worth revisiting as an animated-SVG export; mouse-hover reactivity needs real JS and can't be a static export file — different deliverable, not built). Full manual: `docs/HALIDE.md`.
- **July 26, 2026 (accessible names + contrast)** — Two fixes, both found by measuring rather than assuming. **(1) Accessible names:** audited all six panels and found **121 of ~210 form controls with no accessible name** — a slider sat next to a `.ctrl-label` reading "Grid width" with nothing tying them together, so a screen reader announced "slider" with no name. Same markup pattern (row → label + control, no `for`/`aria-labelledby`) in every tool, so fixed once as `Organica.autoLabelPanel(document)` in `organica-core.js` instead of 121 hand-edits. **Two bugs caught only by re-measuring after the first pass, not by reading the diff:** the first edit deleted `global.Organica = Organica;` while adding the new function, which would have broken *every* tool silently (no console error until something called `Organica.*`); and the `hasName()` check treated any `el.closest('label')` as "already named", so Strata's toggle switches (checkbox wrapped in an empty `<label class="toggle">` styled as a slider knob, no text) were skipped as false-positives — fixed by requiring the wrapping label to have non-empty text. Final: **0 of ~210 unnamed**, segmented buttons keep their own visible text (the function skips buttons that already have text — `aria-labelledby` replaces a name, it doesn't add to one), zero runtime errors. **(2) Contrast:** requested a "just a little more margin" on `panel/value`/`panel/sub-label`; measuring first showed it was already a **real AA failure**, not a margin problem — `--mid` was **three different values** across tools (Strata's own `--muted: #888` at 3.17:1, Spore's `--mid: #c8c0b0` at **1.62:1**, everyone else's `#726a5e` at 4.43:1 *on panel background* — all below the 4.5:1 floor). Unified to **`#696256`** everywhere (5.4:1 paper / ≥5.0:1 panel, real headroom, computed the same way as `--border-strong`). Also separately answered a **px vs rem** question: kept px — browser zoom at 200% already scales the panel correctly with px (verified: no overflow, no clipping), rem's only real benefit is respecting an OS-level font-size override (rarer path than zoom), and the panel is pixel-grid-locked (26px rows, 1px borders, a hierarchy resting on a single px of difference) so a partial rem conversion would break alignment — documented as a conscious choice, not silently decided. Docs: `docs/DESIGN-SYSTEM.md` §5 (`--mid`) and §6 (accessible names).
- **July 25, 2026** — Shipped **Komorebi** (`/komorebi/`), the first **WebGL2** tool in Organica (a browser API, not a framework — holds the vanilla/single-file rule; a Canvas2D radial blur would be ~9M ops/frame, dead). Replicates 木漏れ日 — dappled sunlight through a canopy — via the three real graphics mechanics Diego named: **(1) light cookie / gobo** — a canopy mask (procedural 2-layer fBm foliage combined with `max()` so gaps only exist where both layers open; OR an uploaded silhouette, so a Halide 1-bit export / Strata trace / photo drops straight in as the canopy) projected onto a ground plane in perspective, two layers domain-warped at different rates = wind; **(2) penumbra + pinhole** — the detail nobody implements and the thing that *makes* it komorebi: the sun subtends ~0.53°, so a gap at height h blurs its shadow edge by ≈ h/108, and once a gap is smaller than that blur it becomes a pinhole projecting a round disc of the sun instead of the leaf shape. Implemented as ONE "Height" control = a disc-kernel sample radius over the cookie (golden-angle taps, per-sample rotation), so small gaps naturally wash into soft coins of light — verified visibly in Gobo mode; **(3) god rays** — post-process radial blur of an occlusion buffer (Mitchell, GPU Gems 3 ch.13), *no* shadow map / *no* 3D scene — the occluder is the same 2D canopy, so pass A (occlusion, half-res) and pass B (composite) share the CANOPY_LIB + PROJECT_LIB GLSL chunks and can never disagree. Two modes: **Scene** (horizon + canopy ceiling + rays + dappled floor — the picture) and **Gobo** (orthographic flat fill — the tileable *pattern*, the Phase-4 feed). **Wind: Sway | Drift** — Sway is built from a single phase angle so the field is exactly periodic → the **WebM export is a genuinely seamless loop** (Phase-6 installation deliverable); Drift travels and deliberately doesn't loop. WYSIWYG discipline (same as Pollen/Halide/Living Path): one `renderFrame(w,h)` serves preview, PNG/JPG (`toDataURL`, sync — `toBlob` raced the canvas resize-back), WebM frames, and the pixel source the SVG reads. **SVG export = posterised tone-band separation** — a volumetric field has no honest 1:1 vector form, but N flat luminance bands traced into one path each *is* honest and is a real print/riso separation (bands stacked as "≥ level" regions so no hairline seams); reuses **Halide's rectilinear contour tracer verbatim** (evenodd fill). 8 built-in presets (Forest Floor / High Noon / Cathedral / Shoji / Undergrowth / Riso Two-Tone / Dusk Ember / Bamboo), each a complete deterministic state layered over the Forest-Floor baseline; custom presets via `localStorage`. Drag on canvas = move the sun (rays + light pools both read `uSunUV`). Hub nav + `/komorebi/` route added. Manual: `docs/KOMOREBI.md`.
- **July 25, 2026 (panel layout)** — Consolidated to **one control panel, always on the right**. Strata's only panel was on the left (grid flipped to `1fr var(--panel-w)`, aside moved to `grid-column: 2 / grid-row: 2` — the first attempt used `grid-row: 1/-1` and the aside fought the header for row 1, pushing it below the panel). Living Path had **two** panels; merged into one, ordered *what you load → what you apply → what you inspect* (Input · Presets · Effect stack · Source view), giving the stage the full width. Genesis Library keeps its left column on purpose — it lists **sets**, which is navigation, not controls. Verified: all six tools now report exactly one panel, on the right, 248px, zero uppercase, zero runtime errors.
- **July 25, 2026 (panel component)** — Audited the six control sidebars (Strata, Spore, Pollen, Living Path, Halide, Komorebi): **four different section-heading styles** (9/400, 10/500, 12/600×3), **three widths** (240/244/280), **two slider tracks** (2px/3px), two label sizes, **76 uppercase elements**, and two markup vocabularies (`.ctrl-row` vs `.row`). New **`shared/organica-panel.css`**, modelled on Figma's Design panel: **sentence case everywhere, no tracking on labels**, three levels of hierarchy (section → sub-label → row), one row height so controls align down the column, tabular values so numbers don't jitter under a dragging slider. Section title went 12px/600 uppercase+0.1em → **11px/500 sentence** — smaller on the page *and* faster to scan, which is the whole argument for dropping caps. Class names match what the tools already used, so adoption was "link the file, delete the local copy"; `.sec h3` / `.row` / `.group-label` are aliased so Strata's and Living Path's JS is untouched. Verified: **0 uppercase** across all six, one width (248px), one slider (2px), one heading (11px/500), nothing below 9px, all Manrope, zero runtime errors. Two things worth knowing: the tool's `<style>` loads *after* the component, so a leftover local rule silently wins — migrating means deleting, not just linking (Living Path's `.sec h3` survived my first pass and its headings fell back to the browser's default `h3` size); and Strata deliberately keeps its full-width slider with min/max captions ("Clean → Raw") — the slider's visual style is unified but that layout does real work for its audience, so it's a documented variant, not drift. Also lifted 6px index numerals and 8px captions in Spore/Pollen to `--fs-micro`.
- **July 25, 2026 (header component)** — Audited every header outside the hub: **five different structures for ten pages** (`#topbar` in Spore/Pollen/Halide/Komorebi, `.top` in Living Path, `<header>`+`.toolbar` in Strata, an editorial `<header>` in Genesis index/Indicators, `.toolbar` in Library/Creator), plus a stale `#organica-banner` migration notice on three pages. The four main tools had **8–9 interactive controls in a 40px bar** and, measured: no `<header>`/`<nav>`/`<main>` landmark at all, **zero `aria-*` attributes**, a `#status-text` mutated from JS with no live region (so export progress was never announced), `:focus` defined on only 3 of 10 pages, `title=`-only explanations (unreachable by keyboard), and `--border` #d0c8b8 at **1.49:1** — failing even the 3:1 UI minimum. New **`shared/organica-header.css`** + `Organica.status()` / `Organica.popover()`: one component, five slots (identity · context · status · spacer · actions), three variants (Tool / Catalog / Editor — only the Context slot differs). **The rule: identity, context, status, at most three actions.** Everything else moved to where it acts — Export Scale + Simplify + PNG/JPG/SVG into an **Export popover** (with `aria-expanded`, Escape, click-outside and focus-return, verified step by step), Komorebi's Play/Pause/Reset onto the **canvas HUD** next to the clock they control (they were split: time read bottom-left, controlled top-right), and REC into the popover's Motion group since it is an export, not a transport. Applied to Halide (8→4 controls) and Komorebi (9→3). `--border-strong` is **computed, not eyeballed**: a first pass at #b5a992 *looked* right and measured 2.08:1; the shipped #958462 is 3.27:1 on paper and 3.03:1 on panel. Logo link target went 14px→26px (WCAG 2.2's inline-link exception doesn't cover a nav control). Responsive fixed too — the old bar never overflowed, flex just squeezed until logo/"EXPORT SCALE"/"→ FIGMA" each wrapped onto two lines inside 40px; the component sheds the least-critical text instead (verified at 700px: no wrap, no clip). Template updated so new tools inherit it. **Dev gotcha, third time:** `python3 -m http.server` cached `organica-core.js` so hard the page ran a stale copy and threw `Organica.status is not a function` with an empty console — replaced with a `no-store` dev server; when a change seems not to apply, suspect cache before code. Still to migrate: Spore, Pollen, Living Path, Strata, Genesis pages.
- **July 25, 2026 (system pass)** — Three cleanups that turn "coherence by copy-paste" into an actual system, done ahead of the Phase-4 Pattern Engine. **(1) `shared/organica-core.js`** — the copy-shared JS is now one module: download, preset storage + legacy migration, Figma postMessage, hex validation, the rectilinear contour tracer, zoom/pan. The copies had **already drifted**, which is the whole argument: Living Path revoked its download URL after a delay (correct) while three tools revoked immediately (can cancel the download); Komorebi's tracer emitted `.toFixed(2)` coordinates while Halide emitted integers; Komorebi validated colour hex while **Halide did not** (typing junk in a hex field set an invalid colour). Each merged routine is the better of the variants — the unified tracer emits the shortest correct form, so it is byte-identical to Halide's *and* **43% smaller than Komorebi's**, proven on 48 equivalence cases incl. checkerboard saddles, ring-with-hole, solid and empty masks (geometry identical everywhere). Wired into Halide, Komorebi, Pollen, Spore. **(2) Typography unified to Manrope** via `shared/organica-tokens.css`. Killed four families (Syne, Syne Mono, DM Sans, DM Mono, Georgia, ui-monospace) and four variable names for one idea (`--font`/`--sans`/`--mono`/`--display` → **`--font`**, old names kept as aliases). **The real find: six of eleven pages declared `'DM Mono'` and never loaded it** — Spore, Pollen, Halide, Komorebi, Genesis Library and Creator had been silently rendering system monospace, so the design was never what the CSS claimed. Verified: all 11 pages carry the tokens, zero old families, zero stale imports, Manrope loads as a variable font (200–800). **(3) `docs/DESIGN-SYSTEM.md` + `docs/UI-SHELL.md` + `shared/_template.html`** — type scale (1.2 ratio anchored at 11px), weights, tracking, spacing, per-tool accents, each with the **Figma text-style mapping** (Figma tracking is a *percentage*, CSS is em — they map 1:1, don't type 0.08 into Figma). The template is a working page, verified rendering with zoom/pan already wired from the core. **Known drift left honest, not hidden** (documented in UI-SHELL §7): panel widths still 240/244/260, Genesis pages use a different shell entirely, Strata and Living Path weren't retro-fitted, and Spore/Pollen/Halide still run their own inline zoom/pan — migrating those is safe but untested, so it's a follow-up rather than done blind.
- **July 25, 2026 (housekeeping)** — Two architectural cleanups ahead of the Phase-4 Pattern Engine, which will be the 8th consumer of the shared library. **(1) Split `organic-library.css`** — it mixed the 77 keyframes with the Genesis *catalog page* layout (`body{padding}`, `header`, `.grid`, `.cell`), so any tool wanting the animations inherited page chrome. Now `organic-animations.css` (pure, reusable, append-only rule moved here) + `organic-page.css` (chrome), with `organic-library.css` kept as an `@import` shim so every existing link is untouched — page chrome imports first, animations second, order load-bearing. **Verified rule-for-rule** on index/library/creator: identical computed vars, body metrics, 77 reachable keyframes, exact flattened rule counts (372/350/353 = lib+inline as before), and forms 1/29/36/48 computing identical fill/stroke/animation. **The load-bearing discovery:** the animations need `--bg-cell`, but *none* of the three Genesis pages define it — they inherited it only from the `:root` inside the old bundle, and only forms 29 (iris) and 36 (moon mask) use it to punch holes. A naive split would have turned exactly two forms into black blobs and left the other 53 fine — near-invisible. That contract is now documented at the top of `organic-animations.css` and in `docs/SHARED-LIBRARY.md`. **(2) Standardised localStorage** to `organica.<tool>.<thing>` (was three different separators: `pollen-presets`, `halide-presets`, `organica_komorebi_presets`, `organica_library`). Each tool migrates forward on first read and **deliberately does not delete the legacy key** — presets and custom forms are real user work, and leaving the old copy means a rollback still finds them. Genesis is the delicate one: `organica.library.forms` is read/written by *both* `library.html` and `creator.html`, so they carry an identical `readLibraryRaw()` that must stay in sync. Verified end-to-end per tool (seed legacy → read → migrate → save new → legacy stays frozen) and cross-file for Genesis. **Dev gotcha that cost a cycle:** the Genesis migration looked broken on reload and was just the dev-server cache — `?v=N` fixed it, exactly as `docs/LIVINGPATH.md` already warned. New doc: `docs/SHARED-LIBRARY.md`.
- **July 25, 2026 (later same day)** — Komorebi: **Scene mode removed from the UI** per direct feedback — Diego wants pattern-making (Riso Two-Tone, Shoji direction), not a landscape picture; the Scene shader code stays in place but inert (`uMode` now hardcoded to gobo in JS) so nothing downstream (occlusion pass, rays, SVG separation) had to change. Canopy got two new controls for organic realism: **Clumping** (a slow low-frequency field that varies local leaf density across the frame — denser thickets, wider gaps, instead of one uniform threshold) and **Leaf detail** (a fine high-frequency layer that scallops the leaf edges and flecks pinholes through the interior, instead of smooth blob edges). Rebuilt the preset set around Gobo-only patterns: **Forest Floor / Riso Two-Tone / Shoji / Dense Canopy / Sparse Grove / Fine Foliage** (the old Scene-tuned presets — High Noon, Cathedral, Undergrowth, Dusk Ember, Bamboo — were dropped, not just hidden). Caught in verification: the first "Dense Canopy" tuning (density 0.74 + leaf-detail 0.55) rendered near-solid black — Leaf detail's nibble subtracts as often as it adds, so on an already-high-density field it closed off the few remaining gaps instead of texturing them; fixed by lowering density to 0.60 and leaf-detail to 0.35. Lesson for tuning future presets: Leaf detail and Density fight each other at the high end, verify visually rather than reasoning from slider values alone.
- **July 2026** — Shipped **Living Path** (`/livingpath/`), a full web port of ivangrozny/LivingPath (single-file vanilla). Two engines (Vector node-effects | Raster "glyph hydrography"), layer **groups + blend modes**, 9 raster algorithms (dilate/erode, blur, threshold, noise, particles, center-line, polygonize, seam-carve, reaction-diffusion), 24 presets mapped to the author's example sheets, **Vector→Raster chaining**, live multi-language **text specimen**, and installable **OTF export** (charset/name/HTML specimen/`.lvp` projects/**randomised alternates** via `rand`+`aalt`) run off a **Web Worker**. Manual: `docs/LIVINGPATH.md` (incl. §12 Development notes). Key invariant: preview = specimen = export share one normalised 1000-box scale (WYSIWYG). **Dev gotchas** (also in the manual): the dev server caches aggressively — hard-refresh / `?v=` when testing; `<svg>` and `display:flex/grid` both defeat the `hidden` attribute (toggle a class instead); verify visibility via `getComputedStyle().display`, not the attribute. **Still open:** Genesis Creator Bézier tangent-handle drag/edit (flagged since setup, unresolved).

- **July 27, 2026** — Komorebi: widened Canopy from a single foliage generator into a **6-pattern gobo library** (Foliage / Cellular / Veins / Drop mark / Blinds / Blob), per direct steer — "pattern naturali o meno su diverse superfici e diverse luci... concentriamoci prima sui pattern, poi le animazioni" (wind/animation is deliberately parked for a later pass). All six are `float mask(vec2 p)` functions sharing the same six sliders (Scale/Density/Edge/Layer2/Clumping/Leaf detail), each repurposed to fit — the panel relabels the rows per pattern (`applyPatternLabels()`), so no new UI real estate per type. **Drop mark** uses Organica's own gesture (a jittered grid of gravity-tailed circles + a smaller satellite-droplet layer) as a gobo, not just foliage — the most on-brand of the five new masks. **Blinds** is the one deliberately non-natural pattern (venetian slats / grid), per the brief's own "natural or not." Added a **Canvas Bloom** preset (Blob pattern) built from two of Diego's reference photos (painted canvas studies — soft organic two-tone blobs, blurred edge). **Real bug caught by verifying the vector export, not just the PNG preview:** the reference's sage green measured luminance ≈0.64 — the *same* posterize band as the cream background at Bands=2 — so `buildSVG()` silently produced one full-canvas rectangle instead of the traced blob shape, even though the live preview looked correct. Diego's requirement that **export must always be a genuine vector file** is what caught this (checked by calling `buildSVG()` directly and inspecting path count/geometry, not just eyeballing the PNG). Fixed by darkening the shadow colour to luminance ≈0.45 and documented the rule for future two-tone presets in `docs/KOMOREBI.md` §16: colours must straddle the band threshold in *luminance*, matching on screen isn't sufficient. All six patterns + the "Foliage" default + existing built-in presets (Riso Two-Tone etc.) verified regression-free after the `canopy()` refactor. Full detail: `docs/KOMOREBI.md` §16. **Not yet done:** wind/animation tuning per-pattern (Sway/Drift currently apply uniformly; some patterns like Blinds or Drop mark may want different wind semantics — parked per Diego's own sequencing).

- **July 27, 2026 (later same day) — Komorebi off WebGL/GLSL onto Canvas2D/JS.** Direct feedback on the 6-pattern work above: the raw GLSL was suspected as "the problem" — it wasn't a library (Komorebi never depended on one; the §16 bug was a luminance-math oversight, not a library fault) but it *is* genuinely hard to read/debug (no breakpoints, no console.log). Once the priority was stated plainly — best possible pattern **shapes**, always exportable as real vector SVG; wind/animation explicitly deferred — the GPU stopped earning its complexity cost. Checked `shared/organica-core.js` first per instruction: nothing pattern/noise-specific there to reuse (it's UI/utility — download, presets, Figma, the rectilinear contour tracer, zoom/pan, header, sliders), so it was untouched and still fully reused (contour tracer, hex validation, preset storage). Rewrote Komorebi's entire render pipeline: no WebGL, no GLSL, no shader compilation — every mask/noise/compositing routine is a direct, checkable port to plain JS functions on scalar (x,y). Biggest architectural change: **no continuous animation loop** — wind/time is frozen (Sway/Drift are now two different *static* warp shapes, not motions), and rendering is on-demand (`scheduleRender()`, one rAF-coalesced call wired via a single input/change listener on the panel). Added occlusion caching so colour/grade/ray tweaks (~10ms) don't pay the noise-recompute cost that shape changes do (~120ms preview, ~480ms full export with rays — all measured, all comfortably interactive for an on-demand model). Penumbra went from 14-tap per-pixel disc sampling to a separable box blur — cheaper and reads as exactly what it does. SVG export got *simpler*, not just ported: `ctx.getImageData()` off a real Canvas2D context is top-down already, so the old WebGL readPixels y-flip code is just gone. **Verified the thing that matters most first**: re-ran the exact Canvas Bloom luminance check from §16 (`buildSVG()` → 2 bands, 2 real contour paths, ~4.5 KB) to confirm the rewrite didn't regress the one bug that was actually found and fixed there. All 6 patterns + all 6 built-in presets re-verified visually against their pre-rewrite look; WebM recording disabled (not deleted) since recording a static frame isn't a real feature until animation returns. Full detail incl. the one disclosed simplification (dropped sun-parallax "sunShift", re-addable later): `docs/KOMOREBI.md` §17.
- **July 27, 2026 (still later same day) — Komorebi quality fix + control audit.** Immediate feedback on the Canvas2D rewrite: "il risultato è pessimo e ci sono tanti controlli della precedente libreria che non servono." Two real, separate findings, not vague dissatisfaction. **(1) A genuine bug, not a taste call**: every pattern rendered as a muddy wash. Isolated by setting Dapple Height to 0 and comparing — the box blur standing in for the GPU's penumbra sampling was ~5× too strong, because a dense box average over a radius is a much stronger low-pass than the old **14-tap sparse Monte-Carlo disc sample** at that same nominal radius (sparse sampling under-samples fine noise and leaves visible texture; a full average erases it). Fixed with an empirically-verified ×⅕ correction, verified against Foliage, Cellular, Veins, Drop mark, and Canvas Bloom before/after. **Canvas Bloom barely moved** (Height≈0.02 already) — exactly why the bug survived the §16 verification pass, which leaned on that preset. **(2) A real control audit, not just this bug**: confirmed **Colour → Canopy** swatch is never read anywhere in gobo-mode rendering (grep'd before deleting, not assumed) — pure Scene-mode leftover — removed. Confirmed **Wind → Speed/Loop** are not just visually inert but literally unread by `windOffset()` since time is frozen — removed. The one genuinely debatable cut — the whole **Rays + Sun position** cluster (god rays, Density/Weight/Decay/Exposure, Azimuth/Altitude/Size) — was real working code but the single biggest thing still pulling Komorebi toward "photoreal light sim" against the stated shape/vector priority, and the single most expensive render pass (O(w·h·32) ray march); asked Diego rather than deciding unilaterally, since it's a bigger scope cut than dead-code removal. **Answer: remove both.** Colour → Sun (the tint swatch, unrelated to the deleted Sun position) stays. Full detail: `docs/KOMOREBI.md` §18.
- **July 27, 2026 (still later same day) — Komorebi tree/foliage variety.** Ask: "ho bisogno di komorebi che richiamino diversi tipi di alberi e fogliame." Asked which species first (broadleaf, conifer, sparse birch/poplar, palm/bamboo all named) rather than guessing, since each implies genuinely different mask maths. Shipped: **Foliage renamed "Broadleaf"** (label only, same code — its existing dense rounded look already reads as oak/beech). **Conifer** (new pattern) — needle tufts via a single cosine angular-modulation "star" SDF per grid cell, no per-needle loop. **Fronds** (new pattern) — covers palm *and* bamboo as one mask; a repurposed "Spread" control (angular window width) is narrow for near-parallel upward canes, wide for a full radiating palm burst. **Sparse birch/poplar needed no new mask** — just a **Birch Grove** preset tuning existing Broadleaf (low density, small scale, high clumping/leaf-detail) — a case of "this is parameter tuning, not a new shape generator," decided rather than defaulted into a 7th pattern type. 4 new tree-themed presets total (Birch Grove/Conifer Grove/Palm Shade/Bamboo Grove), each own palette. All verified visually (Palm Shade's radiating stars in particular look strong) and via `buildSVG()` producing real multi-path vector geometry, not degenerate rectangles. Full detail: `docs/KOMOREBI.md` §19.
- **July 27, 2026 (still later same day) — reference-photo workflow.** Created `komorebi/reference-photos/` (kept out of git, same convention as `halide/test-photos/`) so Diego can teach the target look via real photos instead of describing it. 18 photos uploaded on the first pass (dappled-shadow photography, painted canvas studies, riso/grain prints) — read all of them and reported back the recurring themes not yet covered (leaf silhouettes, pinhole-dot bursts, density gradients, heavy grain/riso texture, chromatic-aberration edge fringing, visible surface material under the light) rather than building blind. Two actioned: **(1) Blinds retuned** to match a specific reference (2×2 window projected on a wall) — pure parameter tuning, no new code: the fix was **Spread down to 0.03** (not Scale, whose slider floor of 0.4 can't reach few-enough repeats given Blinds' hardcoded ×6/×5.4 internal frequency), Slat width at its floor, Cross grid maxed — shipped as **Window Light** preset, verified against the photo and via `buildSVG()`. **(2) Leaves (new pattern)** — first "recognizable silhouette" request: a lens/vesica leaf shape (width tapers to zero at both base and tip, unlike every other pattern's shape), Elongation controls round-vs-slender, Serration adds a toothed margin. Caught and fixed a real correctness bug before shipping: the width formula is only valid for t∈[0,1], so a naive "distance = |x| − width(t)" reads as false-positive "inside" far outside the leaf along its own centreline — fixed with a capsule-style fallback outside that range. Shipped as **Leaf Litter** preset. Full detail: `docs/KOMOREBI.md` §20.
- **July 27, 2026 (still later same day) — Komorebi "Cluster": the organic-randomness technique.** Diego's critique was about **method**, not subject: "non focalizzarti su 'cosa' (leaves) ma sul come giocare sulle forme in maniera totalmente casuale." **Diagnosis:** every shape pattern so far (Drop mark/Conifer/Fronds/Leaves) places one same-size shape per jittered-grid cell — and jitter can't hide a lattice, because equal shape-count-per-cell and equal size are the statistics the eye actually reads. **Three attempts failed and were each isolated and rendered ALONE before judging** (the discipline that mattered here): a thresholded ridged-fbm "bold branch" layer gave cow-spot patches, not a flowing branch; continuous spatial modulation of the sampling *frequency* gave wood-grain/fingerprint rings (classic FM artifact — scaling coordinates by a spatially varying factor makes that factor's iso-contours visible); blending two fixed-frequency fields' *values* was artifact-free but no better than the plain mask, so it was reverted rather than kept as noise. `foliageMask` was restored byte-for-byte after all three. **The fix came from re-reading the reference photos for MECHANISM instead of subject** — the two painted studies make it unambiguous: everything is a **union of round blobs with enormously varying radii, merged smoothly where they overlap** (sparse → isolated dots of wildly different size; dense → one flowing mass with bulging joins). That's a placement+combination property, which is precisely why every noise-thresholding attempt missed it. **Shipped as `clusterField`:** (1) 3-octave scatter with each octave in its **own rotated frame** (irrational-ish angle → no two lattices can align, something a single jittered grid can never do); (2) **heavy-tailed radii** `r = 0.10 + 0.75·u^p`, p = 1+Layer2, so mostly tiny with a rare few huge; (3) **`smin` smooth union** instead of `min()` — and this is where branch-like continuity comes from: it *emerges* from chains of overlapping discs rather than being drawn, exactly what attempt (1) failed to draw explicitly. Two presets at the range's ends (Cluster Bloom / Cluster Scatter), both verified for real vector SVG output; cold render ≈200ms, comparable to Fronds. **Not done:** retro-fitting the same three ingredients into Drop mark/Conifer/Fronds/Leaves — proven now, but it changes four shipped patterns so it wants its own pass. Full detail: `docs/KOMOREBI.md` §21.
- **July 27, 2026 (still later same day) — scatter engine retro-fitted into every shape pattern.** The §21 follow-up: **Drop mark, Conifer, Fronds, Leaves now all run on one shared `scatterField(x,y,P,shapeFn,cfg)`**, supplying only their own shape SDF (`shapeDrop`/`shapeNeedle`/`shapeFrond`/`shapeLeaf`/`shapeDisc`). The shape does its **own rotation**, because orientation isn't uniform — a drop may only tilt (gravity has a direction), a frond points mostly up, the rest are free 360°. **Two new controls, and they had to be new**: Size range and Merge belong to the *scatter*, not to any shape, and all six existing sliders already carried shape meanings — reusing one would make a control mean different things per pattern. Shown only for the five scatter patterns, hidden for the noise-field ones (dead controls are a bug here, §18). **Real discovery while retuning:** Size range had to gate the **octave count** too, not just the radius tail — extra octaves *are* a size difference (a whole population at a smaller scale), so at the slider's floor Bamboo Grove kept sprouting tiny specks. One control, one meaning. **Two controls freed by the refactor were given real jobs rather than left dead**: Drop mark's Layer 2 (its hand-rolled satellite-droplet pass is now the engine's finer octave) → **Splat**; Cluster's Layer 2/Leaf detail (they were standing in for Size range/Merge) → **Elongation**/**Irregularity**, so its blobs read as hand-made lumps not geometric circles. **Presets re-checked visually one by one, not assumed** — Palm Shade and Leaf Litter improved on defaults; **Bamboo Grove is the useful counter-example: bamboo canes are genuinely near-uniform, so the heavy tail that flatters palm actively hurt** (ships at Size range 0.30). All 14 presets + 10 patterns verified, real vector SVG throughout. **Design note:** the engine is now the default way to place shapes — a new shape pattern is a `shapeFn` + `cfg`, not a new grid loop. Full detail: `docs/KOMOREBI.md` §22.
- **July 27, 2026 (still later same day) — Raking light.** Asked for a preset matching a reference photo of low sun on leather: soft light pools all elongated along **one shared diagonal**. **No existing control could do it** — Cluster's Elongation rotates every blob independently, so raising it gives ellipses pointing every which way; per-shape elongation and *shared* directionality are different things. Took the mechanism from the physics instead: glancing light foreshortens the whole projection along the direction of incidence, so every shape stretches the same way because the *projection* is anisotropic, not the shapes. Implemented in `groundPoint()` as an anisotropic squash of the sampling coordinates (rotate into the rake frame → scale that axis → rotate back), with two controls in Format: **Raking** + **Rake angle**. Because it acts on the coordinates feeding the mask it applies to **all 10 patterns for free**, noise-field ones included — Broadleaf/Veins/Blinds/Blob all gained a directional variant a per-shape control could never have given them. **Verified as a true no-op at 0 rather than assumed**: sampled 500 random pixels, ground coords bit-identical to the pre-Raking formula (max delta 0), and all 14 pre-existing presets confirmed at Raking = 0, so nothing shipped changed. Shipped as the **Raking Leather** preset. Full detail: `docs/KOMOREBI.md` §23.

- **July 27, 2026 (still later same day) — Strata "Refine paths" verified, fixed and shipped.** A Paper.js post-trace editor existed uncommitted in the working tree; Diego asked for it to be verified rather than deployed blind. **It had been written but never once executed** — running it found **five defects, all fixed**: (1) `paper.view.fitBounds is not a function` — fatal, `fitBounds` is an `Item` method not a `View` one, threw inside `importSVG`'s `onLoad` so the editor never loaded and clicking Refine only logged an error; (2) a **layout feedback runaway** (flex item with default `min-height:auto` + in-flow canvas → canvas grows wrap grows canvas) measured reaching **16,777,215 px**; (3) **device pixel ratio applied twice**, canvas coming out 2× its container on HiDPI — root cause was `fitToView` measuring *the canvas*, which paper.js resizes via inline style, making it a one-way ratchet; now measures the container, which is also what the existing `ResizeObserver` watches so re-fits happen automatically; (4) **style silently rewritten** — `styleTraceItem()` forced fill/no-stroke on every path and export reads from those same items, so touching Refine turned a "stroke only" trace into filled shapes and discarded Strata's own Fill mode; (5) **units dropped on export** — the header was rebuilt from parsed numbers, turning `570.000000pt` into `570`, a **1.33× size change** on Figma import. (4) and (5) mattered most: they silently altered output for a tool whose whole job is print-ready vectors. **Two false alarms I caught by testing instead of reporting**: "Unite all loses geometry" (no — overlaps merging *is* union; verified with disjoint squares, area preserved exactly) and "anchor drag is broken" (no — my own screenshot-px vs CSS-px error; verified the moved distance matches the drag exactly through `view.zoom`). Also a **deliberate behaviour change kept**: export now follows the selected region tab instead of always emitting the full SVG — the same WYSIWYG rule as Komorebi/Halide/Pollen; cross-region contamination verified impossible. **First third-party runtime library in Organica**: `shared/paper-full.min.js` (Paper.js 0.12.17, MIT, sha256 recorded in the manual) vendored rather than CDN — no build step exists, and a CDN would be the system's only external runtime dependency and would break the offline/local-backend flow. Load order `core → paper → organica-paper` is load-bearing (organica-paper merges into the namespace, so loading it before core lets core clobber it). Shipped to production at Diego's call to test on real cases and iterate. Manual: `docs/STRATA-REFINE.md`. **Known sharp edge, not a bug:** anchors need a precise (~8px) click and have no persistent visual indicator — clicking the stroke selects but does not drag.

- **July 31, 2026 — Strata now traces in production (Vercel Python functions).** Strata had never worked on the deployed site: `BACKEND_URL` was `null` on any non-localhost host, so the whole tool was local-only while sitting in the hub next to 8 tools that work everywhere. Now `api/trace.py` + `api/health.py` run the **same pipeline** as Vercel Python functions; `backend/process.py` stays the single source of truth and is shared verbatim (`vercel.json` → `functions.includeFiles: backend/**`), with `api/_shared.py` supplying only the serverless plumbing. Frontend picks the endpoint automatically — `localhost:5050` locally, `/api` deployed, identical response shapes. **Three read-only-filesystem blockers, all found by measuring rather than guessing:** (1) `cv2.imwrite('output/debug_trace_input.png', …)` was an unconditional **relative-path** write depending on cwd — raises on any read-only FS; now conditional on a real `output_dir`. (2) All server writes routed through `writable_dir()`, honouring `STRATA_WORK_DIR` (`/tmp` on Vercel, `backend/output/` locally). (3) **potrace is an external binary** that doesn't exist on Vercel — the fallback now catches the missing executable and returns a valid empty SVG instead of a 500. Per-invocation scratch dirs are removed in a `finally`, verified clean after repeated calls since **Fluid Compute reuses instances** and a leak would accumulate. **Bundle size was the real go/no-go and I measured it from the actual manylinux wheels** rather than estimating: opencv-headless 143 MB + numpy 57 + pillow 19 + vtracer 3 = **222 MB against Vercel's 500 MB limit** (not the 250 MB I first assumed — checking the docs changed the decision from "too tight, don't" to "comfortable"). `opencv-python-headless` replaces `opencv-python`; the GUI bindings can't load there anyway. **A real bug this uncovered in shipped work:** `handle_send_to_figma` preferred `output/full.svg` whenever it existed and only then fell back to the POSTed SVG — so the Refine editor's edited geometry, and any region other than "full", **never reached Figma**. Verified with a marker SVG that the POSTed body was discarded. My earlier Refine verification had stopped at the frontend boundary (I confirmed the payload carried the refined SVG, never that the backend honoured it) — a gap in my own testing, not inherited code. **Deliberately still local-only: the Figma push.** It works by the plugin fetching a URL the local server keeps on disk, which serverless has nowhere to put; the plugin is shared by every Organica tool so it gets one unified pass rather than a Strata-specific workaround. Deployed, → Figma downloads the SVG and says so. Verified end to end: functions run under a **read-only cwd** (the real serverless condition), local backend unchanged (byte-identical trace output), and the full browser flow trace → Refine → export with style/units preserved. Manual: `docs/STRATA-REFINE.md` §6.

- **August 1, 2026 — Strata Refine/Preview state-sync + production 413 fixed.** Diego reported five bugs from live use of the Refine editor shipped July 31. All five verified live (real trace, real sketch upload) and fixed: **(1) Refine edits silently reverted** — `state.svgData` was written once right after trace and never updated again, so reopening Refine or switching to Preview reloaded that stale snapshot and discarded any edit (delete/colour/smooth); fixed by baking every Paper.js `onChange` straight back into `state.svgData`, and only reimporting into the editor on an actual new trace, not every tab switch. **(2) Preview showed stale content** — same root cause; Preview's `#svg-embed` was only ever painted at trace time. **(3) sketch/trace offset residual** — a *second*, independent cause from the one fixed July 27: the sketch underlay was scaled to the trace's full padded viewBox, but the backend pads 50px on every side (`copyMakeBorder`), so the sketch stretched into that padding; fixed by subtracting the padding from the scale calculation (position was already correct since padding is symmetric). **(4) 37 shapes but 36 exported paths** — `simplify(4)` could reduce a shape to zero segments, which `exportSVG()` silently drops from the markup while the shape stayed in the live model/pills as a "ghost"; fixed by pruning degenerate shapes after simplify. **(5) Smooth ignored selection** (unlike Fill/Border/Simplify) — fixed to scope to the selected shape, same rule as the others; this also fixed a related minor bug where `collectPaths` recursed into a CompoundPath's own children after already matching the CompoundPath itself, double-processing it. Also hardened `backend/server.py`'s `Content-Length` parsing (missing/non-numeric/negative/oversized header now returns a real error instead of an uncaught exception or unbounded read) — verified in isolation since the shared local dev server was mid-use by another session. **Separately, a production-only bug**: tracing a full-resolution phone-photo crop returned an opaque "Server error 413" on the deployed site only. Root cause: Vercel Serverless Functions hard-cap the request body at 4.5MB (platform-level, confirmed via Vercel's own docs, not app-configurable) — measured a real crop's PNG at **12MB**. Fixed by re-encoding the crop as JPEG before upload (cv2.imread is format-agnostic) with progressive quality/downscale fallback — same photo now uploads at **1.76MB**, verified end-to-end (200 OK, real trace). Also fixed `showServerGuidance()`, which picked its message off `BACKEND_URL` truthiness — true both locally AND in production (`/api` fallback) — so a production trace error always showed the stale "Strata runs locally, can't run on this static site" text even though tracing has run in production since July 31; now branches on `isLocal` and on whether the server actually responded, with a real "Trace failed" message for the reached-but-errored case. Manual: `docs/STRATA-REFINE.md` §6 (Refine bugs), inline comments in `backend/server.py` (Content-Length).

- **August 1, 2026 (later same day) — Shipped Camouflage (`/camouflage/`), Gray-Scott reaction-diffusion patterns.** Scoped alongside Komorebi at Diego's request, then deliberately kept as a **separate standalone tool** rather than a Komorebi pattern or a Komorebi feature addition — confirmed with Diego before building (asked, didn't assume): Gray-Scott is an iterative PDE that has to *converge* over many steps, a different interaction model from Komorebi's instant on-demand masks, and folding both in one file would repeat Living Path's own two-engine complexity. Two other asks were explicitly declined by Diego rather than silently reopened: reintroducing Komorebi's wind/animation loop (removed July 27, §17/§18) and Komorebi's Sun-position/disc-ellipse controls (removed July 27, §18) both stay removed. Built on the **same shell as Komorebi/Halide** (`organica-tokens.css`/`organica-header.css`/`organica-panel.css`, `organica-core.js` — no new shared code needed, every building block already existed) — genuinely new work is only the simulation core. **The one place in Organica where a continuous `requestAnimationFrame` loop is correct** rather than the on-demand scheduling every other tool uses: the loop *is* the algorithm (Gray-Scott has no single-frame result to schedule), same as Living Path's own CPU reaction-diffusion. Auto-pauses once the field's mean per-cell change stays below a threshold for 40 consecutive frames ("Simmering…" → "Settled · f=… k=…"), so it doesn't spin forever once converged. **Toroidal (wrap-around) grid from the start**, not retrofitted — makes the Zoom control (repeats the same seamless field, no reseed, no artefacts) and every export's seamless tiling free instead of two separate features. **Spatial gradient** blends a second f/k target across the canvas (Horizontal/Vertical/Diagonal/Radial) — e.g. leopard spots dissolving into labyrinth on one canvas — verified by sampling the live field (left-third vs right-third mean A−B measurably different, 0.56 vs 0.41) not just eyeballing it. SVG export traces the **reaction-diffusion field directly** (not a re-rasterised image) using the same posterised band-separation tracer Halide/Komorebi already share. **Honesty finding, disclosed in the manual rather than hidden**: isotropic Gray-Scott cannot produce true parallel stripes from random seeding — verified by testing the brief's own suggested "stripes" f/k and a transition zone, both gave labyrinth/worm patterns, not straight lines; shipped as an honestly-labelled "Stripes / worms" regime instead of a promise the algorithm can't keep, with an anisotropic-diffusion follow-up documented for later. **Real bug caught during verification, not before it**: init-order bug where `syncColor()` (painting the Ink/Paper swatches on load) triggered a render before the simulation grid was allocated — threw on first load, canvas stayed blank; fixed by allocating/seeding the grid first. Manual: `docs/CAMOUFLAGE.md`.

- **August 3, 2026 — Camouflage reframed as a 3-mode creative disruption engine (Phase 1 of the `docs/organica-camouflage-tool-brief.md` scope).** Diego supplied the missing research docs (`docs/Research_Report_Camouflage.md`, `docs/Research_Report_GenerativeArt.md`) and the brief itself, reframing the tool explicitly away from "match this environment" concealment toward Disruptive Pattern Material (Hardy Blechman's term) — camouflage as design language (dazzle ships, adaptive brand identity, Thayer's countershading), judged on aesthetic merit only, with an explicit non-goal (no adversarial/ML-detection-evasion functionality — noted, held to). Scoped via three rounds of clarifying questions before writing code (Structural mode: shared palette layer, not a 4th engine — confirmed; naming: kept "Camouflage" — confirmed; physical-output pipeline: both export-tuning AND a JS algorithm replica — confirmed, deferred to its own pass; "Concealed Motion" 7th animation category: deferred to its own pass, since it touches `docs/ANIMATION-SYSTEM.md`/`organic-animations.css`, a file the other 55 forms depend on). Also evaluated and **declined** p5.js as a runtime dependency for this work — it would be Organica's first creative-coding framework, breaking the vanilla-Canvas2D consistency every tool (including Komorebi's own deliberate move *off* WebGL/GLSL) has held; used only as personal prototyping reference, same as the research report's own recommendation to study Shiffman's p5.js Coding Challenge #13 rather than embed it. **Shipped**: header rearchitected onto `.org-header--editor` + `.org-tabs` (a shared component defined in `organica-header.css` since before Camouflage existed but never adopted by any tool — confirmed by grep before using it) for Disruptive/Dazzle/Countershade mode-switching. **Dazzle** (new): recursive polygon fragmentation, cut angles snapped toward 0/45/90/135° + jitter for a bold zigzag rather than generic-irregular tessellation, sibling facets from one split always differently coloured, pure vector end-to-end (canvas preview and SVG export share one facet list, no tracing). **Countershade** (new): Thayer-principle tonal gradient (dark-light-dark = fake volume), Linear (cylinder) or Radial (sphere), seeded Grain overlay for the "same design, every physical unit unique" packaging case; exports as a genuine SVG gradient def, not a raster. **Structural palette** (new, shared across all three modes, not a 4th mode — decided explicitly to avoid re-deriving Disruptive/Dazzle's geometry a second time just to recolour it): hue from local orientation (field gradient direction / facet cut angle), lightness from a local scalar — genuinely iridescent, Morpho-butterfly-adjacent result verified visually on Disruptive (rainbow rings around each spot). **Reproducible numeric Seed** (new, shared across modes, one `mulberry32` PRNG each generator resets itself from at the start of its run): verified directly, not assumed — regenerating Dazzle at the same seed twice (with a different seed run in between) produced byte-identical SVG both times. **Drop-mark seeding** (new, Disruptive only): seeds the simulation from Organica's own gravity-tailed teardrop gesture (a simplified port of Komorebi's `shapeDrop` SDF) instead of plain circles — the concrete link the research report draws between the drop mark and Thayer/Cott's disruptive coloration. **A real dead-control bug caught during verification, not before it**: the shared Palette's Threshold row stayed visible in Dazzle/Countershade under Duotone even though neither mode's render/export ever reads it — same class of bug as Komorebi §18 ("a control with no effect is a bug"); fixed by gating its visibility on `mode === 'disruptive'`. Per-mode preset stores (three separate `localStorage` keys, since the three modes' control sets don't overlap) rather than one shared list that would need per-mode filtering. Verified end-to-end in a live browser session: all three modes' render + SVG export in both Duotone and Structural (6 combinations, no exceptions), PNG export at ×2 scale with correct canvas restore, presets save/apply/delete per mode, 0 of 38 controls unaccessibly-named. **Deferred to their own passes, not built this round**: physical-output pipeline (Ink/Stitch density maps, vpype-flow-imager-style flow hatching, k-means/Lab screen-print separation) and the "Concealed Motion" animation category. Manual: `docs/CAMOUFLAGE.md`.

- **August 3, 2026 (later same day) — Camouflage output-quality audit: four measured defects, all fixed.** Diego's verdict on the three-mode build was that output quality was "limitata e mediocre". Rather than tune by eye, each mode was **measured**, and four distinct root causes came out — all the same underlying mistake in different clothes: **an internal representation leaking into the output**. **(1) Render resolution.** 54% of a settled field's cells hold an *intermediate* value — more than half the field is edge information — and the build threw all of it away by thresholding per CELL, writing an N×N ImageData and letting `drawImage` upscale: on a 128 grid at 640px, each of ~1,717 boundary cells became a hard **5-pixel stair-step**. Now the field is sampled **bilinearly at output-pixel resolution** with the threshold applied there; the simulation grid is untouched, this is purely how it's drawn (9.2ms/frame, 74fps with the solver — an affordable trade for the most visible defect in the tool). **(2) Dazzle facet distribution.** Measured: largest facet **29.6%** of canvas, top three **65.2%**, largest:smallest **1162×**, two thin slivers (perimeter²/area 689 vs 16 for a square) — i.e. "three blobs", not dazzle. Two causes: the cut point was **uniformly random in the bbox** (near an edge half the time = one huge piece + one sliver) and recursion was **depth-driven**, so a facet that won the big half stayed big regardless of size. Now cuts pass near the polygon **centroid** (±18%) and recursion is **area-driven**. After: largest **4.1%**, top three **12.1%**, ratio **3×**, worst sliver **55**, zero over 100. `Fragments` changed meaning from recursion depth (2–8) to approximate **facet count** (4–80) — what its label always implied. **(3) Structural colour.** The build mapped angle onto the **full 0–360° hue wheel** at fixed saturation: the classic rainbow colormap, ignoring the user's own Ink/Paper — and backwards from the phenomenon it cites, since `Research_Report_Camouflage.md` §1 is explicit that a Morpho's blue is a Bragg stack reflecting *"una banda stretta di lunghezze d'onda (~460nm)"*. Now a **76° arc anchored on the user's Ink hue** (falling back to Morpho blue 220° when Ink is achromatic, as the default is). A second defect found alongside: the gradient was sampled over a **sub-cell step**, and bilinear interpolation has a constant derivative inside a cell — so every pixel in a cell got the same hue, producing visible cell-sized facets; the step is now a full cell. **(4) SVG geometry.** The shared `Organica.contoursToPathD` walks CELL edges and emits axis-aligned segments — exactly right for Halide (where the subject *is* dithered square pixels), exactly wrong for organic blobs: measured **26 right-angle vertices per spot**, 1,220 straight segments, **zero curves**. Camouflage now reuses the shared tracer for **topology** (loop/hole detection, which is correct and worth sharing) and replaces only the **geometry** — decimate, low-pass filter, emit Catmull-Rom **cubic Béziers**: 1,209 curves, 0 straight lines, verified by overlaying the exported SVG on the canvas. The shared tracer is deliberately **not** modified (Halide depends on it), and Dazzle correctly still exports straight lines only since its facets are meant to be sharp. **(5) Countershade shading curve** — a physics error rather than a measurement: it ramped **linearly** from a flat plateau, which is why it read as "a flat area with dark edges" instead of a rounded form. Real self-shading has no plateau — a cylinder's Lambertian brightness is **√(1−x²)**, the rounded shoulder that *is* Thayer's countershading. Now 17 sampled stops shared by canvas and SVG so the two can't drift, with Falloff as the power. Verified end to end: all 7 mode×palette combinations export valid SVG, seed reproducibility intact, canvas restore after export correct, 0 unnamed controls, no console errors. Full before/after numbers: `docs/CAMOUFLAGE.md` §11b.

- **August 3, 2026 (still later same day) — Disruptive enrichment: anisotropic stripes + seed-from-shape.** Diego asked for four Disruptive enrichments (anisotropic stripes, seed-from-shape, cephalopod-style layered noise, Web Worker for higher resolution); sequenced the architectural one (Worker) last on purpose, so it ports one stable set of logic instead of being built into. Two landed this pass. **Anisotropic diffusion — three attempts, in order, the first two real failures caught by measuring, not eyeballing:** (1) a rotated diffusion tensor through axis-aligned finite differences (textbook formula) worked at θ=0°/90° but produced a **maze, not stripes** at θ=50° — the 4-point corner estimate of the mixed ∂²/∂x∂y term is too coarse once D⊥ gets small. (2) Rotating the *sampling* instead, with **bilinear** taps, fixed the topology at 50° but a direct ratio measurement (mean-squared step along vs across the stripe) explained why it still read soft: **9.24× at θ=0°** (exact grid lookup) collapsed to **2.15× at θ=50°** (bilinear blends in neighbours, leaking diffusion into the axis meant to be suppressed) — Catmull-Rom made it *worse*, confirming the leak scales with interpolation smoothness. (3) **Shipped: nearest-neighbour taps** — no interpolation, no blur to leak, clean directional split verified at every angle tested; the trade (faint lattice-aliasing stipple) is minor at Grid=Fine and part of why the new "True stripes (zebra)" preset sets it. A separate, earlier bug in the same feature: the unscaled tensor Laplacian diverged (field oscillating at the ±1 clamp bounds, `lastDiff` stuck ~2.0) because a plain second-difference stencil is ~5× the magnitude of the existing hand-tuned 9-point kernel at the same nominal coefficient — fixed by matching scale (×0.2), not by guessing a smaller number. New controls: Anisotropy › Amount (0 = original kernel, unchanged — every existing preset stays on it) and Direction. At high Anisotropy, `seed()` also switches from random blobs to seeds **laid out along the stripe direction and repeated across the perpendicular axis** — the first version kept random placement and produced clean stripes near the few seed points with blank canvas everywhere else, since suppressed perpendicular diffusion can no longer carry information sideways to unseeded regions. **Seed from shape**: a third Seed shape option opens a file picker (Halide/Strata output drops straight in) and seeds B only inside the dark region via the same **masked reaction-diffusion** Living Path already uses on glyphs — cells outside the mask are pinned to the resting state every step, not just at t=0, so the pattern can't leak past the silhouette no matter how long it runs. Verified directly: seeded from a synthetic star, ran to settlement, confirmed **0 of ~24,957 outside cells** had moved — 0.00% leak, not "looks contained." Full regression pass (all 6 Disruptive presets + Dazzle + Countershade) confirmed no breakage. Deferred to their own passes: cephalopod-style layered noise (a genuine new noise primitive, bigger scope) and the Web Worker. Full detail: `docs/CAMOUFLAGE.md` §11c.

- **August 3, 2026 (still later same day) — Camouflage preset rebuild + three new Disruptive parameters.** Ask was to verify the presets actually make sense, build better ones, and survey the other tools for reusable parameters/functions. Ran a cross-tool inventory first (Komorebi's noise/scatter/raking stack; Living Path's 9 raster algorithms, group/blend architecture and marching-squares contours; Pollen's blue-noise + flow-field streamlines; Halide's dithering) before writing any code. **Presets were measured, not eyeballed** — each run to settlement from a fixed seed and fingerprinted (ink %, connected components, mean component area, local-density spread). Two of seven failed on their own terms: **`Drop-mark disruption` was a straight duplicate of `Labyrinth / coral`** (identical f/k, settled to 42.9% vs 40.8% ink with the same topology — a seed *shape* leaves no trace once a Gray-Scott field converges, since the seed decides *where* features land, never *what kind*), and **`Stripes / worms` settled at 63.7% ink**, so dark it read as solid with light cracks. Now **15 presets**, verified by signature check to have **0 identical field-generating parameter pairs**. **Useful caveat about the metric itself**: it is rotation-invariant, so `True stripes` and `Tiger diagonal` fingerprinted identically (52.8/11/1487 vs 52.7/11/1485) — a separate directional measurement confirmed stripe angles 0° vs 65°, anisotropy ratios 9.4 vs 12.0. The metric was wrong for that pair, not the presets. **Three new parameters, each fixing something the tool couldn't do:** **(1) Feature size** — `dA`/`dB` had been hardcoded at 1.0/0.5 forever, leaving no way to change how *big* features are (Grid changes cost, Zoom repeats a tile), even though `Research_Report_Camouflage.md` §1 states outright that diffusion coefficients are the scale control. The obstacle was stability: explicit Euler holds only while dt·D ≲ 1 and dA was **already exactly at that limit**, so Scale raises D and lowers dt together (dt·dA pinned at 1) — dt changes only how fast a reaction-diffusion system reaches its steady state, not what that state is. Verified bit-identical at s=1 (no shipped preset moved), and mean component area came out **49 → 101 → 164 → 215** for s=0.5/1.0/1.6/2.4, proportional to s exactly as area ∝ λ² ∝ D predicts. **A real physical bug surfaced doing this**: at s=2 the pattern died out entirely (0 blobs) because nucleation sites sized for D=1 disperse below critical density once D doubles — seed size now scales with √s. **(2) Modulation** (the cephalopod-skin layer): a slow noise field varies f/k per cell so one canvas carries several regimes at once; `hash2`/`vnoise`/`fbm` ported verbatim from Komorebi rather than reinvented. **Defect caught by measuring the field itself, not the output**: plain fbm's *mean drifts with frequency*, so `fbm*2−1` measured entirely negative at low Region size (−0.29…−0.53) and pushed f/k consistently one way — quietly changing regime instead of texturing it; fixed with `fbmSigned` (accumulates `vnoise−0.5`, centred at zero by construction), and the slider floor set to 1.8 after measuring residual bias across 6 seeds (0.188 at 1.2 → 0.076 at 2.0), since below that less than one noise cycle fits the canvas and it degenerates into the ramp Gradient already does better. **(3) Persistence** — re-injects B at the original sites every step instead of only at t=0, so the drop-mark shape survives into the settled pattern; verified by correlating the result against the injection map: **−0.02 → 0.35 → 0.45** for 0/0.35/0.8, where the −0.02 restates the duplicate-preset diagnosis numerically. Also exposed **Sites** (nucleation count, previously hardcoded `5 + rng()*4`). Full regression: 15/15 presets apply, 7/7 mode×palette SVG exports valid, canvas restore correct, seed reproducibility intact, 0 unnamed controls, no console errors. **Deferred, evaluated but not built** (Diego's call this round): Living Path's `blendField`/`rasterFieldFromGroups` group+blend architecture, its `skeleton` and marching-squares contours, Pollen's flow-field streamlines, Halide's dithering. Full detail: `docs/CAMOUFLAGE.md` §11d.

- **August 3, 2026 (still later same day) — Camouflage: two real bugs + convergence readout.** Asked to re-explain the Disruptive algorithm and all its presets; documenting it against the actual code surfaced two bugs the UI was hiding. **(1) `Sites` was dead whenever Anisotropy > 0.3** — the anisotropic branch of `seed()` lays nucleation points out as rows along the stripe direction and never read the control; measured at Anisotropy 0.85, Sites 5/20/40 all seeded exactly **675 cells**. Sites now drives the row spacing, but over a deliberately narrower range than on the isotropic path, and that is physics rather than a UI compromise: suppressed perpendicular diffusion can't carry the pattern sideways into an unseeded band, so sparse rows leave bare canvas. **My first attempt did exactly that** — mapping Sites straight onto a row count seeded 3 rows on a 176 grid at the default and left most of the frame empty; caught by looking at the render, not at the numbers, and fixed by interpolating between "just dense enough to fill" and "tightly packed" with the default landing on the previously-hardcoded spacing. **(2) The canonical f/k values were literally unreachable** — a range input snaps to `min + n·step`, and at `step="0.0005"` the Pearson/Munafo spots-mitosis pair fell between stops: f 0.0367 → **0.0365**, k 0.0649 → **0.0650**. So the `Leopard spots` preset declared one pair and the simulation ran another (visible in the status bar all along). Step on f/k/f2/k2 is now 0.0001 and both are exact; Coral labyrinth's 0.0545/0.0620 happened to land on a stop and were always right. **(3) The simulation stopped too early** — the symptom Diego actually reported ("l'animazione a un certo punto si ferma"). The stop is deliberate (Gray-Scott converges), but the test was "mean per-cell change per step under 4e-5", and measuring showed three things wrong with it: `lastDiff` **never reaches zero** (it settles on a noise plateau at ~2.4e-5, barely under the cutoff, so the cutoff fired mid-evolution — and lowering it would mean never stopping); it is **not monotonic** (traced `f19 6.2e-5 ↓ · f31 5.1e-5 ↑ · f55 1.6e-4 ↑`, because Gray-Scott has an *expansion* phase where spots multiply and change climbs again); and at the old stop point the structure was still forming (47 blobs / 25.8% ink, versus 49 / 26.7% when left to run — it was stopping mid-mitosis). **Two replacement criteria failed before the third worked**, each caught by measurement: "stopped improving" on `lastDiff` fires during the expansion climb and was *worse* (stopped at 1.2s with **30.7%** of cells still to flip); "drift stopped improving" never fires at all, since drift decays smoothly and indefinitely (Giant cell 35% → 11% → 8% → 4% → 1% of peak, no plateau) so it ran to the 30s cap while ink% had been flat since 7.5s. Shipped: measure the **field** the way a viewer would — snapshot, wait a window, see how far the picture moved — and stop when drift falls to **12% of that run's own peak**, counted cumulatively because drift spikes back up occasionally (7.4% → 15.0% → 8.0%) and a consecutive counter resets on every blip. Also scaled **steps per frame by 1/dt**, since Feature size lowers dt and `Giant cell` was overrunning the cap purely because each frame advanced less simulated time. Result: Leopard spots 6.6s/11.0%-still-to-flip → 8.5s/8.5%; Giant cell 30s-cap → 6.0s/4.0%. **Disclosed honestly**: the residual never reaches zero — once the structure is fixed the features still migrate slowly forever (16.3% → 7.9% → 5.2% flip at 6k/12k/20k steps); the criterion guarantees the *structure* has settled, not every pixel. **(4) Convergence readout** — "Simmering…" now carries a percentage. Two refinements from watching a real run: the signal **rises** during growth, pinning a first-sample baseline at 0% for half the run (fixed by baselining on the peak), and it fluctuates near the end, making the readout go **85% → 81% → 99%** (fixed by making it monotonic). Full detail: `docs/CAMOUFLAGE.md` §11e. Full regression: 15/15 presets, 7/7 mode×palette SVG exports, seed reproducibility, canvas restore, 0 unnamed controls, no console errors. Also: the 23 generated preset previews were pulled back out of git (`camouflage/previews/`, 1.6MB of tool *output*, regenerable on demand) and added to .gitignore alongside the existing reference/test-imagery block.

- **August 10, 2026 — Shipped Warping (`/warping/`), plus a shared noise module and a bug fix to Camo Turing's committed anisotropic-diffusion work.** Session started as a Camo Turing follow-up (domain-warping the anisotropic stripe direction with `uWarpAmount`/`uWarpScale`, using `hash2`/`vnoise`/`fbm`); Diego stopped it mid-implementation and redirected to a standalone tool instead, so those in-progress Camo Turing edits were reverted (`git checkout`) and Camo Turing itself was **not modified** this session. Asked for a reuse analysis before writing code, then three explicit decisions: **Canvas2D/JS** over WebGL (same reasoning as Komorebi's own July move off GLSL — the pattern family is a pure per-pixel function of (x,y), no iterative convergence, and Canvas2D stays debuggable); **extract `hash2`/`vnoise`/`fbm`/`ridgedFbm`/`voronoiF1F2` into `shared/organica-noise.js`** now that a third consumer exists (Warping), rather than a third private copy — ported verbatim from Komorebi's own JS (itself a port of older GLSL), then **Komorebi switched to consume the shared module** (one-line alias `const { hash2, hash2b, vnoise, fbm, ridgedFbm, voronoiF1F2 } = Organica.noise`) and verified regression-clean: Cellular pattern (its own `voronoiF1F2` user) renders identically, Canvas Bloom's `buildSVG()` still produces the exact real 2-loop evenodd path the §16 fix required (not the single full-canvas rectangle that bug produced), zero console errors. Load order is load-bearing and documented in the new file's own header: core → noise → tool script, since core's own last line (`global.Organica = Organica`) would clobber a `.noise` attached before it. **All three pattern families in v1**: Wood grain / Marble veining (fbm turbulence — `sin` of radius or position plus fbm-driven phase), Cellular network (Worley F1/F2, `edge = f2−f1` thresholded — the direct "giraffe reticulation" case from the original ask, reusing Komorebi's own Cellular-mask technique verbatim), Level curves (fbm field sliced into N threshold bands — topographic contours). **Warp is the tool's namesake control**: bends the *sampled coordinates* (two decorrelated fbm taps, same `(5.2, 1.3)` offset trick as Komorebi's `windOffset`) before any pattern function runs, so it's one shared mechanism across all four patterns rather than a per-pattern reimplementation. **Two real bugs caught by looking at the render, not by reasoning about the numbers**: (1) the first version's world-coordinate scale made every slider's own stated meaning wrong — "Rings: 6" produced ~25 stripes, because `rings` multiplied raw un-normalised coordinates directly; fixed by normalising every pattern's frequency input by a shared `NORM` constant, so a slider now reads as "cycles across the canvas half-width" consistently across all four patterns. (2) Seed was baked into the *base* coordinate before the geometric radius/direction math, which for Wood/Marble silently shoved the pattern's own centre far off-frame at the default seed — wood rings read as near-straight parallel lines instead of concentric rings, since far from a circle's centre the local curvature is negligible; fixed by keeping Seed's effect confined to each pattern's own *noise* sampling (grain/turbulence/warp), never the geometric position term. Verified visually after the fix: Oak plank reads as genuine concentric rings, Burl wood (high Warp) as visibly twisted grain, Giraffe hide as a real polygonal reticulation, Topographic map as real closed contour lines. Export reuses Komorebi's own `buildSVG()` shape verbatim (posterised N-band luminance separation via the shared rectilinear tracer, "≥ level" stacking so bands can't gap) — confirmed real multi-path geometry (5 paths / 6 bands on Giraffe hide, ~245 KB at scale ×2), not a degenerate rectangle. 8 built-in presets across the four patterns, `Organica.presetStore('warping', …)`, 0 of 23 controls unaccessibly-named (`Organica.autoLabelPanel`), zero console errors across every preset switch tested. Hub nav + `/warping/` route (`vercel.json`) added. **Not yet done**: the marble preset reads more like directional brushed grain than classic branching veins and could use a further tuning pass.
- **August 10, 2026 (later same day) — Wrote `docs/CAMO-TURING.md` and `docs/WARPING.md`, the two manuals missing for the repo's two newest tools.** Camo Turing had never had a manual at all (only the stale `docs/CAMOUFLAGE.md`, describing the removed predecessor tool); read the actual shipped file end to end — Seeds' six modes, Anisotropy, Layers' two independent blend systems (field-space shape vs. per-layer tint colour), the Transformers raster stack ported from Living Path, Compare/diff, Palette's three colour modes, Paper background image/stencil's real `<clipPath>`+`<image>` SVG embedding, and the 23 built-in presets (mrob/Munafo catalogue + animal/plant) — rather than writing from the session summary alone, since that only covered a fraction of what the file actually does. Corrected the tools table's Camo Turing row in the process: it had accumulated a claim (domain-warped stripe direction) from the reverted false-start earlier this same session that was never actually shipped.

- **August 10, 2026 (still later same day) — Shipped Loom (`/loom/`) Phase 1 MVP, from a Notion brief (`Grid Definition`) Diego shared and asked for analysis-only first.** Read the full brief before writing anything, then presented an architecture analysis rather than starting to build (per the brief's own instruction: "Always explain architectural decisions before writing code"), covering the pipeline (Canvas → Grid Definition → Constraint Engine → Universal JSON Model → renderers), the elementary-generators-vs-named-presets distinction the brief's own two lists imply, and three explicit forks put to Diego: Kiwi.js (Cassowary constraint solver) vs. a hand-rolled parametric solver for the Constraint Engine, single-file vs. native ES modules for the file structure, and math.js vs. hand-rolled math. **Decisions**: hybrid solver (Kiwi for modular/column/row/Bento-family grids where equal-by-default-but-overridable sizing is the real behaviour wanted — verified feasible by fetching and inspecting the actual npm package before committing to it; parametric direct math for continuous-function generators like Sinusoidal, since a sine curve isn't a linear constraint); native ES modules (the one deliberate deviation from every other Organica tool's single-file convention — a real module boundary exists here that a single huge file would obscure); math hand-rolled for the MVP's two generators, math.js deferred until a generator actually needs it. **Vendored `shared/kiwi.min.js`** (Cassowary solver, MIT/BSD-3-Clause, 14.5KB, fetched from the npm registry tarball and inspected — not npm-installed, since this repo has no build step) — same vendoring convention as Paper.js in Strata: a real file, not a CDN, license recorded. **Shipped**: Canvas Manager (size/unit/margin/safe area/bleed, 7 industry presets), two generators — **Bento** (Kiwi-solved: seeded random cell-merge spans on a lattice, equal-by-default track sizing with a hard minimum and a hard canvas-fill sum) and **Sinusoidal** (parametric: sine-modulated track *sizes*, disclosed honestly as not wavy *boundaries* since neither CSS Grid nor an axis-aligned SVG grid can represent that) — the Universal JSON Model (`tracks` + cell spans as the canonical source, pixel rects always derived, never stored redundantly), and three renderers: a live preview that is a genuine `display:grid` (not simulated with absolute positioning), SVG (outlined cells — grid guides, not filled shapes), and PNG; Figma export reuses the existing `figma-plugin/`'s SVG-import path verbatim, honestly labelled as not-yet-native-Auto-Layout (that's Phase 5). **One real bug caught during Bento's own build, before it shipped**: an initial track-weighting scheme (wider cells should pull their tracks wider) divided each cell's contribution by its own span, which made wide-cell tracks *thinner* — the opposite of the intended look; reverted to plain equal-by-default sizing, since Bento's visual variety already comes entirely from span topology. Hub nav link added directly after Creator (Diego's own placement request) + `/loom/` route in `vercel.json`. Verified live: both generators render correctly, `variety=0` is an exact no-op (16 plain 1×1 cells), Kiwi-solved equal columns sum exactly to the inner canvas width net of margin and gaps (checked numerically, not just visually), all four exports (PNG/SVG/HTML/JSON) and the Figma send run without throwing, 0 of 20 controls unaccessibly-named, zero console errors. Manual: `docs/LOOM.md`, including the full Phase 2–6 roadmap (remaining ~23 elementary/preset generators, WYSIWYG cell editing, native Figma Auto Layout frames, tests).

- **August 11, 2026 — Loom: Export moved from the header to the shared bottom-centre floating toolbar.** Diego caught that Loom's header still had its own `Export ▾` + `→ Figma` buttons — a leftover from `shared/_template.html`'s own layout, which predates the product-wide move (documented in `camo-turing/index.html`'s own comment, and mirrored verbatim in `strata/index.html`) of Export into `shared/organica-floatbar.css` and the outright removal of the Figma button pending its return as an Export-popover tab. Every other Organica tool (Strata, Spore, Pollen, Halide, Komorebi, Living Path, Genesis Creator, Camo Turing) already follows this; Loom now matches: header is logo + status only, the Export popover (PNG/SVG/HTML/JSON, PNG scale) lives in the floatbar fixed bottom-centre over the canvas, and `sendToFigma()` stays defined as a plain, unwired function — same "ready to reuse once it's a popover tab" convention Strata's own comment states. One real markup bug caught in the move, not before it: a stray `</path>` left over from editing the Export icon's SVG, which is invalid but silently tolerated by browsers (closing tags on void/self-closed elements are ignored) — found by re-reading the diff, not by any visible symptom. Verified live: floatbar renders bottom-centre offset for the right panel exactly like Komorebi/Camo Turing, popover opens upward, all four export functions run without throwing, zero console errors.

- **August 11, 2026 (same day) — Loom: mouse-wheel zoom/pan, via the shared `Organica.createZoomPan` rather than another private copy.** Checked first: Halide/Spore/Pollen/Strata each still carry their OWN private zoom/pan implementation, and only `shared/_template.html` actually calls the shared `Organica.createZoomPan` — a real, already-known drift (flagged back in the July 25 system-pass session notes). Rather than add a fifth private copy, wired Loom directly to the shared function, on `#canvas-frame` (a pure CSS transform — never touches `canvas.width/height` or the JSON model, so it can't affect any export; verified live that all four exports still run correctly mid-zoom). **A real UX gap found during verification, not assumed**: `createZoomPan`'s default zoom range is `[1, 12]` — zoom-OUT below 100% isn't possible by default, and Loom's canvas is rendered at its own real declared size (anywhere from an A4's 297px to a 1920px social preset), unlike Halide/Komorebi's own capped ~900px preview resolution — so a large canvas landed clipped by `#canvas-wrap`'s `overflow:hidden` with no way back except a keyboard shortcut. Fixed two ways: `min:0.1` so the user can always zoom out manually, and a `fitToViewIfNeeded()` that runs once whenever the canvas's own physical size changes (not on every unrelated param tweak) so a freshly picked preset lands fully visible by default instead of clipped. Verified: 1080×1080 auto-fits to ~63–72% depending on viewport, A4 portrait (small) stays at a true 100% (fit-scale correctly clamps at 1, never enlarges), zoom toward cursor confirmed via a dispatched `WheelEvent`, zero console errors.

- **August 11, 2026 (same day) — Loom: right-panel slider readouts were silently frozen, fixed once for the whole panel.** Asked to debug the right column and specifically verify whether the Grid sliders update their displayed numbers. They didn't: `Organica.enhanceSliders`'s own delegated listener (`organica-core.js`) only maintains each range input's `--fill` CSS custom property (the coloured track) and a click-to-edit affordance on the value span — it never syncs the span's TEXT from a drag, that's always been each tool's own job via a hand-typed `oninput="syncVal(this,'v-x')"` per control. Loom's Canvas (Margin, Safe area) and Grid (all 13 Bento/Sinusoidal params) sliders never got that wired, so dragging any of them left the number frozen at its initial HTML value while the grid itself rebuilt correctly underneath — confirmed live: `rg-bento-cols` set to 8 rebuilt the JSON model to 8 real column tracks, while `v-bento-cols` kept reading "4". Fixed with one delegated listener on `#panel` instead of 13 hand-typed attributes (the exact failure mode that caused the bug — easy to add a 14th control later and forget it again): reads any `input[type=range]`'s own `.ctrl-row`, finds its `.ctrl-val`, writes the value. Verified across Bento and Sinusoidal, including a value that got step-snapped by the browser (0.77 → 0.78 at `step=0.02`) — confirms the fix reads the slider's actual post-snap value, not a stale JS-side number.

- **August 11, 2026 (same day) — Loom: grid wasn't centred in its own canvas, root cause in the CSS Grid renderer.** Diego caught it visually after the slider-readout fix. Measured before touching anything: `#grid-preview`'s inline style sized the container to the FULL canvas (`width/height: 1080px`) while `grid.tracks` (Kiwi/parametric-solved) only sum to the INNER rect after margin (993.6px at 4% margin) — a plain `display:grid` container with no padding places its tracks starting at the top-left content edge by default, so all 86.4px of margin space landed entirely at the bottom-right instead of split across all four sides. Fixed in `html-renderer.js`'s `buildGridCSS`: container stays sized to the full canvas but now gets the margin as real `padding` (`box-sizing:border-box`), so the already-correct track sizes centre themselves naturally — no separate inner wrapper needed, and the fix applies identically to the live preview and the exported HTML snippet since both come from the same function. Verified numerically, not just visually: left/top/right/bottom gaps from canvas edge to the outermost cells now measure 27px/27px/27px/27px (scaled by the current fit-to-view zoom) on both Bento and Sinusoidal, where before the bottom/right gaps were visibly larger than top/left. SVG/PNG/JSON exports unaffected (they read `resolveCellRects`, a separate code path that was never wrong), reconfirmed running clean after the fix.

- **August 11, 2026 (same day) — Loom: grid line colour changed to Photoshop's default guide blue (`#3399ff`), everywhere at once.** Added `--guide-blue` to Loom's own `:root` (a content colour — the line the tool draws — not shared UI chrome, same exception CLAUDE.md already carves out for Halide's ink/paper), applied to `.loom-cell`'s border. SVG/PNG renderers took a `lineColor` param instead of a second hardcoded hex, and `main.js` reads the CSS variable once via `getComputedStyle` and passes it to both — so the live preview and every export read the exact same value rather than two hand-matched literals that could quietly drift apart later. Verified: preview renders visibly blue; captured the actual exported SVG blob (temporarily intercepting `Organica.download`) and confirmed it contains `#3399ff` and zero remaining `#0a0a0a` cell strokes; PNG/HTML/JSON exports still run clean.

- **August 11, 2026 (same day) — Loom: overlapping cell borders now draw as one line, not two stacked.** Asked to fix "when two lines overlap, show it as one line." Measured first rather than assuming a positioning bug: at Gap 0, adjacent cells' touching edges land at a 0.0000076px difference — geometrically exact, not misaligned. The real cause was every cell independently stroking all 4 of its own sides, so a shared boundary between two adjacent cells got drawn TWICE, exactly coincident; two overlapping anti-aliased 1px strokes compound into a visibly bolder/darker line than a genuine single border, in both the vector exports and (potentially) the CSS preview. Fixed at the source in three places sharing one idea — draw each unique boundary once: **(1)** `json-model.js` gained `collectEdges(rects)`, a de-duplicated segment list (canonical-keyed, coordinates rounded first so solver float noise can't split one edge into two keys). **(2)** `svg-renderer.js`/`raster-renderer.js` now stroke that edge list (`<line>` / one `ctx.stroke()` path) instead of one `<rect>`/`strokeRect()` per cell — verified the exported SVG for a 10-cell Bento at Gap 0 contains 32 `<line>`s and only the 2 unrelated background/guide `<rect>`s, zero per-cell rects. **(3)** The live CSS preview needed a different technique (a DOM grid can't draw an independent line) — every `.loom-cell` now sets only `border-right`/`border-bottom` inline, plus `border-left`/`border-top` on col-0/row-0 cells only, so a shared edge is always drawn by exactly one of the two neighbouring cells. Caught a related latent bug while touching this: the exported standalone HTML snippet referenced `var(--guide-blue)` with no fallback, which resolves to nothing (invisible border) once pasted into a page that doesn't load Loom's own `:root` — added the `, #3399ff` fallback. Verified across Bento (Gap 0 and Gap 12) and Sinusoidal, PNG/SVG/HTML/JSON exports all clean, zero console errors.

- **August 11, 2026 (same day) — Loom: Shuffle numbers.** New `Numbering` panel section (`Shuffle` / `Sequential`). Each cell gained a `number` field, independent of its `col`/`row`/`span` — shuffling relabels which block shows which number via a Fisher–Yates permutation (not the classic biased `sort(() => Math.random() - 0.5)`) without touching any actual layout geometry. `paintGridDOM`/`buildHTMLSnippet` read `c.number` (falling back to array index for safety). Sequential numbering is the default on every rebuild — deliberately not persisted as a param: any panel change re-runs `build()`, which regenerates `cells` from scratch and resets to sequential, since a shuffle is a one-off action on the current grid, not part of the reproducible generator pipeline. Verified: shuffled output is a true permutation of 1..N (same set, new order), JSON model reflects it, all four exports still run clean, 0 console errors.

- **August 11, 2026 (same day) — Loom: cell Padding, plus a Canvas Manager extraction note.** Evaluated (not built) turning Loom's Canvas Manager into a shared component: split it in two on the call — the zoom/pan + fit-to-view shell is genuinely universal and already duplicated privately in Halide/Spore/Pollen (only `shared/_template.html` and now Loom call the existing `Organica.createZoomPan`), but deferred per Diego's own call; the physical-space data model (unit/margin/safe-area/bleed/presets) stays Loom-only for now — every prior Organica `shared/` extraction happened at a second real consumer, never speculatively, even though it's a plausible seed for `docs/ROADMAP.md`'s own Phase 6 print/mural output pipeline. Documented the trigger condition in `docs/LOOM.md` §8 rather than just dropping the idea. **Shipped**: a `Padding` control (Grid section) that insets each cell's own visible box inward from its track allocation — independent of `Gap` (which changes the track sizes the Kiwi/parametric solvers actually resolve). Implemented as `grid.padding` on the Universal JSON Model (a persisted render parameter, not ephemeral UI state, so every renderer reads the same value): `resolveCellRects` shrinks each SVG/PNG rect symmetrically (clamped at 0, verified no throw at max Padding on the smallest cells), and the CSS/HTML renderer applies it as `margin` rather than literal `padding` — a literal CSS padding would only push the already-centred number label further from an unmoved border, a no-op, where `margin` is what actually shrinks the bordered box within its grid area. A real side effect, not a bug to special-case: once Padding pulls two adjacent cells apart, `collectEdges` naturally stops finding a shared edge to dedupe — it doesn't need to know Padding exists. Verified: live readout syncs (reuses the existing delegated panel listener), 0 is an exact no-op (no `margin` written to the DOM at all), Padding persists in the exported JSON, all four exports run clean at both 0 and the slider's max.

- **August 11, 2026 (same day) — Loom: cm and m added as canvas units, via a canonical-mm conversion layer, not a label-only add.** Analysed first, presented the fork explicitly: mm/cm/m convert cleanly with decimal factors, px does not (no fixed physical size, converting it would assume an unstated DPI) — Diego confirmed the real conversion layer over the faster label-only option, since the latter leaves Gap/Padding meaningless at metre scale (a mural's Gap slider would range 0–40 *metres*). **Found and fixed alongside** (Diego's own call, same code path anyway): SVG/CSS export never actually emitted a unit suffix — an A4 canvas exported `width="210"` (210px per the SVG/CSS spec's own bare-number default), not 210mm; `docs/LOOM.md`'s prior claim that this worked was wrong, never verified. **Shipped**: `canvas-manager.js` canonicalises any mm/cm/m input to an internal mm-equivalent (`canvas.width`/`height`), which every generator and the on-screen sizing already read directly — `canvas.displayWidth`/`displayHeight`/`unit` keep the raw typed number, unconverted, for labels only. Gap/Padding (absolute numbers, unlike percentage-based Margin/Safe area) route through the same canonicalisation (`unitVal()`), and their own slider range/step now rescales on unit change (`rescaleUnitField`) to preserve the physical size rather than leaving the raw number meaning something completely different — verified live end to end: 12mm Gap → 1.2cm → 0.02m (step-snapped, expected) switching units in sequence, then back to px correctly restored the 0–40 range. Export unit suffix: neither SVG nor CSS has a native metre unit, so any non-px unit exports as literal `mm` off the already-canonical number (a 3m×2m mural canvas verified exporting `width="3000mm" height="2000mm"`, dimensionally exact) — `viewBox` and the grid's own internal CSS stay bare canonical numbers rendered as px, only the outer declared size carries physical meaning. **A real bug caught by testing preset switches, not assumed away**: `applyCanvasPreset()` set `sel-unit` and a bare `lastUnit` tracking variable directly, bypassing the rescale entirely — picking a px preset right after an mm one left Gap's slider stuck at the mm range while the Unit field said px. Fixed by routing the preset's own unit change through the same `onUnitChange()` path a manual switch uses. All four exports verified clean at px/mm/cm/m, zero console errors.

- **August 11, 2026 (same day) — Loom: cm/m made the canvas disappear — three real bugs, all found by reproducing before fixing.** Diego reported the canvas vanishing under cm/m; reproduced first rather than guessing. **(1) Zoom clamp edge case**: switching Unit to cm with the default "1080" left unchanged (a real px→cm value has no conversion to preserve, see the cm/m session note above) reinterprets it as 1080cm = 10.8m — `fitToViewIfNeeded` needed ~5% zoom to fit, but `Organica.createZoomPan`'s own `min: 0.1` clamped it to 10%, and the shared library zeroes pan outright whenever zoom clamps to exactly its own minimum — reproduced via `getBoundingClientRect()` landing at `x:-5087, y:-4908`, entirely off-screen. Fixed by lowering `min` to 0.01 (covers real canvases up to ~50–100m before the same edge could recur). **(2) Double conversion**: made Width/Height rescale between physical units too (mm↔cm↔m preserve real size, same as Gap/Padding already did) — but `applyCanvasPreset()` still routed through the same `onUnitChange()`, which reconverted a value the preset had ALREADY written correctly (A4 landed at "2100" instead of "210" after switching units by hand, then picking the preset). Fixed by splitting Gap/Padding rescaling (`rescaleGapPadding`, used by both paths) from Width/Height rescaling (only in the manual `onUnitChange()`, never in `applyCanvasPreset()`, which supplies its own already-correct numbers). **(3) Uncaught solver crash**: a physically tiny canvas (typed while working in m, then Unit switched to px with — correctly — no conversion, since px has no physical equivalent to convert to) crashed the whole app with Kiwi's own uncaught "unsatisfiable constraint" exception. A fixed canonical-size floor was tried first and rejected: the real minimum depends on whatever Columns/Gap/Margin happen to be dialled in, not a constant. Fixed properly with a try/catch around `generator.generate()` in `build()`, degrading to a clear status message ("Canvas too small for this grid") for ANY unreachable parameter combination, not just this one path into it. All three verified live after the fix: the original repro (px→cm, unchanged number) now renders and stays centred, the preset-after-manual-unit-change sequence no longer double-converts, and the tiny-canvas sequence shows the status message with zero console errors instead of crashing.

- **August 11, 2026 (same day) — Loom: canvas size capped at 50 000 canonical units (50m at any physical unit).** Diego's own call after the cm/m debugging above. Applied as a ceiling alongside the existing floor (`MIN_CANONICAL`) at `createCanvas`'s single choke point — `clampCanonical()`. 50m chosen to stay well inside the range already verified working this session (`createZoomPan`'s `min:0.01` fits up to ~50–100m before its own zoom-clamp/pan-zero edge case could resurface) while covering every realistic Studio Rann mural. Verified live: 500m clamps to exactly 50 000 canonical units in the JSON model, canvas still renders centred and fit-to-view, zero console errors — `displayWidth` (what Diego actually typed) is deliberately left unclamped, only the geometry-driving canonical value is capped.

- **August 11, 2026 (still later same day) — Loom: Voronoi, the first polygon-shaped generator.** Asked to pick the next generator off the brief's own list; recommended Voronoi (direct reuse of `shared/organica-noise.js`) and Diego confirmed — but building it surfaced that the "direct reuse" plan was wrong: `voronoiF1F2` is a per-pixel RASTER query (nearest/second-nearest distance at one point, Komorebi's own Cellular-pattern technique), not something that produces cell polygon VERTICES — a grid-definition tool needs the actual boundary geometry, not a distance field. **Real architecture fork, surfaced and handled rather than faked**: Voronoi cells are arbitrary convex polygons with no honest fit onto Bento/Sinusoidal's shared `tracks + col/row/span` model (a bounding-box approximation would be exactly the kind of degenerate output this project's conventions already refuse elsewhere — the Padding no-op discipline, the real-vector-SVG rule). Extended the Universal JSON Model with `grid.cellShape: 'rect' | 'polygon'` as the fork every renderer branches on. **Cell construction**: seed points scattered (seeded RNG) in the inner rect, each cell built by half-plane intersection — Sutherland–Hodgman clip of the inner rect against every other seed's perpendicular bisector, O(N²), far simpler to get right than Fortune's algorithm at the cell counts this tool actually uses. `solver: 'geometric'` — a genuine third category alongside Kiwi/parametric, since there's no track lattice and no linear constraint system involved at all. **Renderers**: `svg-renderer.js`/`raster-renderer.js` now branch internally (moved the rect-vs-polygon resolve INSIDE each renderer, so main.js's export call sites don't need to know or care which kind of grid they're exporting); a new `collectPolygonEdges` (json-model.js) generalises the existing `collectEdges` rect-dedup to arbitrary polygon edges — two adjacent Voronoi cells share the exact bisector segment bounding them both, so stroking each cell's own full outline independently would have reintroduced the identical doubled-line bug `collectEdges` fixed for rect grids earlier this session. **No honest CSS Grid form exists for a polygon cell**, so `paintGridDOM`/`buildHTMLSnippet` branch to a new `buildPolygonSVG` (embedded SVG, built from the same de-duplicated edges the SVG *export* uses — about as close to byte-identical preview/export as this tool gets) instead of forcing one. Numbers are drawn at each polygon's centroid in that preview SVG — disclosed as staying consistent with the pre-existing convention that numbers were always a preview/HTML-only feature, never in the SVG/PNG export, for rect grids too. Gap/Padding hidden (not left inert) for Voronoi, since neither has a defined meaning on a polygon cell yet — same "a dead control is a bug" rule Komorebi's own audit established. Verified live: real polygon diagram renders (not a raster field), SVG export contains 55 deduplicated `<line>`s and zero `<rect>`/`<polygon>` cell shapes for an 18-point diagram, Shuffle produces a true permutation of the SVG `<text>` labels, switching back to Bento/Sinusoidal shows zero regression (live CSS Grid preview, Padding row reappears), 0 of 23 controls unaccessibly-named, zero console errors throughout.

- **August 12, 2026 (later same day) — Loom: fixed a focused-slider/wheel-zoom conflict, not a Hexagonal geometry bug.** Diego reported specific hexagons (39, 44) looking wrong after using the new generator. Investigated the geometry first rather than assuming the report was right: fetched the actual JSON model and SVG DOM for both cells — regular hexagons, all 6 sides equal length, all 6 boundary edges present exactly once in the rendered SVG, full correct 6-neighbour tessellation (44's missing neighbours were legitimately off-canvas, the rightmost column). Cell-level geometry was never wrong. **The real bug, found by reproducing my own testing steps**: after dragging a panel slider (e.g. Seed), that `<input type=range>` keeps keyboard focus — and Chromium routes mouse-wheel scroll to a focused range/number input *regardless of where the cursor actually is on the page*, independent of the `wheel` listener Loom's own zoom/pan already had on the canvas (which does call `preventDefault()`, but that has no effect on this separate focus-driven browser behaviour). So scrolling over the canvas to zoom in for a closer look — the exact workflow used to inspect the grid — silently also nudged whichever slider was last touched (reproduced live: Gap drifted 0.06→0.1, Seed 7→0 purely from scrolling near the canvas with Seed focused), regenerating the grid under different params and making specific cells look different from what was seen a moment before. This is a shared-module bug, not a Loom-only or Hexagonal-only one: `shared/organica-core.js`'s `createZoomPan` (used directly by Loom and `shared/_template.html`; other tools still run their own private zoom/pan, per the July 25 system-pass notes) now blurs `document.activeElement` at the start of its `wheel` handler whenever focus sits on a control outside the zoom/pan surface itself — wheel-over-canvas now means "zoom," never "zoom and also adjust whatever slider I used last." **Verified the fix directly reproduces the exact failure and resolves it**: focused the Seed slider, scrolled over the canvas at the same coordinates as the original repro — before the fix, Gap/Seed drifted and focus stayed on the slider; after, both values held exactly (0.06/7) and focus moved to `<body>`. Zoom itself still works (tracked 65% after the same scroll), keyboard zoom shortcuts and drag-pan untouched. Only Loom is a live consumer of `createZoomPan` today, so blast radius is contained to this one tool. Zero console errors.

- **August 12, 2026 — Loom: Hexagonal, the second polygon-shaped generator.** Continued the generator roadmap after Voronoi; picked Hexagonal because it's the cheapest possible proof that Voronoi's `grid.cellShape: 'polygon'` fork actually generalises — every renderer (SVG/PNG export, the embedded-SVG live preview, `collectPolygonEdges` dedup) already branches on that one flag, so the new generator only had to emit cells shaped `{ points, centroid }`; nothing outside `hexagonal.js`/`registry.js`/the panel block changed. Real regular-hexagon tessellation (standard redblobgames axial spacing — flat-top: 1.5r horizontal/√3·r vertical with odd columns offset half a row; pointy-top swaps the axes), not a distorted approximation. `Columns` sets target density (derives hex radius from `inner.width / cols`) rather than a literal count, so it behaves consistently on any aspect ratio. `Gap` shrinks each hexagon toward its own centre pre-clip (the polygon analogue of rect-cell Padding) — **verified 0 is an exact no-op**, edge-to-edge tessellation with a single de-duplicated boundary line, no doubled-stroke artefact. `Jitter` offsets each centre by a seeded random amount, breaking the lattice into an organic hand-set-tile look — verified visually at 0 (perfect grid) vs. 0.7 (overlapping, organic). Clipping to the inner rect reuses Voronoi's own Sutherland–Hodgman technique, generalised from one half-plane per neighbour seed to 4 fixed half-planes. **Dev-environment gotcha hit again** (documented since the Genesis-migration session, still true): the running `python3 -m http.server` served a browser-cached pre-edit copy of `registry.js` even after a hard navigate with a new query string — confirmed via `fetch(url, {cache:'no-store'})` that the disk file was correct all along; verification only succeeded after spinning up a throwaway `Cache-Control: no-store` Python server on a different port. Verified end to end: both orientations render correctly, all four generators (Bento/Sinusoidal/Voronoi/Hexagonal) regression-tested together with zero console errors, real de-duplicated polygon SVG export (276 `<line>`s on a 95-cell canvas), 0 of the new block's controls unaccessibly-named. Manual: `docs/LOOM.md` §4.

- **August 12, 2026 (still later same day) — Loom: Hexagonal's Flat-top/Pointy-top toggle replaced with a continuous Rotation slider.** Diego's own follow-up request: the two-option segmented control was never a real binary — "pointy-top" is just "flat-top" rotated 30°, and a rigid hex tiling tiles at any angle, not only those two. The naive implementation (rotate each hexagon's own shape, leave the lattice centres where they were) is wrong and was avoided rather than shipped-then-caught: a hexagon's edges only meet its neighbours' edges when its orientation matches the lattice's, so rotating the shape alone opens visible gaps/overlaps at any angle that isn't a multiple of 60°. Fixed by rotating the CENTRES and the SHAPE together — a rigid rotation of the whole pattern, which a honeycomb's own rotational symmetry guarantees still tiles everywhere. Building this needs the lattice generated in its own unrotated frame first, sized from the inner rect's own **diagonal** (not width/height — a rotated lattice needs to cover every corner of the fixed, unrotated inner rect it'll be clipped against), then rotated as a whole around the inner rect's centre, then clipped. `Rotation`'s range is exactly `[0, 60)` since that's the hexagon's true rotational period — 0 is the old "Flat-top", 30 the old "Pointy-top", anything past 60 just repeats. Verified live: 0° and 30° reproduce the two old named orientations exactly; 15° (a genuinely in-between angle) tiles the full canvas edge-to-edge with zero gaps or overlaps, confirmed both visually and via a real de-duplicated SVG export (220 `<line>`s on a 73-cell canvas); Gap 0 stays an exact no-op at every rotation tested; all four generators (Bento/Sinusoidal/Voronoi/Hexagonal) regression-clean, zero console errors. Manual: `docs/LOOM.md` §4.

- **August 12, 2026 (still later same day) — Loom: Hexagonal gained per-cell "Spin", distinct from the whole-lattice "Rotation" shipped minutes earlier.** Diego's follow-up: "what if we rotate each individual hexagon?" Flagged the real consequence before building rather than after: rotating each hexagon's OWN orientation, independent of its neighbours, necessarily breaks edge-to-edge tiling (gaps/overlaps) the same way Jitter already trades off for position — this is a genuinely different control from `Rotation`, not a finer-grained version of it. "Rotate each hexagon" doesn't name one specific effect, so rather than guess, asked which flavour was meant; Diego said all three, so shipped all three as one `Spin mode` selector: **Random** (seeded, uniform per-cell offset — fractured mosaic look), **Checkerboard** (alternates by lattice row+col parity — regular woven pattern, not random), **Radial** (scales with distance from canvas centre — smooth centre-to-edge unfurling gradient). Applied to each hexagon's local vertex angles *before* the rigid whole-pattern `Rotation` step, so the two compose correctly instead of fighting. `Off` (default) is an exact no-op, verified pixel-for-pixel against the pre-Spin render. **One UI correction caught live, before shipping**: the first pass used a 4-way segmented control (matching Loom's existing `seg-ctrl` pattern for Orientation/Axis), but "Random"/"Checkerboard"/"Radial" don't fit a 4-way segmented control in a 248px panel without truncating ("Ra..", "Ch.." on screen) — switched to a dropdown, the same pattern already used for Grid type. All three modes verified visually distinct and correct; SVG export still real de-duplicated vector geometry (mostly no-longer-deduped once spun, correctly, since neighbouring edges stop aligning); 0 unaccessibly-named controls; all four generators regression-clean. **Correction to this entry, found in the very next round of work** (see the Spiral/Noise entry below): the "stale console errors, not a real bug" call made during this pass was wrong — `window.syncHexSpinRow` was genuinely never exported, so every real spin-mode switch threw; it went undetected here only because the specific verification pass that checked a fresh tab's console never actually exercised a spin-mode switch in that tab. Lesson: a clean console after switching *tabs* doesn't confirm a clean console after switching *modes* — verify the exact interaction, not a proxy for it. Manual: `docs/LOOM.md` §4.

- **August 12, 2026 (still later same day) — Loom: two more Spin modes (Spiral, Noise) + two real bugs caught by properly verifying this time.** Asked Diego what other per-cell Spin flavours might be worth adding rather than guessing; recommended Noise (organic, on-brand, reuses shared code) and Spiral (near-free, reuses Radial's own distance math swapped for angle) and Diego said both. **Spiral**: angle around the centre (`atan2`), not distance — a true pinwheel/vortex twist, with an honest, disclosed seam at ±π (a real spiral has a seam; smoothing it would make it a wave, not a vortex — a different mode). **Noise**: `Organica.noise.fbm` sampled at each cell's own centre — Loom's first use of `shared/organica-noise.js`, ported rather than becoming a fourth private copy after Komorebi/Camo Turing/Warping (load order added: core → noise → kiwi → the tool's own modules, documented same as the other two). Needed its own second control, `Noise scale` (field cycles across canvas), since chaos-amount and swirl-size are genuinely independent knobs — shown only when Noise is selected. **Caught two real bugs this round, properly this time**: (1) `window.syncHexSpinRow` was never exported, so every actual Spin-mode switch threw — invisible in the *previous* round's verification because that pass checked a fresh tab's console without ever actually switching modes in it, a proxy check standing in for the real one; fixed the missing export line, then re-verified by switching through all 6 modes in a single pass with the console open throughout, catching it live before calling it done a second time. (2) A pre-existing bug, unrelated to Spin, found purely by re-testing carefully: a *fresh Loom load with zero interaction* — plain default Bento — was landing at 1% zoom, fully off-screen, every single time, because `fitToViewIfNeeded`'s first call ran before `canvas-wrap`'s own layout had settled, read a `clientWidth` small enough that the 64px-margin subtraction went negative, and that negative "available space" fed a negative scale straight into the zoom-pan module's own floor clamp. Fixed by retrying on the next animation frame instead of fitting to garbage dimensions the moment the measured space isn't yet positive — verified a fresh load now lands at a sane fit (57% in the tested viewport) automatically, no manual Reset needed. **The actual lesson from both bugs, not just the fixes**: "the console was clean" only proves something if the interaction that would trigger the bug was actually exercised in that exact check — a passing check on a *different* action, or on page-load alone, isn't evidence about the action actually being verified. Final state: 4 generators × 6 Spin modes all regression-clean, SVG export real vector geometry in every mode, 0 unaccessibly-named controls, zero console errors confirmed by actually flipping every switch. Manual: `docs/LOOM.md` §4.

- **August 12, 2026 (still later same day) — Loom: Radial, a genuinely new structure, not another variant.** Continued the generator roadmap; recommended Radial (rings × sectors, dartboard/sunburst/mandala) over the other remaining Phase 2 options as the most structurally distinct pick — Bento/Sinusoidal are a rect track lattice, Voronoi/Hexagonal are a tessellation of one repeated cell shape, Radial is true polar coordinates, a third real category, not a fourth flavour of an existing one. Third `cellShape: 'polygon'` generator; each wedge's two curved edges are approximated as short arc segments (~1 vertex per 10°) rather than the straight edges every prior polygon generator emits — a real arc has no exact polygon form, so it's subdivided finely enough to read as smooth at any Rings×Sectors count; `collectPolygonEdges`'s existing dedup needed no changes, since two adjacent wedges still share one exact straight radial edge. Genuinely simpler than Voronoi/Hexagonal in one respect: no inner-rect clipping needed at all, since the whole field is built inside a circle already inscribed in the inner rect by construction. `Inner radius` (0–0.8) is the donut-hole control — 0 gives true converging pie slices at the centre, handled through the *same* arc-point code as every other ring (a near-zero-radius arc just lands every point at the centre) rather than a special case. `Gap` insets each wedge on both its radial and angular sides; `Start angle` rotates the whole field. Verified live, properly this time (every mode/param switch exercised in the same tab the console was watched in, the discipline learned the hard way in the previous Spin round): Inner radius 0 converges cleanly with no glitch, Gap 0 is an exact edge-to-edge no-op (single de-duplicated lines throughout — concentric circles + spokes), both extremes tested without error (1 ring×3 sectors and 12 rings×36 sectors=432 cells), SVG export real vector geometry (213 `<line>`s on a 48-cell canvas), 0 unaccessibly-named controls, all five generators regression-clean. Manual: `docs/LOOM.md` §4.

- **August 12, 2026 (still later same day) — Loom: Triangular, the fourth polygon generator, plus a design mistake caught before shipping.** Continued the roadmap with the equilateral-triangle tessellation (alternating up/down triangles via the standard row-height construction, not a rectangle split diagonally — the cheap version only tiles at 90°/45° and reads as a cut rectangle, not a real triangular mesh). **Wrote the header comment first, as usual, then caught it was wrong before writing the code to match**: the draft claimed "no inner-rect clipping needed, same as Radial" by analogy — but Radial's field is bounded by construction (built inside a circle inscribed in the inner rect), while a plain triangle row/column loop has no such built-in boundary and would visibly overflow the canvas edges with nothing to trim it. Caught by re-reading my own reasoning rather than by a failed test, corrected before ever running the code: added the same Sutherland–Hodgman `clipToRect` Hexagonal already uses (each polygon generator keeps its own private copy). `Columns` sets density (triangle side length), `Gap` shrinks each triangle toward its own centroid, `Jitter` offsets each triangle independently (same accepted "breaks the exact shared edge" trade-off as Hexagonal's own Jitter, not the more involved shared-lattice-vertex jitter a fully "correct" irregular mesh would use — flagged as a future refinement, not built this round). Verified live: Gap 0 is an exact edge-to-edge no-op with correct clipping on all four sides (no overflow, confirmed visually after the fix), Jitter 0.5 gives a genuinely distinct scattered texture, SVG export real vector geometry (576 `<line>`s on a 180-cell canvas), 0 unaccessibly-named controls, all six generators regression-clean with the console watched through every actual mode switch. **Same round, before any commit — Diego asked for Hexagonal's Rotation, then Spin, ported onto Triangular too**: `Rotation` (0–60°) is the identical rigid whole-lattice rotation (the triangular lattice is the hexagonal lattice's own dual, same 6-fold symmetry, so the same "rotate centres and shapes together, not the shape alone" construction applies unchanged); `Spin mode` is the identical five modes (Random/Checkerboard/Radial/Spiral/Noise), rotating each triangle's own local orientation around its jittered centroid before the rigid rotation. **Remembered the exact bug from the previous Hexagonal-Spin round and avoided repeating it**: exported `syncTriSpinRow` on `window` in the same edit that defined it, rather than only after the console caught the omission a second time. Verified live: Rotation 0 reproduces the pre-Rotation render exactly, Rotation 30 still tiles perfectly at a visibly different angle, all six Spin modes show correct row visibility and render distinctly, SVG export real vector geometry (544 `<line>`s on a 172-cell Noise-spun canvas), 0 unaccessibly-named controls, all six generators regression-clean with the console watched live through every actual switch in the same tab. Manual: `docs/LOOM.md` §4.

- **August 12, 2026 (still later same day) — Loom: Diamond, and a real geometry bug caught before ever running the code.** Fifth polygon generator: squares on a plain uniform lattice, rigidly rotated as a whole field — Rotation 0 reads as a plain axis-aligned square grid, Rotation 45 reads as the classic diamond/argyle pattern, one continuous `[0,90)` control (a square's point-group order is 4, not the 6 every other polygon generator here uses) morphing between what would otherwise be two separate named generators; ships with Rotation defaulting to 45 so it reads as "Diamond" out of the box. **The first draft was wrong, and caught by hand-deriving the geometry rather than by running it**: it built diamond-oriented squares directly on an offset row/column lattice, guessing spacing by analogy to Hexagonal's own offset-row construction — checking where same-row diamonds' vertices actually landed showed they only touched at a single VERTEX, not a shared EDGE, meaning real gaps between rows, not a valid tiling. Rebuilt from a different, safer starting point: a plain (non-offset, trivially edge-to-edge) square grid, each square's own vertices placed at its corners so Rotation 0 gives axis-aligned squares, then the exact same rigid whole-field rotation mechanism already proven correct by Hexagonal and Triangular — a rigid rotation of a tiling that already tiles is still a tiling at any angle, with zero new spacing math to get wrong. Spin mode/amount/Noise scale ported identically from Hexagonal/Triangular. **Remembered to export the new sync function on `window` in the same edit that defined it** (third generator in a row to get this right, after catching the omission live on Hexagonal's own Spin). Verified live: Gap 0 is a true edge-to-edge no-op (the exact property the first draft got wrong, confirmed fixed by continuous zig-zag lines with no gaps); Rotation 0 renders a correctly-counted plain square grid (81 cells, 9×9); Rotation 45 renders diamonds; an in-between angle (20°) tiles perfectly; Noise Spin shows the same organic wobble as the other generators; SVG export real vector geometry (340 `<line>`s on an 85-cell canvas); 0 unaccessibly-named controls; all seven generators regression-clean, console watched live through every actual switch. Manual: `docs/LOOM.md` §4.

- **August 12, 2026 (still later same day) — Loom: Seed had zero effect on Spin's Noise mode across all three generators, fixed.** Diego asked for a targeted check of Diamond's Seed control specifically rather than a general review — the right instinct, since it surfaced a real gap a broad pass might have glossed over. Tested every Spin-mode combination by diffing the exported JSON model (not eyeballing): Seed correctly matters for Random and for Jitter, correctly does nothing for Checkerboard/Radial/Spiral (genuinely deterministic by design, not seeded randomness at all), but ALSO did nothing for Noise — a real dead-control case, unlike the other three. Root cause: `Organica.noise.fbm` (`shared/organica-noise.js`) has no seed parameter of its own; it's a pure hash of `(x,y)`. Fixed by sampling `fbm` at a per-generation coordinate offset drawn from a SECOND, independent `mulberry32` stream — seeded from `seed ^ 0x9E3779B9`, deliberately never sharing draws with the main `rng` that feeds Jitter and Random spin, so the fix changes only Noise's own output and leaves Random/Jitter identical at any given seed. Same bug, same fix, applied to all three generators that share the copied `spinFor` (Hexagonal, Triangular, Diamond) — confirmed it wasn't Diamond-specific before starting, then fixed all three in one pass rather than three separate rounds. Verified by re-running the exact diffing test that found the bug: Noise now genuinely differs across Seed values on all three generators, Random's own seed-dependence confirmed unaffected, all seven generators regression-clean, zero console errors. Manual: `docs/LOOM.md` §4.

- **August 12, 2026 (still later same day) — Loom: Circular, and a design mistake caught by Diego rather than by testing.** First draft clipped a plain rect lattice inside a circular boundary — a grid of squares that happens to sit inside a circle. Diego caught it immediately: the base CELL shape has to actually be a circle, matching every sibling generator's own naming convention (Hexagonal's cells are hexagons, Triangular's are triangles, Diamond's are squares). Rebuilt as a genuine circle-packing grid: centres on the identical triangular/hex-packing lattice as Hexagonal's own offset-row construction (a hexagonal tiling's centre lattice and a circle-packing's centre lattice are the same lattice, just different spacing constants — circles pack point-tangent, not edge-to-edge: same-row spacing = full diameter, row spacing = `d·√3⁄2`, odd rows offset `d/2`). Disclosed honestly, same as Sinusoidal's own wavy-boundary note: circles can't truly tessellate, so at Gap 0 they touch at a single point per neighbour and real triangular voids remain between any three mutually-touching circles — correct circle-packing geometry, not a rendering bug, and why the edge dedup finds nothing to merge here (no circle shares an actual edge with another, unlike every rect/hex/triangle/diamond pair). Inner-rect clipping reused the same `clipToRect` every polygon generator already carries. Verified live: Gap 0 shows circles genuinely touching (visible triangular voids, no overlap, no gap); Gap > 0 opens clean space between them; SVG export real vector geometry (3579 `<line>`s on a 95-cell canvas — a high count is correct here, not a regression, since there's nothing to dedup); 0 unaccessibly-named controls; all eight generators regression-clean, console watched live through every switch. Manual: `docs/LOOM.md` §4.

- **August 14, 2026 — Loom: Circular gained Rotation + Jitter/Seed, per Diego's own follow-up ask.** Asked what other parameters would suit Circular before building anything (exploratory question, 2-3 sentence answer per house style); recommended Rotation and Jitter+Seed as the low-risk reuses of proven infrastructure, and a per-circle radius variation ("pebble" look, Komorebi-Cluster-style) as a bigger, deferred idea. Diego confirmed the first two. `Rotation` (0–60°, same period as Hexagonal) rigidly rotates the whole lattice of centres, built the identical way as every other polygon generator this session (unrotated frame sized from the inner rect's diagonal, rotated as a whole, then clipped) — the one thing worth stating plainly: since a circle's own outline is rotationally symmetric, Rotation never changes an individual cell's shape, only how rows/columns align and therefore how the boundary clips (confirmed visibly different diagonal edge-cuts at 30°, circles themselves unchanged). `Jitter`/`Seed` offset each circle's centre before clipping, same accepted trade-off as every sibling's own Jitter — verified a genuinely different scattered "pebble/bubble" look at Jitter 0.4 (circles overlapping/unevenly spaced) versus a perfect packing at 0. Verified live: Rotation 0 reproduces the pre-change render exactly, SVG export stays real vector geometry throughout, all eight generators regression-clean, zero console errors. Manual: `docs/LOOM.md` §4.

- **August 14, 2026 (later same day) — Loom: every SVG/PNG/HTML export replaced with real closed, fillable per-cell shapes.** Diego reported opening an exported SVG and finding each cell was actually a pile of independent `<line>` segments that only *looked* like one closed shape on screen — no path a design tool could select and fill with a colour, across every generator. Root cause traced to a deliberate original design choice: `svg-renderer.js`/`raster-renderer.js`/`html-renderer.js`'s `buildPolygonSVG` all drew a de-duplicated edge list (`json-model.js`'s `collectEdges`/`collectPolygonEdges`) instead of one shape per cell, specifically to avoid two adjacent cells each stroking their own copy of a shared boundary (a real, documented cosmetic issue from earlier in the project — "two lines look like one, but bolder"). That trade-off was wrong once weighed against the actual requirement: closed, fillable geometry matters more than avoiding a faint doubled stroke on shared edges. Fixed by switching all three renderers to draw one real closed shape per cell — `<rect>` for rect-lattice grids (Bento/Sinusoidal), `<polygon>` for every polygon-cellShape grid (Voronoi/Hexagonal/Radial/Triangular/Diamond/Circular) — reading `resolveCellRects`/`cell.points` directly instead of the edge-dedup functions, which were then deleted from `json-model.js` as dead code (nothing called them any more). Updated every stale doc/comment reference to the removed dedup mechanism across `docs/LOOM.md` and the generator files' own headers, rather than leaving them describing code that no longer exists. **Verified thoroughly, not just visually**: exported SVG for all 8 generators — zero `<line>` elements in any of them, real `<rect>`/`<polygon>` counts matching each generator's own cell count exactly; parsed an exported Hexagonal SVG, applied real fill colours to each polygon programmatically, rendered it standalone in the browser and confirmed the fills render as clean, correctly closed regions (screenshot-verified, not assumed); PNG export still produces valid non-empty files for every generator; HTML export's embedded polygon SVG also fixed and verified (93 real closed polygons on Circular, zero lines); live preview (both the real CSS Grid path for rect cells and the embedded-SVG path for polygon cells) unaffected and correct; 0 unaccessibly-named controls; zero console errors throughout. Manual: `docs/LOOM.md` §6.

- **August 14, 2026 (still later same day) — Loom: three canvas verification toggles + bigger numbers.** Diego's own follow-up, straight after the closed-shape export fix: (1) a Background grid overlay, density derived from the canvas's own width rather than a fixed pixel count; (2) a Preview fill toggle, tinting every cell so a real closed/fillable shape is obvious at a glance — the direct visual counterpart to the export fix that made those shapes actually fillable in the first place; (3) bigger numbers (9→13px on both preview paths) plus a toggle to hide them. All three are pure `#canvas-frame` CSS state (`syncCanvasOverlays()`), never touch the model, and were verified never to leak into any export (checked directly: an SVG exported with all three toggles on still comes out `fill="none"` with zero grid-overlay artefacts, since the export functions read the model, never the DOM). **One real bug caught immediately by looking at the render, not by reasoning about the CSS**: the background grid was invisible on first try — every renderer's own opaque white background rect (there so a standalone exported file has one) fully covered a reticle placed behind it; fixed by drawing the overlay *above* the grid content instead, the same way a guide layer sits above content in a real design tool. Preview fill needed no renderer changes at all — a single CSS rule overriding the polygon/rect `fill="none"` presentation attribute (CSS beats SVG presentation attributes) plus the equivalent `background-color` for the CSS-Grid rect-cell path. Verified live across all 8 generators: background grid renders correctly on both the SVG-preview and CSS-Grid-preview paths, preview fill visibly fills every cell with no gaps (confirming the earlier closed-shape fix), numbers hide/show correctly, toggle state persists across generator switches, 0 unaccessibly-named controls, zero console errors. Manual: `docs/LOOM.md` §3.

- **August 14, 2026 (still later same day) — Loom: cross-tool grid import, the first real step of the "define once in Loom, reuse everywhere" workflow.** Diego described the target workflow (save a grid in Loom, import it into any other Organica tool — or Figma/Affinity — with different behaviours once there, e.g. slicing an image into the cells vs. giving each cell its own independent structure) and asked how to test/verify/build it. Recommended building the shared import path first (needed either way) then proving the simpler of the two behaviours — Slice & Reposition, tool-agnostic pure compositing — before touching any host tool's own generative core; deferred Mix & Restructure (deeper per-tool integration, already proven separately by the Bento×Camo-Turing reaction-diffusion concept test) to its own pass. Shipped: **`Organica.loadLoomGrid(json)`** in `shared/organica-core.js` — reads Loom's own JSON export and resolves it into absolute per-cell geometry (rects or polygons) any other Organica tool can consume with zero knowledge of tracks/spans/Kiwi, ported (not imported — every other tool is a plain script, no bundler) rather than pulling Loom's own ES module in. **Save grid** in Loom itself — named presets via the same shared `Organica.presetStore` every other tool's own presets already use, with a REAL round-trip load (`applyGridParamsToUI`, the mirror image of `readGridParams`'s own explicit per-generator branches), not just a re-display. **Verified live**: saved a Hexagonal grid (Rotation 30°, Spin Noise) mid-edit, switched to a totally different generator (Bento), loaded the save back — every control re-synced exactly, render matched pixel-for-pixel; `loadLoomGrid` round-tripped real exports of both a rect grid (Bento) and a polygon grid (Hexagonal, 67 cells) correctly, and threw a clean user-safe error on invalid input rather than crashing. Built and verified (not shipped into any tool yet) a standalone concept-test page, `loom/_test-slice-reposition.html`: exported a real grid from Loom, loaded it via `loadLoomGrid` in a separate page, sliced a synthetic test image into the grid's own cells and re-rendered it both as a faithful slice (image stays continuous across cell boundaries — confirms the geometry is exact) and shuffled (a genuine "puzzle remix," confirmed visually with a numbered test image) — proven on both rect and polygon cellShapes. All 8 Loom generators regression-clean throughout, 0 unaccessibly-named controls, zero real console errors (one unrelated stray 404 to Strata's own backend port, confirmed not from either test page). Manual: `docs/LOOM.md` §3.

- **August 14, 2026 (still later same day) — Loom: Mix & Restructure concept-proven, the second of the two brief-named cross-tool behaviours.** Followed the plan from the loader/Slice-&-Reposition round: prototype the deeper per-tool integration on ONE host tool (Spore, the simplest canvas-based one) before touching any product code. Researched Spore's own real algorithm first rather than guessing: `doRender()` (`spore/index.html`) is a hex-jittered lattice (rows at 0.866× spacing, alternate rows offset half a step — not blue-noise/Poisson-disc), mark size linearly mapped from local darkness (40–100% of a base size), points above `luma 0.92` skipped, marks drawn as full replayed Genesis SVG symbols. Ported the lattice/sizing math faithfully into a standalone test page, simplified marks to plain filled circles (disclosed — full Genesis-symbol replay needs `organic-forms.js`'s mark data, more than a concept test needs). Each grid cell gets its OWN independent stippling pass over its own source-image sub-region, with its own spacing/size/rotation (a small fixed preset cycled by cell index — reproducible, not random-every-render) — the genuine "different structure per container" the whole thread of work set out to test, not Slice & Reposition's shared-crop version. **Verified live on both a 3×3 Bento grid and a 45-cell Hexagonal grid**: "Vary structure" shows clearly distinct textures per cell while each cell still faithfully tracks the source image's own luminance (the dark vignette/ellipse test shapes read correctly through every different stipple density); "uniform structure" (unchecked) reads as one continuous pattern only interrupted by the clip shape — the correct contrast baseline, confirming the varied version's difference is real structure, not a rendering artefact. Both prototypes (`loom/_test-slice-reposition.html`, `loom/_test-mix-restructure.html`) share one architecture: `Organica.loadLoomGrid` for the grid, an offscreen canvas per cell for the source-image sub-region, `ctx.clip()` to the cell's own rect/polygon shape. Zero console errors throughout. Manual: `docs/LOOM.md` §3.

- **August 14, 2026 (still later same day) — Loom: the three concept-test pages committed for reuse, plus a ninth generator, Noise.** Diego asked to document and keep the three prototypes (`_test-camo-cells.html`, `_test-slice-reposition.html`, `_test-mix-restructure.html`) since they'll seed later feature work, rather than leave them as throwaway uncommitted files — committed as-is, `docs/LOOM.md` §3 updated to describe each rather than just point at it. Then continued the generator roadmap: **Noise**, Sinusoidal's own direct sibling — identical `solveTracksParametric` architecture (Columns×Rows lattice, rhythm in track SIZE not topology), a 1D slice of `Organica.noise.fbm` in place of a sine wave. Not a new mechanism: `constraint-engine.js`'s own header already named "sine, noise, radial, polar…" as the intended extension points back when Sinusoidal shipped. Two DECORRELATED noise curves for Columns vs Rows from one seed (fixed, different y-offsets sampled from the shared 2D field — the same trick Warping's own domain-warp taps use), verified genuinely different (not the same curve twice) by diffing the exported track arrays, not just eyeballing the render. Verified: Amount 0 is an exact uniform no-op; Amount 0.6 gives a visibly irregular, non-periodic rhythm distinct from any Sinusoidal wave; Seed changes the pattern (confirmed via diff); Save-grid round-trip works (including the segmented Axis control restoring correctly); SVG export real closed rects, zero stray lines; the "Colu.." truncation on the Axis control is a pre-existing cosmetic quirk shared with Sinusoidal's own identical control (confirmed by direct comparison before assuming it was a new bug); all nine generators regression-clean, 0 unaccessibly-named controls, zero console errors. Manual: `docs/LOOM.md` §4.

- **August 14, 2026 (still later same day) — Loom Phase 2/3/4 completed in sequence (18 new/combined generators + Phase 4 editing), plus a real cross-tool bug found and deliberately parked.** Continued the roadmap straight through: **Phase 2** (8 elementary generators — Column, Row, Modular, Rectangular, Diagonal, Angular, Polar, Elliptical), then on request Column+Row were combined into one **Linear** generator (Axis: Columns/Rows/Both, plus Rotation/Jitter/Seed matching Hexagonal/Triangular/Diamond) after Diego flagged via a screenshot annotation that rotated Linear strips left two canvas corners uncovered — root cause was a fixed-count strip span that only covers the canvas at Rotation 0 (where the division axis IS the canvas axis); fixed with the same oversized-lattice-then-clip technique Diamond/Hexagonal already use, verified corners now covered at any angle with Rotation-0 cell counts unchanged. **Phase 3** (Masonry/Fractal/Recursive/Organic/Spiral — five real new generators; Swiss/Golden Ratio/Rule of Thirds/Timeline/Isometric turned out to be pure parameter presets on generators that already existed, shipped as read-only `BUILTIN_GRID_PRESETS` instead of duplicated code). **Phase 4** (per-side margins with a Link toggle; real Kiwi edit-constraint use via a new `solveTracksKiwiWithEdit`; WYSIWYG drag-to-resize scoped to the 5 `cellShape:'rect'` generators). Two real bugs caught by testing rather than assumed away: the drag-handle visibility check was backwards (`cellShape !== 'rect'`, when no rect generator ever sets that flag explicitly — inverted from every other cellShape check in the codebase), and Save grid silently discarded a drag on Load (the generic rebuild regenerates fresh equal tracks from params, the same reset Shuffle Numbers already relies on) — fixed by restoring the saved model's own tracks after rebuild. Loom now has **21 generators**, every item from the original Grid Definition brief is live. Full detail: `docs/LOOM.md` §4/§7a.
  **Separately, before starting what would have been Phase 5** (native Figma Auto Layout frames): discovered `Organica.sendToFigma()` — the shared function behind the "→ Figma" button on every Organica tool except Strata (Loom, Komorebi, Camo Turing, Warping, …) — posts a `postMessage` of type `'organica-svg'` that `figma-plugin/code.js` **never listens for** (its handler only checks `'reload'`/`'import-svg'`/`'import-regions'`, confirmed via grep — zero matches for `'organica-svg'` anywhere in the plugin). Strata's own Figma integration works today only because it uses a completely different mechanism (the plugin UI polls a local Python backend on port 5050), not `sendToFigma()` at all. Net effect: **"→ Figma" has been silently doing nothing on every tool but Strata**, not just something newly broken by this session's work. Given this changes the shape of any Figma-frame-generation work (Phase 5 can't produce anything if the channel itself is dead), asked Diego how to scope the fix rather than deciding unilaterally; his call was to flag it and stop, not fix it this session — recorded here and as a background task suggestion (`task_8f56904e`) rather than touched.

- **August 15, 2026 — Shipped Soul (`/soul/`), the animation engine, from a scoping conversation through a working first vertical slice.** Diego's own framing: Genesis is animated but the animation is hand-typed CSS per form; the real need is a general engine — input (SVG/text/image) → decomposed into primitives → placed on a Canvas+Grid (already Loom's job) → brought to life by a motion engine (the new part). Scoped across several rounds before writing code: evaluated a general multi-grid layer system for Loom first (declined as a Phase-7-sized rebuild, not needed — the actual ask was two grids compared visually, shipped instead as a much smaller **Overlay grid** feature, reusing the existing preset store, `cellsMarkup`/`drawCells` extracted from the SVG/raster renderers so a second grid's shapes bake into the same export without a second background wiping out the first); evaluated p5.js (declined a second time, same reasoning as Camouflage's own August 3 call — duplicates `organica-noise.js`, wants to own its own canvas — kept as algorithm-reference-only) and Three.js (accepted for future large-particle-count rendering, since it's already vendored for Camo Turing — zero new cost, and GPU is justified there the same way it's justified for Camo Turing's own Gray-Scott, a genuinely parallel workload) against GSAP (accepted — free "no charge" license confirmed live from gsap.com, not assumed; timeline/stagger/easing/morph/draw all map directly onto documented needs). Diego's own direct request mid-scoping — compare p5's noise() against `organica-noise.js`'s own value-noise — led to a real finding (p5's `noise()` isn't gradient/Perlin noise despite the name, same value-noise family as ours; the genuine gaps were p5's native 3rd time-axis and non-repeating hash) and a concrete deliverable: **Simplex noise** (`simplex2`/`simplex3`/`simplexFbm2`/`simplexFbm3`, Ken Perlin's real 2001 algorithm) added to `organica-noise.js`, additive only, verified over 200k samples (range ≈[-1,1], deterministic, time axis genuinely continuous — max delta 0.004 at a 0.001-step in t). **Named "Soul"** (Diego's own choice, offered alongside "Anima"/"Moto"/"Palpito") — hub link + `/soul/` route added, rose/pulse identity colour. **Shipped**: `shared/organica-motion.js` — `parsePrimitives(svgString)`, one parser for every tool's SVG export (no per-tool adapter, since every tool already vectorises — Halide/Komorebi/Camo Turing/Warping's own raster output already goes through the shared contour tracer first); the 6 Genesis physics patterns rebuilt parametric on GSAP (`PATTERNS.pressure/gravity/growth/environmental/rotation` — "Collective behaviour" isn't a 7th pattern, it's any of the 5 plus a `staggerDelay` formula, exactly Genesis's own documented relationship); GSAP + MorphSVG + DrawSVG vendored (`shared/gsap*.min.js`). **Two real bugs caught by testing against actual Loom exports, not synthetic cases**: `getBBox()` returns an all-zero bbox for a detached SVG host in Chromium (fixed by attaching it off-screen, not `display:none`, which also breaks measurement); SVG fill/stroke inheritance from an ancestor `<g>` (Loom's own convention: style set once on the wrapping group, not per-shape) wasn't propagated, so every Loom export rendered in Soul's own fallback colour instead of the real one — fixed by threading an `inherited` style context through the parse recursion, verified before/after on a real Hexagonal export. Soul's own stage moved from Canvas2D to real SVG DOM once animation entered the picture — GSAP/DrawSVG/MorphSVG need actual elements, not canvas draw calls; `buildElement()` reconstructs each primitive's real tag from its own preserved `sourceTag`/`sourceAttrs`, the same technique Pollen's own `drawSvgEl()` already uses for canvas replay. **Verified thoroughly, not just "no console error"**: all 5 patterns confirmed via live `gsap.getProperty()` reads showing real values changing over time (not just visual glancing); stagger confirmed two ways (different elements at different phases in one snapshot; a longer-delayed element still at rest while an earlier one had started); scale-tested on a real 32-cell Hexagonal Loom export — `pressure`+distance-stagger produces a genuinely organic phase-shifted "breathing" field, `growth`+index-stagger produces a real staggered self-drawing effect across the grid (both screenshot-confirmed); counts cross-checked against Loom's own regression test exactly (11/68 primitives matching Bento/Hexagonal's own known cell counts); 0 of 5 new Motion controls unaccessibly-named; zero console errors across every load/pattern/play/stop tested. **Not built yet** (see `docs/SOUL.md` §7): export pipeline (playback only today), MorphSVG actually invoked by a feature (vendored/registered, unused), large-particle-count rendering (the Three.js path), a real layer/group concept (`rotation`'s per-element parity split is a stand-in), loading a Loom grid directly via `loadLoomGrid` instead of through an SVG round-trip. Manual: `docs/SOUL.md`.

- **August 15, 2026 (later same day) — Soul: direct feedback on the first vertical slice, two timing fixes + two genuinely new patterns.** Diego's own critique after using it: "ci vuole sempre un po' ad iniziare l'immagine" (a real delay before motion becomes visible) and "le animazioni sono davvero basiche" (the animations feel basic). Neither was tuned by eye — both measured before deciding what to change. **Timing**: `growth`'s `power1.inOut` ease measured genuinely flat at the start (via the live `strokeDasharray` segment length) — only 1.85% of the path drawn 300ms into a 3s duration; switched to `power1.out`, verified 18.4% at the same point after. `gravity`'s own slow start (`power1.in`) is physically correct — a falling object starts at rest — but spanned 40% of total duration, so 1.2s of near-invisible motion at the default 3s; recalibrated the keyframe percentages (0/40/55/70/100 → 0/18/32/50/100) to keep the exact same physical shape while reaching each stage sooner (0.19px → 1.1px moved at 300ms, ~5.8× improvement, measured both ways). **"Basic" led to two new pattern types**, both built from infrastructure that already existed but had never been used by a real feature: **Organic wobble** — continuous, non-repeating motion, each primitive sampling `simplex3(x,y,t)`'s own genuinely continuous time axis (built earlier the same day, verified but unused until now) at its own `(cx,cy)` every `gsap.ticker` frame, so neighbours drift correlated but never identical; implemented as a `{kill(),delay()}` object driven by `gsap.ticker.add()` rather than a real GSAP tween, since there's no fixed duration to hand back — a second legitimate strategy behind `animate()`'s own same two-method contract. **Morph** — MorphSVGPlugin (vendored since Soul's first commit, registered, never invoked), morphing a circle into a noise-perturbed organic blob built from its own radius/seed and back; the first pattern that changes a primitive's own SHAPE, not just position/scale. **A real bug caught by testing immediately, not assumed from GSAP's own docs**: a bare `morphSVG` tween on a `<circle>` does nothing at all — no error, `d` never appears, `r` unchanged — MorphSVGPlugin does NOT auto-convert a non-`<path>` target; isolated with a 2-line repro (identical tween on a real `<path>` worked immediately) before touching the real code. Fixed with `MorphSVGPlugin.convertToPath(el, true)`, which converts AND swaps the new `<path>` into the live DOM — the primitive's own `.el` reference updated to match, since a second bug would otherwise follow (anything reading the primitive afterward would target a detached node). A related gap: `d` is an SVG attribute GSAP's `clearProps` can't restore, so Stop left morphed shapes frozen mid-blob — fixed by stashing the pre-morph circle-as-path `d` on the primitive the moment `convertToPath` produces it, restored explicitly in `stopMotion()`. **The "test every option, not a sample" lesson from earlier the same day held**: writing `morph`'s own blob-path generator, the exact same bare-`noise.` mistake (vs. `Organica.noise.`) got typed again — caught by re-reading the new code against the lesson already recorded, before ever running it. Verified: all 7 patterns × 4 stagger formulas (28 combinations) run without error; wobble confirmed via `gsap.getProperty` showing 3 elements at the same timestamp genuinely NOT identical (each samples its own noise position) and continuing to change over 3 consecutive samples; morph confirmed via the real `d` attribute genuinely distorting mid-animation and reverting byte-exact to the captured rest value after Stop; 15 wobble play/stop cycles leave zero leaked `gsap.ticker` listeners. Manual: `docs/SOUL.md`.

- **August 15, 2026 (still later same day) — Soul: real-world stress test against a Pollen "Hatch Flow" export, a real performance bug found and fixed, and a new scanline-relief prototype.** Diego pointed at a reference (generative-gestaltung.de's `P_4_3_1_01`, p5.js — studied for the technique only, no code imported, same "reference not runtime" rule already applied to p5 during scoping). Checked it against real Pollen code before assuming a gap: modes 1 (line angle from brightness) and 3 (variable-size dots) are already covered by Pollen's own Line marks + `Angle Range`, and Pollen's **Flow** mode (align to image isophotes) is actually more sophisticated than the reference's flat brightness ramp. Verified end to end with a real photo: Pollen's built-in **"Hatch Flow"** preset → real SVG export (5748 line-mark paths) → loaded into Soul. **Found a real, serious bug at this scale**: `wobble` measured **1fps** on 5748 primitives — isolated by testing a real GSAP tween (`pressure`) on the identical element count at a smooth 51fps, ruling out "too many SVG elements" and pointing at wobble's own architecture (5748 independent `gsap.ticker.add()` callbacks). Fixed by batching into ONE ticker loop writing `el.style.transform` directly. Re-profiling after the fix found the remaining cost isn't JS at all (17,244 `simplex3` calls = 2ms, 5748 style writes = 10ms, both isolated) — it's the browser's own SVG paint/composite cost; binary-searched (after a first sequential sweep gave nonsense numbers across every scale including n=100, caught as a harness bug — not a real per-scale finding — by re-testing n=100 alone and getting a normal 62fps) to a real, sharp cliff: **smooth to ~3000 elements, 1-3fps by ~4000**. Added a UI warning in Soul (Wobble, >3000 primitives) rather than letting the tab silently freeze — matches the Three.js/WebGL follow-up already flagged during scoping. **An open finding, disclosed rather than chased to ground or hidden**: `growth` (DrawSVG) showed inconsistent behaviour on a new scanline-relief prototype's own many-segment polyline paths — `strokeDasharray` stuck at zero for a disproportionate fraction of the tween in repeated isolated tests (reproduced in a fresh tab, ruling out session degradation), independent of segment count. `wobble` on the identical content works correctly and looks genuinely good. Not root-caused tonight — flagged for a focused pass rather than either claimed-working or silently dropped. **New prototype**: `/_test-scanline-relief.html` (Loom's own "test before deciding where it lives" discipline) — the reference's mode 5 (brightness as vertical displacement along horizontal scanlines, a topographic/relief look), no equivalent anywhere in Organica today, real luminance sampling and real SVG path export (125 verified real `<path>` elements). Where it belongs architecturally is still open (closest kin: Halide, or a small new tool — not Soul itself, which animates primitives rather than decomposing raster images). Manual: `docs/SOUL.md` §6a.

- **August 15, 2026 (still later same day) — Soul: major UI overhaul (Seeds panel, floatbar consolidation, zoom/pan, video export) then a design-system audit that found four real cascade/token bugs.** Diego drove a rapid sequence of UI corrections: Source's own bespoke paste-textarea block replaced by a real centralised **Seeds panel** matching Camo Turing's own tabbed component exactly (Genesis/SVG/Text — Image deliberately excluded per instruction, corrected after an initial wrong interpretation of "seeds centralizzato" that had instead built a floatbar Open button); Primitives panel removed (Motion kept); Play/Stop, Loop (to a configurable duration), and Export (PNG/SVG/**Video**) all consolidated into the shared `org-floatbar` bottom-centre component (Halide/Komorebi/Loom/Camo Turing's own convention), replacing separate header controls entirely; the header's own "N primitives loaded" status text moved into the same floatbar pill (`Organica.status()` works anywhere via class selector, not literal `<header>` placement, so this needed zero JS changes); mouse-wheel zoom + drag-pan added via the shared `Organica.createZoomPan`, same wiring as Loom's own. Verified thoroughly: all 7 patterns × 4 stagger formulas (28 combinations) re-checked clean after the restructuring, video export produces a genuinely decodable file (loaded into a real `<video>`, correct duration/dimensions), 0 unaccessibly-named controls, zero console errors across every tab/source/pattern/zoom/export combination tested. **Then, per Diego's own explicit follow-up ask ("verifica che sia tutto centralizzato e che riusi i componenti giusti") — a direct design-system audit, not a visual scan**: diffed Soul's local `<style>` block's selector list against every shared file it links, and grepped for hardcoded values that should be tokens. Found **four real defects, same root mistake four times**: Soul's own local `<style>` loads AFTER the shared `<link>`s, so any selector it redefined silently WON the cascade even though the correct shared rule was already linked. **(1)** `.panel-select`/`.ctrl-row`/`input[type=range]` local copies blocked the shared filled-track slider system entirely — fixed by deleting them and wiring `Organica.enhanceSliders(document)`. **(2)** `.mini-btn` local copy reintroduced 9px uppercase tracked text on the "Apply text" button, the exact style the July 25 panel-component pass moved every tool away from — deleted. **(3)** `#panel` local copy redeclared width at a stale 260px against the shared unified `--panel-w` (248px) — deleted, panel now visibly narrower and correct. **(4)** `--mid` shipped as `#726a5e`, the pre-July-26-fix value that failed AA at 3.17:1 — every sibling tool already carries the unified `#696256` (5.4:1); Soul, built after that fix, should have inherited it and hadn't. Also checked and explicitly left alone: `#stage-frame`'s white background (the canvas/artwork surface, the same content-colour exception already carved out for Halide's ink/paper — not chrome) and a couple of raw-px values (`#zoom-hud` position, `#drop-hint` gap/icon size) that were copied verbatim from Loom's/`_template.html`'s own identical components, not local drift. One value-identical tokenisation made in passing (`#canvas-wrap` padding 24px → `var(--space-7)`, exact match, no visual change). Four separate commits, each independently revertable; each verified live (screenshot + zero console errors + functional replay) before moving to the next. Manual: `docs/SOUL.md` §6b (UI overhaul), §6c (audit).

- **August 15, 2026 (still later same day) — Soul: floatbar icon parity + real Play/Pause toggle.** Two follow-ups on the design-system audit, both prompted by Diego catching things a first pass had wrongly called clean. First: "non è vero le icone sono diverse" — I'd claimed the floatbar icons matched Camo Turing's without ever diffing the actual SVG path data; two genuinely didn't (Play was a solid filled triangle with an arc-based path vs. Camo Turing's stroked-outline straight-line triangle; Loop's reused-Reset-arrow icon had its arrowhead coordinates altered, not byte-identical) — both fixed to match exactly. Second: "manca il controllo su pause e l'icona" — Soul only had Play/Stop, no Pause; Camo Turing's floatbar is a real Play/Pause toggle (freeze in place, resume) plus a separate Reset, and Soul's Stop only covered the reset half. Replaced the two buttons with one `#btn-playpause` toggle, Camo Turing's own id/icon-swap/aria-label technique verbatim. GSAP tweens (every pattern but wobble) already had `.pause()`/`.resume()` built in; wobble's own batched ticker handle didn't, so `pause()`/`resume()` were added to `animateWobbleBatch` in `organica-motion.js`. **First version had a real bug, caught by a controlled test, not assumed correct from the icon/status changing**: it shifted `startTime` using `gsap.ticker.time` deltas, but `ticker.time` only advances when the ticker actually ticks — with wobble as the ticker's sole listener, pausing it can let GSAP's ticker go idle for the whole pause, so `ticker.time` reads the same stale value at pause and resume, computing a zero shift. A same-script busy-wait test proved it: continuous play across a 300ms real gap moved the shape ~26px on the next tick (expected); the paused case produced the same size jump instead of a small one. Fixed by measuring the pause duration with `performance.now()` instead, independent of whether the ticker itself is running — re-verified with the identical test: 0px jump on resume. Manual: `docs/SOUL.md` §6c.

- **August 15, 2026 (still later same day) — Soul gains an 8th pattern, Flow field, imported from the standalone `explorations/flow-field/` p5.js tool built earlier the same day.** Two real architecture questions settled with Diego before writing code, not assumed: how a free-particle flow field maps onto Soul's "pattern applied to existing primitives" model (decided: each loaded primitive drifts through the field from its own position — the field is the only piece ported unchanged, the particle concept doesn't carry over, a primitive IS what moves), and whether to keep p5.js or rewrite to vanilla JS/Canvas2D per CLAUDE.md's own production rule (Diego's call: keep p5.js — loaded in **instance mode**, `new p5((sk)=>{sk.setup=()=>{sk.noCanvas();...}})`, so none of p5's own globals touch Soul's script scope and no visible `<canvas>` is ever created, verified live). `animateFlowBatch()` follows `wobble`'s own batched-`gsap.ticker` architecture in `organica-motion.js`, but stays Soul-local rather than joining the shared pattern registry, since it's the one pattern that needs p5.js — no other Organica tool loads it. Own params (Cell size/Noise scale/Angle mult/Speed) replace Amount/Duration, which hide for this pattern rather than sitting there inert. **A real bug from the exploration was already fixed at its source before this port**: an earlier version wrap-teleported particles to the exact opposite canvas edge, which could trap one oscillating forever at a noise-field seam discontinuity (39 of 40 stuck in one test) — fixed there by respawning instead of teleporting; Soul's own version resets a primitive's drift to 0 (home) past a distance threshold, the same principle adapted since primitives don't need a literal edge-wrap. Verified end to end: full Play/Pause/Resume/Stop lifecycle confirmed via live transform inspection (paused position byte-identical across forced ticks, resume continues with no jump — no elapsed-time compensation even needed, since drift accumulates incrementally rather than from an absolute time coordinate), ~60fps at 24 primitives, zero console errors, zero regression on `pressure`/`wobble` re-tested after the change. Manual: `docs/SOUL.md` §6d.

- **August 15, 2026 (still later same day) — Soul's Flow field pattern gains free particles, the piece the primitive-only port had left out.** Diego caught it directly: the Flow field port only moved primitives already loaded on the stage; the exploration's own free-roaming marks (independent particles drawing their own accumulating trails) weren't there. Added as a "Free particles" toggle inside Flow field's own panel, layered onto the same field/speed the primitive-drift side already reads — not a separate mode. Rendered as real SVG `<path>` elements on the stage (one per particle, its whole trail folded into a single `d` string per tick, not one element per trail point), so N particles cost exactly N DOM elements and export automatically alongside everything else already on the stage — verified: 60 particles → 60 `<path>`s, both live and in the serialised export. Mark styles (Line/Dots/Drop marks, the exploration's own three) reimplemented as SVG path data instead of canvas draw calls, with one disclosed simplification — SVG has no per-vertex opacity, so the true per-point fade became a single opacity per path, with Dots/Drop marks still tapering in size toward the tail so the trail visibly narrows regardless. Respawn-not-teleport on exit reuses the same fix already applied to primitive drift. Verified live: all three styles visually distinct and screenshot-confirmed, Pause freezes a sampled particle's `d` byte-identical across forced ticks, Stop removes every particle `<path>` (0 remaining, counted not assumed), 60fps holding at 600 particles (the slider's own max, Drop marks, the priciest style), 0 of 9 new controls unaccessibly-named, zero console errors across every combination tested. Manual: `docs/SOUL.md` §6d.

- **August 25, 2026 — Shipped Vortex (`/vortex/`), migrated from `explorations/vortex/` the same way Membrane was — and backfilled Membrane's own missing CLAUDE.md documentation while at it (its migration had shipped without ever getting a Tools-table row or Repo Structure entry, a real gap found while adding Vortex's own). Three decisions Diego made directly before any code: **(1) Palette** — Membrane's own RMX chip UI (up to 8 colours now, not 5 — Membrane's cap was a Camo Turing GLSL-array leftover that doesn't apply to Vortex's plain `i % n` cycle), **plus** a new reproducible numeric **Seed**. **(2) Zoom/pan** — keep Vortex's own real 3D camera (drag-orbit, Shift-drag-roll, Camera distance) as the ONLY zoom mechanism; explicitly do NOT layer `Organica.createZoomPan` on top of a 3D scene. **(3) Export** — PNG+SVG+Video all available whether paused or running, pausing being the natural (not enforced) way to frame a shot. One fact verified directly before designing around it: the exploration's own `draw()` starts with an unconditional `background(6)` — **Vortex redraws from scratch every frame, it never accumulates** — which is why SVG export needed none of Membrane's `shapeHistory`/throttle/cap machinery: `state.lastItems` (the current frame's already depth-sorted segment list) is complete on its own. Seed reproducibility hinges on `segConstCache` invalidation (`applySeed()` clears it on every seed change, plus `p.noiseSeed()` and a `mulberry32`-derived `seedOffset` for belt-and-braces decorrelation) — verified directly, not assumed: set seed 42 → visit a different seed → set 42 again → `state.lastItems` came back byte-identical (`JSON.stringify` equality), while a genuinely different seed produced genuinely different geometry. PNG export re-renders at scale for real (`renderFrame(g, {scale})`, one `g.scale(s)` wrapping the whole draw — item positions always computed in unscaled `state.W/H` space) rather than upsampling a bitmap, which Vortex can afford precisely because it already redraws from scratch regardless. SVG export verified structurally, not just "opens without erroring": real `<line>` count matching the active segment count exactly, 142 distinct stroke-widths and 71 distinct segment-lengths on one 260-segment export (confirms `scaleAmt` is applied to both, per-depth, not just one), zero `<image>` tags, non-empty at rest and mid-transition. Camera drag-while-paused (the export-framing workflow) verified working; Shift+drag-roll verified correct at the **logic** level (`state.shiftDown` branch rolls only, confirmed by injecting `state.shiftDown=true` directly and redrag — roll changed, yaw/pitch didn't) — the browser-automation test harness's own modifier-drag doesn't fire real `keydown`/`keyup` events for the held key, a test-tool limitation disclosed rather than glossed over, not an app bug (real browsers fire genuine keyboard events for held modifiers). 60fps confirmed holding at 800 segments with Spin+Orbit active, matching the original exploration's own measured perf-fix result. Canvas resize deliberately does **not** reset the running animation (divergence from Membrane, which loses its accumulated trail on resize) — Vortex reconstructs every pixel from `(state, animClock, camera)` each frame regardless, confirmed live: resizing through several presets mid-run caused no jump, no reset, vortex stayed centred (`cx,cy = state.W/2, state.H/2` recomputed every frame, no stored centre to go stale). Accent colour `#6d4bd8` (deep indigo) chosen after checking the full `--tool:` registry — the exploration's own `#e94f37` collides with both Strata and Membrane's warm-red band. Hub nav link added after Membrane; the original `/explorations/vortex/` link deliberately left in place, same as Membrane's own exploration survived its migration. 0 of ~50 panel/floatbar controls unaccessibly-named (the 4 that show as "unlabeled" by attribute alone all carry real visible button text — PNG/SVG/Start recording/Reset camera angle — which is a valid accessible name on its own).

- **August 25, 2026 (still later same day) — FVS ↔ Genesis Creator: seeds from Creator's library, Loom grid variety, and a ported transform-preview strip (Phases A + C of a 3-phase evaluation; Phase B deferred).** Diego asked to evaluate linking FVS's own Element/Seed definition to "the Creator tool," using Paper.js where it fits. Two Explore agents reframed the question before any code: **Genesis Creator (`genesis/creator.html`) is already a full shape-authoring tool** (Draw/Import/Generate modes + a live Appearance transform + its own `organica.library.forms` library), not just a composer over the 55 built-in forms — a much closer fit than assumed. **Paper.js's only proven use in the repo (Strata's Refine editor) is shape *correction*** (anchor-drag/smooth/simplify/unite) — zero rotate/scale/flip/transform-preview usage anywhere, which is Canvas2D/SVG territory everywhere it's actually needed, including FVS's own preview strip. Diego confirmed the architecture directly: **Creator** authors/stores/(future) animates Seeds; **FVS** builds Components and Symbols from them. Scoped to 3 phases, then EXPANDED Phase A on his own follow-up ("includes already the grid 2x2 and other type of grids... include also some basic geometric and organic shapes") before any code was written. **Phase A shipped**: `fvs/index.html` gained a "Creator library…" picker reading `organica.library.forms` and feeding a form's own `.svg` through the *already-built* `extractElementFromSVG` (the exact upload code path, different source) — caught and fixed a **real bug before it ever ran**: the plan's own research had the wrong data shape (`set.forms` as full objects); reading `creator.html`'s actual `saveForm()` showed `set.forms` is really just an array of ID strings referencing a **top-level** `lib.forms` array — a naive implementation would have silently produced `undefined` id/name/svg for every real Creator-saved shape while "working" against hand-built test data that happened to share the same wrong assumption. Grid variety: `getGrid()`/`resolveGridCells()`/`frameSize()` generalized to Square 2×2/3×3/4×4 plus a genuine `Organica.loadLoomGrid` JSON import (rect *and* polygon cellShape, verified with real fabricated exports of both), with the 7 four-cell-only symmetry rules (Pinwheel/Mirror/Diagonal/Checkerboard/Row/Column/Radial) greyed out via `syncRuleAvailability()` whenever the active grid isn't 4 cells, bouncing off an invalidated selection rather than leaving it silently stuck; Random/Exhaustive fall back to a per-cell-independent draw at any other cell count (an honest, uncurated fallback, not a fake generalization of rules hand-derived for exactly 4 cells); Manual mode's mini-editor and its `Organica.autoLabelPanel` call now rebuild for the real cell count instead of a hardcoded 4. **A second real bug caught by testing the actual sequence, not just the destination**: Exhaustive's own over-cap `Generate` disable never got cleared when switching to any OTHER rule (only re-checked on returning to Exhaustive) — Pinwheel silently stayed disabled after visiting an over-cap Exhaustive; fixed in `syncRuleUI()`. **A third gap caught by a live accessibility check**: `buildManualEditor()` rebuilds its DOM on every grid change, but `Organica.autoLabelPanel` had only ever run once at page load — rebuilt rows went unlabeled; fixed by calling it again at the end of `buildManualEditor()` itself (confirmed idempotent — skips rows that already have a name). Shipped a **"Basic Shapes" seed set** (Square/Circle/Triangle/Arc/Hexagon/Star, real SVG markup reusing FVS's own path formulas) seeded once into `organica.library.forms` from both `genesis/creator.html` and `genesis/library.html` (kept in sync per that file's own existing comment) — deliberately NOT hooked into the more special `organic-forms.js`-driven "Organic Forms" built-in-set machinery, which doesn't accept plain `.svg` entries; shipped as a normal (non-builtIn) set instead, the same shape `saveForm()` itself produces, verified rendering correctly in both Creator's own picker and `library.html`. **Phase C shipped**: ported FVS's own 7-state rotation/flip preview strip into Creator's Appearance panel (`renderTransformPreview()`, hooked into the existing `render()` so it updates for free on every mode switch or geometry change) — built from Creator's *own* `geometry()`/`formTransform()` primitives rather than FVS's data model, verified as genuinely complementary to (not a duplicate of) the existing continuous Rotation/Scale sliders: changing those sliders correctly leaves the strip's own fixed reference states unchanged, while an actual geometry change (switching Circle/Poly/Star, adding draw points) correctly updates it. **Phase B (Paper.js-powered drawing in Creator) deferred** — the plan's own largest, riskiest piece, explicitly flagged as needing an isolated prototype before touching Creator's real Draw mode; not started this pass. Verified end to end: real (not hand-assumed) Creator library data round-trips into FVS including a custom-uploaded shape's own path data; a real 6-cell rect Loom export and a 3-cell polygon Loom export both resolve and render correctly; 0 unlabeled controls confirmed via a live accessibility check that actually exercised Manual mode at a rebuilt cell count, not a stale snapshot; zero console errors on fresh tabs throughout (distinguishing real errors from this browser tool's own known stale-console-buffer artifact, re-confirmed several times this session).

- **August 25, 2026 (still later same day) — FVS: "Save to library" debugged and found genuinely broken, not a test artifact.** Diego asked to debug Save to Library directly. Reproduced it for real — a real click through the actual UI, no stubbed `window.prompt` this time (the earlier session's own automated tests had all stubbed `window.prompt` to make them deterministic, which silently hid this) — and it threw `Error: prompt() is not supported` the instant Save was clicked, an uncaught exception that aborted the whole save with zero visible feedback to the user, exactly the "doesn't work" symptom reported. Root cause: `window.prompt()` isn't just occasionally cancelled (returns `null`) in some embedding contexts, it can be entirely disabled and throw outright — a real risk for any Organica tool, which can run embedded in varied contexts. **Checked whether this is FVS-only or systemic before fixing**: grepped for `prompt(` across every tool and found the identical pattern in **five others** — Camo Turing, Komorebi, Halide, Warping, and Pollen all name their own preset via a bare `prompt()` call, meaning this exact failure mode is very likely latent product-wide, not an FVS-specific bug. Fixed FVS's own instance now (in scope); the other five are a separate, flagged background-task candidate rather than fixed blind here. **Fix**: replaced the blocking native dialog with a plain inline row (a labelled text input pre-filled with a sensible default name, plus Save/Cancel buttons and Enter-to-confirm/Escape-to-cancel) that depends on nothing but the DOM — works in any embedding context. Verified via real UI clicks end-to-end: Save opens the row with the input pre-selected, clicking Save adds the real thumbnail to the Library and closes the row, Cancel and Escape both close it without saving anything. One thing worth naming precisely for future debugging in this environment: a stale, already-fixed error can keep reappearing in `read_console_messages` checks on the SAME tab after a fix — traced twice now to the tool buffering console history across the tab's lifetime rather than resetting per navigation; a genuinely fresh tab is the reliable way to confirm a fix actually landed, not re-checking the same tab. 0 of 46 controls unaccessibly-named (added a real `aria-label` to the new name input rather than leaning on its placeholder, which isn't a reliable accessible name), zero real console errors on a clean tab.

- **August 25, 2026 (still later same day) — FVS: Element preview strip + a local Component Library, from a "valuta due cose" evaluation ask.** Diego asked to evaluate (not just build) two things: a step working exclusively on Element definition before Component generation, and a saveable/reusable Component library feeding a future Symbols tier and Figma export. Researched before designing: confirmed `Organica.presetStore` is a fully generic JSON-blob store (Loom's own "Save grid" already persists its whole Universal JSON Model this same way — the right precedent to follow), and confirmed via direct code check that `Organica.sendToFigma`'s `'organica-svg'` postMessage type is **still** never listened for by `figma-plugin/code.js` — the same product-wide bug found during Loom's own work in August, still unfixed. Presented both forks to Diego before building: **(1)** a literal wizard/step flow vs. an always-visible preview strip in the current single-page panel — Diego chose the strip, keeping FVS consistent with every other Organica tool's own "everything visible, live" convention (no tool in the product has steps/tabs). **(2)** fix the shared Figma-plugin bug now vs. wire FVS into the same broken channel anyway vs. skip Figma this pass — Diego chose to skip it entirely rather than ship a button that silently no-ops. **Shipped**: an Element preview strip (`buildElementPreviewSVG`/`renderElementPreview`) showing the current Element at 7 fixed reference states (0/90/180/270° + flipH/flipV/both) — genuinely useful for an asymmetric or uploaded shape, verified with a deliberately asymmetric L-shaped upload where all 7 tiles read as visibly distinct (a symmetric shape like the triangle only shows the vocabulary correctly, not the value — confirmed both). Wired into every Element-affecting change (type switch, Base/Height/Thickness, upload success, Ink colour, Paper colour) — same trigger points `renderGallery()` already used. **Component Library**: `Organica.presetStore('fvs')` holding full self-contained snapshots (Element incl. an upload's own `customElement` geometry — not just its type string, Grid, Palette, Paper, and the exact generated cells, not a re-derivable reference) — Save/Load/Remove, library thumbnails rendered from each entry's OWN saved state via a `buildComponentSVGWithPaper` wrapper (temporarily swaps `state.paperColor` so a saved thumbnail can never drift just because the live panel keeps changing after saving). **A real bug caught by testing, not assumed working**: the first `buildLibraryEntry()` only saved `{cellSize, gap}` for Grid, omitting `cols`/`rows` that `gridCellCenters()` needs — `TypeError: Cannot read properties of undefined (reading 'cx')` on the very first save+render. Fixed by storing `getGrid()`'s own full return value instead of hand-picking fields. **A separate false alarm, caught and correctly dismissed rather than "fixed" blind**: the same error kept reappearing in console checks even after the fix and after clearing `localStorage` — traced to `read_console_messages` buffering across the tab's whole lifetime rather than resetting per-navigation; confirmed by opening a genuinely fresh tab, which loaded clean, proving the bug was already fixed and the persistent-looking error was a tool artifact, not live app state. (A related, already-known artifact resurfaced too: `<input>` values bleeding across tabs on the same origin — same phenomenon documented earlier in this session for Vortex/Loom, not an FVS bug.) Verified end to end on a clean tab: full save→edit-everything→load round-trip restores every control and the exact saved cells (not a re-generated arrangement); a saved custom-uploaded element's own path `d` string round-trips byte-identical; Remove drops the entry from both the DOM and `localStorage`. 0 of 44 controls unaccessibly-named, zero real console errors.

- **August 25, 2026 (still later same day) — FVS: Small/Medium/Large scale values converted from number inputs to sliders, debugged end-to-end.** The three Scale-axis values (used by Random/Exhaustive's own family parameter spaces) were `<input type="number">` — swapped for `<input type="range">` (`rg-scale-small/medium/large`, 10–400%) with real `<span class="ctrl-val">` readouts, matching every other slider in the panel and picking up `Organica.enhanceSliders`'s filled-track styling for free (a plain number input never had that). Debugged as asked, not assumed working: dragged each slider and confirmed three things independently — the readout text updates live; `scaleValues()` (which `axisValues()`/`FAMILIES` read) returns the exact dragged percentages as `[0.4, 1, 2]` after setting Small=40/Large=200; and those exact values actually reach generation — Random mode at those settings only ever produced cells scaled `0.4`, `1`, or `2` (checked by collecting every distinct scale value across 60 generated components), and Exhaustive's own live "N possible" count recomputed correctly from the new values too. Screenshotted the resulting gallery to confirm the scale spread is visually real, including the expected overflow at 200% (a triangle scaled past its own cell is supposed to spill over — not a bug, Base/Height=100 already fills the cell at 100%). Zero console errors, 0 of 41 controls unaccessibly-named.

- **August 25, 2026 (still later same day) — FVS: the 4 fixed arc-thickness elements collapsed into 1 Arc element with a live Thickness slider.** Direct follow-up: the 4 preset elements (`arc25/50/75/100`) already shared one underlying `arcGeometry(thicknessPct)` generator, so exposing thickness as a real control rather than 4 fixed dropdown entries was a UI simplification, not new geometry work — `ELEMENT_TYPES.arc.geometry(p) → arcGeometry(p.thickness)`, `getElement()` now also reads `thickness` from a new `rg-thickness` slider (range 1–100, default 100 — the solid quarter-circle, same default the standalone presets converged on). `syncElementUI()` extended to show the Thickness row only for `arc` (mirroring how Base/Height already only show for `triangle`). Element dropdown now just Triangle/Arc/Custom instead of 5 entries. Verified: dragging Thickness live-updates the gallery (path `d`'s inner-radius term matches `100*(1-thickness/100)` exactly, checked directly against the rendered `d` string), and Radial at Thickness=100 still produces the exact same clean full circle as before — no regression from the refactor. 0 of 41 controls unaccessibly-named, zero console errors.

- **August 25, 2026 (still later same day) — FVS: 5 real elements (triangle + 4 arc thicknesses), SVG upload, two rules derived directly from a second Figma reference, plus Ink/Paper.** Diego shared a second reference (a real Figma component library: Elements→Components) and asked for the 5 shown elements built for real, SVG upload as a general way to add new ones, and the shown Components' construction rules derived — "Analizza" again, so two genuinely ambiguous reads were confirmed with him before building rather than guessed: **(1)** the dashed-box "4 arc icons" are 4 different **thicknesses** of one arc shape (not 4 rotations of one arc, which the existing Rotation axis would already produce for free) — confirmed directly, changed the whole element plan. **(2)** Components 4/5 ("S curve"/"bowtie") aren't full independent-4-cell arrangements; Diego specified them precisely — component 4: top row (cells 1,2) as-drawn, bottom row (cells 3,4) reflected on X (flipV); component 5: cell 1 reflected on Y (flipH), cell 2 as-drawn, repeated for cells 3/4 — two genuinely new **row-grouped** and **column-grouped** 2-state families, distinct from the existing diagonal-grouped Checkerboard. **Element model upgraded from polygon point-lists to real SVG path `d` strings** (`ELEMENT_TYPES[type].geometry(params) → {d, normTx, normTy, normScale}`) — needed because arcs have real curves; SVG render now emits `<path d>` and Canvas2D draws via `ctx.fill(new Path2D(d))` (replacing a manual `moveTo/lineTo` loop, and natively supporting the arc `A` command). The `norm*` fields are a **second, separate transform layer** (nested `<g>`/`ctx.translate+scale`, nothing baked into `d` itself) — deliberately chosen over writing a general path-data coordinate transformer, since arbitrary uploaded paths (arcs/beziers/whatever) have too many token shapes to safely regex-rewrite; one more transform nesting handles any path losslessly with zero parsing. **4 arc presets** (`arc25/50/75/100`, apex anchored at the box's own corner, outer radius fixed at 100, inner radius derived from thickness%) share one `arcPathD(thicknessPct)` generator — verified visually on first render that the sweep direction was already correct, no flag-flip needed. **SVG upload**: hidden `accept=".svg"` input + button (Membrane's own convention), `DOMParser`, first drawable shape only (`path/circle/rect/ellipse/polygon/polyline` — confirmed via explore agent that no ready-made "SVG→one flat path" helper exists anywhere in the repo, this is genuinely new code), measured via a real off-DOM `<svg>` host's `getBBox()` (Pollen's own technique) to derive the `norm*` fit. Verified directly, not just "no throw": a `<path>`, a `<circle>` (manually converted to a 2-arc path), and a non-square `<polygon>` (200×50 triangle) all extracted with correct `normScale`/centering math, a multi-shape SVG correctly used only the first element, and a shape-less SVG threw a clean readable error. **The two derived rules verified against Diego's own spec by diffing the actual generated cells, not eyeballing**: Row mirror's first render matched `[-,-,V,V]` exactly; Column mirror's first render came out `[-,H,-,H]` — backwards from Diego's own "cell 1 reflected, cell 2 original" — caught by the diff, fixed by swapping which column carries the flip, re-verified `[H,-,H,-]`. **Radial** (the harder derived piece, for the reference's circle/rosette/star): realized Pinwheel's own rotation stepping follows ROW-MAJOR cell order (TL,TR,BL,BR), which is not the grid's actual rotational adjacency (TL,TR,BR,BL, walking the square's real corners) — built a second cell-order constant (`RADIAL_ORDER`) for true rotational tiling. **Worked on the first attempt, no iteration needed**: `arc100` (solid quarter-disc) through Radial produced a pixel-clean full circle, and the same rule with `arc25` (thin arc) produced a genuine ring/donut and, at other base/chirality combinations in the same 8-component set, both a 4-pointed star and a 4-petal rosette — directly matching all three of the reference's otherwise-unspecified Components 1–3 in one generation. **Ink/Paper**: audited where colour was actually used before adding anything (per Diego's own "fai un check") and found a real gap — no background concept existed anywhere (hardcoded CSS white on the thumbnail, no `<rect>` in the SVG export at all, PNG export started from a transparent `clearRect`). Added a `Paper` swatch (`Organica.createColorSwatch`, same convention as every other tool's Background swatch) wired into all three render paths at once, then deleted the now-redundant hardcoded `.fvs-thumb` CSS background so the thumbnail shows exactly what exports, not a CSS approximation — verified by sampling an actual exported PNG corner pixel against the swatch's own hex, exact match. **Gap now defaults to 0.** **Cell size "doesn't work"**: traced end-to-end rather than assumed — it was never broken, `rg-cellsize` correctly threads through to the exported SVG/PNG's own pixel dimensions (verified: 120px at Cell size 60 vs 520px at Cell size 260, exact doubling+ratio match); the reported symptom is real but was a legibility gap — `.fvs-thumb`'s fixed 96×96 CSS box always renormalizes the SVG's own viewBox to fill it, so Cell size's only remaining on-screen effect is a gap-to-cell ratio too small to perceive, made totally invisible once Gap defaults to 0 in this same pass. Fixed with a clarifying panel hint rather than inventing a fake preview-scaling feature that would make the gallery unpredictable at high component counts. 0 of 40 controls unaccessibly-named (added a missing `aria-label` on the hidden upload `<input>`, caught by the same accessibility check every pass here runs), zero console errors across every element/rule/upload/colour combination tested.

- **August 25, 2026 (still later same day) — FVS: Base/Height default to 100 (perfect cell fit), Fill replaced by a centralised Palette, and Random/Exhaustive rebuilt around named symmetry families instead of independent per-cell combinatorics.** Three direct asks. **(1)** Base/Height now default to 100/100 — already geometrically correct (the triangle's own point set already spans the full 0–100 box at those values, `apexY=50-height/2` etc.), so this was a default-value change only, verified: cells now tile edge-to-edge with zero gap at Gap=0. **(2)** Fill moved out of Element (now pure shape geometry, no colour) into a new **Palette** panel section — the RMX chip UI Membrane/Vortex already established, min 1 (not their own min 2, since a single flat colour is a legitimate baseline here) max 8, `colorAt(i)` cycling by cell index into `buildComponentItems`'s own per-item `color` field, which both `drawComponentCanvas` and `buildComponentSVG` now read instead of one shared `element.fill`. **(3)** The substantial one — analysed the existing 6 rule modes before touching code and found a real split: Identity/Pinwheel/Mirror always look designed because they encode actual symmetry, while Random/Exhaustive chose each of the 4 cells fully independently, which mostly reads as noise once more than one axis is active (confirmed the reasoning wasn't just aesthetic guessing: verified numerically that the old exhaustive space with Rotation+Flip active was 65,536 — almost entirely "unrelated cells," with the handful of genuinely symmetric arrangements buried in it being exactly what Pinwheel/Mirror already produce directly). Presented this analysis and three concrete directions via AskUserQuestion rather than picking one; Diego chose all three, which turned out to compose into one coherent fix rather than three separate ones. **Shipped**: two new named symmetry rules — **Diagonal mirror** (on-diagonal pair TL/BR shares rotation R, off-diagonal pair TR/BL shares R's 90°-reflection `(90−R) mod 360`) and **Checkerboard** (TL/BR = state A, TR/BL = state B, exactly 2 distinct states) — alongside the existing Identity/Pinwheel/Mirror. Then rebuilt **Random and Exhaustive to draw from a `FAMILIES` registry** of these same five named rules instead of choosing 4 cells independently: each family exposes `paramSpace(activeAxes)` + `build(params)`, gated by `requiresAxis` (Pinwheel/Diagonal need Rotation, Mirror needs Flip, Checkerboard needs any one active axis, Identity has no requirement so `activeFamilies()` can never return empty). This directly answers all three requested directions at once: every Random/Exhaustive result is now a member of a real symmetry family (direction 1), the two new named rules expand the "always good" set rather than patching the "sometimes bad" one (direction 2), and low entropy falls out structurally — every family caps distinct-states-across-4-cells at 1–4 rather than allowing full independence — so no separate probability-weighting hack was needed (direction 3). Verified directly: Random at 16 components showed 1–4 distinct states per component, never fully independent, and every thumbnail read as a genuine composition (pinwheels/diagonals/checkerboards), a stark visual improvement over the prior noise-heavy gallery. Exhaustive's own total is now computed per-family and is dramatically smaller in the common case — Rotation+Flip active dropped from the old design's 65,536 (hard-refused) to a real, generatable 288 — while the same honest 512-cap-refuses-cleanly behaviour still holds at the extreme (all 3 axes active → 2,400, correctly refused with the gallery left untouched). Same-seed Random reproducibility re-verified after the rewrite (byte-identical). All geometry factored into shared `build*Cells()` functions so the standalone dropdown rules and the `FAMILIES` registry can't drift apart from each other. 0 of 35 controls unaccessibly-named, zero console errors throughout.

- **August 25, 2026 (later same day) — Shipped FVS (`/fvs/`), a new tool modelled on Figma's own component system, from a reference screenshot Diego shared (Elements → Components → Symbols/Assets → Applications). Scoped to the first stage only per his own ask ("iniziamo con un test"): Elements→Components, verified with one authored test Element (a black isosceles triangle) in a square 2×2 grid — Symbols/Pattern-tiling/Applications are later phases, not built this pass.** Checked the codebase for reuse before writing anything: **no "generate N transformed shape copies into a grid" engine exists anywhere in Organica** (Loom/Genesis/Soul all checked) — genuinely new work. Loom's own grid model has no cheap "just give me a plain 2×2" call (`json-model.js` needs a generator or hand-built tracks), so FVS builds its own trivial even-division grid directly rather than routing through Loom's Kiwi solver for a case that doesn't need one. The real reuse point is `Organica.loadLoomGrid` (`shared/organica-core.js`) — already resolves any Loom JSON export into absolute per-cell geometry with zero ES-module imports; FVS's own internal grid shape was deliberately kept compatible with its return value so "import a Loom-designed grid" is a future data swap, not a rewrite. Architecture pattern reused directly from Vortex: **one item-descriptor array** (`cx,cy,rotation,flipH,flipV,scale`) feeds both the Canvas2D PNG export and the SVG string, identical transform math in both — same discipline as `vortex/js/render.js`/`svgexport.js`. Confirmed the full transform vocabulary and rule-engine scope with Diego via AskUserQuestion before building (all three transform axes; all four generation modes plus manual) rather than assuming a narrower v1. **Shipped**: Rotation (4 values) / Flip (4) / Scale (3, editable %) as independently-toggleable "active axes"; **Identity** (1 component), **Pinwheel** (up to 8 — 4 rotation offsets × 2 chirality, deduped), **Mirror/Kaleidoscope** (up to 4, one per "seed corner"), **Random seeded** (configurable count, `mulberry32` — verified reproducible: same seed after visiting a different one produced byte-identical output), **Exhaustive** (literal per-cell-independent cross-product of active axes, **hard-capped at 512** — refuses cleanly with the exact computed total above cap, e.g. "65,536 possible — exceeds the cap," rather than silently truncating or fake-sampling; verified the refused click leaves the existing gallery untouched), and a **Manual** 2×2 mini-editor (12 labelled selects) that appends one hand-built component without disturbing the rest of the gallery. A `dedupeComponents()` pass (JSON-stringify-keyed) runs after every named-rule generation so Pinwheel/Mirror can never present two combinatorially-identical arrangements as distinct — Random and Exhaustive skip it deliberately (Random because the requested count should be the count delivered; Exhaustive because independent-cell cross-products can't collide by construction). **A real bug caught by diffing the actual generated cell data, not by eyeballing the render**: `ruleMirror`'s first implementation (a generic "clockwise cycle then re-index into a fixed flip-pattern list") put the diagonal cell's "both flips" state on the wrong corner — invisible on screen because the isosceles triangle is itself left-right symmetric, so a wrong `flipH` produces no visible difference on this particular test shape. Rewrote as a direct adjacency table (`MIRROR_RELATIONS`: for each possible seed corner, its actual horizontal/vertical/diagonal neighbours by grid position) instead of the generic cycle, then re-verified numerically — `[TL,TR,BL,BR] = [-, H, V, HV]` for the TL-seed case, exactly the spec. **Accent colour `#3fa876`** (emerald) chosen after auditing all 12 tools' `--tool:` hues — a genuinely open gap between Komorebi's olive (83°) and Camo Turing's teal (192°). Verified end to end: all 6 generation modes produce correct transform data (checked numerically, not just visually, throughout); gallery selection/highlight correct; PNG and SVG export both run clean through the real floatbar UI; exported SVG structurally verified (exactly 4 `<polygon>` elements per component, each `rotate()` matching the component's own stored per-cell value exactly, zero `<image>` tags); Fill colour swatch live-repaints the whole gallery and propagates into export; 0 of 36 panel/floatbar controls unaccessibly-named (the 4 that show as "unlabeled" by bare attribute check all carry real visible button text — PNG/SVG/Generate/Clear); hub nav link renders in the new accent colour after Vortex; zero console errors throughout. Manual/docs not yet written beyond this note — a dedicated `docs/FVS.md` can follow once the Symbols/Pattern/Applications phases give it more to document.

- **August 25, 2026 (still later same day) — Phase B shipped: Paper.js-powered drawing in Genesis Creator, replacing its hand-rolled bezier anchor editor.** Completes the 3-phase plan from earlier the same day (Phase A: FVS reads Creator's library + grid variety + Basic Shapes set; Phase C: transform-preview strip ported into Creator) — Phase B was deliberately deferred until last since the plan itself flagged it as the largest, most novel piece needing an isolated prototype first. New `Organica.createPaperDrawEditor(canvasEl, opts)` in `shared/organica-paper.js`, additive alongside Strata's own `createTraceEditor` (untouched, unaffected): click-to-place anchors on a single `paper.Path`, drag a segment's point, drag its mirrored handleIn/handleOut for manual tangent control, toggle Closed/Smooth, undo, simplify, `getPathData()`. **Smooth is "live"** the way Creator's own hand-rolled `tangentOf()` was — every edit re-derives ALL auto segments' handles via real `path.smooth({type:'continuous'})` (paper.js has no live-recompute mode of its own), while a `manualSegments` Set remembers which segments were hand-tangented and restores them after each re-smooth, so a manual edit survives editing elsewhere on the path (double-click a segment releases it back to auto — same convention Creator's own hand-rolled version used). **Prototyped first in isolation** (`genesis/_test-paper-draw.html`, matching Loom's own `_test-*.html` convention) before touching Creator itself, per the plan's own instruction — caught and fixed two real bugs there before they could reach the real tool: (1) toggling Closed never recomputed smoothing for the new wraparound segment, so it rendered as a straight line even with Smooth checked — fixed by calling `resmooth()` inside `setClosed()`, not just setting the flag; (2) `simplify()` was destructively cumulative like Strata's own original `simplifyAll` — dragging the tolerance slider back down couldn't recover discarded detail — fixed with the same `simplifyLive` pattern Strata already established (a `baselineJSON` snapshot via `path.exportJSON()`, re-imported before every `simplify()` call, updated after every real draw action but never after simplify itself). Verified live in the prototype: draw/drag/close/smooth/undo/simplify all correct, plus the harder-to-verify parts confirmed directly via console inspection rather than fighting pixel-perfect drag targeting — manual handle drag mirrors correctly (`handleIn = -handleOut`), a manually-tangented segment's handle survives byte-identical after dragging a different anchor elsewhere, and double-click correctly releases it back to the auto Catmull-Rom-equivalent value. **Integration into `creator.html`**: a `#draw-canvas` overlay sits exactly over `#art` (both fixed 460×460 CSS px, confirmed from the existing CSS rather than assumed responsive) with a `fitBox: {x:0,y:0,width:200,height:200}` option (new on the factory) mapping Paper's project space 1:1 onto the artboard's own 0–200 box, so `getPathData()` drops straight into `geometry()`'s draw branch with zero coordinate conversion — replacing `tangentOf()`/`bezierPathD()`/`S.points` entirely (deleted, along with the old hand-rolled pointerdown/pointermove/pointerup anchor-drag logic in `bindArtboard()`, the old SVG-rendered `.handle`/`.thandle`/`.thline`/`.guide` markers in `render()`'s handles-layer block, and their now-dead CSS — Draw mode's anchor/handle overlay is Paper's own `fullySelected` rendering on the canvas now, not hand-drawn SVG). The canvas's own path is stroked `transparent` — the SVG form-layer stays the shape's one visual source of truth (styled per Fill/Stroke mode exactly as before), the canvas contributes only the edit affordances on top. **One disclosed simplification, not silently dropped**: new points land in raw 0–200 space regardless of the live Appearance transform, where the old version ran clicks through `invTF()` so a new point landed under the cursor even mid-rotation (`applyTF`/`invTF` deleted as genuinely dead code once nothing else called them) — drawing new points while a non-default rotation/scale/move is already dialled in is a rare workflow against the natural "draw first, then transform" order, so this wasn't carried over. **A real crash caught immediately on Creator's first real load** (not the prototype, which never happened to toggle Closed at 0 segments): `path.smooth()` throws on a path with under 2 segments, and Creator's own init calls `drawEditor.setClosed(true)` right away to match `#ck-close`'s default-checked state — fixed with a length guard in `resmooth()`. **A second false alarm, correctly dismissed rather than chased**: the exact same stale-console-buffer artifact documented repeatedly earlier this project (a tab's `read_console_messages` keeps showing an already-fixed error) reappeared here too — resolved by opening a genuinely fresh tab, which loaded clean, before concluding the fix had actually landed. **A real, pre-existing Phase A bug found only by this phase's own end-to-end verification**: saving a drawn shape and loading it back through FVS's "Creator library" picker (Phase A, shipped hours earlier) threw `clone.getBBox is not a function` — traced to `extractElementFromSVG`'s `DOMParser(...,'image/svg+xml')` call silently parsing every element into the null namespace (no `getBBox`, no `SVGElement`) whenever the source string has no explicit `xmlns` attribute on its root `<svg>` tag. Real uploaded SVG FILES always carry `xmlns` (why file upload "worked" when Phase A was verified earlier), but **every single Creator-authored library string does not** — inline markup dropped into `innerHTML` never needs the attribute, so nobody had written it, and Phase A's own earlier verification never happened to run a saved Creator form through this exact `getBBox` code path (only through thumbnail rendering, a more forgiving path). This means Phase A's whole "Creator library" picker — the 6 built-in Basic Shapes AND every user-drawn form, not just the one drawn this session — was silently broken for every entry until now. Fixed once at the top of `extractElementFromSVG` with `ensureSvgNamespace()` (injects `xmlns` only if missing, verified as a true no-op on an already-namespaced string via direct comparison). **Verified end to end after both fixes**: drew a real shape in Creator, confirmed it renders through the existing `formMarkup()`/Appearance pipeline (rotation applied and reset correctly, edit-canvas overlay staying in its own raw space exactly as documented), saved it, round-tripped it through FVS's Creator-library picker (previously the exact broken path), generated a real 16-component gallery from it with zero errors, then separately re-tested one of the 6 Basic Shapes (Triangle) through the same previously-broken path to confirm the fix isn't specific to freshly-drawn shapes. Also regression-checked Import/Generate modes still work post-refactor and that `#draw-canvas` correctly stops/resumes intercepting clicks when switching modes away from and back to Draw. Zero console errors throughout, confirmed on fresh tabs. Not yet done: retrofitting `docs/` with a Phase A/B/C summary (this note is the only record so far) and re-running the full accessibility-label check across Creator's changed markup.

- **August 25, 2026 (still later same day) — Creator: full Draw + Generate debug pass, 4 real bugs found and fixed.** Diego asked for a complete debug of Draw and Generate. Worked through both systematically (0/1/2/N points, Close/Smooth toggles, Style Fill↔Stroke, Stroke W, Rounded caps, Scale/Width/Length/Rotation/Move, Fit to Grid, anchor drag, manual tangent-handle drag, double-click reset, Undo/Ctrl+Z/Clear including past-empty, Save with each Type pill, mode-switch round-trips; Generate's Circle/Poly/Star, Sides, Radius at both extremes) plus a regression pass on Import, since shared code (`formMarkup`/`styleAttrs`) was touched. **(1)** The Phase C preview strip (shipped earlier today) hardcoded `fill="var(--ink)"` on every tile regardless of the real Style toggle — switching to Stroke changed the main artboard but the 7 preview tiles kept showing solid filled silhouettes. Fixed by factoring the fill/stroke logic `formMarkup()` already had into a shared `styleAttrs()`, used by both — the same "one function so two call sites can't drift" fix already applied once this session (FVS's `xmlns` bug), now applied to a second, unrelated drift of the identical shape. **(2)** Generate mode's Sides field: typing "99" correctly clamped the actual rendered polygon to a 20-gon internally, but the input's own displayed value stayed "99" forever — nothing ever wrote the clamped value back. Fixed with a `blur` handler that syncs the field to the true `S.gen.n` once the user leaves it (deliberately not on every keystroke, which was tried first and rejected: clamping live while typing "20" would clamp "2" to "3" mid-keystroke, making "20" untypeable). **(3)** `#status-text`/`#status-dot` in the header were pure static markup — grepped and confirmed zero JS ever touched them, so the header always read "Draw a shape to begin" regardless of mode or whether a shape existed (Generate mode always has a shape; Draw mode kept saying "begin" after drawing one). `Organica.status()` already exists in `shared/organica-core.js` for exactly this and is already used by Loom/Komorebi — Creator had just never called it. Wired via a new `updateStatus()` in `render()`: Draw shows point count once ≥2 (`"3 points · shape ready"`), Generate shows the shape+sides (`"star (5-sided) generated"`), Import shows imported/not-yet-imported. **(4)** Caught while verifying (3) — switching from Draw (with a shape mid-edit) to Generate left the Paper.js draw-canvas's own selection overlay (blue path outline + anchor squares) visibly stacked on top of Generate's circle, because `#draw-canvas.active`'s CSS only toggled `pointer-events`, and Paper.js keeps rendering its canvas regardless of whether it's receiving events. Fixed by also toggling `opacity` (0 when inactive) — verified the overlay no longer bleeds through, and that switching back to Draw still shows the exact preserved in-progress shape (Paper.js state isn't tied to visibility, only the CSS was hiding it late). All four verified via before/after screenshots + live JS state inspection (not just visual glancing), zero console errors across the full pass on fresh-tab checks.

- **August 25, 2026 (still later same day) — FVS: Symbols, a second tier, added alongside Components.** Diego shared a Figma-style Assets reference (Elements → Components → **Symbols** → Applications) and asked for the Symbols tier: a Loom grid whose cells each hold either a raw Element or a whole saved Component, nested and scaled into the cell, filled manually or via seeded Generate. Scoped via Plan Mode with two confirmed decisions: **built directly into `fvs/index.html`** (a new `Components`/`Symbols` tab pair, `.org-tabs`/`org-header--editor`, the exact component Genesis Creator's own Draw/Import/Generate tabs already use — not a throwaway prototype), and **nested Components render as real vector**, not a flattened snapshot — a Symbol cell referencing a saved Component re-reads that Component's own `{element, grid, cells}` and redraws its actual geometry scaled into the cell, a second transform layer nested around the existing per-item `normTx/normScale` layer. **Two real Loom grids**, captured live from `/loom/` (not hand-built): a small **Bento** (Columns/Rows 3×3, Variety 0.35, Gap 8, Seed 7 → 7 merged rect cells) and a **Hexagonal** (Columns 3, Gap 0.08, Seed 7 → 17 polygon cells, the "more complex" case) — read directly off Loom's own "View JSON" panel via the live app rather than guessing generator output, since `generator.generate()` returns fully-resolved geometry (absolute rects or polygon points+centroid), not just params. Both embedded as `SYMBOL_GRID_PRESETS` constants, one click each. **Reused, unmodified**: `Organica.loadLoomGrid`, `resolveGridCells`/`frameSize` (already grid-kind-agnostic), `ELEMENT_TYPES`, `buildComponentItems`, and `mulberry32` (the same reproducible-seed discipline `ruleRandom()` already established). New: `state.symbolGrid`/`state.symbolCells` (independent of Components' own `state.loomGrid`/`state.components`), `getSymbolGrid()` (wraps the raw `loadLoomGrid` output into the `{kind:'loom',...}` shape `resolveGridCells` expects — found and fixed before it ever ran, by re-reading `getGrid()`'s own equivalent wrapping step rather than assuming `state.symbolGrid` could be passed straight through), `buildSymbolItems`/`buildSymbolSVG`/`drawSymbolCanvas` (one more transform layer around a nested Component's own per-item markup — `translate(cx,cy) rotate scale(flip×scale×fit) translate(-half,-half)` wrapping the *entire* nested per-item loop, `fit = symbolCell.cellSize / frameSize(nestedGrid)`), a per-cell Manual editor (mirrors `buildManualEditor()`'s own one-row-per-cell shape; a Source select toggles Element ↔ Component fields, and only THAT control rebuilds the editor DOM — per-cell Rotation/Flip/Scale/Type/Component selects re-render the canvas only, so editing one row never resets scroll/focus on the others), seeded Generate (50/50 element/component per cell, 100% element if the Component Library is empty), and a **separate** `Organica.presetStore('fvs-symbols')` library (matching the project's own established "per-mode preset stores, not one shared list" convention) — `state.symbolGrid` is stored verbatim as `gridModel` and fed straight back into `Organica.loadLoomGrid()` on reload, since it's already exactly the shape that function accepts. **A real, easy-to-miss bug caught by testing the nested-Component nesting itself, not just the top-level render**: `buildComponentItems()`'s own `color: colorAt(i)` always reads the CURRENT live Palette, never a saved entry's own colours — Components' own `renderLibrary()` thumbnails already work around this with a `savedColorAt` remap; `buildSymbolItems()` needed the identical remap for nested nested-Component cells, or a Symbol's own colours would silently drift every time the live Palette changed after saving. Verified end to end in a live browser session: both presets render their correct default all-triangle fill immediately; Manual mode edits (Element↔Component switch, Type/Rotation/Flip/Scale) apply live with zero editor-rebuild flicker on non-shape changes; Generate at seed 1 → seed 42 → back to seed 1 produced byte-identical `state.symbolCells` (checked via direct JSON diff, not eyeballing); SVG export of a Symbol containing nested Components produced 47 real `<path>` elements and zero `<image>` tags; Save/Load round-tripped a 17-cell Hexagonal Symbol exactly (cross-checked the restored `state.symbolCells[0]` against the saved value); switching Components ↔ Symbols repeatedly left each tier's own state untouched, Palette shared correctly; 0 of 153 controls unaccessibly-named. **Deliberately out of scope for this pass** (disclosed in the plan, not silently dropped): per-cell custom-uploaded Elements in Symbol cells (Triangle/Arc only — a custom shape is still reachable indirectly via a saved Component that itself uses one), a multi-result gallery for Symbols (one seeded composition + Regenerate, not Components' own Random/Exhaustive breadth), and recursive Symbol-inside-Symbol nesting.

- **August 26, 2026 — FVS Symbols: grid section reordered above Palette, a "Show loaded grid" outline toggle, and both presets re-captured at 0 gap/0 canvas margin.** Three quick follow-ups on the Symbols tier shipped the day before. **(1)** "Symbol grid" moved to the top of the Symbols panel, above the shared Palette section — Palette itself is one DOM block shared by both tiers (Components' own layout is untouched), so the fix was moving "Symbol grid" into its own `tier-block[data-tier=symbols]` positioned BEFORE Palette in the DOM, while Fill/Symbol library stayed in a second `tier-block` after it — no CSS reordering tricks needed. **(2)** New "Show loaded grid" checkbox draws a wireframe outline of the grid's own cell boundaries (rect or polygon) on top of the live preview — `buildGridOutlineSVG()` reuses the *exact* same origin/half offset math `resolveGridCells()`/`buildSymbolItems()` already use, so the outline lines up exactly with whatever is actually rendered per cell; verified pixel-aligned on both Bento and Hexagonal. Deliberately preview-only, same rule as Loom's own "Background grid" toggle — appended to the live SVG string inside `renderSymbolCanvasOnly()` only, never inside `buildSymbolSVG()`/`drawSymbolCanvas()` (the functions PNG/SVG export actually calls) — confirmed directly by checking the exported SVG string contains zero occurrences of the outline's own stroke colour. **(3)** Diego asked to re-check and re-import both `SYMBOL_GRID_PRESETS` grids with 0 gap and 0 canvas margin (they'd shipped at Gap 8/Canvas margin 4 for Bento, Gap 0.08 for Hexagonal) — re-captured both live from `/loom/` the same way as the original capture (View JSON panel, not hand-edited), confirming the seed/topology stays identical (same 7-cell Bento layout, same 17-cell Hexagonal layout) while the geometry shifts to genuinely edge-to-edge cells and a canvas with no border — verified visually (Hexagonal's cells now share exact edges with no gap-inset, unlike the visible gap in the original capture) and via the grid outline overlay from fix (2), zero console errors on a fresh tab for both presets.

- **August 26, 2026 (later same day) — FVS Symbols: per-cell Fit/Scale/Anchor/Padding controls, plus a real click-to-select Manual workflow.** Diego asked for a full evaluation of how a Symbol cell's content is fit/scaled/positioned, then a redesign of Manual mode: select one or more cells directly and assign a Component/Element with those properties applied to the whole selection at once. Scoped via Plan Mode with two confirmed decisions: **click-to-select on the canvas itself** (Shift-click for multi-select — the same convention Strata's own Refine editor already uses), and **three Fit modes**: Contain (kept, the old only behaviour), Fill/cover (new — independent X/Y scale to touch all 4 edges), Fixed size (new — an absolute size ignoring the cell's own dimensions). Also shipped: a continuous Scale slider (replacing the old 3-value dropdown), a 9-way Anchor grid (`object-position`-style), and a per-cell Padding percentage — all four apply uniformly to both source types (Element and nested Component), since they already shared the same transform fields. **Data model**: `state.symbolCells[i]` gained `fitMode`/`fixedSize`/`anchorX`/`anchorY`/`padding`; old saved Symbols (missing these fields entirely) are backfilled via a new `withPlacementDefaults()` at load time, and `resolveCellPlacement()`'s own fallback logic (`cell.padding || 0` etc.) makes the render math itself tolerant of the old shape too — verified by hand-constructing a pre-feature Symbol entry and confirming it renders pixel-identical to before. `resolveGridCells()` gained `cellW`/`cellH` alongside the pre-existing `cellSize = min(w,h)` (which Fill mode needs the real asymmetry Components never needed — added, not replaced, so Components' own call sites are untouched). One new shared function, `resolveCellPlacement(cellW, cellH, natural, cell)`, computes `{scaleX, scaleY, offsetX, offsetY}` from fit mode + anchor + padding + scale/fixedSize — used identically by both source types and both render paths (SVG string, Canvas2D), reducing to exactly the old centered/uniform-fit math at the defaults. **A real bug caught immediately during manual testing, not found by inspection**: clicking anywhere in a cell's own empty margin (the space a Contain-fit shape leaves inside a non-square Bento cell) selected nothing at all — a bare `<g data-cell-index>` wrapper has no hit-test area of its own in SVG, only the visible content inside it does. Fixed by adding an invisible `fill="transparent"` (not `"none"`, which has no hit area either) rect/polygon covering each cell's own full boundary, drawn first inside that same `<g>` — confirmed harmless in the exported file (zero visual effect, `transparent` paints nothing) but a real, disclosed departure from "export contains only meaningful geometry": every exported cell now also carries one invisible hit-shape, verified present in `buildSymbolSVG()`'s own output (7 `fill="transparent"` elements on the 7-cell Bento) alongside the real content paths. Selection highlight (`buildSelectionOutlineSVG()`) reuses the exact technique the "Show loaded grid" outline already established — a second, differently-coloured preview-only `<g>`, confirmed absent from `buildSymbolSVG()`'s own export output (checked directly: zero occurrences of its stroke colour). The old one-row-per-cell Manual list (`buildSymbolManualEditor()`) is gone entirely, replaced by one static "Cell properties" panel (`renderCellPropertiesPanel()`) that reflects/edits whatever `state.symbolSelection` currently holds — a batch edit, verified directly: Shift-clicking two cells then changing Rotation set both cells' `rotation` to the new value in one action, not just the last-clicked one. Verified end to end: Contain-default renders pixel-identical to before; switching a wide Bento cell to Fill visibly touches all 4 edges; Fixed size renders the same absolute size regardless of the cell's own dimensions; all 9 anchor positions visibly relocate content within a cell; a Component-sourced cell selects and edits correctly through the same panel (nested geometry, 10 real `<path>` elements on export, 0 rasterized); 0 of the new controls unaccessibly-named; zero console errors throughout on fresh-tab checks.

- **August 26, 2026 (later same day) — FVS Symbols: full regression/debug pass on the Fit/Scale/Anchor/Padding + click-to-select work, 2 real bugs found and fixed.** Diego asked for organized regression and debug testing on the feature shipped earlier the same day. Worked through it systematically — default-render regression, all 3 fit modes on both rect (Bento) and polygon (Hexagonal) cells, the 9-way anchor grid, multi-select batch-edit, Save/Load round-trip of the new fields, Generate reproducibility, export sanity (no NaN/Infinity, no selection overlay leak), tab-switch state isolation, and edge cases (empty Component library, a deleted/orphaned Component reference, extreme values). **(1)** Selecting "Component" as Source while the Component Library is empty silently produced an invisible cell — `componentName` ended up `undefined`, `buildSymbolItems()`'s existing `if (!entry) return null` swallowed it with zero console error, so the cell just vanished with no explanation. The old one-row-per-cell editor had guarded this (`disabled` on the option when the library was empty) but the new shared panel never carried that guard forward. Fixed two ways: the "Component" option is now disabled in `renderCellPropertiesPanel()` whenever the library is empty (blocks a real click), and the Source `change` handler also refuses the value programmatically (`.value` can still be set on a disabled option from code, so the click-blocking alone wasn't a complete fix — caught by deliberately bypassing the disabled state in a test to check). **(2)** Clicking the canvas *outside* any cell (the paper background) silently left an existing selection in place — a real gap against the plan's own stated reference convention (Strata's Refine editor deselects on an empty-space click). `bindSymbolCanvasSelection()`'s early-return on "no cell hit" never distinguished "nothing to do" from "should clear" — fixed by clearing the selection on a non-Shift click that misses every cell, verified the fix reproduces the Strata-like behaviour exactly (selection non-empty → click background → empty) without disturbing Shift-click's own additive semantics (a Shift-click on empty space now correctly leaves the selection untouched, tested separately). **Fixed on Diego's own follow-up**: the disclosed "deleted Component reference renders as nothing" gap above got a real fix rather than staying a known limitation — `buildSymbolItems()` now returns a `type:'missing'` placeholder (a dashed warning-red rect with a diagonal X, sized/positioned exactly where the real content would have been) instead of `null` for an orphaned `componentName`, rendered by both `buildSymbolSVG()` and `drawSymbolCanvas()` — deliberately visible in exported files too, since a silently missing chunk of a design is worse than a clearly-flagged one once the file leaves the tool. `renderCellPropertiesPanel()`'s own Component `<select>` also now surfaces the dangling reference explicitly (`⚠ name (missing)`) instead of silently mismatching whatever real option happened to render first — verified the normal case (a Component that still exists) shows no such option and renders unaffected (10 real paths, none dropped), while the orphaned case shows the placeholder in canvas/export and the flagged option in the panel, with zero console errors either way. Verified stable at deliberately extreme inputs (Scale 400%, Padding 40%, corner Anchor) — real overflow, no NaN/Infinity in the generated SVG. All fixes verified on fresh tabs after confirming a stray, unrelated `localhost:5050` 404 (Strata's own local backend, not running) was stale console-buffer noise, not a new regression — re-confirmed clean via a genuinely fresh tab per this project's own established discipline.

- **August 26, 2026 (still later same day) — FVS Symbols: a visual "Choose content" overlay replacing the plain Source/Type/Component selects, plus click-and-drag marquee multi-select.** Diego's own framing: choosing a cell's content by picking a Component's name from a dropdown gives no visual sense of what it looks like, and selecting several cells to batch-edit had no faster path than repeated Shift-clicks. Asked directly whether Rotation/Flip still make sense for a Component cell (his own instinct going in): confirmed via AskUserQuestion — **removed entirely for Component-sourced cells** (a Component is already an internally composed arrangement, often symmetric by construction; re-rotating/flipping the whole thing as one more transform layer added a control with no clear use, not a real capability gap), **kept unchanged for Element cells**. Also confirmed the overlay's own trigger and the marquee's scope in one question: a plain click **immediately opens** the picker for that cell (no separate "select then open" step); Shift-click stays pure add/remove-from-selection with no overlay; a docked **"Choose…"** button reopens the overlay for whatever is currently selected, which is how a multi-selection (built via Shift-click or a drag) gets batch-assigned. **Docked panel restructure**: the Source/Type/Component `<select>` trio is gone from `#symbol-cell-props`, replaced by a one-line content readout + **Choose…** button; Rotation/Flip rows now hide (`display:none`, the same toggle pattern already used elsewhere in this panel) whenever the selection's first cell is Component-sourced. **The overlay** (`#symbol-content-overlay`, `role="dialog" aria-modal="true"`) is genuinely new UI with no existing precedent to reuse — `Organica.popover` is anchored-to-a-trigger-button and doesn't fit "opens wherever a cell was clicked on the canvas," so this is a plain fixed-position centered modal instead, disclosed as such rather than forced into the wrong existing component. Two tile sections reuse the exact preview techniques already proven elsewhere: Elements render via the same geometry+fill the preview strip uses, Components via the identical `buildComponentSVGWithPaper` thumbnail technique the Library view already uses (a saved entry's own colours/paper, not the live palette — so a tile can't misrepresent what picking it will actually place). Picking any tile applies Diego's own stated defaults to every currently-selected cell in one shot: `scale:1, fitMode:'fill', padding:0, anchorX:0, anchorY:0`, plus `rotation:0/flipH:false/flipV:false` reset to identity even for a re-picked Element. **Marquee drag-select**: `bindSymbolCanvasSelection()` rewritten from a single `click` listener to `mousedown`/`mousemove`/`mouseup` with a 5px-movement drag threshold (Creator's own old draw-mode click handler used the identical pattern) — a plain click with no movement keeps the click-opens-overlay behaviour, movement past the threshold switches to a live marquee instead. New `svgPointFromEvent()` converts a mouse event into the SVG's own internal coordinate space via `getScreenCTM().inverse()` (re-queries the SVG element fresh on every call, not cached — matters because a render mid-drag replaces the SVG's own DOM node via `innerHTML`); new `symbolCellBounds()` reuses `resolveGridCells()`'s already-existing `cellW`/`cellH` for a per-cell axis-aligned box (a deliberate approximation for hexagon/polygon cells — their bounding box, not their true outline, same notion of "cell size" this feature already uses elsewhere); a plain AABB intersection test drives live selection updates as the drag proceeds, unioned with the selection already held at drag-start when Shift is down. The live marquee rectangle is appended in `renderSymbolCanvasOnly()` only (the same preview-only-overlay convention `buildGridOutlineSVG`/`buildSelectionOutlineSVG` already established) — never inside `buildSymbolSVG()`, which every export call reads directly, so it can't leak into a saved file. **Verification found the coordinate math correct and a first-pass test wrong, not the app**: an initial JS-simulated drag test produced an empty selection despite a large-looking marquee rect with a suspicious negative Y coordinate; traced to the test itself precomputing screen coordinates once from a stale CTM before the drag's own first `mousemove` triggered a re-render (which replaces the SVG DOM node) — recomputing the CTM fresh immediately before each dispatched event, exactly matching what a real continuous mouse drag does, produced correct results immediately. Verified properly from there: a full-canvas drag on the 7-cell Bento grid selected all 7 cells; a drag confined to the top-right selected only cell 1; a subsequent Shift+drag over the bottom-left added cell 5 without dropping cell 1; a drag over the left ~40% of the 17-cell Hexagonal (polygon) grid correctly selected the 10 cells whose bounding boxes actually intersect that band; no overlay ever opens from a drag release, matching the plan's own explicit rule that dragging builds a selection rather than committing content. `buildSymbolSVG()` (the real export path) confirmed to contain zero marquee/overlay markup after building up a selection and drag history in the same session. 0 of 4 overlay controls unaccessibly-named on a fresh tab (`Organica.autoLabelPanel`), zero console errors.

- **August 26, 2026 (still later same day) — FVS Symbols: cell-overflow behavior defined — "Clip to cell" (default on), a bleed toggle, and a badge for the intentional-overflow case.** Diego asked to define what happens when a cell's content exceeds its own bounds, then to test it. Confirmed live first, before any code: a 720×360 Bento cell set to Fixed size 900 rendered a triangle covering three neighboring cells entirely — no clipping existed anywhere in `buildSymbolItems`/`resolveCellPlacement`/`buildSymbolSVG`/`drawSymbolCanvas`, in both preview and real export. Presented three options (clip always, allow-with-indicator, a toggle) via AskUserQuestion; Diego asked whether they could combine, and they do cleanly: **shipped all three as one design** — a per-Symbol **"Clip to cell"** checkbox, default **ON**, next to the existing "Show loaded grid" toggle. **Prototyped live in the browser first** (pure runtime JS against the running page, no file edits) before writing the plan — a `<clipPath>` per cell (`<rect>` for rect cells, `<polygon>` for polygon cells) reusing the *exact* geometry `buildSymbolSVG()` already computes for its invisible click hit-shape, confirmed clean on both the Bento (rect) and Hexagonal (polygon) grids with the same oversized-triangle repro — zero new geometry math needed, which is why the real implementation shipped in one pass with no surprises. **Shipped**: `state.symbolClipEnabled` (bool, Symbol-level, not per-cell); `buildSymbolSVG()` emits one `<clipPath id="clip-cell-N">` per cell into a new `<defs>` block and adds `clip-path="url(#clip-cell-N)"` to each cell's `<g>` when the toggle is on, omitting both when off (byte-for-byte pre-feature output); `drawSymbolCanvas()` mirrors it with `ctx.save()+ctx.clip()+ctx.restore()` wrapped around each cell's own draw calls, using the identical rect/polygon source data so preview/PNG and SVG export can't disagree. **The bleed case stays available and is now flagged, not silent**: `cellOverflowInfo(index)` reuses `resolveCellPlacement()`'s own already-computed `scaleX/scaleY` to check whether a cell's content footprint exceeds its own `cellW/cellH` (the same disclosed axis-aligned-footprint approximation `symbolCellBounds()` already uses elsewhere in this feature — rotation isn't accounted for precisely) — while the toggle is off, `renderCellPropertiesPanel()` shows a "⚠ Extends beyond cell bounds" row for a selected overflowing cell, hidden for a normal one, and not computed at all while clipping is on (the concept is moot once clipped). Persisted into saved Symbol snapshots (`buildSymbolLibraryEntry`/`applySymbolLibraryEntryToUI`/the library-thumbnail temporary-state-swap in `renderSymbolLibrary`) with a `clipEnabled !== false` backfill so Symbols saved before this feature still load and render exactly as they did (clip on, today's only real behaviour). `loadSymbolGrid()` resets the toggle to `true` for a freshly loaded/uploaded grid (a clean baseline, distinct from restoring a saved Symbol's own choice). Verified end to end: toggle-on crop confirmed identical to the live prototype on both grids, in both the DOM preview and the actual `buildSymbolSVG()` export string; toggle-off reproduces today's bleed exactly (regression-clean) with the badge showing only for the genuinely overflowing cell; toggling off→on→off leaves cell data byte-identical (only render/indicator behaviour changes); a save/load round-trip preserves `clipEnabled:false` exactly, and a hand-simulated pre-feature entry (field deleted entirely) backfills to `true` with no exception; 0 of 23 controls unaccessibly-named and zero console errors on a fresh tab.

- **August 26, 2026 (still later same day) — Genesis Creator: Mask/Container Type pills given real behavior via an attached "Underlying layer," plus a real browser rendering bug found and worked around.** Diego asked to work on Creator so an Element/Seed/Component could become a mask or a "total mask" (rendering the shape empty/transparent); scoped over several rounds of clarification down to a concrete, self-contained mechanism: "un element può essere una maschera di un altro element … lo stesso vale per i component." Explored first rather than assuming — found Creator's own **Type pills already had `mask`/`container` values** (alongside `asset`/`variant`) sitting in the UI since before this session, but **purely cosmetic**: read only by `genesis/library.html` to label tiles, with zero effect anywhere on render or save, confirmed by grepping the whole repo for `clipPath`/`mask`/`fill-rule` (the only real prior art found was FVS's own just-shipped "Clip to cell"). Confirmed design via several rounds: **Container** — the current shape gets an attached **underlying layer** (another shape picked from `organica.library.forms`, the same store/picker pattern as FVS's own Creator-library reader) shown **only inside** the shape's own outline; **Mask** — the same attached layer shown **everywhere except** inside the outline, a real hole, via an actual SVG `<mask>` element (not just `fill-rule`) — matching the original ask's "rendendo vuoto o trasparente la forma": the top shape's own flat fill disappears, it purely defines a boundary. The underlying layer inherits the shape's own transform (moves/scales/rotates together, no separate Appearance controls for it in this pass) and the **composited result bakes into the real saved SVG on Save** (`saveForm()` needed no changes at all — it already calls `formMarkup()`, which now returns the composite). Scoped explicitly to **Creator only this pass** — the confirmed Component-masks-Component analog lives in FVS's own Component/Symbol data model and is a distinct Phase 2, not built here. **A real, empirically-verified browser bug found mid-implementation, not assumed away**: the first working version rendered completely blank for Container — isolated with a minimal repro outside Creator's own code entirely (a bare `<clipPath><g><path/></g></clipPath>`, no transform at all) and confirmed **any `<g>` nested inside a `<clipPath>` renders the whole clip as empty** in this browser, even a transform-less wrapper — while the identical wrapping pattern inside a `<mask>` element rendered correctly. Fixed by moving `transform` onto the `<clipPath>` element itself instead of a nested `<g>` (geometrically identical, avoids the bug entirely) for the Container branch; the Mask branch was already safe since `<mask>` tolerates `<g>` wrapping fine, verified separately before relying on it. **Disclosed, not fixed, simplification**: this clipPath-transform-hoisting trick only works because Draw/Generate-mode geometry is a bare `<path>`/`<circle>` with no wrapping `<g>` of its own — an **Import-mode** shape's `geometry()` output IS itself a `<g>` (built by `importSVG()`), so picking an imported shape as a Container's own boundary would hit the identical browser bug; not fixed this pass, noted in the code as a known gap rather than silently broken. A second, separate disclosed simplification carried from the original design: the Mask case's forced `fill="#000"` on the boundary is a more-specific-attribute loser against an Import shape's own explicit `fill="var(--ink)"`, so an imported mask boundary isn't guaranteed a pure binary cutout (usually fine in practice since `--ink` is dark). New picker overlay (`#layer-picker-overlay`) reuses the exact tile-preview visual pattern FVS's own Choose-content overlay established, ported to Creator's own CSS tokens (`--ink-muted`/`--border` etc., not FVS's `--mid`/`--panel`). `renderTransformPreview()`'s 7-tile strip was refactored onto the same shared `compositeMarkup(tf, idSuffix)` function `formMarkup()` calls (an `idSuffix` per tile avoids duplicate `id`/`url(#...)` collisions across the 7 simultaneously-rendered mini-SVGs) — deliberately reusing the exact discipline the file's own `styleAttrs()` comment already documents ("one function means the two can't drift apart the same way again"), since the preview strip had silently ignored the Style toggle once before. Verified end to end: Container clips a picked layer exactly to a star's outline (screenshot-confirmed); Mask punches a real star-shaped hole through the same circle (screenshot-confirmed); Rotation 45°/Scale 0.6 moves the boundary and the underlying layer together as one rigid unit (both share the identical `rotate(45)` in the live markup); Remove reverts to plain flat fill with the Type pill itself untouched; a real Save produces a library entry whose own `.svg` string already contains the `<clipPath>` composite baked in, not just the bare shape; switching back to Asset hides the panel and reverts rendering to plain fill even with a layer still attached in state (dead until Mask/Container is reselected, not silently applied); 0 of 29 controls unaccessibly-named (including the new picker overlay's own tiles/buttons); zero console errors on a fresh tab.

- **August 26, 2026 (still later same day) — FVS: Phase 2 of the mask/container work — one saved Component can now mask/contain another.** Diego's own priority call: build the confirmed FVS analog ("lo stesso vale per i component") before committing anything from this session, rather than leaving it as a disclosed follow-up. New **Role** panel section in the Components tab (Normal/Container/Mask select, mirroring Creator's own pills) + "Pick underlying component…" (shown only for Container/Mask, the same dead-control rule this project applies everywhere) opening a tile picker of the shared Component Library. Applies uniformly to **every generated gallery candidate** (not just the selected one) and bakes into Save — `buildLibraryEntry()` gained `role`/`underlyingComponentName`, round-tripped through `applyLibraryEntryToUI()` with a `role || 'normal'` backfill for pre-feature saves. **A harder problem than Creator's own version**: a Component's cells are inherently multi-shape and always `<g>`-wrapped (per-cell rotation/scale), so the exact browser bug found earlier the same day (`<g>` nested inside `<clipPath>` renders empty) would hit basically every real Component, not just an Import-mode edge case. Fixed for Container by **flattening**: `componentBoundaryClipContent()` consolidates each cell's two transform levels into ONE `transform` attribute on a bare `<path>` (no `<g>` at all), and `<clipPath>` accepts multiple such direct-child paths, unioning them automatically per spec — geometrically identical to the normal per-cell markup, just restructured to dodge the bug. Mask's own boundary (`componentBoundaryMaskContent()`) keeps the normal nested-`<g>` structure unchanged, since `<mask>` already confirmed tolerant of it. **A second, real bug caught by testing, not assumed correct from the SVG structure alone**: the first Mask version painted this Component's own Paper rect *outside* the masked group (as the tool's usual unconditional background), so the "hole" only erased the underlying Component's own content and still showed opaque paper beneath — not a real hole. Fixed by moving the Paper rect *inside* the same masked `<g>` for the Mask case specifically (Container keeps the ordinary unconditional Paper rect, since its boundary only ever covers its own small item area) — verified directly by sampling raw pixel alpha, not just eyeballing: a point inside the boundary silhouette reads `[0,0,0,0]` (true alpha-zero) both in the live SVG markup structure and via a real `drawComponentCanvas()` render read back with `getImageData`, while a point outside reads the underlying Component's own opaque colour. **Canvas2D needed no such workaround** — it has no clipPath-style bug at all, since it never parses markup: `drawComponentCanvas()` builds ONE combined `Path2D` unioning every cell's own transformed outline (`Path2D.addPath` + a per-cell `DOMMatrix`), then either `ctx.clip()`s it (Container) or erases it via `globalCompositeOperation:'destination-out'` (Mask) — the same real-alpha-knockout mechanism confirmed for Creator, now proven consistent in both this tool's rendering backends. Underlying Component fits via a plain uniform `scale(size/under.size)` from the origin (both frames are square, so this covers corner-to-corner with no centring math needed). Deliberately **does not recurse** into the underlying entry's own role/underlying (even a literal self-reference just renders that entry's plain cells once) — makes runaway recursion structurally impossible rather than merely guarded, a disclosed simplification. `buildComponentSVGWithPaper()` (already the established "render a saved entry's own values, not the live ones" wrapper for Paper colour) extended to also swap `componentRole`/`underlyingComponentName`, so Library thumbnails and the Symbols tier's own "Choose content" tile previews render the correct composite automatically. **Explicitly still Phase 3, not built here**: a masked/container Component nested as a *Symbol cell's* own content — Symbols' `buildSymbolItems()`/`buildSymbolSVG()`/`drawSymbolCanvas()` use their own specialized nested-rendering path (`nestedItems`/`resolveCellPlacement`) distinct from `buildComponentSVG`/`drawComponentCanvas`, so wiring the same effect there is a separate, disclosed follow-up. Verified end to end: Container clips a real underlying Component (a distinctly-coloured Arc/Radial) to every one of 8 generated Pinwheel-triangle candidates uniformly (screenshot-confirmed, using a deliberately different colour after the first pass's same-default-black test data made the effect invisible by coincidence — a real lesson about picking diagnostic test data, not a bug); Mask shows a genuine alpha-zero hole confirmed by direct pixel sampling in both SVG and Canvas2D; Remove reverts cleanly with Role itself untouched; a real Save produces a Library entry whose own thumbnail already renders the composite; loading that entry back via `applyLibraryEntryToUI()` restores Role/underlying and the select/panel state exactly; 0 of 40 controls unaccessibly-named; zero console errors on a fresh tab.

- **August 26, 2026 (still later same day) — "Element" renamed to "Seed" product-wide, plus a real parametric-seed mechanism proven with Arc.** Diego wants the authored-shape building block — "Element" in FVS, "Form"/"shape" in Genesis Creator/Library — called only **Seed**, everywhere that concept appears. Audited the whole repo first: the concept is concentrated in three files (FVS's Element panel, Creator's "Form" panel + Type-pill system, Library's "Form" panel + "Basic Shapes" set), while OTHER "shape" mentions elsewhere are genuinely different concepts and were explicitly confirmed OUT of scope — Komorebi/Camo Turing's scatter-mask shapes, Halide/Strata's traced raster regions, Soul's parsed SVG primitives, Loom's grid-cell shape. **A real, deliberate exclusion found during implementation, not assumed away**: Membrane's own "Seed shape" control (Circle/Line/Point — the starting point-set shape for its particle sim) uses "seed" for a completely unrelated, pre-existing concept (a simulation's initial configuration, same idea as "random seed") that predates and has nothing to do with the Genesis-authored-shape concept — left untouched rather than blindly renamed just because the word matched. **Full rename, confirmed explicitly by Diego** (test phase, no real user data to protect): persisted JSON field names (`entry.element`→`entry.seed`, `cell.elementType`→`cell.seedType`, etc.) and internal identifiers (`ELEMENT_TYPES`→`SEED_TYPES`, `extractElementFromSVG`→`extractSeedFromSVG`, etc.) renamed too, not just UI labels — done via a Python find-replace on `fvs/index.html` (its own single biggest concentration of the concept) with the 4 real DOM API names that happen to contain "Element" (`createElement`, `createElementNS`, `getElementById`, `SVGPathElement`) protected first via placeholder swap. **A genuine implementation bug caught immediately, not shipped**: the first placeholder attempt used tokens like `@@CREATEELEMENTNS@@`, which themselves contain the substring "ELEMENT" — the very next replace pass (`'ELEMENT'→'SEED'`) mangled the protection tokens before they could be restored, silently corrupting every `createElementNS`/`createElement`/`getElementById` call into garbage (`ZZZ`-style placeholders left in the shipped file). Caught by grepping for the DOM API names afterward and finding zero matches instead of the expected ~20 — restored from a pre-edit backup and re-ran with placeholder tokens that share no substring with "element" in any case, verified clean on the second pass. Deliberately kept unchanged: the SHARED `organica.library.forms` wire format itself (its own `.svg`/`.forms`/`.sets`/`.type` property names and the storage key) — multiple already-shipped tools outside this pass's file list (Soul, Camo Turing, Loom) read that exact shape, and CLAUDE.md's own standing rule already treats localStorage keys as sacred; only `fvs/index.html`'s OWN component/symbol schema (which only FVS itself reads) got the full field rename. **A second real bug found while regression-testing the rename, not before**: `renderLibrary()` threw on the very first fresh load — traced to stale pre-rename Component Library data left in `localStorage` from earlier in this session (old entries still shaped `{element: ...}`, new code reading `entry.seed`); confirmed as a stale-test-data artifact rather than a real code bug by testing against a genuinely empty library first, then clearing the leftover keys per Diego's own "test phase, no migration needed" ruling. **Parametric seeds**: rather than inventing a separate parametric-entry schema, extended Creator's existing Generate mode (already "adjust live before saving") into "adjust live, saved, AND still adjustable after loading back" — Arc added as Generate mode's 4th type (`arcPathD(thicknessPct)` ported verbatim from `fvs/index.html`, 2×-scaled for Creator's 200-unit box vs. FVS's own 100-unit one, verified pixel-for-pixel identical at 25/50/100% thickness), `saveForm()` now persists `genType`/`genParams` alongside the baked `svg` (every `.svg`-only consumer elsewhere is unaffected, purely additive), and a new **"Load seed…"** picker in Creator's Source panel restores a saved seed's live Generate-mode controls exactly as saved (verified: reset to Circle, loaded a saved Arc-at-65%-thickness back, mode/type/slider-value/DOM-slider all restored correctly and still draggable). `genesis/library.html` got the matching live parameter slider inline for any selected seed carrying `genType` — dragging it regenerates that entry's own `svg`/`genParams` immediately (verified: tile updates live). **A wrong initial assumption, caught and corrected rather than left as a false claim**: expected Basic Seeds' entries to reset to their shipped defaults on reload (matching the plan's own "built-in protection" framing) — measured instead that edits persisted across a reload, then traced why: `basicShapeForms()`'s entries (now `basicSeedForms()`) never actually set `builtIn: true` at the per-form level in the ORIGINAL pre-existing design either (only the separate "Organic Forms" catalog's own entries are truly read-only) — so Basic Seeds were already meant to be a freely-editable starting-point set, and persisting Arc's own thickness edits in place is consistent with how Square/Circle/etc. could already have their Name/Type edited before this session, not a new gap. Renamed "Basic Shapes"→"**Basic Seeds**" (`BASIC_SHAPES_SET_ID`→`BASIC_SEEDS_SET_ID`) in both `creator.html` and `library.html` (kept in sync per this project's own documented dual-copy convention), with the set's own Arc entry now the parametric one instead of the old fixed quarter-disc. Verified end to end: fresh-tab loads clean (zero console errors) on FVS/Creator/Library/Camo Turing; FVS's full regression re-run post-rename (Components tier Normal/Container/Mask, Symbols tier Choose-content overlay's "Seeds"/"Components" tiles, Save/Load, `buildSymbolSVG()` export) all functionally unchanged; 0 unaccessibly-named controls on every new picker/slider. **Light touch, not a rewrite**: Camo Turing's existing "seed shape" hint phrasing (4 hint paragraphs + 2 aria-labels, all genuinely about a Genesis-sourced shape) tightened to plain "Seed"; nothing else in that file touched.

- **August 26, 2026 (still later same day) — Creator's Generate-mode "Kind" control became a thumbnail dropdown, reusing an existing shared component instead of building a new one.** Diego asked for the Circle/Poly/Star/Arc picker to become a dropdown with thumbnails, styled per the design system. Checked first rather than building blind: `shared/organica-panel.css` + `shared/organica-transformer.js`'s `Organica.Transformer.buildPicker` already implement exactly this — a `.preset-trigger`/`.preset-menu` thumbnail dropdown, built for Camo Turing/Living Path's own preset pickers and never yet used in Creator. Reused it wholesale rather than inventing a parallel component: linked `organica-panel.css` (verified its shared class names collide with several of Creator's own local ones — `.panel-row`/`.panel-label`/`.seg`/etc. — but Creator's own `<style>` block loads after the `<link>`, so the existing look is unaffected, confirmed live) and `organica-transformer.js` (after `organica-core.js`, which it extends). Replaced `#gen-seg`'s 4 plain segmented buttons with an empty `.presets` host + `buildPicker(host, GEN_KIND_REGISTRY, {...})`, `GEN_KIND_REGISTRY` supplying one small representative SVG silhouette per kind (filled circle/pentagon/star/quarter-arc). **One real, disclosed extension to the shared component**: `buildPicker` always prepended an empty `''`/"None" entry, which makes no sense for a control that must always have a real value — added an opt-in `opts.noNone` flag (default `false`, so every OTHER existing caller of this shared function is unaffected) rather than special-casing it locally. Verified end to end: dropdown opens with all 4 thumbnails and no None option, selecting Star updates the trigger face + regenerates the shape + still correctly shows the Sides row (`syncGenerator()` untouched); the "Load seed…" picker's restore path now calls the new picker's own `setActive()` instead of hand-toggling `.seg-btn.active` classes, verified the trigger face updates correctly on a loaded Arc seed too; zero console errors, `aria-label="Generate kind"` reachable via `Organica.autoLabelPanel`.

- **August 26, 2026 (still later same day) — Creator's Generate mode: 3 enrichments to existing Kinds + 5 new Kinds, from an explicit "analyse first" ask.** Diego asked for an analysis of what parameters could enrich Circle/Poly/Star/Arc and what other shape types were worth adding, then confirmed a large, concrete scope across a few rounds (including a "fundamental geometric shapes" reference he shared mid-analysis, which settled Triangle/Square/Segment as real additions and Point as a deliberate non-addition, redundant with Circle at a small radius). **Enrichments**: Poly gained **Corner rounding** (0–50%, standard cut-corner-then-quadratic-bezier technique) and **Irregularity** (0–100%, seeded per-vertex radius jitter via a `mulberry32` PRNG — same reproducible-seed discipline as FVS/Vortex/Camo Turing); Star gained a real **Inner radius** slider (10–90%, replacing a hardcoded `r*0.45` — verified the new default of 45% reproduces the exact old geometry, distance-checked not just eyeballed); Arc was generalized from a fixed 90°-corner-anchored quarter-shape into a real **wedge/pie/ring generator** via **Sweep** (10–350°) + **Pivot** (Corner/Center), built as one `wedgePathD(cx,cy,outerR,startDeg,sweepDeg,innerR)` function — **verified algebraically and then empirically that the old fixed formula is an exact special case** (`cx=cy=0, outerR=200, startDeg=0, sweepDeg=90`), confirmed via a coordinate-array diff (not string equality, which failed only on decimal-formatting noise, correctly diagnosed as a test artifact rather than a regression) — so every already-saved Arc seed still renders identically. **5 new Kinds**: Triangle (Base/Height, the FVS Isosceles-triangle convention — genuinely distinct from Poly's forced-equilateral n=3), Square (Width/Height + the same Corner-rounding function as Poly, proving the shared-rounding design), Segment (an open stroked line — picking it auto-switches Style to Stroke as a helpful default, not a hard lock), Drop mark (an analytic teardrop — 2 cubic Béziers + a semicircle arc — disclosed as a faithful *visual* port of Komorebi's own `shapeDrop`, not a literal formula port, since that original is a per-pixel raster SDF and Generate mode needs real vector paths), Organic blob (adapted from `shared/organica-motion.js`'s existing `organicBlobPath`, ported directly rather than adding a script dependency on Soul's own motion file — 32 points around a circle offset by `Organica.noise.simplex2` sampled on a unit circle around the seed, avoiding the polar-pole artifact a naive angle-based sampling would hit; needed `shared/organica-noise.js` newly linked into Creator, after `organica-core.js`). **Architecture stayed disciplined**: no new mechanism invented — every addition slots into the existing `S.gen`/`geometry()`/`syncGenerator()`/thumbnail-picker model already proven this session for Arc. Two real refactors done *because* the kind count grew to 9: `saveForm()`'s genParams serialization switched from a per-kind hand-picked field list to saving the whole `S.gen` bag (disclosed as a few harmless extra bytes traded for not needing a code change every time a future kind gains a param), and `loadSeedIntoWorkingShape()` now resets `S.gen` to a shared `DEFAULT_GEN` baseline before merging a saved recipe on top — **a real latent bug caught by design, not by testing**: without the reset, loading a plain Circle right after tuning Poly's Irregularity would have silently left the stale Irregularity value sitting in `S.gen`. Verified end to end: all 9 Kind tiles show distinct icons; Arc's three regimes (default quarter, Center+Sweep~350°+low Thickness = a real ring, Center+Sweep 45° = a clean pie wedge) all screenshot-confirmed; Poly's Corner rounding and Irregularity+Seed reproducibility (same seed twice → byte-identical path) verified; Drop mark degrades gracefully (no NaN) at Tail=0; Blob's Amount=0 baseline and seed-reproducibility confirmed (one earlier "not reproducible" result was traced to a test-script race reading the DOM before a slider's own `input` event had fully applied, not a real bug — confirmed by re-running the identical sequence cleanly); save→reset-to-Circle→"Load seed…" round-trip verified for a saved Drop mark, restoring the exact Tail value with a live, draggable slider; 0 of 28 controls unaccessibly-named after cycling through every Kind; zero console errors on a fresh tab.

- **August 26, 2026 (still later same day) — Hub redesigned (light theme, single left-hand nav column) + the cream "paper" background swapped to white product-wide.** Two related but distinct asks. **Hub (`index.html`)**: rebuilt from a centred dark hero (giant "ORGANICA" wordmark, all-caps wrapped nav, `--bg:#0a0a08`) into a light shell — white background, the same organic-noise canvas animation kept but recoloured (darker, more saturated strokes at higher opacity) so it still reads against white instead of near-black; "Organica" now appears exactly once, small, top-left in a real header; every tool link moved into one vertical column, left-aligned, smaller font, natural Title Case (removed `text-transform:uppercase`, the HTML text was already correctly cased); the centre of the page is deliberately left empty — reserved for a future "displays" feature, not built this pass. Per-tool hover accent colours all carried over, individually darkened to hold contrast against the new white page (a color tuned to be visible on near-black doesn't survive the same brightness on white unchanged). **Product-wide background**: separately, replaced the cream/parchment "Pollen palette" tone (`#f5f2ec` and its handful of near-identical variants — `#f1f1ef` in `genesis/index.html`, `#f4f1ea` in `genesis/indicators.html`, a stray `#f5f2ed` second declaration in `strata/index.html`) with pure white (`#ffffff`) across every tool's own `--bg`/`--paper` token — 19 files total (all live tools, `shared/_template.html`, `design-system/index.html` including its own documentation code-sample, and the `genesis/_test-paper-draw.html` prototype). **Deliberately scoped narrowly, disclosed rather than silently expanded**: only the page/paper background token changed — `--surface`/`--panel` (the secondary panel/sidebar tone, a visually distinct grey-beige already) and `genesis/indicators.html`'s own dark/warm/cool theme-picker alternates were left untouched, since neither was what "questo giallino" referred to and changing them wasn't asked. Verified live across the hub, Creator, FVS, and Loom: white page background with the existing grey panel/canvas tones still providing visual hierarchy, zero console errors, no contrast regressions (dark text against a lighter background than before, if anything, gains contrast rather than losing it).

- **August 26, 2026 (still later same day) — Strata removed entirely; four-part maintenance pass (removal + bento bug fix + Design System refresh + cross-tool drift fixes).** Diego's call to remove Strata stood independent of a direct finding that its production pipeline actually worked (`/api/health` → 200, `/api/trace` → 200 with a real non-degenerate SVG on a repo test photo) — confirmed explicitly ("Non importa perché vogliamo comunque eliminarlo") rather than assumed. Deleted `strata/`, `api/` (the Vercel Python functions), `backend/` (the whole trace pipeline, its own `venv/`/`test-photos/`/`output/` — all gitignored, cleaned via `rm -rf`), root `requirements.txt`, `docs/STRATA-REFINE.md`; removed the hub nav link + its CSS, `vercel.json`'s `/strata/` rewrite and its now-orphaned `functions` block, and `.claude/launch.json`'s `strata-backend` entry. This file's own Tools-table row, Repo Structure entries (`strata/` and `backend/`, the latter now nonexistent too), and the `strata/`/`backend/` "do not touch" rules are removed here; Phase 5 of the Roadmap above (was "Strata AI") is retired. Passing mentions across `docs/VISION.md`, `docs/ROADMAP.md`, `docs/UI-SHELL.md`, `docs/KOMOREBI.md`, `docs/LOOM.md`, `docs/SOUL.md`, `docs/CAMOUFLAGE.md` adjusted; dated historical session-note prose (including in this file) was deliberately left alone as record, not live instruction. **Bento bug**: forms 52/53 on the hub rendered with a zero-size bounding box (`getBBox()` confirmed `{w:0,h:0}`) because they `<use href="#cA">`-reference Genesis's own "terrazzo chip" defs, never copied into the hub's inline `<defs>` when form 35 was fixed earlier — fixed by copying the 7 `<path id="cA">`…`<path id="cG">` defs verbatim from `genesis/indicators.html`; both forms now report real non-zero bboxes. **Design System refresh**: `docs/DESIGN-SYSTEM.md`'s accent table gained the 9 tools shipped since July 25 (Camo Turing/Warping/Loom/Soul/Membrane/Vortex/FVS/TuneSutra/Mycel) and lost Strata's row; added a "Hub nav categories" table (the real functional groups, including "Coloring & palette" — previously undocumented); de-staled the §6 accessibility-audit language to name its July 26 date instead of reading as a current claim; date stamps updated. `design-system/index.html`'s three "Used in" component tags (Panel, header `--editor` variant, Floatbar) corrected to the current tool list. `shared/organica-tokens.json` lost its `strata` accent entry and gained the same 9 new tools' hex values, verified as valid JSON after editing. **Cross-tool drift fixes** (confirmed explicitly — "Includili ora" — rather than deferred): **(D1)** `mulberry32` consolidated into `Organica.mulberry32` (`shared/organica-core.js`), 6 independent copies (FVS, Living Path, Mycel, Camo Turing, Genesis Creator, Vortex) now delegate to it — Genesis Creator's own copy had a subtle correctness gap (`t += …` vs `t = (t + …) | 0`) fixed as a side effect; Loom's `js/generators/*.js` deliberately excluded, since their own documented zero-coupling architecture is intentional, not drift. **(D2)** The RMX/palette-chip pattern consolidated into `shared/organica-palette-chip.js` (`Organica.createPaletteChips`) — 6 tools retrofitted (Camo Turing, Membrane, Vortex, FVS, Pollen, Spore), each verified live via real add/remove/recolour DOM interaction, each tool's own `min`/`max` preserved. **(D3)** Inline hex-validation regexes replaced with `Organica.normalizeHex` in Spore/Pollen/Halide/Camo Turing — Halide and Pollen's hex text fields moved from a live per-keystroke `oninput` guard to `onchange` (matching Komorebi/Warping's own established convention: commit-time validation with fallback to the previous valid value, verified live in all 4 tools). **(D4)** Camo Turing's private scale-only zoom (`zoomLevel`, wheel + dblclick, no pan) migrated to the shared `Organica.createZoomPan` — gained drag-pan as a side effect of the shared implementation; the click-to-reseed handler needed no change since `getBoundingClientRect()` already reflects the transform either way; verified live (wheel-zoom, badge, drag-pan, dblclick-reset). **(D5)** Warping's Export UI moved from the header into the shared bottom-centre `.org-floatbar` (added the missing `organica-floatbar.css` link), matching FVS/Loom/Camo Turing's own convention — no `→ Figma` header button (product-wide pattern: `sendToFigma()` stays defined, ready for a future Export-popover tab); all 3 export formats verified firing correctly from the new location. **(D6)** Removed the dead duplicate `.panel-section` CSS override in `warping/index.html` and `loom/index.html` (both silently redeclared the identical shared value, `--space-5` = `--panel-pad` = 14px — confirmed identical before deleting, zero visual diff); `docs/UI-SHELL.md` gained an `organica-palette-chip.js` mention alongside the other required/optional includes. Every touched tool re-verified on a fresh tab: zero console errors, correct behaviour exercised through actual DOM interaction (not just "no error"), consistent with this project's own repeated lesson that a clean console only proves something if the exact interaction was actually triggered.

- **August 27, 2026 — Shipped Rhizome (`/rhizome/`), a node-based visual workflow canvas chaining the other Organica tools as pipeline stages.** Preceded by a very detailed analysis (6 parallel research passes: what's already reusable — `Organica.loadLoomGrid` as the one genuine cross-tool JSON contract, `Organica.motion.parsePrimitives`, `Organica.createZoomPan`, `Organica.traceContours` — vs. what's genuinely absent — zero DAG, zero live wire-follows-drag, zero typed ports, the Figma bridge confirmed silently dead since at least two prior dated session notes; plus a full categorized catalog of every tool's own INPUT/ENGINE/TRANSFORM/EFFECT/MOTION/OUTPUT functions) written up as the project's approved plan before any code. Named **Rhizome** (Diego's own pick from a shortlist — botanical connective-network metaphor, distinct from Mycel's fungal-network domain). **Phase 0**: fixed the dead Figma bridge — `figma-plugin/code.js`'s `onmessage` never listened for `'organica-svg'` (only `'import-svg'`/`'import-regions'`), silently breaking "→ Figma" on every tool using `Organica.sendToFigma` (Spore/Pollen/Halide/Komorebi/Loom/Camo Turing/Warping) since it shipped; one-line alias fix, benefits all seven tools immediately. **Phase 1 MVP** (built after a standalone `_test-mvp.html` prototype validated the interaction mechanics first, same "prototype in isolation before real architecture" discipline as Loom's own `_test-*.html` pages): a two-tier node model — Tier 1 native (zero-porting wrappers on already-pure functions: Loom's `generateBento`/`generateHexagonal` imported as real ES modules, `Organica.loadLoomGrid`, `Organica.traceContours`) vs. Tier 2 bridge (a hidden sandboxed iframe loading the real tool page + a new `rhizome-set-input`/`rhizome-output-ready` postMessage protocol, ~20-30 lines added to each bridged tool's own file) — chosen over porting every tool's algorithm natively specifically to avoid a maintenance-drift risk (a ported copy silently diverging from the real tool over time) for a solo-designer tool where iframe round-trip latency doesn't matter. Typed ports (`svg`/`image`/`grid`/`color`/`number`/`points`) with exactly 3 disclosed auto-adapters (`svg→points`, `grid→points`, `grid→svg` — each a direct port of what a real tool already does by hand, no speculative conversion matrix). Execution engine: Kahn's-algorithm topological sort (throws a readable error on a cycle rather than hanging), per-node dirty-flag caching. Canvas: per-node drag (extends FVS's own threshold-drag technique, but is the first place in the repo that actually *writes* a dragged object's new position back to state), live wire-follow (extends Mycel's own curve math, made interactive), and a new `panAlways` opt-in added to `Organica.createZoomPan` itself (opt-in, zero behaviour change for the 6+ existing callers) since node-graph canvases need to pan at any zoom level, unlike every other tool's own image/canvas zoom. Shipped with 6 Tier-1 nodes (Loom Grid — 5 generators after Phase 2 below, Loom Grid→Geometry, Contour Trace, SVG→Points, Merge, Export with PNG/SVG/→Figma) + 1 Tier-2 proof of concept (Genesis Seed). **Real bugs found and fixed during live verification** (not just "no console error" — every mechanic exercised through actual dispatched DOM events): a leftover `position:absolute` CSS rule overlapping the floatbar's two buttons; the single most serious one — `bindPortInteractions` attached its connect-drag listeners by iterating `portEls` ONCE at boot, when the map was still empty, so **zero nodes added after page load ever had a working connect gesture at all** — fixed with event delegation on the graph container instead; a missing `stopImmediatePropagation` that let a port-drag also start a canvas pan simultaneously (both bound to the same element); the Genesis bridge sending `formMarkup()`'s bare `<g>` fragment instead of a wrapped standalone `<svg>` document, which silently rendered as escaped text instead of a shape. **Phase 2** (scoped to Camo Turing plus the lower-risk bridges per Diego's own sequencing, Membrane deferred): added 3 more Loom generators (triangular/diamond/circular — same param shape as hexagonal, zero-cost integration) to the Loom Grid node; shipped Komorebi Pattern and Warping Pattern bridges (both simple pattern-select + `buildSVG()`, no seed-loading complexity); shipped Soul (pass-through) — the first bridge node with a real upstream SVG *input*, not just a source; **found and fixed a second real race-condition bug**: two `recompute()` calls firing close together (e.g. add-node immediately followed by connect) each create/reuse the same per-node bridge iframe, and the second call's response-listener registration silently overwrote the first's in `bridge-iframe.js`'s `pending` map — the first call's `await` never resolved, hanging that whole `runGraph()` invocation forever with no error surfaced; reproduced deliberately in isolation before fixing, fixed by serializing `recompute()` (a later call while one is in flight just re-runs once more after, instead of running concurrently); re-verified against the exact original failing sequence. Also fixed a real `prompt()`-based Save button (same failure class already fixed in FVS in August — `prompt()` throws in some embedded contexts) with an inline text field, matching every other tool's own established convention. **Camo Turing bridge** (the flagged-risk one, tackled last, on its own per Diego's request): confirmed directly by reading the file that Camo Turing's Gray-Scott core has **no convergence/auto-pause detection at all** (its own comment: "no auto-stop — Camo Turing has no convergence/auto-pause detection to race against, unlike Camouflage," its removed predecessor) — so the bridge runs a disclosed, tunable fixed step count (`stepSim()` called directly in a synchronous loop, bypassing the rAF loop entirely so it can't be interleaved) rather than a true "wait until settled," exposed as the node's own `steps` param instead of a hidden guess. Reuses `applyPreset()` (all 23 built-in presets, including the 3 anisotropic ones) + `resetSim()` + the existing `exportSVG()`'s own pause-required guard. Bridge timeout raised from 8s to 15s specifically to give this one genuinely slow bridge room (every other bridge responds in well under a second). Verified live: Leopard spots and Tiger stripes (anisotropic) both render correctly and distinctly, a full Camo Turing→Export chain produces a real 354KB SVG with genuine path geometry and embedded `<metadata>` (f/k/engine info). **Phase 3**: added Rhizome to the hub nav (a new "Workflow & pipelines" category — it didn't fit any existing functional group) and `vercel.json`'s rewrite list, both real gaps (the tool was fully built but unreachable from the hub until now). Final state: **11 node types**, 0 of ~40 controls unaccessibly-named across all of them (checked per-type, not sampled), zero real console errors on every fresh-tab check throughout (only the expected iframe-sandbox and canvas-readback browser warnings). **Not yet done, disclosed rather than silently skipped**: Membrane bridge (generative accumulation over time, same class of complexity as Camo Turing, deferred past this session), graph preset picker UI beyond the raw localStorage save/load, adapter expansion (deliberately not speculative — only added when a real need shows up in use). Manual/docs beyond this session note and the plan file itself not yet written.

- **August 27, 2026 (later same day) — Rhizome Phase 4: 5 more bridges (Membrane, Living Path, Spore, Pollen, Halide), undo/redo, multi-select/marquee-select, variadic Merge inputs, and `docs/RHIZOME.md`.** Scoped precisely by Diego's own explicit list, deliberately excluding Vortex/TuneSutra/Mycel from the broader Phase-3-remainder options ("fai solo Living Path, Spore, Pollen, Halide"). **Membrane bridge**: the module-scoped (`<script type="module">`) case — unlike every classic-script tool, Membrane's `main.js` exposes nothing on `window` automatically, so its listener lives inside the module itself using already-imported bindings (`state`, `reseedLinear`, `buildExportSVGString`); sets `state.movementPattern`/resets `state.moveClock`/unfreezes, waits a real `seconds`-driven delay (up to 10s — the reason the shared bridge timeout was raised from 15s to 20s), then exports. Verified live: real `<metadata>{"tool":"Membrane"...}` SVG with a genuine visible orbit trail. **Living Path bridge**: 29 presets (5 vector + 24 raster) collapsed into one flat `tech:name`-prefixed dropdown, since Rhizome's param system has no cross-param-reactive dropdown support to show a second "which engine" selector; seeds from a Genesis form (id 38 default), switches engine, applies the preset, waits a flat 150ms (covers raster's real 110ms debounce, harmless no-op for vector's synchronous render). Verified live: `raster:Coral` and `vector:Shatter` both produced genuinely distinct, correct-looking output. **The critical bug, found once and fixed identically in 3 places**: Spore/Pollen/Halide all gate rendering on `loadImage(input)`, whose `img.onload` is asynchronous even for `data:` URLs — a poll loop checking the tool's own render-completion signal immediately after calling `loadImage()` sees the signal's still-pre-render state and exits on the very first check, returning stale/empty output with zero console error (found by reproducing, not assumed: Spore Stipple's first connect+wait test showed an empty `—` preview). Root-caused by reading all 3 tools' actual `loadImage` source rather than guessing. Fixed with an identical **two-phase wait** in all three: first poll `#drop-hint.classList.contains('hidden')` (each tool's own reliable "image genuinely finished decoding" signal, set inside its own `onload`), then poll the tool's own real render-completion flag (`#btn-save-svg.disabled` for Spore, `#btn-stop.disabled` for Pollen, the equivalent gate for Halide). Verified live post-fix, all three via a fake-file-upload test harness (constructed `File`→`DataTransfer`→dispatched `change`, since each tool's `sourceImage` is a private non-window-scoped `let`): **Spore** produced real stipple dots, denser in the darker regions of a synthetic gradient test image; **Pollen** produced a real Hatch-Flow line-stipple pattern correctly oriented/densified around a dark test circle; **Halide** (entirely untested before this pass) produced genuine Ditherface-preset output — background removed, outlined, visible Atkinson dither texture — confirmed via connect→wait→screenshot→fresh-tab-console-check, the same four-step discipline applied to every other bridge. **Variadic Merge**: `meta.inputs` was a static array on every node type; Merge now exports `getInputs(node)` (computing `in0..inN` from `params.inputCount`, 1–6), and a new `getNodeInputs(node)` dispatcher in `node-registry.js` is what every consumer (execution engine, `node-card.js`'s port-row builder, main.js's connect-time checks) reads instead of `meta.inputs` directly. `node-card.js`'s port rows became a standalone rebuildable `buildPortRows()` so changing `inputCount` rebuilds just the ports, not the whole card; edges pointing at a since-dropped port are pruned in main.js. Verified: dragging `inputCount` 2→4 live-rebuilt the port rows to `in0..in3`, connecting a real seed to `in0` produced correct composited SVG output. **Multi-select/marquee**: `canvas/selection.js`, deliberately gated on **Shift+drag** rather than plain drag, since plain-drag is already the canvas pan gesture (`Organica.createZoomPan`'s own `panAlways` option from Phase 1) — a capture-phase `mousedown` listener with `stopImmediatePropagation()` intercepts Shift+drag before the pan handler ever sees it. `bindNodeDrag` gained a `getDragGroup(node)` callback so dragging one selected node moves the whole multi-selection together. Verified live: Shift+drag over two spread-apart nodes selected both (dashed `--tool`-colour outlines, count confirmed at 2). **Undo/redo**: `history.js`, a plain JSON-snapshot stack over the canonical `{nodes,edges}` model — pushed only at real checkpoints (add/remove node, connect/disconnect, drag-**end** not every tick, param-**commit** via a slider's `change` event not its `input` event) so continuous interaction never floods the stack. **Real bug caught by testing the full cycle, not assumed working**: `updateHistoryButtons()` was only ever called from `commitHistory()` (new pushes), never from the Undo/Redo button click handlers or the keyboard-shortcut handler — so after undoing back to the empty baseline, the Undo button still read enabled; fixed by calling it from both button handlers and the keydown handler too, re-verified through a full add→undo→redo cycle toggling correctly at every step. A second real bug in the same area: Image Upload's preview rendered `[object Object]` for an `{dataURL}`-shaped value, since `guessValueType()` had no `image` branch — fixed by adding one to both the type-guesser and `node-card.js`'s `renderPreview` (a real `<img>` thumbnail). **Final verification**: 0 of 27 controls unaccessibly-named on the base 3-node graph, 0 of 3 on the Merge inspector (`inputCount`/`offsetXStep`/`offsetYStep`), zero console errors confirmed on a genuinely fresh tab (not just the working tab's own possibly-stale buffer, the discipline this project has needed repeatedly). Manual: `docs/RHIZOME.md`, covering the two-tier model, the full 17-node registry, the async-image-load bug writeup, the variadic-port dispatcher pattern, and what's still deferred (Vortex/TuneSutra/Mycel bridges, a real thumbnail preset picker, further adapter expansion, the still-open product-wide Figma-plugin gap for tools other than what Phase 0 already fixed).

- **August 27, 2026 (still later same day) — Hub bento showcase rebuilt as a masonry gallery, modelled on giuseppelaezza.com/playground per Diego's direct reference.** Inspected the reference live (screenshot + computed-style/DOM queries, not assumed from memory): CSS multi-column masonry (`columns:3`, `column-gap:10px`), full-bleed saturated cards (`border-radius:15px`), an always-visible top-left name pill, and a blur(8px)→none load reveal. Confirmed scope and two deliberate deviations with Diego via AskUserQuestion before touching code: **(1)** stay on Organica's own token scale (`--radius-lg` 8px, `--space-*`) rather than the reference's raw 15px — no new token invented; **(2)** cards go full-bleed (the SVG motif fills/bleeds the whole card on a saturated per-tool background) rather than staying a small centred icon; **(3)** scope is the bento section only, the left sidebar nav stays untouched. Replaced the old fixed 8×100px span-grid (`size-lg/wide/tall/sm` → `grid-column/row: span N`) with `columns: 4 220px` masonry — `.bento-cell` now carries a per-item `aspect-ratio` (`--ar`, from the same 4 old size labels re-mapped to ratios) instead of a grid span, `break-inside:avoid` keeps each card whole across columns, and `.display-area` scrolls vertically (removed the old `align-items:center;justify-content:center` single-viewport centering). Each cell's inline `--ink` custom property (previously read directly by `organic-animations.css`'s own `fill:var(--ink)` rules AND used as the card's neutral-beige-adjacent background) was **split into two variables** to avoid a real naming collision: `--tool-color` (the per-item accent, driving the card background) and a **locally re-scoped `--ink: #fff`** on `.bento-cell` itself — since `--ink` cascades to descendants, this makes every Genesis form (via the shared, never-modified `organic-animations.css`) and every hand-built `.tp-*` tool-preview motif render in white automatically, with zero per-rule edits to either stylesheet. The tag pill went from the old opacity-0-until-hover text label to a real always-visible white chip. **A real, measured accessibility bug caught before shipping**: white-pill-with-tool-color-text failed AA at small (9px, `--fs-micro`) size for 7 of 26 cells (as low as 3.28:1 for the `--accent` green) — the same class of bug as the July 26 `--mid` contrast fix. Fixed with a new `pillTextColor(hex)` helper that programmatically darkens a copy of the accent (in 8% steps) until it clears 4.5:1 against white, applied only to the pill's own text via a second `--tag-color` variable — the card background and shape fill keep the original vivid `--tool-color` unchanged, since that's a large-area colour where the same hex already passes. Verified live: 0 of 26 cells failing AA (re-measured programmatically after the fix, not eyeballed), masonry degrades cleanly from 4 columns (1400px) to 2 (900px) to 1 (500px, below the sidebar's own fixed width) with no horizontal overflow at any width, all 26 cells remain real `<a href>` links (0 broken), zero console errors on a genuinely fresh tab. Single file touched: `index.html` (sidebar/nav untouched per the confirmed scope).

- **August 27, 2026 (still later same day) — 4 hand-built bento motifs replaced with real tool exports (Camo Turing, Komorebi, Halide, Membrane), each given its own genuine animation from Organica's own 6-pattern vocabulary.** Diego supplied two real SVG exports from his Downloads and asked to swap them into the bento, then added four more mid-turn. **Inspected every file before touching anything, not assumed safe**: Camo Turing's labyrinth export (411KB, 2 real `<path>`s) was fine as-is; **Membrane's own file turned out to be a raster fallback** — a single `<image>` embedding a 1600×1600 base64 PNG (2.3MB inline), not vector, almost certainly because its `rainbow` per-segment colour mode has no flat-path representation for `shapeHistory` to serialise. Flagged this to Diego rather than silently embedding 2.3MB into the homepage or silently downscaling it; his call was to re-export properly. Re-exported it myself, live: opened `/membrane/`, switched Movement to Orbit, let a real torus trail accumulate, and captured the export by patching `URL.createObjectURL` (a global `Organica.download` override didn't work — Membrane's `<script type="module">` holds its own imported reference to the shared `download` function, invisible to a `window.Organica.download` patch) — landed a genuine 155KB/220-path vector export (`colorSrc:"ink"`, confirmed via its own `<metadata>`), replacing the raster one entirely. **A second real batch of 4 more files arrived mid-turn** with no accompanying instruction; asked Diego directly rather than guessing scope — confirmed "all 6" (in practice 5 usable, see next) and separately flagged that one of the two Komorebi exports tiles a repeated portrait/face silhouette (likely a gobo-stencil source photo) — Diego's own call was to exclude it from the public homepage on privacy grounds, so it was left out entirely, not incorporated anywhere. **Picked one file per already-represented tool** (Camo Turing's cream/navy/orange reaction-diffusion labyrinth, the mint/black Komorebi canopy blobs, Halide's dithered leaf portrait, the new Membrane orbit) rather than force in the second Camo Turing export too, since Membrane had no existing bento tile to receive it (added as a new tile) while a second Camo Turing card would have been a redundant duplicate of an already-covered tool — a scope call made and disclosed, not asked as a fourth round of questions. **A real CSS collision caught before shipping, not after**: the pre-existing `.tp-turing path { fill: var(--ink); }` rule (written for the old 2-path hand-drawn motif) would have silently overridden the real labyrinth export's own two explicit literal fills (`#e94f37`/`#f6e8c3`) with flat white, since a class-based CSS rule always beats an SVG presentation attribute regardless of the element's own markup — caught by checking which of the 4 real files used the exact tag/selector combinations any existing `.tp-*` rule already targeted (only this one collided; Komorebi/Halide/Membrane's own real tags didn't match any existing rule). Fixed by dropping the legacy `tp-turing` class from that item entirely rather than patching the shared rule, since the rule is still correct for anywhere else it might apply. **A second, more consequential bug found while trying to scroll to verify the new cards**: `.display-area` had been given `min-height:100vh` in the earlier masonry redesign — a floor, not a ceiling, so the box simply grew to fit all 27 cells with nothing left to constrain it, and `overflow-y:auto` never engaged; `body`'s own pre-existing `overflow:hidden` then made that entire uncapped overflow **unreachable** — most of the bento grid was invisible and unscrollable to any real visitor, undetected by the first round of "no console error" verification because a JS-driven headless check never actually exercised scrolling past the fold. Fixed by using `height:100vh` (a real ceiling) instead, re-verified via `element.scrollHeight`/`clientHeight` (2396 vs 951, confirming genuine overflow) before trusting a screenshot again. **Animation, one deliberately matched pattern per file rather than one generic effect for all**: `cellBreathe` (Internal Pressure — reused scale-pulse concept, redefined locally since organic-animations.css's own `breathe` keyframe's 0.78 floor would gap a full-bleed card) for Camo Turing/Komorebi's filled reaction-diffusion/canopy regions; `cellDrift` (Environmental Forces — slow Ken-Burns creep) for Halide's dithered photograph; `cellSpin` (Differential Rotation — continuous slow rotation) for Membrane, chosen directly from its own export metadata reading `movementPattern:"orbit"`; and a genuine **Growth by Tracing** one-time reveal for Membrane's 220 individually-recorded trail paths — `animateTrailReveal()` reads each path's own real `getTotalLength()` (not a shared guess) and staggers `stroke-dashoffset` 0.4–2.0s after load, replaying the trail as if being drawn rather than an arbitrary uniform sweep. Verified live: 0 console errors on a fresh tab, all 220 Membrane paths confirmed at `strokeDashoffset:0px` post-reveal, 27/27 cells still real links, 0/27 tag pills failing AA (re-run after the new items), all 4 real exports screenshot-confirmed rendering full-bleed with correct pills and running animations. **Disclosed cost, not hidden**: `index.html` grew to ~1.48MB (four real vector exports embedded inline, largest being Halide's 775KB/16,375-`<rect>` un-simplified tile export) — a real, order-of-magnitude jump from the hand-built motifs' few hundred bytes each; flagged here rather than silently shipped, no further action taken this round since Diego's own scope answers already weighed this tradeoff.

- **August 27, 2026 (still later same day) — New exploration: Blob Boundary (`/explorations/blob-boundary/`), a working prototype of the giuseppelaezza.com/playground GIF effect.** Diego linked a reference GIF (a grey mask morphing between a circle and a diagonal band, with a field of yellow dots that stay small/isolated deep inside the mask but enlarge into an overlapping gooey fringe exactly along its boundary) and asked whether Organica already had a tool for it. It didn't — checked the real code, not assumed: Soul's own `morph` pattern (`shared/organica-motion.js:381`) already vendors and correctly drives MorphSVGPlugin, but only for one primitive morphing circle↔noise-blob, never a canvas-wide boundary shape between named presets; Pollen/Komorebi's scatter engines have no "dot size driven by distance to a shape's own edge" concept at all. Reported this gap, then built the prototype on request. **Mechanism, confirmed by directly reproducing it rather than guessing from a single frame** (screenshotted the reference GIF repeatedly to watch it loop): no blur/goo filter needed at all — the lumpy edge is just plain same-colour circles overlapping. Implementation: a `<path>` boundary cycles between preset shapes (circle/diagonal band/organic blob/starburst, all sampled as 72-point closed polygons so MorphSVGPlugin — already vendored for Soul — has comparable material regardless of which two are morphing) via a looping GSAP timeline; a fixed dot lattice is classified **every frame against the boundary path's own live, currently-interpolated geometry** using the real `SVGGeometryElement.isPointInFill()` API (general-purpose and correct for ANY in-between morph state, not per-shape distance formulas) — a dot whose own probe samples around its radius disagree with its centre is "straddling the edge" and gets enlarged; fully inside stays small; fully outside is hidden. Classification (the expensive pass, N dots × sample-count `isPointInFill` calls) is throttled to ~20fps independent of the boundary's own smooth 60fps GSAP tween, disclosed in the file's own panel hint rather than left as an unexplained knob. Verified live: mid-morph, paused, and shape-switched screenshots all show interior dots correctly small/separated and boundary dots correctly enlarged/overlapping into the fringe, matching the reference; 0 console errors on a genuinely fresh tab; Pause/Play and a live shape-select swap both re-verified through real dispatched interaction. Added to the hub's own Explorations nav group (`index.html`), same convention as Flow Field/the Loom `_test-*.html` prototypes — a standalone single-file page, no shared-token/organica-core dependency, matching every other file in `explorations/`.

- **August 27, 2026 (still later same day) — TuneSutra's garment pictogram reshaped from a vector Diego supplied ("Group 34.svg"), plus a real code-duplication fix caught while touching it.** The old garment geometry (a 200×280 box, ported from a photographed reference book illustration) replaced with the new vector's own 326×670 geometry: rectangular body, a wider trapezoid hem, a V-neck collar triangle, and two new bottom-corner accent triangles the old shape didn't have. Role mapping wasn't guessed — the source file itself only uses 3 fills across its 5 shapes (body / hem / everything else black), so the collar triangle and both bottom-corner triangles all share the SAME fill in the original, the direct signal that all three belong to one role (Accent) rather than inventing a 4th zone, which would have broken the garment view's own explicit, already-documented "capped at N=3" constraint. **Found and fixed a real pre-existing duplication while touching this code**: the garment's zone list was hand-typed twice — once in `garmentMarkup()` (SVG/on-screen) and again inline inside `exportPNG()` (Canvas Path2D) — the exact "two call sites that can silently drift apart" shape this project has fixed repeatedly elsewhere. Consolidated into one shared `GARMENT_ZONES` constant both functions now read, so a future geometry change can't update one and miss the other. Verified live: the new shape renders pixel-for-pixel matching the supplied vector (screenshot-compared), clicking the collar triangle correctly selects the Accent role in the side panel, `svgString()` export contains exactly 5 real `<path>` elements, a live Canvas Path2D re-render using the new shared constant produces a valid PNG data URL with no exception, zero console errors on a genuinely fresh tab.

---

*Studio Rann · Updated August 2026*
